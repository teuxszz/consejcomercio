---
phase: 06-pwa-push-notifications
plan: 04
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/lib/pwa.ts
  - src/hooks/usePushSubscriptions.ts
  - src/lib/query-keys.ts
  - src/components/me/NotificacoesPanel.tsx
  - src/components/me/InstalarAppCard.tsx
  - src/pages/MeEspacoPage.tsx
autonomous: true
requirements:
  - PUSH-01
  - PUSH-02

must_haves:
  truths:
    - "Em Chrome desktop/Android no modo standalone, ligar 1º toggle Push em /me/preferencias dispara `Notification.requestPermission()` → aceitar → `pushManager.subscribe()` → upsert em push_subscriptions com onConflict perfil_id+endpoint (PUSH-02 D-12)"
    - "Recusar permission → switch volta a OFF + toast.error('Permissão negada') (R-L5 lógica explícita)"
    - "Em iOS Safari não-standalone, coluna Push mostra 4 switches `disabled` + Tooltip 'Instale o app primeiro: Compartilhar → Adicionar à Tela' (D-13 iOS Gate)"
    - "Em Chrome com `beforeinstallprompt` capturado, `InstalarAppCard` em /me/preferencias renderiza botão 'Instalar app' que dispara `deferredPrompt.prompt()`"
    - "Em iOS Safari não-standalone, `InstalarAppCard` renderiza instruções estáticas 'Compartilhar → Adicionar à Tela'"
    - "Quando `isStandalone()` retorna true, `InstalarAppCard` NÃO renderiza (return null — usuário já instalou)"
    - "Toggle Push OFF mantém row em push_subscriptions (D-03 — re-ligar = instantâneo sem novo prompt); apenas prefs muda"
    - "Multi-device: subscribe em 2 browsers cria 2 rows distintas (UNIQUE perfil_id+endpoint não viola — endpoints diferentes)"
    - "Mesmo browser re-subscribe: upsert ON CONFLICT atualiza last_seen_at sem duplicar (Pitfall 7)"
  artifacts:
    - path: "src/lib/pwa.ts"
      provides: "isStandalone, isIOS, canSubscribePush, urlBase64ToUint8Array, arrayBufferToBase64Url, subscribePush, unsubscribePush, type PushBlockReason"
      contains: "export function canSubscribePush"
      min_lines: 60
    - path: "src/hooks/usePushSubscriptions.ts"
      provides: "usePushSubscriptions(perfilId) query, useSubscribePush mutation, useUnsubscribePush mutation"
      contains: "useSubscribePush"
      min_lines: 70
    - path: "src/lib/query-keys.ts"
      provides: "QUERY_KEYS.pushSubscriptions = { all, byPerfil }"
      contains: "pushSubscriptions"
    - path: "src/components/me/NotificacoesPanel.tsx"
      provides: "Matriz 4×3 com coluna Push + iOS gate + permission flow inline (D-12, D-13)"
      contains: "canSubscribePush"
    - path: "src/components/me/InstalarAppCard.tsx"
      provides: "Card em /me/preferencias com botão install OU instruções iOS conditional (D-09)"
      contains: "InstalarAppCard"
    - path: "src/pages/MeEspacoPage.tsx"
      provides: "Mount InstalarAppCard acima de NotificacoesPanel (tab Notificações)"
      contains: "InstalarAppCard"
  key_links:
    - from: "src/hooks/usePushSubscriptions.ts"
      to: "supabase/migrations/036 push_subscriptions table"
      via: "supabase.from('push_subscriptions').upsert({...}, {onConflict: 'perfil_id,endpoint'})"
      pattern: "push_subscriptions.*upsert|onConflict"
    - from: "src/components/me/NotificacoesPanel.tsx"
      to: "src/lib/pwa.ts (canSubscribePush) + src/hooks/usePushSubscriptions.ts (useSubscribePush)"
      via: "import { canSubscribePush } from '@/lib/pwa' + import { useSubscribePush } from '@/hooks/usePushSubscriptions'"
      pattern: "canSubscribePush|useSubscribePush"
---

<objective>
Subscribe flow client-side: helpers PWA puros (`src/lib/pwa.ts`), hook TanStack Query (`usePushSubscriptions`), e UI integration na matriz `NotificacoesPanel` (4×2 → 4×3) com permission flow inline (D-12), iOS gate (D-13) e R-L5 rollback no toggle. Também: `InstalarAppCard` em `/me/preferencias` (D-09 — porta sempre aberta pra instalar).

Purpose: completar PUSH-02 (subscription persistida em DB ligando toggles em /me/preferencias) e a parte "card persistente" de PUSH-01 (D-09). Usuário consegue subscrever via UI normal — backend de Plan 03 já está pronto para entregar.
Output: 4 novos arquivos + 2 edits + query-keys. Matriz se torna 4×3 com Switch Push respeitando iOS gate e permission flow async com rollback. `InstalarAppCard` cobre ambos Chrome (botão install via beforeinstallprompt deferred) e iOS Safari (instrução estática).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pwa-push-notifications/06-CONTEXT.md
@.planning/phases/06-pwa-push-notifications/06-RESEARCH.md
@.planning/phases/06-pwa-push-notifications/06-PATTERNS.md
@.planning/phases/06-pwa-push-notifications/06-01-SUMMARY.md
@.planning/phases/06-pwa-push-notifications/06-02-SUMMARY.md
@src/lib/utils.ts
@src/lib/cnpj.ts
@src/lib/query-keys.ts
@src/hooks/usePreferenciasNotif.ts
@src/hooks/useReenviarNotificacao.ts
@src/components/me/NotificacoesPanel.tsx
@src/components/me/PerfilPanel.tsx
@src/components/ui/switch.tsx
@src/components/ui/tooltip.tsx
@src/pages/MeEspacoPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4.1: `src/lib/pwa.ts` (helpers puros) + `src/lib/query-keys.ts` (pushSubscriptions keyset)</name>
  <read_first>
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§3 Subscription Flow — pwa.ts skeleton completo, §6 iOS Gate detection canônica, Pitfall 5/9)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (§src/lib/pwa.ts — style utils.ts/cnpj.ts)
    - src/lib/utils.ts (style de named exports + cn helper)
    - src/lib/cnpj.ts (style de discriminated unions de domínio)
    - src/lib/query-keys.ts (pattern Phase 5 — pushSubscriptions: { all, byPerfil })
    - src/lib/__tests__/pwa.test.ts (scaffold RED Plan 01 Task 1.1)
  </read_first>
  <behavior>
    - `urlBase64ToUint8Array('BA1...')` retorna Uint8Array com length apropriado (server key VAPID = 65 bytes raw)
    - `arrayBufferToBase64Url(uint8.buffer).then(s => urlBase64ToUint8Array(s))` round-trips byte-for-byte
    - `canSubscribePush()` em jsdom (sem PushManager) retorna `{ ok: false, reason: 'unsupported' }`
    - `canSubscribePush()` com mockMatchMediaStandalone(false) + UA iOS retorna `{ ok: false, reason: 'ios-not-standalone' }`
    - `canSubscribePush()` com mock Notification.permission='denied' retorna `{ ok: false, reason: 'denied' }`
    - `isStandalone()` retorna true quando `matchMedia('(display-mode: standalone)').matches === true`
    - `isIOS()` retorna true para UA contendo iPhone/iPad/iPod (e false para Android UA)
    - Testes scaffold de Plan 01 Task 1.1 viram GREEN
  </behavior>
  <action>
    1. **`src/lib/pwa.ts`** — copiar skeleton completo RESEARCH §3 (linhas 338-413):
       - `isStandalone()`: combo `matchMedia('(display-mode: standalone)').matches || (window.navigator as any).standalone === true` (iOS legacy fallback) — Open Question 4.
       - `isIOS()`: `/iPad|iPhone|iPod/.test(navigator.userAgent) && !(window as any).MSStream`
       - `type PushBlockReason = 'ios-not-standalone' | 'unsupported' | 'denied'`
       - `canSubscribePush(): { ok: boolean; reason?: PushBlockReason }`:
         - Feature detect `'serviceWorker' in navigator && 'PushManager' in window && 'Notification' in window` → senão `unsupported`
         - `if (isIOS() && !isStandalone())` → `ios-not-standalone`
         - `if (Notification.permission === 'denied')` → `denied` (Pitfall 5)
         - Default: `{ ok: true }`
       - `urlBase64ToUint8Array(base64String: string): Uint8Array` — pattern canônico de 6 linhas (padding + atob + Uint8Array fill).
       - `arrayBufferToBase64Url(buffer: ArrayBuffer): string` — btoa + replace `+/` por `-_` + strip `=`.
       - `async function subscribePush(vapidPublicKey: string): Promise<{ endpoint, p256dh, auth } | null>`:
         - `navigator.serviceWorker.ready` → `pushManager.subscribe({ userVisibleOnly: true, applicationServerKey: urlBase64ToUint8Array(vapidPublicKey) })`
         - Extrair `subscription.getKey('p256dh')` e `getKey('auth')` via `arrayBufferToBase64Url` → return `{ endpoint, p256dh, auth }`. Se getKey retornar null → return null.
       - `async function unsubscribePush(): Promise<{ endpoint: string } | null>`:
         - `navigator.serviceWorker.ready` → `getSubscription()` → se null return null → senão guardar endpoint, chamar `subscription.unsubscribe()`, return `{ endpoint }`.
       - JSDoc curto em cada helper.

    2. **`src/lib/query-keys.ts`** — adicionar dentro do objeto `QUERY_KEYS` (alfabetical ou no final do block Phase 5):
       ```typescript
       pushSubscriptions: {
         all: ['pushSubscriptions'] as const,
         byPerfil: (perfilId: string) => ['pushSubscriptions', perfilId] as const,
       },
       ```

    Rodar testes:
    ```
    npm test -- src/lib/__tests__/pwa.test.ts --reporter=verbose
    ```
    Esperado: todos os casos behavior viram GREEN (com push-mocks aplicado).
  </action>
  <verify>
    <automated>npm test -- src/lib/__tests__/pwa.test.ts --reporter=verbose 2>&1 | tail -20 && grep -cE "canSubscribePush|isStandalone|isIOS|urlBase64ToUint8Array|subscribePush|unsubscribePush|pushSubscriptions" src/lib/pwa.ts src/lib/query-keys.ts</automated>
  </verify>
  <done>pwa.ts exporta 7 helpers/tipos; query-keys.ts tem pushSubscriptions block; testes scaffold viram GREEN. Grep >= 7 ocorrências combinadas. Per D-13.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.2: `src/hooks/usePushSubscriptions.ts` (query + useSubscribePush + useUnsubscribePush mutations)</name>
  <read_first>
    - src/hooks/usePreferenciasNotif.ts (LINHAS INTEIRAS — pattern de query+mutation com optimistic update + rollback)
    - src/hooks/useReenviarNotificacao.ts (pattern de mutation com toast)
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§3 Hook usePushSubscriptions — pseudocódigo + Pitfall 7 upsert ON CONFLICT)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (§src/hooks/usePushSubscriptions.ts)
    - src/hooks/__tests__/usePushSubscriptions.test.tsx (scaffold RED Plan 01)
    - src/lib/pwa.ts (Task 4.1 — assinatura subscribePush/unsubscribePush)
    - src/lib/query-keys.ts (Task 4.1 — QUERY_KEYS.pushSubscriptions)
    - src/types/index.ts (Plan 01 Task 1.5 — PushSubscriptionRow)
  </read_first>
  <behavior>
    - `usePushSubscriptions(perfilId)` retorna query `PushSubscriptionRow[]` filtrada por perfil (enabled apenas se perfilId truthy)
    - `useSubscribePush()` mutation:
      - Recebe nada (lê VAPID public key de `import.meta.env.VITE_VAPID_PUBLIC_KEY` + perfilId do contexto auth)
      - Chama `subscribePush(VITE_VAPID_PUBLIC_KEY)` do pwa.ts
      - Se retorno null (getKey falhou) → throw com mensagem clara "Falha ao gerar chaves de subscription"
      - `supabase.from('push_subscriptions').upsert({ perfil_id, endpoint, p256dh, auth, user_agent: navigator.userAgent }, { onConflict: 'perfil_id,endpoint' })` (Pitfall 7)
      - onSuccess: invalidate `QUERY_KEYS.pushSubscriptions.byPerfil(perfilId)` + toast.success("Notificações push ativadas")
      - onError: toast.error(e.message ?? 'Erro ao ativar push')
    - `useUnsubscribePush()` mutation:
      - Recebe `{ endpoint }` opcional — se omit, chama `unsubscribePush()` do pwa.ts pra obter endpoint do browser
      - `supabase.from('push_subscriptions').delete().eq('perfil_id', perfilId).eq('endpoint', endpoint)`
      - onSuccess: invalidate cache + toast.success("Notificações push desativadas neste device")
      - onError: toast.error
    - Testes Plan 01 Task 1.1 viram GREEN
  </behavior>
  <action>
    Criar `src/hooks/usePushSubscriptions.ts` com 3 exports espelhando `usePreferenciasNotif.ts` pattern:

    1. `usePushSubscriptions(perfilId: string | null | undefined)`:
       - `useQuery<PushSubscriptionRow[]>({ queryKey: perfilId ? QUERY_KEYS.pushSubscriptions.byPerfil(perfilId) : QUERY_KEYS.pushSubscriptions.all, enabled: !!perfilId, queryFn: async () => { ... select ... return data ?? [] } })`

    2. `useSubscribePush()`:
       - Recebe `perfilId` na mutateAsync (mais clean que reading auth context internamente). Assinatura: `useMutation<void, Error, { perfilId: string }>`.
       - mutationFn:
         ```typescript
         const vapidKey = import.meta.env.VITE_VAPID_PUBLIC_KEY
         if (!vapidKey) throw new Error('VAPID public key não configurada (VITE_VAPID_PUBLIC_KEY)')
         const sub = await subscribePush(vapidKey)
         if (!sub) throw new Error('Falha ao gerar chaves de subscription')
         const { error } = await supabase.from('push_subscriptions').upsert(
           { perfil_id: perfilId, endpoint: sub.endpoint, p256dh: sub.p256dh, auth: sub.auth, user_agent: navigator.userAgent.slice(0, 500) },
           { onConflict: 'perfil_id,endpoint' }
         )
         if (error) throw error
         ```
       - onSuccess + onError padrão Phase 5 (toast.success/error + invalidate).

    3. `useUnsubscribePush()`:
       - Assinatura: `useMutation<void, Error, { perfilId: string; endpoint?: string }>`.
       - mutationFn:
         ```typescript
         let endpoint = vars.endpoint
         if (!endpoint) {
           const r = await unsubscribePush()
           endpoint = r?.endpoint
         } else {
           // Apenas DB delete — caller já fez unsubscribe via DevTools (smoke) ou queremos limpar row órfã
         }
         if (!endpoint) throw new Error('Subscription não encontrada')
         const { error } = await supabase
           .from('push_subscriptions')
           .delete()
           .eq('perfil_id', vars.perfilId)
           .eq('endpoint', endpoint)
         if (error) throw error
         ```

    Rodar tests:
    ```
    npm test -- src/hooks/__tests__/usePushSubscriptions.test.tsx --reporter=verbose
    ```
  </action>
  <verify>
    <automated>npm test -- src/hooks/__tests__/usePushSubscriptions.test.tsx --reporter=verbose 2>&1 | tail -20 && grep -cE "useSubscribePush|useUnsubscribePush|onConflict|VITE_VAPID_PUBLIC_KEY" src/hooks/usePushSubscriptions.ts</automated>
  </verify>
  <done>3 hooks exportados; testes scaffold viram GREEN; grep >= 4 ocorrências. Per D-01 (upsert onConflict), D-07 (VAPID), Pitfall 7.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4.3: Criar `src/components/me/InstalarAppCard.tsx` + mount em `MeEspacoPage`</name>
  <read_first>
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§7 Install Banner skeleton — `beforeinstallprompt` handler + iOS Safari branch)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (§src/components/me/InstalarAppCard.tsx — estrutura visual card NotificacoesPanel)
    - src/components/me/NotificacoesPanel.tsx (header com ícone 10×10 + `#0089ac` BG — copiar estilo)
    - src/components/me/PerfilPanel.tsx (estrutura de card de aba MeEspaco)
    - src/pages/MeEspacoPage.tsx (estrutura de tabs — onde mountar)
    - src/lib/pwa.ts (Task 4.1 — isStandalone, isIOS, canSubscribePush)
  </read_first>
  <action>
    1. **`src/components/me/InstalarAppCard.tsx`** — novo componente. Lógica:
       - State: `const [deferredPrompt, setDeferredPrompt] = useState<BeforeInstallPromptEvent | null>(null)` e `const [installed, setInstalled] = useState(isStandalone())`.
       - useEffect com 2 listeners (RESEARCH §7 + Pitfall 9):
         - `'beforeinstallprompt'`: `e.preventDefault()`, `setDeferredPrompt(e)`.
         - `'appinstalled'`: `setInstalled(true); setDeferredPrompt(null)`.
         - Cleanup: removeEventListener.
       - **Early return** se `installed === true` (return null — Pitfall 9 + D-09).
       - **Render conditional** baseado em ambiente:
         - **Caso A — Chrome com `deferredPrompt`:** card com ícone `<Download />` (lucide), título "Instale o CRM como app", subtítulo "Receba notificações push e acesse direto da home screen", botão "Instalar app" que chama `await deferredPrompt.prompt(); const { outcome } = await deferredPrompt.userChoice; if (outcome === 'accepted') setInstalled(true); setDeferredPrompt(null)`.
         - **Caso B — iOS Safari não-standalone (`isIOS() && !isStandalone()`):** card com ícone `<Smartphone />` ou `<Share />`, título "Instale o CRM no seu iPhone", subtítulo + lista numerada PT-BR:
           - "1. Toque no botão **Compartilhar** (ícone de quadrado com seta) na barra do Safari."
           - "2. Role e toque em **Adicionar à Tela de Início**."
           - "3. Toque **Adicionar** no canto superior direito."
           - "Pronto! Abra o ícone CONSEJ da sua tela inicial para ativar notificações push."
         - **Caso C — Chrome sem deferredPrompt + não-iOS (raro):** card estático com instrução "Use o menu do seu navegador → Instalar app" (texto curto).
       - Estilo: copiar `bg-card rounded-2xl border p-6` + header com ícone 10×10 em `#0089ac` (mesmo padrão NotificacoesPanel:71-86).

    2. **`src/pages/MeEspacoPage.tsx`** — mount `<InstalarAppCard />` ACIMA do `<NotificacoesPanel />` na tab Notificações. O `InstalarAppCard` faz early-return se já standalone, então não polui a tab quando irrelevante.

    NÃO criar onboarding modal (D-09 deferido). NÃO mexer em `InstallAppBanner` (Plan 05 cuida disso — banner topo é separado do card).
  </action>
  <verify>
    <automated>grep -cE "BeforeInstallPromptEvent|isStandalone|isIOS|deferredPrompt|appinstalled|Adicionar à Tela|InstalarAppCard" src/components/me/InstalarAppCard.tsx src/pages/MeEspacoPage.tsx</automated>
  </verify>
  <done>InstalarAppCard existe com 3 branches (Chrome deferred / iOS Safari / fallback); mount em MeEspacoPage; grep >= 5 ocorrências combinadas. Per D-09.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4.4: Estender `NotificacoesPanel.tsx` (matriz 4×2 → 4×3, iOS gate D-13, permission flow D-12)</name>
  <read_first>
    - src/components/me/NotificacoesPanel.tsx (LINHAS INTEIRAS — atual matriz 4×2 + lógica de salvarPrefs)
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§3 Integração com NotificacoesPanel.tsx + R-L5 lógica explícita)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (MODIFIED NotificacoesPanel — diff conceitual)
    - .planning/phases/06-pwa-push-notifications/06-CONTEXT.md (D-12 permission flow, D-13 iOS gate)
    - src/components/ui/tooltip.tsx (signature do Tooltip + TooltipProvider)
    - src/lib/pwa.ts (Task 4.1 — canSubscribePush)
    - src/hooks/usePushSubscriptions.ts (Task 4.2 — useSubscribePush, useUnsubscribePush)
  </read_first>
  <action>
    Editar `src/components/me/NotificacoesPanel.tsx`:

    1. **Imports**: adicionar `import { canSubscribePush } from '@/lib/pwa'`, `import { useSubscribePush, useUnsubscribePush, usePushSubscriptions } from '@/hooks/usePushSubscriptions'`, `import { Smartphone } from 'lucide-react'`, `import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/components/ui/tooltip'`.

    2. **Estado local**:
       - `const pushGate = useMemo(() => canSubscribePush(), [])` — calcula uma vez no mount; troca-se quando user instala app (re-render natural por navegação OU listenar `appinstalled` se necessário).
       - `const subs = usePushSubscriptions(perfilId)` — saber se já tem subscription neste browser (informativo, não bloqueia toggle).
       - `const subscribeMutation = useSubscribePush()` + `const unsubscribeMutation = useUnsubscribePush()`.

    3. **Mudar grid layout** (linha ~95 atual + linhas de mapping):
       - Header: `grid-cols-[1fr_auto_auto_auto] gap-6 items-center px-3 py-2`. Adicionar 3ª coluna span com `<span className="w-14 text-center">Push</span>` (ou ícone `<Smartphone />` + label).
       - Cada `TIPOS.map(t => ...)` row: ` grid-cols-[1fr_auto_auto_auto]`. Adicionar 3ª `<div className="w-14 flex justify-center">` com `<Switch>` ligado a `draft[t.id].push`.

    4. **iOS Gate (D-13)** — quando `pushGate.ok === false && pushGate.reason === 'ios-not-standalone'`:
       - Envolver os 4 switches Push em `<Tooltip>`/`<TooltipTrigger>`/`<TooltipContent>` (Radix). Trigger: `<div className="..."><Switch disabled ... /></div>`. Content (PT-BR): "Instale o app primeiro: toque em Compartilhar → Adicionar à Tela na barra do Safari."
       - Quando `pushGate.reason === 'denied'`: switch fica disabled + tooltip "Você bloqueou notificações. Reative em Configurações do navegador → Notificações."
       - Quando `pushGate.reason === 'unsupported'`: switch disabled + tooltip "Seu navegador não suporta push."
       - Quando `pushGate.ok === true`: switch enabled.

    5. **Permission flow inline (D-12, R-L5)** — substituir o `onCheckedChange` default do switch Push para o flow async com rollback:
       ```typescript
       async function handlePushToggle(tipo: TipoNotif, next: boolean) {
         // Caso A: desligar — não precisa permission, só atualiza draft
         if (!next) {
           setDraft(prev => ({ ...prev, [tipo]: { ...prev[tipo], push: false } }))
           return
         }

         // Caso B: ligar pela 1ª vez (ainda sem permission)
         if (Notification.permission === 'default') {
           setDraft(prev => ({ ...prev, [tipo]: { ...prev[tipo], push: true } })) // optimistic
           try {
             const perm = await Notification.requestPermission()
             if (perm !== 'granted') {
               toast.error('Permissão negada')
               setDraft(prev => ({ ...prev, [tipo]: { ...prev[tipo], push: false } })) // rollback
               return
             }
             // Subscribe e persiste
             await subscribeMutation.mutateAsync({ perfilId })
             // Persiste prefs será feito no botão Salvar global do panel (mantém UX existente)
           } catch (e) {
             toast.error(e instanceof Error ? e.message : 'Erro ao ativar push')
             setDraft(prev => ({ ...prev, [tipo]: { ...prev[tipo], push: false } }))
           }
           return
         }

         // Caso C: já tem permission granted — só atualiza draft; se ainda não tem sub no DB, cria
         if (Notification.permission === 'granted') {
           if (subs.data && subs.data.length === 0) {
             try {
               await subscribeMutation.mutateAsync({ perfilId })
             } catch (e) {
               toast.error(e instanceof Error ? e.message : 'Erro ao ativar push')
               return // sem rollback porque draft ainda não mudou
             }
           }
           setDraft(prev => ({ ...prev, [tipo]: { ...prev[tipo], push: true } }))
         }
         // Caso D: permission='denied' → switch já está disabled via iOS gate render, não chega aqui
       }
       ```
       Bind no onCheckedChange do switch Push: `onCheckedChange={(v) => handlePushToggle(t.id, v)}`.

    6. **Manter `Salvar Preferências` flow** (botão atual): quando usuário clica Salvar, `useSalvarPrefs` (existing) faz UPDATE do JSONB inteiro — incluindo o campo `push` para cada tipo (que o draft já reflete). Nenhuma mudança no Salvar logic.

    7. **Acessibilidade**: cada switch Push tem `aria-label={\`${t.label} via Push\`}`. TooltipContent tem `role="tooltip"`.

    NÃO mudar layout das colunas Slack/Email (devem continuar funcionando idênticas). NÃO criar coluna master switch (D-03).
  </action>
  <verify>
    <automated>grep -cE "canSubscribePush|useSubscribePush|grid-cols-\\[1fr_auto_auto_auto\\]|handlePushToggle|TooltipContent|Adicionar à Tela" src/components/me/NotificacoesPanel.tsx</automated>
  </verify>
  <done>NotificacoesPanel tem matriz 4×3, iOS gate via Tooltip, permission flow async com rollback, grep >= 5 ocorrências. Per D-12, D-13, R-L5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (authenticated) → push_subscriptions INSERT | Cliente cria sua própria row via RLS |
| Browser → Notification permission prompt | Native OS UX; cliente não controla resposta |
| `import.meta.env.VITE_VAPID_PUBLIC_KEY` → bundled JS | Public key é por design exposta no client (não secret) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Spoofing | INSERT push_subscriptions com perfil alheio | mitigate | RLS WITH CHECK perfil_id = auth.uid (migration 036 Plan 01). Hook usa `perfil_id: perfilId` mas RLS bloqueia se perfilId ≠ auth.uid. |
| T-06-16 | Repudiation | Usuário nega ter ativado push | mitigate | Row em push_subscriptions tem created_at + user_agent — auditável. Coord+ pode SELECT (RLS role-aware). |
| T-06-17 | Spoofing | VAPID public key mismatch entre client e server | mitigate | A2 / R-L2: Plan 01 Task 1.3 validação manual VITE_VAPID_PUBLIC_KEY === VAPID_PUBLIC_KEY. Run-time error claro ('VAPID public key não configurada') se var não setada. |
| T-06-18 | Tampering | XSS no user_agent string | accept | user_agent salvo em `text` column, lido só por coord+ debug. slice(0, 500) em Task 4.2 evita storage attack via UA gigante. |
| T-06-19 | Spoofing (R-L5) | Switch Push em estado inconsistente após permission denied | mitigate | handlePushToggle (Task 4.4) faz rollback explícito do draft quando requestPermission retorna != 'granted'. |
</threat_model>

<verification>
- Testes scaffold Plan 01 viraram GREEN: pwa.test.ts + usePushSubscriptions.test.tsx — Tasks 4.1 e 4.2
- Tipos compile-check: `npx tsc -b --noEmit` exit 0
- Manual UAT (Plan 05 final ou aqui):
  - Chrome desktop: toggle Push 1º vez → prompt permission → aceitar → row em DB
  - Chrome: toggle Push OFF → row mantida (D-03) → re-ON → sem novo prompt
  - iOS Safari não-standalone: 4 switches Push disabled + tooltip
  - iOS standalone (após Install): toggles enabled normalmente
- RLS smoke: criar 2 perfis, cada um cria 1 row, perfil A não vê row do perfil B (Plan 01 Task 1.1 RLS test cobre)
</verification>

<success_criteria>
Wave 3 (parte UI subscribe) completo quando:
- `src/lib/pwa.ts` (7 exports) + tests GREEN
- `src/hooks/usePushSubscriptions.ts` (3 exports) + tests GREEN
- `src/lib/query-keys.ts` tem pushSubscriptions
- `NotificacoesPanel` shipped como 4×3 com iOS gate + R-L5 permission flow
- `InstalarAppCard` mount em MeEspacoPage tab Notificações
- Smoke manual: subscribe via UI persistiu em DB (replaceando Plan 03 Task 3.4 manual subscribe via DevTools)
</success_criteria>

<output>
Criar `.planning/phases/06-pwa-push-notifications/06-04-SUMMARY.md` documentando:
- Quantos testes scaffold Plan 01 viraram GREEN (X de Y)
- Manual UAT subscribe via UI (passo a passo + screenshot/descrição)
- Eventual iOS testing (sim/não + iOS version + Safari version)
- Surpresas no fluxo de permission (e.g., Chrome agrupou prompts diferentemente)
- Confirmação que Phase 5 Slack+Email continuam funcionando na matriz 4×3 (sem regressão visual)
</output>
