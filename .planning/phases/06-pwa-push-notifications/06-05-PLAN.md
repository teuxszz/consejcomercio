---
phase: 06-pwa-push-notifications
plan: 05
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/components/layout/InstallAppBanner.tsx
  - src/components/layout/AppLayout.tsx
  - src/pages/TarefasPage.tsx
  - src/pages/IndicacoesPage.tsx
autonomous: false
requirements:
  - PUSH-01
  - PUSH-04

must_haves:
  truths:
    - "Em Chrome desktop/Android (não-standalone, não dismissed), `InstallAppBanner` aparece no topo do CRM após `beforeinstallprompt` ser capturado (D-09)"
    - "Click no botão 'Instalar app' do banner dispara `deferredPrompt.prompt()` nativo do Chrome → instala app"
    - "Click no botão dismiss (X) seta `localStorage.consej_install_banner_dismissed = '1'` e remove banner permanentemente (pattern consej_kanban_hint — Open Question 6)"
    - "Quando `appinstalled` ou `display-mode: standalone` é detectado, banner some automaticamente (Pitfall 9)"
    - "iOS Safari NÃO dispara `beforeinstallprompt` → banner não aparece (usuário cai no `InstalarAppCard` em /me/preferencias)"
    - "AppLayout escuta `navigator.serviceWorker` `message` event → quando recebe `{ type: 'PUSH_NAVIGATE', url }` valida same-origin E chama `navigate(pathname + search)` (PUSH-04 deep link com tab CRM aberta)"
    - "TarefasPage detecta `?highlight=<tarefaId>` em searchParams → scroll + abre TarefaModal (PUSH-04 happy path para tarefa)"
    - "IndicacoesPage detecta `?highlight=<indicacaoId>` → scroll/highlight (PUSH-04 happy path para indicação)"
    - "Rotas existentes `/leads/<id>` e `/contratos/<id>?tab=renovacao` continuam funcionando sem mudança (já cobrem PUSH-04 via React Router default)"
  artifacts:
    - path: "src/components/layout/InstallAppBanner.tsx"
      provides: "Banner topo CRM com beforeinstallprompt capture, install button, dismiss localStorage, hide on standalone"
      contains: "consej_install_banner_dismissed"
      min_lines: 50
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Mount InstallAppBanner no topo do <main> + listener PUSH_NAVIGATE com same-origin validation"
      contains: "InstallAppBanner"
    - path: "src/pages/TarefasPage.tsx"
      provides: "highlight searchParam handler — abre modal/scroll na tarefa"
      contains: "highlight"
    - path: "src/pages/IndicacoesPage.tsx"
      provides: "highlight searchParam handler"
      contains: "highlight"
  key_links:
    - from: "src/components/layout/AppLayout.tsx"
      to: "public/sw.js (Plan 02 — postMessage PUSH_NAVIGATE)"
      via: "navigator.serviceWorker.addEventListener('message', handler)"
      pattern: "PUSH_NAVIGATE"
    - from: "src/pages/TarefasPage.tsx (highlight handler)"
      to: "src/components/tarefas/TarefaModal (existing)"
      via: "useSearchParams().get('highlight') + setState para abrir modal"
      pattern: "searchParams.get..highlight"
---

<objective>
Install banner topo CRM (D-09) + SW deep link wiring no AppLayout + page hooks para `?highlight=<id>` (PUSH-04 happy path). Fecha as últimas peças visíveis da phase: usuário em Chrome vê banner discreto pra instalar, e clicar push abre a entidade exata.

Purpose: completar PUSH-01 (banner install — D-09) + completar PUSH-04 (deep link em rotas com modal/highlight — D-14). Após este plan, end-to-end push está clickable; UAT pode validar device → CRM tab navega corretamente.
Output: 1 componente novo (InstallAppBanner) + 3 edits (AppLayout mount + listener, TarefasPage handler, IndicacoesPage handler). Rotas `/leads/<id>` e `/contratos/<id>` não exigem edit (React Router já navega pra rota dedicada).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pwa-push-notifications/06-CONTEXT.md
@.planning/phases/06-pwa-push-notifications/06-RESEARCH.md
@.planning/phases/06-pwa-push-notifications/06-PATTERNS.md
@.planning/phases/06-pwa-push-notifications/06-02-SUMMARY.md
@.planning/phases/06-pwa-push-notifications/06-03-SUMMARY.md
@.planning/phases/06-pwa-push-notifications/06-04-SUMMARY.md
@src/components/layout/AppLayout.tsx
@src/components/shared/QuotaResendBanner.tsx
@src/components/leads/KanbanBoard.tsx
@src/pages/TarefasPage.tsx
@src/pages/IndicacoesPage.tsx
@src/pages/LeadsPage.tsx
@src/lib/pwa.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 5.1: Criar `src/components/layout/InstallAppBanner.tsx` (beforeinstallprompt + dismiss localStorage)</name>
  <read_first>
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§7 Install Banner — fluxo beforeinstallprompt completo, Pitfall 9, Pitfall 10)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (§InstallAppBanner analog: QuotaResendBanner + KanbanBoard localStorage dismiss)
    - src/components/shared/QuotaResendBanner.tsx (estilo banner CONSEJ — rounded-lg border bg amber)
    - src/components/leads/KanbanBoard.tsx (linhas 22, 94 — pattern dismiss consej_kanban_hint)
    - src/lib/pwa.ts (isStandalone)
  </read_first>
  <action>
    Criar `src/components/layout/InstallAppBanner.tsx`:

    1. **Types**: declarar `interface BeforeInstallPromptEvent extends Event { prompt(): Promise<void>; userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }> }` no topo do arquivo (não está no TS lib default em todos os configs).

    2. **State**:
       ```typescript
       const [deferredPrompt, setDeferredPrompt] = useState<BeforeInstallPromptEvent | null>(null)
       const [dismissed, setDismissed] = useState(() => localStorage.getItem('consej_install_banner_dismissed') === '1')
       const [installed, setInstalled] = useState(() => isStandalone())
       ```

    3. **useEffect** (RESEARCH §7):
       ```typescript
       const handler = (e: Event) => {
         e.preventDefault()
         setDeferredPrompt(e as BeforeInstallPromptEvent)
       }
       const onInstalled = () => {
         setInstalled(true)
         setDeferredPrompt(null)
       }
       window.addEventListener('beforeinstallprompt', handler)
       window.addEventListener('appinstalled', onInstalled)
       return () => {
         window.removeEventListener('beforeinstallprompt', handler)
         window.removeEventListener('appinstalled', onInstalled)
       }
       ```
       Cleanup obrigatório (StrictMode duplo mount em dev pode duplicar listeners).

    4. **Early return** se `installed || dismissed || !deferredPrompt`. Em iOS Safari (não dispara `beforeinstallprompt`) banner naturalmente não aparece — usuário cai em `InstalarAppCard` (Plan 04 — porta sempre aberta).

    5. **Render** (estilo banner CONSEJ — adaptar QuotaResendBanner style com cor `--cyan-hi` ou `#0089ac` — alinha com NotificacoesPanel ícone):
       ```jsx
       <div role="region" aria-label="Instalação do app"
            className="flex items-center gap-3 rounded-lg border px-4 py-2 text-sm"
            style={{ background: 'rgba(0,137,172,0.10)', borderColor: 'rgba(0,137,172,0.35)', color: 'var(--cyan-hi, #0089ac)' }}>
         <Download className="w-5 h-5 shrink-0" />
         <div className="flex-1">
           <p className="font-medium">Instale o CONSEJ CRM como app</p>
           <p className="text-xs opacity-90">Receba notificações push e abra direto da home screen.</p>
         </div>
         <Button size="sm" onClick={handleInstallClick}>Instalar</Button>
         <button onClick={handleDismiss} aria-label="Fechar" className="p-1">
           <X className="w-4 h-4" />
         </button>
       </div>
       ```

    6. **Handlers**:
       ```typescript
       async function handleInstallClick() {
         if (!deferredPrompt) return
         await deferredPrompt.prompt()
         const { outcome } = await deferredPrompt.userChoice
         setDeferredPrompt(null)
         // Se accepted, `appinstalled` listener vai disparar e setar installed=true
         // Se dismissed, deixar prompt re-fire em sessão futura (Chrome lembra; não persistir dismiss extra aqui)
       }
       function handleDismiss() {
         setDismissed(true)
         localStorage.setItem('consej_install_banner_dismissed', '1')
       }
       ```

    7. **Imports**: `useState`, `useEffect` (React), `Download`, `X` (lucide-react), `Button` (`@/components/ui/button`), `isStandalone` (`@/lib/pwa`).

    Pitfall 10 (macOS Safari install via Dock — não dispara `beforeinstallprompt`): aceitar como known limitation; banner não aparece pra essa minoria; macOS Safari rodando em browser normal ainda recebe push (16+ permite sem PWA).
  </action>
  <verify>
    <automated>grep -cE "beforeinstallprompt|appinstalled|deferredPrompt|consej_install_banner_dismissed|handleInstallClick|isStandalone" src/components/layout/InstallAppBanner.tsx</automated>
  </verify>
  <done>InstallAppBanner exporta componente com early-return triple (installed/dismissed/no prompt), listeners limpos, install + dismiss handlers. Grep >= 5 ocorrências. Per D-09, Pitfall 9.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.2: Mount banner + listener PUSH_NAVIGATE em `AppLayout.tsx` (deep link com tab CRM aberta)</name>
  <read_first>
    - src/components/layout/AppLayout.tsx (LINHA INTEIRA — pattern atual: Sidebar + main + GlobalSearch + OnboardingWizard + Toaster)
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§8 Deep Link Routing — AppLayout listener pattern + R-S7 same-origin validation)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (MODIFIED AppLayout — mount banner + useEffect listener)
    - public/sw.js (Plan 02 — postMessage shape PUSH_NAVIGATE)
  </read_first>
  <action>
    Editar `src/components/layout/AppLayout.tsx`:

    1. **Import**: `import { InstallAppBanner } from './InstallAppBanner'` (Task 5.1) + `import { useNavigate } from 'react-router-dom'` (se ainda não importado).

    2. **Listener useEffect** — adicionar logo após o useEffect existente de auth:
       ```typescript
       const navigate = useNavigate()
       useEffect(() => {
         if (!('serviceWorker' in navigator)) return
         const handler = (e: MessageEvent) => {
           if (e.data?.type !== 'PUSH_NAVIGATE' || typeof e.data.url !== 'string') return
           try {
             const url = new URL(e.data.url)
             // R-S7 / T-06-07: validar same-origin antes de navegar
             if (url.origin !== window.location.origin) {
               console.warn('[AppLayout] PUSH_NAVIGATE com origem externa ignorado:', e.data.url)
               return
             }
             navigate(url.pathname + url.search)
           } catch {
             console.warn('[AppLayout] PUSH_NAVIGATE com URL malformada:', e.data.url)
           }
         }
         navigator.serviceWorker.addEventListener('message', handler)
         return () => navigator.serviceWorker.removeEventListener('message', handler)
       }, [navigate])
       ```

    3. **Mount banner** — adicionar `<InstallAppBanner />` no topo de `<main>`, ANTES do `<div key={location.pathname} ...>`:
       ```jsx
       <main className="flex-1 overflow-y-auto bg-background">
         <div className="px-6 pt-3"><InstallAppBanner /></div>
         <div key={location.pathname} className="p-6 animate-in fade-in duration-150">
           <Outlet />
         </div>
       </main>
       ```
       O banner naturalmente não renderiza nada (return null) quando irrelevante, então não polui rotas sem o `beforeinstallprompt` event.

    NÃO mexer no resto do JSX (Sidebar, GlobalSearch, OnboardingWizard, Toaster intactos).
  </action>
  <verify>
    <automated>grep -cE "InstallAppBanner|PUSH_NAVIGATE|serviceWorker.addEventListener|url.origin.*window.location.origin|useNavigate" src/components/layout/AppLayout.tsx</automated>
  </verify>
  <done>AppLayout mount InstallAppBanner + listener PUSH_NAVIGATE com same-origin guard + cleanup. Grep >= 4 ocorrências. Per D-09, D-14, T-06-07.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.3: Page hooks `?highlight=<id>` em TarefasPage e IndicacoesPage</name>
  <read_first>
    - src/pages/TarefasPage.tsx (LINHA INTEIRA — pattern atual de useSearchParams, lista de tarefas, modal existente)
    - src/pages/IndicacoesPage.tsx (LINHA INTEIRA — idem)
    - src/pages/LeadsPage.tsx (linhas 60-80 — pattern canônico de useSearchParams para `status` e drill-down; copiar style)
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§8 Deep Link Routing — pseudo-code TarefasPage)
    - .planning/phases/06-pwa-push-notifications/06-CONTEXT.md (D-14 deep link routing strategy)
  </read_first>
  <action>
    Editar 2 páginas:

    **TarefasPage.tsx:**
    1. Imports: garantir `import { useSearchParams } from 'react-router-dom'` e `useEffect` de React.
    2. Adicionar logo após `useTarefas` query existente:
       ```typescript
       const [searchParams, setSearchParams] = useSearchParams()
       const highlightId = searchParams.get('highlight')

       useEffect(() => {
         if (!highlightId || !tarefas) return
         const tarefa = tarefas.find(t => t.id === highlightId)
         if (!tarefa) return
         // Opção A — abrir modal se exists; senão scroll
         setSelectedTarefa?.(tarefa)
         setModalOpen?.(true)
         // Scroll fallback (caso modal não exista, ou ambos)
         requestAnimationFrame(() => {
           document.getElementById(`tarefa-${tarefa.id}`)?.scrollIntoView({
             behavior: 'smooth',
             block: 'center',
           })
         })
         // Limpa o highlight da URL após processar (evita loop em re-render)
         const next = new URLSearchParams(searchParams)
         next.delete('highlight')
         setSearchParams(next, { replace: true })
       }, [highlightId, tarefas])
       ```
       **NOTA:** se TarefasPage atual não tem `setSelectedTarefa`/`setModalOpen` (i.e., não tem modal nativo), apenas implementar scroll + adicionar `id={\`tarefa-${t.id}\`}` no element root de cada card de tarefa (pequena edit no map). Inspecionar arquivo durante implementação e adaptar.

    3. Adicionar `id={\`tarefa-${tarefa.id}\`}` no wrapper de cada card de tarefa renderizado (se não existir já).

    **IndicacoesPage.tsx:**
    1. Idêntico: useSearchParams + useEffect com `searchParams.get('highlight')`, find na lista de indicações, scroll para `#indicacao-<id>` (ou abrir modal/drawer se existir), clear search param.

    2. Adicionar `id={\`indicacao-${ind.id}\`}` no wrapper de cada item.

    Rotas que **NÃO precisam de page hook** (já navegam por rota dedicada via React Router — funcionam out-of-box):
    - `/leads/<id>` → LeadDetailPage (existing) — SW abre URL → React Router monta page
    - `/contratos/<id>?tab=renovacao` → ContratoDetailPage com tab handler existente (verificar — se não tem tab handler, criar como em ContratosPage Phase 4; mas escopo Phase 6 considera "tab=renovacao" como hint, não bloqueante)

    NÃO mudar lógica de query/filter existente das páginas. NÃO criar modal novo se não existe — scroll + visual highlight (ring-2 ring-primary) é suficiente como fallback.
  </action>
  <verify>
    <automated>grep -cE "searchParams.get..highlight|tarefa-.*\\.\\.id|indicacao-.*\\.\\.id" src/pages/TarefasPage.tsx src/pages/IndicacoesPage.tsx</automated>
  </verify>
  <done>TarefasPage + IndicacoesPage reagem a `?highlight=<id>` (scroll/modal + clear param). Grep >= 2 ocorrências combinadas. Per D-14, PUSH-04.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.4: UAT manual completo — 8 itens VALIDATION.md em devices reais</name>
  <files>all-phase-files (e2e validation)</files>
  <action>Gabriel executa 8 cenarios do 06-VALIDATION.md em iOS Safari 16.4+ e Chrome Android reais (install + permission + delivery + iOS gate + 410 cleanup + multi-device + cache-control + banner), depois deleta supabase/functions/_smoke-push.</action>
  <verify><automated>echo Manual gate. See resume-signal for 8/8 checklist</automated></verify>
  <done>pelo menos 6 de 8 UAT items pass; fails documentados como tech-debt; _smoke-push deletado; build prod green; Phase 5 sem regressao.</done>
  <what-built>
    Phase 6 inteiro: PWA installable + push subscribe + entrega multi-canal + deep link clickable. Esta é a verificação final que cobre os 8 manual UAT items do `06-VALIDATION.md` antes de fechar a phase.
  </what-built>
  <how-to-verify>
    Executar os 8 cenários do `.planning/phases/06-pwa-push-notifications/06-VALIDATION.md §Manual-Only Verifications` em sequência. Marcar cada um pass/fail com observações.

    **1. PWA install Chrome Android (PUSH-01):**
    - Abrir prod URL no Chrome Android
    - Após ~30s engajamento: banner "Instalar app" aparece OU menu Chrome → Instalar
    - Tocar Instalar → app aparece no drawer
    - Abrir do drawer = display-mode standalone (sem barra Chrome)

    **2. PWA install iOS Safari 16.4+ (PUSH-01):**
    - iPhone Safari ≥16.4 abre prod URL
    - Em /me/preferencias → `InstalarAppCard` mostra instrução "Compartilhar → Adicionar à Tela"
    - Seguir passo a passo → ícone CONSEJ na home
    - Abrir = standalone

    **3. Push permission request triggered by toggle (PUSH-02, D-12):**
    - Em standalone PWA (Chrome Android OU iOS)
    - /me/preferencias → tocar 1º Switch Push (ex: tarefa)
    - Dialog nativo do browser aparece
    - Caso A: Aceitar → toggle ON + row em push_subscriptions visível em Supabase
    - Caso B: Recusar → toggle volta a OFF + toast PT-BR "Permissão negada"

    **4. Real push delivery iOS lockscreen (PUSH-03, PUSH-04):**
    - iOS PWA com push ON pra tarefa
    - Outro usuário (ou via SQL/Studio) atribui tarefa pra você (INSERT em tarefas)
    - notify-tarefa dispara via trigger; em 10-60s push aparece no lockscreen
    - Tocar push → PWA abre direto em /tarefas?highlight=<id> + modal/scroll na tarefa

    **5. Real push delivery Chrome Android lockscreen (PUSH-03, PUSH-04):**
    - Idem #4 para Android Chrome PWA

    **6. iOS gate desabilita toggle quando Safari não-standalone (D-13):**
    - iPhone Safari NÃO instalado (apenas abrir prod URL)
    - /me/preferencias → 4 switches Push aparecem disabled
    - Hover/tap mostra tooltip "Instale o app primeiro: Compartilhar → Adicionar à Tela"

    **7. Subscription DELETE no 410 Gone (D-02):**
    - Subscribe device A via Plan 04 UI
    - DevTools → Application → Service Workers → unsubscribe via `(await navigator.serviceWorker.ready).pushManager.getSubscription().then(s => s.unsubscribe())`
    - Disparar notify-tarefa pra esse perfil
    - Em SQL: `SELECT COUNT(*) FROM push_subscriptions WHERE perfil_id = '<id>'` retorna 0
    - `SELECT status, error_msg FROM notificacoes_envios WHERE canal='push' ORDER BY created_at DESC LIMIT 1` mostra status='failed' + error_msg contém "410"

    **8. Multi-device subscription (D-01):**
    - Login no Chrome desktop + Chrome Android com mesmo perfil
    - Subscribe em ambos
    - SQL: `SELECT id, endpoint, user_agent FROM push_subscriptions WHERE perfil_id = '<id>'` retorna 2 rows distintas
    - Disparar notify-tarefa → ambos devices recebem push

    **Adicionalmente (R-L4 / banner / housekeeping):**
    - `curl -I https://consej-crm-v2.vercel.app/sw.js` retorna `cache-control: public, max-age=0, must-revalidate`
    - InstallAppBanner aparece em Chrome desktop não-standalone com `beforeinstallprompt`
    - Dismiss persiste após reload

    **Cleanup pós-UAT:**
    - Deletar `supabase/functions/_smoke-push` (Plan 01 Task 1.4 placeholder) via `supabase functions delete _smoke-push`
    - Confirmar nada do código referencia `_smoke-push` (`grep -r _smoke-push src/ supabase/`)
  </how-to-verify>
  <resume-signal>Digite "phase6-uat-passed" com lista de itens 1-8 (pass/fail/skip) + observações sobre tempo de delivery e fricção UX. Se algum falhar, descrever e indicar se é blocker ou tech-debt.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SW postMessage → AppLayout navigate | URL string vindo do SW pode ser atacker-controlled se payload original o foi |
| BeforeInstallPromptEvent → `prompt()` | Evento global window; libs externas teoricamente podem hijackear se compartilharem window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | postMessage PUSH_NAVIGATE com URL externa → open redirect via React Router | mitigate | AppLayout listener (Task 5.2) valida `new URL(e.data.url).origin === window.location.origin` antes de navigate. Defense-in-depth: SW já valida (Plan 02 Task 2.2). |
| T-06-20 | Tampering | URL malformada no postMessage causa throw em URL constructor | mitigate | Task 5.2 wraps `new URL(...)` em try/catch com warn log; ignora message inválida em vez de crashar AppLayout. |
| T-06-21 | Information Disclosure | Banner expõe que app tem PWA install (irrelevante) | accept | Banner é UI público — nada sensível exposto. |
| T-06-22 | Repudiation | Dismiss localStorage permite usuário "esquecer" que viu banner | accept | localStorage é per-device por design; é UX, não auditoria. |
</threat_model>

<verification>
- Manual UAT 8/8 (ou ≥6/8 com fail documentado como tech-debt para Phase 7+) — Task 5.4
- `_smoke-push` removido pós-UAT — Task 5.4 cleanup
- `vercel.json` headers ativos em prod (curl -I) — Task 5.4
- Banner aparece + dismiss persiste — Task 5.4 + UAT item
- AppLayout listener funcional + R-S7 testado — Task 5.4 item 4/5 (click push → navega)
- TarefasPage / IndicacoesPage scroll + highlight visual ao receber `?highlight=` — Task 5.4 item 4
- Build prod sucede sem TS errors — `npm run build` exit 0
</verification>

<success_criteria>
Wave 3 + Phase 6 completos quando:
- InstallAppBanner shipped e ativo em Chrome com beforeinstallprompt
- AppLayout monta banner + listener PUSH_NAVIGATE
- TarefasPage + IndicacoesPage handle `?highlight=<id>`
- 8 UAT manual items executados (passa ou tech-debt documentado)
- `_smoke-push` deletado
- `vercel.json` headers verificados ativos em prod
- Frontend + backend end-to-end push validado em 2 devices reais (iOS PWA + Chrome Android) — PUSH-01..04 todos satisfeitos
- Phase 5 não regrediu (Slack + Email continuam disparando paralelo a Push)
</success_criteria>

<output>
Criar `.planning/phases/06-pwa-push-notifications/06-05-SUMMARY.md` documentando:
- Resultado dos 8 UAT items (X/8 pass, lista failed com causa)
- Tempo médio de delivery push observed (range em segundos por device)
- Surpresas UX (banner intrusivo? tooltip iOS confuso? scroll abrupto?)
- Decisão de cleanup `_smoke-push` (deleted ou kept-for-debug)
- Eventual tech-debt para Phase 7+ (e.g., macOS Safari Pitfall 10 não mitigado)
- Confirmation final que Definition of Done EMAIL+PUSH (REQUIREMENTS.md) está OK pra PUSH-01..04
</output>
