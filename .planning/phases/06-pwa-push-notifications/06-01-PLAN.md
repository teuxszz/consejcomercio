---
phase: 06-pwa-push-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/036_push_subscriptions.sql
  - src/types/index.ts
  - supabase/functions/_shared/perfis.ts
  - supabase/functions/_smoke-push/index.ts
  - tests/rls/push_subscriptions.test.ts
  - src/lib/__tests__/pwa.test.ts
  - src/hooks/__tests__/usePushSubscriptions.test.tsx
  - supabase/functions/_shared/__tests__/push.test.ts
  - src/test/push-mocks.ts
  - vitest.setup.ts
autonomous: false
requirements:
  - PUSH-01
  - PUSH-02
  - PUSH-03
user_setup:
  - service: vapid
    why: "Web Push (VAPID) — Phase 6 D-07. Pré-requisito antes de qualquer envio de push funcionar end-to-end."
    env_vars:
      - name: VAPID_PUBLIC_KEY
        source: "`npx web-push generate-vapid-keys` (rodar local 1x) → setar como Supabase Secret"
      - name: VAPID_PRIVATE_KEY
        source: "mesma execução `npx web-push generate-vapid-keys` → setar como Supabase Secret"
      - name: VAPID_SUBJECT
        source: "valor fixo `mailto:comunicacao.consej@gmail.com` → setar como Supabase Secret"
      - name: VITE_VAPID_PUBLIC_KEY
        source: "mesmo valor de VAPID_PUBLIC_KEY (R-L2) → setar no Vercel env (production + preview)"
    dashboard_config:
      - task: "Setar secrets no Supabase: `supabase secrets set VAPID_PUBLIC_KEY=... VAPID_PRIVATE_KEY=... VAPID_SUBJECT=mailto:comunicacao.consej@gmail.com`"
        location: "Terminal local (Supabase CLI já configurada) OU Supabase Studio → Project Settings → Edge Functions → Secrets"
      - task: "Setar VITE_VAPID_PUBLIC_KEY no Vercel"
        location: "Vercel Dashboard → consej-crm-v2 → Settings → Environment Variables → Production + Preview"

must_haves:
  truths:
    - "Migration 036 aplicada em produção (tabela `push_subscriptions` existe, CHECK em `notificacoes_envios.canal` aceita 'push', trigger `handle_new_user` insere `push: false`, perfis existentes têm `push: false` em todos os 4 tipos)"
    - "`web-push@3.6.7` carrega e gera VAPID JWT em Supabase Edge Runtime via `esm.sh` OU fallback `npm:` validado (R-L1 resolvido)"
    - "Wave 0 test scaffolds existem e falham com mensagem clara (RED) — toda task subsequente tem teste pra rodar antes do GREEN"
    - "`handle_new_user` em migration 036 preserva o lookup `SELECT slack_user_id IS NOT NULL INTO has_slack` herdado de migration 035 — sem regressão da decisão smart-default Phase 5 D-04"
  artifacts:
    - path: "supabase/migrations/036_push_subscriptions.sql"
      provides: "tabela push_subscriptions, ALTER CHECK canal, handle_new_user atualizado (corpo 035 + push:false), backfill"
      contains: "CREATE TABLE IF NOT EXISTS push_subscriptions"
    - path: "src/types/index.ts"
      provides: "CanalNotif inclui 'push', PreferenciasNotif tem campo `push` em cada tipo, PushSubscriptionRow exportado"
      contains: "type CanalNotif = 'email' | 'slack' | 'push'"
    - path: "supabase/functions/_shared/perfis.ts"
      provides: "PreferenciasTipo com campo `push: boolean`"
      contains: "push: boolean"
    - path: "supabase/functions/_smoke-push/index.ts"
      provides: "Smoke-test edge function que carrega web-push e chama setVapidDetails + generateVAPIDKeys (R-L1 gate)"
    - path: "src/test/push-mocks.ts"
      provides: "mockServiceWorker, mockPushManager, mockNotification, mockMatchMediaStandalone helpers"
    - path: "tests/rls/push_subscriptions.test.ts"
      provides: "RLS regression suite scaffolds (RED — pré-implementação)"
  key_links:
    - from: "supabase/migrations/036_push_subscriptions.sql"
      to: "supabase/functions/_shared/push.ts (Plan 03)"
      via: "tabela push_subscriptions + CHECK canal expandido"
      pattern: "push_subscriptions|canal IN \\('email','slack','push'\\)"
    - from: "src/types/index.ts (CanalNotif/PreferenciasNotif)"
      to: "src/components/me/NotificacoesPanel.tsx (Plan 04) + supabase/functions/_shared/perfis.ts"
      via: "TypeScript imports"
      pattern: "PreferenciasNotif|CanalNotif"
---

<objective>
Foundation + de-risk: aplicar a migration 036 (schema base do Phase 6), estender types compartilhados (CanalNotif + PreferenciasNotif + PushSubscriptionRow), atualizar PreferenciasTipo no helper de edge functions, e **smoke-testar `esm.sh/web-push@3.6.7` em Supabase Edge Runtime cedo** (R-L1 — Deno node:crypto + ECDH polyfill). Além disso, criar todos os scaffolds de teste (Wave 0) com RED inicial para que cada plan subsequente tenha gate de teste pronto antes de implementar.

Purpose: derrubar o risco técnico mais alto da phase (web-push em Deno) ANTES de qualquer código depender de `sendPush`, e estabelecer o ground truth (schema + types + test scaffolds) que Plans 02-05 vão consumir em paralelo.
Output: Migration 036 em prod, types estendidos, helper `perfis.ts` atualizado, edge function `_smoke-push` provando que web-push roda no edge runtime, 5 arquivos de teste com casos esperados em RED, mocks de browser APIs para vitest, e relatório claro no SUMMARY se foi necessário fallback para `npm:web-push@3.6.7`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-pwa-push-notifications/06-CONTEXT.md
@.planning/phases/06-pwa-push-notifications/06-RESEARCH.md
@.planning/phases/06-pwa-push-notifications/06-PATTERNS.md
@.planning/phases/06-pwa-push-notifications/06-VALIDATION.md
@supabase/migrations/035_notificacoes_envios.sql
@supabase/migrations/011_auto_profile_trigger.sql
@supabase/migrations/029_rls_role_aware.sql
@src/types/index.ts
@supabase/functions/_shared/perfis.ts
@supabase/functions/_shared/email.ts
@tests/rls/notificacoes_envios.test.ts
@src/test/supabase-mock.ts
@vitest.setup.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Wave 0 scaffolds — test files (RED) + browser API mocks</name>
  <read_first>
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§Validation Architecture, Wave 0 Gaps)
    - .planning/phases/06-pwa-push-notifications/06-VALIDATION.md (Wave 0 Requirements, Manual-Only Verifications)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (§`src/test/push-mocks.ts` analog + tests/rls scaffolding)
    - src/test/supabase-mock.ts (factory pattern de mocks)
    - vitest.setup.ts (current setup — só importa jest-dom/vitest)
    - tests/rls/notificacoes_envios.test.ts (analog para RLS scaffold se existir, senão tests/rls/preferencias_notif.test.ts)
  </read_first>
  <behavior>
    - Test scaffolds existem mas FALHAM (RED) com mensagens descritivas
    - `vitest.setup.ts` exporta mocks de Notification, navigator.serviceWorker, PushManager, matchMedia
    - `src/test/push-mocks.ts` exporta `mockNotificationPermission(state)`, `mockPushManager({ subscribed, endpoint? })`, `mockServiceWorkerRegistration()`, `mockMatchMediaStandalone(boolean)`
    - Test descrições refletem comportamento esperado de PUSH-01..04 (não placeholder "todo")
  </behavior>
  <action>
    Criar 5 arquivos de teste como scaffold (`it.todo` ou `it` com `expect(true).toBe(false)` + mensagem clara explicando o que será testado). Os scaffolds DEVEM derivar dos casos listados em 06-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map) e 06-VALIDATION.md §Wave 0 Requirements:

    1. `src/lib/__tests__/pwa.test.ts`: casos `urlBase64ToUint8Array round-trips com arrayBufferToBase64Url`, `canSubscribePush() retorna 'unsupported' sem PushManager`, `canSubscribePush() retorna 'ios-not-standalone' em UA iOS + display-mode browser`, `canSubscribePush() retorna 'denied' quando Notification.permission === 'denied'`, `isStandalone() detecta display-mode: standalone via matchMedia`, `isIOS() detecta /iPad|iPhone|iPod/ no UA` — todos `it.todo()` ou com falha esperada.

    2. `src/hooks/__tests__/usePushSubscriptions.test.tsx`: casos `useSubscribePush upsert em push_subscriptions com onConflict 'perfil_id,endpoint'`, `useUnsubscribePush DELETE WHERE perfil_id AND endpoint`, `usePushSubscriptions query retorna lista do perfil`, `optimistic update reverte em erro`. Usar `createSupabaseMock` (style de `src/test/supabase-mock.ts`).

    3. `supabase/functions/_shared/__tests__/push.test.ts`: casos `sendPush retorna skipped_no_subscription quando perfil sem sub`, `sendPush retorna skipped_idempotent em 23505`, `sendPush 410 → DELETE push_subscriptions row + status='failed'`, `sendPush 404 → mesmo cleanup`, `sendPush multi-device agrega delivered/failed/cleaned`, `sendPush 5xx não deleta row`.

    4. `tests/rls/push_subscriptions.test.ts`: copiar bootstrap inteiro de `tests/rls/notificacoes_envios.test.ts` (ou `preferencias_notif.test.ts` se 035 não tem RLS suite ainda) — mesmo skipIf, env vars, login helper. Casos: `consultor SELECT só vê próprias`, `diretor SELECT vê todas via is_at_least`, `INSERT com perfil_id != auth.uid bloqueado por WITH CHECK`, `DELETE só próprias`, `upsert ON CONFLICT(perfil_id,endpoint) atualiza last_seen_at`.

    5. **PULAR** `tests/e2e/push-install-banner.spec.ts` desta fase — Wave 0 da VALIDATION.md lista 5 arquivos mas o E2E exige PWA real instalado; cair pra Manual UAT (06-VALIDATION.md já lista). Documentar essa decisão no SUMMARY.

    6. Estender `vitest.setup.ts` adicionando stubs globais (mas SEM ativar — só registrar): export const `installPushMocks = (opts) => { ... }` que pode ser chamado por testes que precisam. Não polui setup global porque outros testes podem checar ausência.

    7. Criar `src/test/push-mocks.ts` espelhando `src/test/supabase-mock.ts` style: factory functions retornando objetos `vi.fn`-based para `mockNotificationPermission(state: 'default'|'granted'|'denied')`, `mockPushManager({ subscribed: boolean; endpoint?: string; subscribeFails?: boolean })`, `mockServiceWorkerRegistration()`, `mockMatchMediaStandalone(standalone: boolean)`. Cada helper instala mocks em `globalThis` / `window` / `navigator` e retorna função de teardown.

    Não implementar lógica real — só scaffolds. Confirmar com `npm test 2>&1 | grep -E '(pwa|usePushSubscriptions|push.test|push_subscriptions)'` que aparecem como suítes pendentes/red.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/lib/__tests__/pwa.test.ts src/hooks/__tests__/usePushSubscriptions.test.tsx supabase/functions/_shared/__tests__/push.test.ts tests/rls/push_subscriptions.test.ts 2>&1 | grep -E "(todo|pending|fail)" | head -20</automated>
  </verify>
  <done>5 arquivos de teste (4 + push-mocks) existem; `npm test` mostra suítes pendentes/red sem crashes de parse; `src/test/push-mocks.ts` exporta os 4 helpers documentados; `vitest.setup.ts` continua importando jest-dom/vitest sem regressão.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.2: Migration 036 — push_subscriptions + ALTER CHECK canal + handle_new_user + backfill</name>
  <read_first>
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§9 Migration 036 — código completo, incluindo notas técnicas críticas sobre ALTER CONSTRAINT, jsonb_set, CREATE OR REPLACE)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (§036_push_subscriptions.sql — header style + RLS + trigger update)
    - supabase/migrations/035_notificacoes_envios.sql (FONTE CANÔNICA do corpo de `handle_new_user` — copiar daqui, NÃO de 011)
    - supabase/migrations/011_auto_profile_trigger.sql (referência histórica APENAS — NÃO usar como template do corpo)
    - supabase/migrations/029_rls_role_aware.sql (função SQL `is_at_least(role)` que será chamada nas policies)
  </read_first>
  <action>
    Criar `supabase/migrations/036_push_subscriptions.sql` implementando D-01, D-04, D-08, D-15. Estrutura exata em 06-RESEARCH.md §9 — copiar literalmente o SQL com 5 seções comentadas:

    1. **Header comentado** (estilo migration 035): "Migration 036: Push Subscriptions + canal 'push' — Phase 6 fundação para Web Push notifications (D-01, D-08, D-15). ⚠️ INSERT/UPDATE em push_subscriptions feitos via cliente authenticated (RLS); DELETE em 410 via service_role nas edge functions."

    2. **CREATE TABLE push_subscriptions** com schema D-01 exato: `id uuid PK DEFAULT gen_random_uuid()`, `perfil_id uuid NOT NULL REFERENCES perfis(id) ON DELETE CASCADE`, `endpoint text NOT NULL`, `p256dh text NOT NULL`, `auth text NOT NULL`, `user_agent text`, `last_seen_at timestamptz NOT NULL DEFAULT now()`, `created_at timestamptz NOT NULL DEFAULT now()`, `UNIQUE (perfil_id, endpoint)`. Adicionar `CREATE INDEX IF NOT EXISTS idx_push_subs_perfil ON push_subscriptions (perfil_id)`.

    3. **RLS** com 4 policies (mirror pattern 035 linhas 130-133 + role-aware 029):
       - `push_subs_select FOR SELECT TO authenticated USING (perfil_id = auth.uid() OR public.is_at_least('coordenador'))`
       - `push_subs_insert FOR INSERT TO authenticated WITH CHECK (perfil_id = auth.uid())`
       - `push_subs_update FOR UPDATE TO authenticated USING (perfil_id = auth.uid()) WITH CHECK (perfil_id = auth.uid())` — necessário pra upsert ON CONFLICT atualizar last_seen_at
       - `push_subs_delete FOR DELETE TO authenticated USING (perfil_id = auth.uid())` — usuário pode unsubscribe; service_role nas edge functions bypassa RLS pra cleanup 410
       - Cada policy precedida de `DROP POLICY IF EXISTS` (idempotência da migration)
       - `ALTER TABLE push_subscriptions ENABLE ROW LEVEL SECURITY`

    4. **ALTER notificacoes_envios.canal CHECK** (D-08): `ALTER TABLE notificacoes_envios DROP CONSTRAINT IF EXISTS notificacoes_envios_canal_check; ALTER TABLE notificacoes_envios ADD CONSTRAINT notificacoes_envios_canal_check CHECK (canal IN ('email','slack','push'))`. Postgres não suporta `ALTER CONSTRAINT CHECK` — DROP + ADD é o pattern canônico (06-RESEARCH.md §9 notas).

    5. **CREATE OR REPLACE FUNCTION handle_new_user()** (D-04, D-15): copiar **literalmente** o corpo da função `handle_new_user` de `supabase/migrations/035_notificacoes_envios.sql` (linhas ~163-220 — NÃO da migration 011, que **não tem** o lookup `SELECT slack_user_id IS NOT NULL INTO has_slack` e reintroduziria a regressão D-04 corrigida na Phase 5). A única alteração permitida é adicionar `'push', false` em cada `jsonb_build_object` do default (4 tipos: tarefa/cadencia/renovacao/indicacao) — preservar todo o resto (DECLARE has_slack, SELECT lookup, smart-default condicional ao slack_user_id, INSERT em perfis com role/tipo/preferencias_notif) sem alteração. Trigger `on_auth_user_created` continua apontando — não recriar trigger.

    6. **Backfill** (D-15): UPDATE em `perfis` usando `jsonb_set` aninhado pra adicionar `push: false` em cada um dos 4 tipos. Cláusula WHERE garante idempotência: `WHERE NOT (preferencias_notif #> '{tarefa,push}') IS NOT NULL` — só roda em perfis ainda sem `push`.

    Não adicionar GRANT EXECUTE (sem RPCs novas — subscribe é INSERT direto). Validar localmente que o SQL parsa: `cat supabase/migrations/036_push_subscriptions.sql | grep -c "CREATE TABLE"` deve retornar 1, `grep -c "POLICY"` >= 4.
  </action>
  <verify>
    <automated>grep -E "CREATE TABLE IF NOT EXISTS push_subscriptions|UNIQUE \(perfil_id, endpoint\)|public.is_at_least|canal IN \('email','slack','push'\)|CREATE OR REPLACE FUNCTION public.handle_new_user|'push', false|jsonb_set" supabase/migrations/036_push_subscriptions.sql | grep -v "^--" | grep -v "^#" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Migration 036 criada com 5 seções (CREATE TABLE + 4 RLS policies + ALTER CHECK + handle_new_user replace + backfill).
    - Verify grep retorna >= 7 ocorrências de tokens críticos (fora de linhas de comentário SQL `--`).
    - **Anti-regressão D-04 (Phase 5):** lookup `SELECT slack_user_id IS NOT NULL INTO has_slack` PRESERVADO no corpo da função.
      Comando: `grep -c "SELECT slack_user_id IS NOT NULL INTO has_slack" supabase/migrations/036_push_subscriptions.sql`
      Esperado: `1` (uma ocorrência exata — herdada de 035, não removida).
    - Migration ainda não aplicada (Task 1.3 aplica).
    - Per D-01, D-04, D-08, D-15.
  </acceptance_criteria>
  <done>Migration 036 criada com 5 seções. Verify grep >= 7. Anti-regressão grep do lookup `slack_user_id IS NOT NULL INTO has_slack` retorna exatamente 1. Migration ainda não aplicada. Per D-01, D-04, D-08, D-15.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1.3 [BLOCKING]: Aplicar migration 036 + setar Supabase Secrets VAPID + Vercel env</name>
  <files>supabase/migrations/036_push_subscriptions.sql (apply only)</files>
  <action>Gabriel aplica manualmente: (a) supabase db push -p PASSWORD OU copy-paste em Supabase Studio SQL Editor; (b) npx web-push generate-vapid-keys e seta VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT como Supabase Secrets; (c) seta VITE_VAPID_PUBLIC_KEY no Vercel env (production + preview) com mesmo valor do VAPID_PUBLIC_KEY (R-L2).</action>
  <verify><automated>echo Manual gate. Verification via SQL queries and supabase secrets list</automated></verify>
  <done>SQL retorna table_ok=true, CHECK contem push, 5 sample perfis push=false; 3 supabase VAPID secrets + Vercel VITE_VAPID_PUBLIC_KEY setados.</done>
  <what-built>
    Migration 036 criada em Task 1.2. Schema DEVE estar aplicado em produção antes de qualquer task que dependa da tabela `push_subscriptions` (Plan 03 `_shared/push.ts`, Plan 04 hook+UI) e antes do smoke-test edge function (1.4). VAPID secrets também são pré-requisito.
  </what-built>
  <how-to-verify>
    **Parte A — aplicar migration 036:**

    Tentativa 1 (CLI — preferida): no terminal do projeto rodar `supabase db push -p "$env:SUPABASE_DB_PASSWORD"` (PowerShell). Se ainda não tem SUPABASE_DB_PASSWORD seteado e tech-debt da STATE.md não resolveu: pegar password em https://supabase.com/dashboard/project/wfnriqwkzdazdbuzbyug/settings/database e usar inline.

    Tentativa 2 (fallback documentado — STATE.md tech-debt): copiar conteúdo de `supabase/migrations/036_push_subscriptions.sql` e colar em https://supabase.com/dashboard/project/wfnriqwkzdazdbuzbyug/sql/new, executar.

    Após aplicar, validar no SQL Editor:
    ```sql
    SELECT to_regclass('public.push_subscriptions') IS NOT NULL AS table_ok;
    SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'notificacoes_envios_canal_check';
    SELECT preferencias_notif #> '{tarefa,push}' AS push_default FROM perfis LIMIT 5;
    ```
    Esperado: `table_ok = true`, CHECK contém `'push'`, todas as linhas retornam `false` (nunca null).

    **Parte B — setar VAPID secrets (D-07):**

    1. Gerar keypair: `npx web-push generate-vapid-keys` (rodar uma vez — anota Public + Private)
    2. Supabase:
       ```
       supabase secrets set VAPID_PUBLIC_KEY="<public>"
       supabase secrets set VAPID_PRIVATE_KEY="<private>"
       supabase secrets set VAPID_SUBJECT="mailto:comunicacao.consej@gmail.com"
       ```
    3. Vercel: dashboard → consej-crm-v2 → Settings → Environment Variables → criar `VITE_VAPID_PUBLIC_KEY` (Production + Preview), valor = EXATAMENTE o mesmo Public usado no Supabase (R-L2 — mismatch = todos os pushes 403).

    Confirmar:
    ```
    supabase secrets list | grep VAPID
    ```
    Deve mostrar 3 chaves VAPID_*.

    Vercel: verificar variável aparece em `vercel env ls production`.
  </how-to-verify>
  <resume-signal>Digite "applied" depois que migration 036 estiver em produção E os 3 Supabase Secrets E o VITE_VAPID_PUBLIC_KEY do Vercel estiverem setados. Se algo deu errado (erro SQL, password Supabase não bate, etc), cole o erro pra eu ajudar.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1.4: Smoke-test edge function `_smoke-push` — R-L1 gate (esm.sh vs npm web-push)</name>
  <read_first>
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§Risks R-L1 + §Standard Stack + §sendPush Helper — mitigation steps)
    - .planning/phases/06-pwa-push-notifications/06-CONTEXT.md (D-06 esm.sh + D-07 VAPID_*)
    - supabase/functions/_shared/email.ts (header com `@ts-nocheck` + import style esm.sh)
    - supabase/functions/notify-tarefa/index.ts (boot pattern de edge function — `Deno.serve`/`serve` + json helper)
  </read_first>
  <action>
    Criar **edge function** `supabase/functions/_smoke-push/index.ts` cujo único papel é validar R-L1 (web-push@3.6.7 carrega + setVapidDetails funciona + generateVAPIDKeys retorna par válido no Deno runtime de produção):

    1. Header `@ts-nocheck` (pattern email.ts), import via `https://esm.sh/web-push@3.6.7` (D-06 locked) e leitura de `Deno.env.get('VAPID_PUBLIC_KEY'|'VAPID_PRIVATE_KEY'|'VAPID_SUBJECT')`.
    2. Endpoint que responde GET → `{ ok, library_loaded, set_vapid_details_ok, generated_keys_ok, public_key_length, errors: [] }`. Cada passo dentro de try/catch — registrar erro mas não derrubar response (queremos diagnóstico, não 500 cru).
    3. Implementar 3 verificações:
       - **A:** import resolveu (`typeof webpush.setVapidDetails === 'function'`)
       - **B:** `webpush.setVapidDetails(VAPID_SUBJECT, VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY)` não lança
       - **C:** `webpush.generateVAPIDKeys()` retorna objeto com `publicKey` (base64url, length entre 80 e 95) e `privateKey` (length entre 40 e 50)
    4. Retornar 200 com diagnóstico mesmo em falha parcial (precisamos do shape do erro). Apenas crash de boot → 500.

    Deployar: `supabase functions deploy _smoke-push --no-verify-jwt` (não precisa auth pra smoke), depois `curl https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/_smoke-push -H "apikey: <SUPABASE_ANON_KEY>"`. Logar resposta completa.

    **Decisão de fallback (R-L1):**
    - Se response.ok === true E todos os 3 passos === true → **manter `https://esm.sh/web-push@3.6.7`** em `_shared/push.ts` do Plan 03. Documentar no SUMMARY: "R-L1 ESM resolved — esm.sh aceito".
    - Se qualquer passo === false (especialmente ECDH em generateVAPIDKeys ou setVapidDetails) → **trocar import para `npm:web-push@3.6.7`** em `_smoke-push/index.ts`, redeploy, re-curl. Se passar com `npm:`, Plan 03 usa `npm:`. Documentar no SUMMARY: "R-L1 ESM failed (motivo X) — fallback `npm:web-push@3.6.7` validado e adotado". Atualizar 06-RESEARCH.md §Open Questions / Assumptions Log A1 marcando como "resolved → npm:".

    **NÃO deletar `_smoke-push` ao final da phase.** Manter deployed como ferramenta de diagnóstico para R-L1 (re-validação on-demand caso esm.sh quebre em update futuro do Deno runtime ou do web-push). Comentar no header: "_smoke-push: mantido como diagnóstico R-L1 — redeploy on-demand se web-push esm.sh falhar. Validado em <data>. Não deletar."
  </action>
  <verify>
    <automated>curl -sS "https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/_smoke-push" -H "apikey: $env:VITE_SUPABASE_ANON_KEY" 2>&1 | grep -E '"library_loaded":\s*true|"set_vapid_details_ok":\s*true|"generated_keys_ok":\s*true' | wc -l</automated>
  </verify>
  <done>Função `_smoke-push` deployed; curl retorna 3 booleans em true (esm.sh OK) OU foi feito fallback para `npm:` e curl retorna 3 booleans em true. SUMMARY documenta qual variante foi adotada. Plan 03 vai consumir a decisão. Função permanece deployed como ferramenta de diagnóstico.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.5: Extender types (`src/types/index.ts`) + helper `_shared/perfis.ts` PreferenciasTipo</name>
  <read_first>
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§10 Type Extensions + §11 Phase 5 helper updates)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (MODIFIED src/types/index.ts + MODIFIED supabase/functions/_shared/perfis.ts)
    - src/types/index.ts (linhas 430-472 — block Phase 5)
    - supabase/functions/_shared/perfis.ts (linhas 1-30 — interface PreferenciasTipo atual)
    - supabase/functions/_shared/email.ts (consumer de PreferenciasTipo — confirmar que `prefs?.tarefa?.email` continua type-safe após adicionar push)
  </read_first>
  <behavior>
    - `CanalNotif` aceita `'push'` em uniões type-safe (tsc não reclama)
    - `PreferenciasNotif` requer `push: boolean` em cada um dos 4 tipos (consumer que esqueceu de set falha em compile)
    - `PushSubscriptionRow` exportado (não colide com DOM `PushSubscription`)
    - `PreferenciasTipo` em `_shared/perfis.ts` ganha `push: boolean` sem regredir Phase 5 (A6: `@ts-nocheck` + access opcional `?.push` mantêm runtime OK)
    - Build TypeScript local passa: `npx tsc -b --noEmit`
  </behavior>
  <action>
    1. **`src/types/index.ts`**: localizar block Phase 5 (linhas ~432-450). Substituir:
       - `export type CanalNotif = 'email' | 'slack'` → `export type CanalNotif = 'email' | 'slack' | 'push'`
       - Cada chave do `PreferenciasNotif` (tarefa/cadencia/renovacao/indicacao) ganha `push: boolean` no objeto interno. Manter ordem `slack | email | push` pra alinhar com matriz UI (Plan 04).
       - Adicionar imediatamente após `PreferenciasNotif`:
         ```
         /** Row da tabela push_subscriptions (Phase 6 migration 036).
          * Nome com sufixo Row pra não colidir com tipo nativo do DOM PushSubscription. */
         export interface PushSubscriptionRow {
           id: string
           perfil_id: string
           endpoint: string
           p256dh: string
           auth: string
           user_agent: string | null
           last_seen_at: string
           created_at: string
         }
         ```
       Per D-16.

    2. **`supabase/functions/_shared/perfis.ts`**: editar `PreferenciasTipo` adicionando `push: boolean` ao lado de `slack: boolean` e `email: boolean`. Comentário inline: `// push adicionado Phase 6 D-16 — D-04 default false`. Confirmar que o arquivo tem `@ts-nocheck` no topo (A6); se não tiver, verificar se acesso ao push no Plan 03 vai ficar type-safe (provavelmente sim porque consumers usam `prefs?.tarefa?.push === true` que é OK com `boolean | undefined`).

    3. Rodar `npx tsc -b --noEmit` no app config (`tsconfig.app.json`) — confirmar compile passa sem erros. Se algum consumer Phase 5 do `PreferenciasNotif` quebrar porque não fornece `push: false`, atualizar consumer (provável local: testes existentes ou fixtures). Caso comum: `usePreferenciasNotif.ts` apenas lê do DB e o JSONB DB já tem push: false (Plan 01 Task 1.3 backfill), então runtime fica OK; só fixtures de teste precisam de update.

    4. Atualizar os scaffolds do Task 1.1 (`src/lib/__tests__/pwa.test.ts`, `src/hooks/__tests__/usePushSubscriptions.test.tsx`) para importar `PushSubscriptionRow` se aplicável — type-only import; ainda mantém RED no runtime.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>&1 | tail -20 && grep -E "PushSubscriptionRow|'push'|push: boolean" src/types/index.ts supabase/functions/_shared/perfis.ts | grep -v "^#" | wc -l</automated>
  </verify>
  <done>`tsc -b --noEmit` retorna 0 erros. `CanalNotif` inclui 'push', `PreferenciasNotif` tem `push: boolean` nos 4 tipos, `PushSubscriptionRow` exportado, `_shared/perfis.ts PreferenciasTipo` tem `push: boolean`. Grep retorna >= 6 ocorrências combinadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase REST (authenticated INSERT em push_subscriptions) | Cliente autenticado (JWT) cria sua própria subscription |
| Edge function (service_role) → push_subscriptions | DELETE em 410/404 bypassa RLS (legítimo cleanup) |
| Deno edge runtime → CDN (esm.sh/npm) | web-push library carregada de origem externa |
| Migration DDL → produção DB | Schema change irreversível (rollback exige DROP TABLE + reset preferencias_notif) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | VAPID private key | mitigate | Setar APENAS via `supabase secrets set` (Task 1.3) — nunca commitar; `.gitignore` cobre `.env*`. Rotação = regenerar par + setar secrets + clear `push_subscriptions` (todos re-subscrevem). |
| T-06-02 | Information Disclosure | RLS push_subscriptions enumeration | mitigate | RLS USING `perfil_id = auth.uid() OR public.is_at_least('coordenador')` — Task 1.2 implementa as 4 policies. Task 1.1 RLS test scaffolds garantem regressão. |
| T-06-03 | Tampering | Migration 036 backfill | mitigate | Backfill com `WHERE NOT (preferencias_notif #> '{tarefa,push}') IS NOT NULL` é idempotente — re-run não corrompe. Task 1.3 valida via SELECT após apply. |
| T-06-04 | Spoofing | INSERT push_subscriptions com perfil_id alheio | mitigate | RLS WITH CHECK `perfil_id = auth.uid()` — Task 1.1 scaffold cobre, Task 1.2 implementa. |
| T-06-05 | Tampering | `_smoke-push` edge function pública sem JWT | accept | Função descartável só lê env vars (não modifica nada). Riscos: exposição de _length_ de chave VAPID. Aceitável MVP (não vaza key real). Mantida deployed pós-phase como ferramenta diagnóstico R-L1. |
| T-06-SC | Tampering | Supply chain — `web-push@3.6.7` via esm.sh | mitigate | RESEARCH §Package Legitimacy Audit confirma slopcheck [OK], 8+ anos repo, 2.5M dl/sem, no postinstall scripts. R-L1 mitigation = smoke-test cedo (Task 1.4). Versão pinned via URL `@3.6.7` (não `@latest`). |
</threat_model>

<verification>
- Migration 036 aplicada em prod (table_ok, CHECK contém 'push', perfis backfilled com push:false em todos 4 tipos) — Task 1.3 manual SQL queries
- 3 Supabase Secrets setados (`supabase secrets list | grep VAPID` mostra 3) — Task 1.3
- Vercel env `VITE_VAPID_PUBLIC_KEY` setado em production+preview — Task 1.3
- Smoke-test `_smoke-push` retorna `library_loaded, set_vapid_details_ok, generated_keys_ok` todos true; SUMMARY documenta qual variante (`esm.sh` vs `npm:`) foi adotada — Task 1.4
- `npx tsc -b --noEmit` retorna 0 erros — Task 1.5
- 4 arquivos de teste + push-mocks existem com casos descritivos (RED não-vazio) — Task 1.1
- Anti-regressão D-04: lookup `slack_user_id IS NOT NULL INTO has_slack` preservado em migration 036 — Task 1.2 acceptance_criteria
</verification>

<success_criteria>
Wave 1 completo quando:
- Migration 036 em produção (table + CHECK + handle_new_user + backfill verificados via SQL)
- VAPID secrets (3) em Supabase + 1 em Vercel
- Decisão R-L1 documentada no SUMMARY (esm.sh OK ou npm: fallback) — Plan 03 consome
- `src/types/index.ts` + `supabase/functions/_shared/perfis.ts` estendidos sem regressão TypeScript
- 5 arquivos de teste RED + push-mocks shipped — Plans 02-05 podem consumir
- Nenhum task subsequente bloqueado por falta de schema/types/secrets/test-scaffolds
</success_criteria>

<output>
Criar `.planning/phases/06-pwa-push-notifications/06-01-SUMMARY.md` documentando:
- Qual variante web-push foi adotada (`esm.sh` ou `npm:`) — Plan 03 referencia
- Resultado do SQL de verificação pós-migration (table_ok, CHECK def, 5 sample push defaults)
- Que VAPID secrets foram setados (sem expor valores)
- Que test scaffolds existem (lista de arquivos) e quantos casos RED
- Confirmação anti-regressão D-04: grep de `slack_user_id IS NOT NULL INTO has_slack` em 036 retorna 1
- Eventuais surpresas (ex: tsc reclamou de um consumer Phase 5 esquecido — qual e como foi resolvido)
</output>
</output>
