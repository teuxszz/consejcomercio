---
phase: 06-pwa-push-notifications
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - supabase/functions/_shared/push.ts
  - supabase/functions/notify-tarefa/index.ts
  - supabase/functions/notify-resumo-diario/index.ts
  - supabase/functions/notify-indicacao/index.ts
  - supabase/functions/notify-renovacao/index.ts
autonomous: true
requirements:
  - PUSH-03
  - PUSH-04

must_haves:
  truths:
    - "`sendPush(supabase, params)` envia Web Push via web-push@3.6.7 para TODAS as subscriptions ativas do perfil (multi-device D-01), agrega delivered/failed/cleaned, e retorna SendPushResult (PUSH-03 D-05)"
    - "410 Gone OU 404 → row de `push_subscriptions` DELETE + status='failed' em notificacoes_envios (D-02 cleanup)"
    - "Outros erros (5xx, 429, 4xx≠410/404) → status='failed' + error_msg, MAS row mantida (D-02 não-cleanup)"
    - "Idempotência via UNIQUE em notificacoes_envios continua funcionando para canal='push' (D-08 — sem regressão Phase 5)"
    - "`sendPush` retorna `skipped_no_subscription` quando perfil não tem nenhuma sub (não loga em notificacoes_envios — não há o que reenviar)"
    - "4 edge functions (notify-tarefa, notify-resumo-diario, notify-indicacao, notify-renovacao) chamam sendPush em paralelo com sendEmail via Promise.allSettled (resiliência cross-canal — Open Question 1)"
    - "Cada caller respeita `prefs?.{tipo}?.push === true` antes de invocar sendPush (D-03 toggle OFF suprime envio)"
    - "Push NÃO usa fallback diretor (Open Question 2 — silent miss aceitável, evita spam liderança)"
    - "Payload push respeita limites (title ≤ 50 chars, body ≤ 150 chars — Pitfall 4)"
    - "Push não consome quota Resend — `quota_resend_atual()` permanece intacto, filtrando canal='email' (D-08)"
  artifacts:
    - path: "supabase/functions/_shared/push.ts"
      provides: "sendPush helper espelhando _shared/email.ts pattern; SendPushParams, SendPushResult, SendPushStatus tipos exportados"
      contains: "export async function sendPush"
      min_lines: 100
    - path: "supabase/functions/notify-tarefa/index.ts"
      provides: "sendPush em Promise.allSettled com sendEmail + slackPromise"
      contains: "sendPush"
    - path: "supabase/functions/notify-resumo-diario/index.ts"
      provides: "idem para cadência"
      contains: "sendPush"
    - path: "supabase/functions/notify-indicacao/index.ts"
      provides: "idem para indicação"
      contains: "sendPush"
    - path: "supabase/functions/notify-renovacao/index.ts"
      provides: "idem para renovação"
      contains: "sendPush"
  key_links:
    - from: "supabase/functions/_shared/push.ts"
      to: "supabase/migrations/036 push_subscriptions table + notificacoes_envios.canal CHECK"
      via: "supabase.from('push_subscriptions').select + supabase.from('notificacoes_envios').insert({canal:'push'})"
      pattern: "push_subscriptions|canal: 'push'"
    - from: "notify-tarefa/index.ts"
      to: "_shared/push.ts"
      via: "import { sendPush } from '../_shared/push.ts' + Promise.allSettled([slackPromise, pushPromise, ...emailPromises])"
      pattern: "from '\\.\\./_shared/push'|Promise.allSettled"
---

<objective>
Send infrastructure: helper compartilhado `_shared/push.ts` que espelha exatamente `_shared/email.ts` (D-05/D-20 pattern) + integração nas 4 edge functions notify-* via `Promise.allSettled` (Open Question 1 — refactor de resiliência cross-canal). Implementa PUSH-03 (helper, não função dedicada — reinterpretação D-05) e a metade servidor de PUSH-04 (payload com deepLink que SW do Plan 02 vai consumir).

Purpose: backend de push 100% pronto para disparar real-world push para devices subscritos, ANTES da UI Switch existir (Plan 04). Permite Gabriel inserir manualmente uma row de teste em `push_subscriptions` (ou subscribe via DevTools console no Plan 02 PWA) e validar end-to-end servidor → push service → device — desacoplando frontend do servidor.
Output: 1 helper novo + 4 edge functions modificadas. Cada notify-* pode disparar push em paralelo com slack/email, ignorando perfis sem subscription (skipped_no_subscription), respeitando preferences, fazendo cleanup automático em 410/404.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pwa-push-notifications/06-CONTEXT.md
@.planning/phases/06-pwa-push-notifications/06-RESEARCH.md
@.planning/phases/06-pwa-push-notifications/06-PATTERNS.md
@.planning/phases/06-pwa-push-notifications/06-01-SUMMARY.md
@supabase/functions/_shared/email.ts
@supabase/functions/_shared/perfis.ts
@supabase/functions/notify-tarefa/index.ts
@supabase/functions/notify-resumo-diario/index.ts
@supabase/functions/notify-indicacao/index.ts
@supabase/functions/notify-renovacao/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1: Criar `supabase/functions/_shared/push.ts` (sendPush helper espelhando email.ts)</name>
  <read_first>
    - supabase/functions/_shared/email.ts (template canônico inteiro — header @ts-nocheck, env vars, types exportados, INSERT atômico, error 23505 handling, UPDATE final)
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§4 sendPush Helper — código completo com setVapidDetails, Promise.allSettled por sub, 410 cleanup; §Push Service Error Codes table)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (§supabase/functions/_shared/push.ts — divergências documentadas)
    - .planning/phases/06-pwa-push-notifications/06-01-SUMMARY.md (decisão R-L1: import via esm.sh OU npm:)
    - supabase/functions/_shared/__tests__/push.test.ts (scaffold RED de Plan 01 Task 1.1 — casos esperados pra cobrir)
  </read_first>
  <behavior>
    Testes Plan 01 Task 1.1 viram GREEN:
    - `sendPush retorna skipped_no_subscription quando perfil sem sub` (sem INSERT em notificacoes_envios — não há o que reenviar)
    - `sendPush retorna skipped_idempotent em 23505` (UNIQUE constraint)
    - `sendPush 410 → DELETE push_subscriptions row + UPDATE notificacoes_envios status='failed' com error_msg contendo "410"`
    - `sendPush 404 → mesmo cleanup que 410`
    - `sendPush multi-device retorna {delivered: N, failed: M, cleaned: K}` agregado
    - `sendPush 5xx OU 429 → status='failed' MAS NÃO deleta row` (transient error)
    - `sendPush ok=true quando pelo menos 1 sub entregou` (multi-device best effort)
    - `sendPush ok=false E delivered=0 quando todas subs falharam`
  </behavior>
  <action>
    Criar `supabase/functions/_shared/push.ts` seguindo skeleton RESEARCH §4 (linhas 451-605). Structure obrigatória:

    1. **Header comentado** (estilo email.ts): "Helper central de envio de Web Push via VAPID. Espelha 1:1 _shared/email.ts (D-05). Divergências: sem checkQuota (D-08), multi-device fanout (D-01), cleanup 410/404 (D-02). Phase 6 D-05/D-06/D-07."

    2. **`@ts-nocheck` no topo** (mesmo motivo do email.ts — URL imports + Deno.env).

    3. **Imports:**
       - `import type { SupabaseClient } from 'https://esm.sh/@supabase/supabase-js@2'`
       - `import webpush from '<DECIDED IN 06-01-SUMMARY>'` — usar exatamente o specifier validado no smoke-test (Plan 01 Task 1.4). Default esm.sh `https://esm.sh/web-push@3.6.7`. Fallback npm `npm:web-push@3.6.7` se R-L1 fall back.

    4. **Env vars** (D-07):
       ```typescript
       const VAPID_PUBLIC_KEY = Deno.env.get('VAPID_PUBLIC_KEY') ?? ''
       const VAPID_PRIVATE_KEY = Deno.env.get('VAPID_PRIVATE_KEY') ?? ''
       const VAPID_SUBJECT = Deno.env.get('VAPID_SUBJECT') ?? 'mailto:comunicacao.consej@gmail.com'
       webpush.setVapidDetails(VAPID_SUBJECT, VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY)
       ```
       Chamada `setVapidDetails` no module load (não dentro da função) — global no web-push.

    5. **Tipos exportados:**
       - `TipoNotif` = `'tarefa' | 'cadencia' | 'renovacao' | 'indicacao'`
       - `EntidadeTipo` = `'lead' | 'cliente' | 'contrato' | 'tarefa'`
       - `PushPayload`: `{ title: string; body: string; data: { deepLink: string; tipo: TipoNotif; entidadeId: string | null } }` (Claude's Discretion em CONTEXT.md — sem icon/badge no payload; SW usa /icon-192.png como default)
       - `SendPushParams`: `{ perfilId, tipo, entidadeId, entidadeTipo, payload }`
       - `SendPushStatus`: `'queued' | 'failed' | 'skipped_idempotent' | 'skipped_no_subscription'`
       - `SendPushResult`: `{ ok, status, errorMsg?, notificacaoId?, delivered, failed, cleaned }`

    6. **`export async function sendPush(supabase: SupabaseClient, p: SendPushParams): Promise<SendPushResult>`**:
       - Passo 1: SELECT `id, endpoint, p256dh, auth` FROM `push_subscriptions` WHERE `perfil_id = p.perfilId`. Se 0 rows → return `{ ok: true, status: 'skipped_no_subscription', delivered: 0, failed: 0, cleaned: 0 }`. Sem INSERT em notificacoes_envios — não loga (não há o que reenviar; idempotência continua via UNIQUE caso re-run).
       - Passo 2: INSERT atômico em `notificacoes_envios` com `canal: 'push'`, `subject: p.payload.title`, `status: 'queued'`, `.select('id').single<{ id: string }>()`. Se erro com `code === '23505'` → return `{ ok: true, status: 'skipped_idempotent', delivered: 0, failed: 0, cleaned: 0 }`. Se erro outro → return `{ ok: false, status: 'failed', errorMsg, delivered: 0, failed: 0, cleaned: 0 }`.
       - Passo 3: `Promise.allSettled(subs.map(async sub => { try { await webpush.sendNotification({endpoint, keys:{p256dh,auth}}, JSON.stringify(p.payload)); delivered++ } catch(e) { const sc = e?.statusCode ?? 0; if (sc===410||sc===404) { await supabase.from('push_subscriptions').delete().eq('id', sub.id); cleaned++; failed++; errors.push(`sub ${sub.id.slice(0,8)}: ${sc} Gone — DELETED`) } else { failed++; errors.push(`sub ${sub.id.slice(0,8)}: ${sc||e?.message}`) } } }))`. MVP **sem retry** (Open Question 5 — aceitar 1 tentativa; botão Reenviar Phase 5 cobre).
       - Passo 4: UPDATE notificacoes_envios SET `status: delivered>0?'delivered':'failed'`, `delivered_at: delivered>0?new Date().toISOString():null`, `error_msg: errors.slice(0,3).join('; ')||null` WHERE id = notificacaoId.
       - Return: `{ ok: delivered > 0, status: delivered>0?'queued':'failed', errorMsg, notificacaoId, delivered, failed, cleaned }`.

    7. **Payload defensive copy** (R-S4 + Pitfall 4): no início da função, truncar title a 50 chars e body a 150 chars antes de stringify:
       ```typescript
       const safePayload: PushPayload = {
         title: p.payload.title.slice(0, 50),
         body: p.payload.body.slice(0, 150),
         data: p.payload.data,
       }
       ```
       Usar `safePayload` em todos os usos subsequentes (INSERT subject, JSON.stringify para web-push).

    Rodar testes do scaffold (Plan 01 Task 1.1):
    ```
    npm test -- supabase/functions/_shared/__tests__/push.test.ts
    ```
    Devem virar GREEN (>= 6 dos 8 casos comportamentais documentados em behavior).
  </action>
  <verify>
    <automated>npm test -- supabase/functions/_shared/__tests__/push.test.ts --reporter=verbose 2>&1 | tail -25 && grep -cE "skipped_no_subscription|skipped_idempotent|push_subscriptions.*delete|setVapidDetails|allSettled" supabase/functions/_shared/push.ts</automated>
  </verify>
  <done>`push.ts` existe com sendPush exportada implementando 4 passos + safePayload + multi-device aggregate. Testes scaffold viram GREEN (>= 6 casos). Verify grep >= 5 ocorrências.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.2: Integrar sendPush em notify-tarefa + Promise.all → Promise.allSettled</name>
  <read_first>
    - supabase/functions/notify-tarefa/index.ts (LINHA INTEIRA — linhas 1-286; especial atenção 174-285 = bloco de dispatch atual)
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§5 notify-* Integration — código completo, decisão NÃO usar fallback diretor)
    - .planning/phases/06-pwa-push-notifications/06-PATTERNS.md (MODIFIED notify-tarefa/index.ts — diff conceitual)
    - .planning/phases/06-pwa-push-notifications/06-CONTEXT.md (D-03 toggle OFF suprime, D-14 deep link query params)
    - supabase/functions/_shared/push.ts (Task 3.1 — assinatura sendPush)
  </read_first>
  <action>
    Editar `supabase/functions/notify-tarefa/index.ts`:

    1. Adicionar import no topo: `import { sendPush } from '../_shared/push.ts'`. Adicionar import do tipo `SendPushResult` se útil para anotação.

    2. **Antes do dispatch** (após `wantSlack` declaration, linha ~217), adicionar:
       ```typescript
       // Decisão Push (D-03 — toggle OFF apenas suprime; D-05 helper compartilhado).
       // Push NÃO usa fallback diretor (Open Question 2 do RESEARCH §5):
       //   - diretores podem não ter PWA instalado → drop silencioso é OK
       //   - spam de notif p/ liderança a cada tarefa órfã é UX ruim
       //   - skipped_no_subscription é resultado válido e auditável
       const wantPush = prefs?.tarefa?.push === true && !fallbackAcionado

       const pushPayload = wantPush ? {
         title: `Nova tarefa: ${tarefa.titulo}`.slice(0, 50),
         body: `${criadorNome} te atribuiu uma tarefa`.slice(0, 150),
         data: { deepLink: link, tipo: 'tarefa' as const, entidadeId: tarefa.id },
       } : null

       const pushPromise = wantPush
         ? sendPush(supabase, {
             perfilId: novoAtribuido,
             tipo: 'tarefa',
             entidadeId: tarefa.id,
             entidadeTipo: 'tarefa',
             payload: pushPayload!,
           })
         : Promise.resolve({ ok: true, skipped: 'push_off' } as const)
       ```

    3. **Substituir `Promise.all` por `Promise.allSettled`** (RESEARCH §Open Question 1 — refactor oportunista de resiliência cross-canal). Linha ~278 atual:
       ```typescript
       const [slackRes, ...emailResArray] = await Promise.all([slackPromise, ...emailPromises])
       ```
       Vira:
       ```typescript
       const settled = await Promise.allSettled([slackPromise, pushPromise, ...emailPromises])
       const [slackSettled, pushSettled, ...emailSettledArray] = settled
       const unwrap = <T,>(s: PromiseSettledResult<T>): T | { ok: false; error: string } =>
         s.status === 'fulfilled' ? s.value : { ok: false, error: String(s.reason ?? 'unknown') }
       const slackRes = unwrap(slackSettled)
       const pushRes = unwrap(pushSettled)
       const emailResArray = emailSettledArray.map(unwrap)
       ```
       Justificativa em comentário: "Promise.allSettled (Phase 6 Open Question 1) — falha de um canal não aborta os outros."

    4. **Atualizar response JSON** (linha ~280-285) para incluir push:
       ```typescript
       return json({
         ok: slackRes.ok && pushRes.ok && emailResArray.every((e) => (e as { ok: boolean }).ok),
         slack: slackRes,
         push: pushRes,
         email: emailResArray,
         fallback_diretor: fallbackAcionado,
       })
       ```

    NÃO mudar a lógica de fallback diretor existente — ela só afeta email (D-05 Phase 5 mantido). NÃO mudar a forma como `link` é construído (já produz `/tarefas?highlight=<id>` ou `/<entidade_tipo>/<id>`) — perfeito para D-14.

    Validar:
    - `deno check supabase/functions/notify-tarefa/index.ts` (se Deno CLI disponível) OU rodar `supabase functions deploy notify-tarefa --no-verify-jwt` em ambiente de dev.
    - `grep -c "sendPush\|Promise.allSettled\|pushRes" supabase/functions/notify-tarefa/index.ts` >= 4.
  </action>
  <verify>
    <automated>grep -cE "sendPush|Promise.allSettled|pushRes|wantPush|push_off" supabase/functions/notify-tarefa/index.ts</automated>
  </verify>
  <done>notify-tarefa importa sendPush, declara wantPush+pushPayload+pushPromise, usa Promise.allSettled, response inclui campo `push`. Grep >= 5 ocorrências. Per D-05, D-14, Open Question 1+2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.3: Aplicar mesmo pattern em notify-resumo-diario + notify-indicacao + notify-renovacao</name>
  <read_first>
    - supabase/functions/notify-resumo-diario/index.ts (INTEIRO — bloco dispatch atual)
    - supabase/functions/notify-indicacao/index.ts (INTEIRO)
    - supabase/functions/notify-renovacao/index.ts (INTEIRO)
    - supabase/functions/notify-tarefa/index.ts (Task 3.2 — pattern já aplicado, usar como template)
    - .planning/phases/06-pwa-push-notifications/06-RESEARCH.md (§5 Aplicar mesmo pattern em...)
  </read_first>
  <action>
    Replicar EXATAMENTE o pattern do Task 3.2 (sendPush + Promise.allSettled + push no response) em 3 funções, variando apenas: nome do `tipo`, campo de prefs lido, payload title/body, deepLink calculado, entidadeId+entidadeTipo.

    **notify-resumo-diario (cadência):**
    - Tipo: `'cadencia'`
    - Pref: `prefs?.cadencia?.push === true`
    - DeepLink: depende do que a função atualmente passa para email — provavelmente `/leads?highlight=<lead_id>` ou `/cadencia` genérico. Ler código atual e reusar o mesmo `link` que o email usa.
    - Payload title: `Cadência: ${qtdAcoes} ações pendentes hoje` (truncar). Body: nome do consultor ou "Abrir o CRM para ver suas ações de hoje". entidadeId: NULL (cadência é agregada por dia, sem entidade única) OR ID do lead se for per-lead — checar arquivo. entidadeTipo: NULL ou 'lead'.

    **notify-indicacao:**
    - Tipo: `'indicacao'`
    - Pref: `prefs?.indicacao?.push === true`
    - DeepLink: `${APP_URL}/indicacoes?highlight=<indicacao_id>` (D-14)
    - Payload title: `Nova indicação recebida` (truncar). Body: `${nomeIndicado} (${cliente_origem?})`. entidadeId = indicacao.id, entidadeTipo = 'cliente' (consultar tipo correto baseado em código atual).

    **notify-renovacao:**
    - Tipo: `'renovacao'`
    - Pref: `prefs?.renovacao?.push === true`
    - DeepLink: `${APP_URL}/contratos/<contrato_id>?tab=renovacao` (D-14)
    - Payload title: `Renovação próxima: ${nomeCliente}` (truncar). Body: `Vence em ${dias} dias`. entidadeId = contrato.id, entidadeTipo = 'contrato'.

    Em CADA arquivo:
    1. Import sendPush
    2. Declarar wantPush + pushPayload + pushPromise
    3. Trocar `Promise.all([...])` → `Promise.allSettled([...])` + unwrap helper
    4. Response JSON inclui `push: pushRes`
    5. Comentário inline citando "D-05 helper + Open Question 1 allSettled"

    Se alguma função tem **fanout** (1 evento → N perfis, ex: indicacao notifica todos diretores), aplicar sendPush para cada perfilId no fanout — wantPush é checado per-perfil.

    Deploy as 4 functions juntas:
    ```
    supabase functions deploy notify-tarefa notify-resumo-diario notify-indicacao notify-renovacao
    ```
  </action>
  <verify>
    <automated>for f in notify-resumo-diario notify-indicacao notify-renovacao; do echo "=== $f ==="; grep -cE "sendPush|Promise.allSettled|pushRes" "supabase/functions/$f/index.ts"; done</automated>
  </verify>
  <done>Cada uma das 3 functions tem >= 3 ocorrências combinadas (sendPush + allSettled + pushRes). Deploy supabase functions sucede sem erros de parse Deno. Per D-05, D-14.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.4: End-to-end smoke — inserir push_subscription manual + disparar notify-tarefa + observar device</name>
  <read_first>
    - supabase/functions/notify-tarefa/index.ts (Task 3.2 — request shape)
    - .planning/phases/06-pwa-push-notifications/06-02-SUMMARY.md (PWA shell shipped — SW handler funciona)
  </read_first>
  <action>
    Smoke-test manual mas autonomizável que NÃO depende do Plan 04 UI:

    1. **Subscribe via DevTools** (no PWA shipped pelo Plan 02):
       - Abrir CRM em Chrome desktop (preview ou prod).
       - DevTools → Console:
         ```javascript
         const reg = await navigator.serviceWorker.ready
         const sub = await reg.pushManager.subscribe({
           userVisibleOnly: true,
           applicationServerKey: '<COLAR_VITE_VAPID_PUBLIC_KEY_AQUI>' // ou usar urlBase64ToUint8Array se já implementado
         })
         console.log(JSON.stringify(sub.toJSON(), null, 2))
         ```
       - Aceitar prompt de permission.
       - Copiar JSON: `endpoint`, `keys.p256dh`, `keys.auth`.

    2. **Inserir row manual em push_subscriptions** (via Supabase Studio SQL Editor):
       ```sql
       INSERT INTO push_subscriptions (perfil_id, endpoint, p256dh, auth, user_agent)
       VALUES (
         '<seu_perfil_id_de_diretor_uuid>',
         '<endpoint do sub>',
         '<p256dh>',
         '<auth>',
         'manual-smoke-test'
       );
       ```

    3. **Ligar prefs Push** via SQL (não tem UI ainda):
       ```sql
       UPDATE perfis
          SET preferencias_notif = jsonb_set(preferencias_notif, '{tarefa,push}', 'true'::jsonb)
        WHERE id = '<perfil_id>';
       ```

    4. **Disparar notify-tarefa via curl** (mesmo perfil como destinatário):
       ```
       curl -X POST "https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-tarefa" \
         -H "Authorization: Bearer <SERVICE_ROLE_KEY>" \
         -H "x-webhook-secret: $env:WEBHOOK_NOTIFY_TAREFA_SECRET" \
         -H "Content-Type: application/json" \
         -d '{"tarefa_id":"<uuid de tarefa existente atribuída a você>"}'
       ```

    5. **Observar:**
       - Response JSON deve ter campo `push.ok = true`, `push.delivered = 1`, `push.failed = 0`.
       - Notificação OS deve aparecer no device em <10s.
       - Click na notificação abre `/tarefas?highlight=<id>` (PUSH-04 happy path).
       - Em Supabase Studio: `SELECT * FROM notificacoes_envios WHERE canal='push' ORDER BY created_at DESC LIMIT 5` deve ter 1 row com status='delivered', delivered_at não-null.

    6. **Smoke 410 cleanup:**
       - DevTools → Application → Service Workers → unsubscribe via `(await navigator.serviceWorker.ready).pushManager.getSubscription().then(s => s.unsubscribe())`
       - Re-disparar curl da etapa 4 (aumentar minutos no UNIQUE: usar entidade_id diferente, ex: outra tarefa).
       - Esperado: response `push.cleaned >= 1`, `push.failed >= 1`, `push.delivered = 0`. Em SQL Studio: `SELECT COUNT(*) FROM push_subscriptions WHERE perfil_id = '<id>'` retorna 0.

    Documentar resultados no SUMMARY:
    - Push entregou? (sim/não + tempo)
    - Click abriu deep link correto? (sim/não)
    - Cleanup 410 funcionou? (linha DELETED do DB)
    - Eventuais surpresas (e.g., FCM delay alto, payload truncado)
  </action>
  <verify>
    <automated>echo "Manual UAT — see <how-to-verify> in SUMMARY. Verify command intentionally records expectation:" && echo "Expected: response JSON push.delivered=1, notificacoes_envios row status='delivered', cleanup test push.cleaned>=1"</automated>
  </verify>
  <done>SUMMARY documenta E2E smoke (subscribe+insert+curl+observe+cleanup). Push real entregou em device. Cleanup 410 fez DELETE da row. End-to-end backend validado SEM dependência de Plan 04 UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge function (service_role) → push service (FCM/Mozilla/Apple) | sendPush envia VAPID-signed payload encrypted |
| Edge function → push_subscriptions DELETE | service_role bypassa RLS para cleanup |
| user-controlled input (tarefa.titulo, cliente.nome) → push payload body | Pode conter caracteres especiais; payload é texto puro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | VAPID private key em env | mitigate | setVapidDetails lê de Deno.env (não fonte literal); secret nunca commitado. Cobertura herdada Plan 01 Task 1.3. |
| T-06-11 | Spoofing | Push payload spoofing | mitigate | Apenas edge function com VAPID private key consegue enviar — atacante externo não consegue (RFC 8292). web-push@3.6.7 implementa assinatura corretamente. |
| T-06-12 | Tampering | XSS via push body | mitigate | `showNotification(title, { body })` API trata body como plaintext (R-S4). Pitfall 4: title ≤ 50 chars, body ≤ 150 chars enforced via slice em Task 3.1 e Task 3.2. |
| T-06-13 | Denial of Service | Subscription stale acumula DB | mitigate | D-02 — 410/404 → DELETE row + log. Task 3.1 implementa cleanup em catch. |
| T-06-14 | Repudiation | Push enviado sem registro auditável | mitigate | notificacoes_envios row criada via INSERT atômico antes do envio (mesmo pattern Phase 5). UNIQUE garante 1 row por (perfil, tipo, canal, dia, entidade). |
| T-06-SC | Tampering | `web-push@3.6.7` supply chain | mitigate | Versão pinned (não @latest), slopcheck [OK], 2.5M dl/sem, 8+ anos repo. Task 1.4 smoke-test valida runtime. |
| T-06-15 | Spoofing | Atacante chama notify-tarefa direto | mitigate | Existing webhook secret check (Phase 5 D-XX) já bloqueia chamadas sem header. Sem mudança nesta phase. |
</threat_model>

<verification>
- `_shared/push.ts` testes scaffold viram GREEN — Task 3.1
- 4 notify-* importam sendPush + usam Promise.allSettled + response inclui push field — Tasks 3.2 e 3.3
- E2E smoke: device recebe push real (entrega + click + cleanup) — Task 3.4
- Phase 5 não regrediu: rodar Phase 5 UAT smoke equivalente (notify-tarefa com email-only perfil ainda envia email)
</verification>

<success_criteria>
Wave 2 (parte send infra) completo quando:
- `_shared/push.ts` shipped com sendPush; testes scaffold Plan 01 GREEN
- 4 edge functions integradas (sendPush + Promise.allSettled + response.push)
- Deploy `supabase functions deploy notify-tarefa notify-resumo-diario notify-indicacao notify-renovacao` sem erros
- E2E smoke: push real entregue + 410 cleanup confirmado em DB
- Phase 5 sem regressão (Slack + email continuam funcionando para perfil sem push subscription)
</success_criteria>

<output>
Criar `.planning/phases/06-pwa-push-notifications/06-03-SUMMARY.md` documentando:
- Quantos testes scaffold viraram GREEN (X de 8)
- Decisão R-L1 final consumida (esm.sh ou npm:)
- Resultado do E2E smoke (entrega, click, cleanup)
- Tempo médio de entrega (browser → device) observado
- Surpresas em alguma das 3 funções extras (e.g., notify-indicacao tinha fanout mais complexo)
- Confirmação que Promise.allSettled refactor não quebrou Phase 5 (Slack ainda dispara independente)
</output>
