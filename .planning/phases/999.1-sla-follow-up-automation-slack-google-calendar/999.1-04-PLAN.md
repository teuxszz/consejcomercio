---
phase: 999.1-sla-follow-up-automation-slack-google-calendar
plan: 04
type: execute
wave: 3
depends_on: [999.1-01, 999.1-03]
files_modified:
  - supabase/migrations/043_cron_sla_scan.sql
autonomous: false
requirements: [SLA-05, SLA-07]
user_setup:
  - service: supabase-vault
    why: "Secret Bearer que o cron injeta no header Authorization ao chamar notify-sla-evento (fail-closed)"
    dashboard_config:
      - task: "Gravar o secret no Vault: SELECT vault.create_secret('<hex-aleatorio>', 'webhook_sla_secret', 'Bearer do cron SLA'); — o MESMO valor de WEBHOOK_SLA_SECRET (Edge secret do Plan 03)"
        location: "Supabase Studio → SQL Editor"
must_haves:
  truths:
    - "A cada 15 minutos, um job pg_cron varre lead_sla e dispara notify-sla-evento para os candidatos"
    - "Candidatos de aviso: resolved_at IS NULL, warning_sent_at IS NULL, deadline_at entre agora e agora+2h"
    - "Candidatos de escalação: resolved_at IS NULL, escalated_at IS NULL, deadline_at <= agora"
    - "Sem o secret webhook_sla_secret no Vault, o cron faz RAISE WARNING e retorna (fail-closed), sem chamar pg_net"
  artifacts:
    - supabase/migrations/043_cron_sla_scan.sql
  key_links:
    - "cron_sla_scan() lê vault.decrypted_secrets WHERE name='webhook_sla_secret' e injeta em Authorization: Bearer"
    - "net.http_post → https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-sla-evento com body { evento, lead_id }"
    - "cron.schedule('sla-scan', '*/15 * * * *', 'SELECT public.cron_sla_scan()')"
---

<objective>
Agendar o SCAN do SLA: a migration 043 cria `cron_sla_scan()` (varredura fail-closed que lê o secret do Vault, encontra candidatos de aviso e de escalação em `lead_sla`, e dispara `notify-sla-evento` via `pg_net`) e o `cron.schedule` a cada 15 minutos. Fecha o loop cron → edge function → Slack/Calendar. Termina com um checkpoint humano de smoke (aviso de 2h fura a DM real).

Purpose: é a peça que torna o motor autônomo — tira o gerente do papel de fiscal manual. Reusa exatamente o padrão de 038 (Vault + loop + net.http_post), com dois loops (aviso e escalação) em vez de um. Frequência de 15min é escolha do planner dentro da sugestão de RESEARCH (~15min).
Output: `supabase/migrations/043_cron_sla_scan.sql` + validação de smoke assistida.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-sla-follow-up-automation-slack-google-calendar/999.1-CONTEXT.md
@.planning/phases/999.1-sla-follow-up-automation-slack-google-calendar/999.1-RESEARCH.md

# Padrão a espelhar 1:1 (Vault + loop + net.http_post + cron.schedule):
@supabase/migrations/038_cron_aprovacoes_stale.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 043 — cron_sla_scan() (2 loops fail-closed) + cron.schedule 15min</name>
  <files>supabase/migrations/043_cron_sla_scan.sql</files>
  <read_first>
    - supabase/migrations/038_cron_aprovacoes_stale.sql:120-199 (cron_disparar_aprovacoes_stale: leitura de secret do Vault, RAISE WARNING + RETURN se ausente, loop com net.http_post, cron.schedule idempotente)
    - .planning/phases/999.1-sla-follow-up-automation-slack-google-calendar/999.1-RESEARCH.md:334-390 (código de referência do cron_sla_scan com os dois loops)
    - supabase/migrations/041_lead_sla.sql (colunas/índice de lead_sla — Plan 01)
    - supabase/functions/notify-sla-evento/index.ts (contrato do payload { evento, lead_id } — Plan 03)
  </read_first>
  <action>
    Crie `supabase/migrations/043_cron_sla_scan.sql` (aditiva, idempotente). `CREATE EXTENSION IF NOT EXISTS pg_cron; CREATE EXTENSION IF NOT EXISTS pg_net;` (defensivo, já presentes). Função `public.cron_sla_scan()` RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path=public:
    (1) Lê `decrypted_secret` de `vault.decrypted_secrets WHERE name='webhook_sla_secret'` em v_secret; se NULL → `RAISE WARNING 'cron_sla_scan: secret webhook_sla_secret ausente no Vault'` e RETURN (fail-closed, T-999.1-13). Define v_url = 'https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-sla-evento'.
    (2) Loop de AVISO: FOR r IN SELECT ls.lead_id FROM lead_sla ls WHERE ls.resolved_at IS NULL AND ls.warning_sent_at IS NULL AND ls.deadline_at - now() <= interval '2 hours' AND ls.deadline_at > now() — PERFORM net.http_post(url:=v_url, body:=jsonb_build_object('evento','warning','lead_id',r.lead_id), headers:=jsonb_build_object('Content-Type','application/json','Authorization','Bearer '||v_secret)).
    (3) Loop de ESCALAÇÃO: FOR r IN SELECT ls.lead_id FROM lead_sla ls WHERE ls.resolved_at IS NULL AND ls.escalated_at IS NULL AND ls.deadline_at <= now() — PERFORM net.http_post(... 'evento','escalonamento' ...).
    (4) `SELECT cron.schedule('sla-scan', '*/15 * * * *', 'SELECT public.cron_sla_scan()')` (idempotente por jobname).
    Cabeçalho em comentário: o secret NÃO fica no arquivo (Vault, ver user_setup); a idempotência de envio é do edge function (CAS); os filtros WHERE ... IS NULL são defesa em profundidade contra ticks sobrepostos (Pitfall 4).
  </action>
  <verify>
    <automated>grep -v '^\s*--' supabase/migrations/043_cron_sla_scan.sql | grep -Eic "cron_sla_scan|vault.decrypted_secrets|net.http_post|'warning'|'escalonamento'|cron.schedule"</automated>
  </verify>
  <done>
    - Função cron_sla_scan SECURITY DEFINER SET search_path=public, fail-closed sem o secret
    - Dois loops (aviso: deadline entre agora e +2h; escalação: deadline <= agora), ambos filtrando resolved_at IS NULL e o respectivo *_sent_at/escalated_at IS NULL
    - cron.schedule com '*/15 * * * *' e jobname 'sla-scan'
    - Grep de verificação retorna ≥ 5
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Smoke do motor de SLA (aviso DM único + escalação fail-safe)</name>
  <action>Verificação humana do fluxo cron → notify-sla-evento → Slack em produção. Claude já automatizou o código (migration 043, edge function, secrets documentados no user_setup); o humano confirma o comportamento observável descrito em how-to-verify e sinaliza approved/problema.</action>
  <what-built>
    Motor de SLA completo em produção: migration 043 aplicada (cron a cada 15min), edge function notify-sla-evento deployada, secrets setados (Vault webhook_sla_secret == Edge WEBHOOK_SLA_SECRET; SLACK_BOT_TOKEN; APP_URL). O caminho de AVISO (DM Slack) funciona sem depender do Google (fallback só-Slack).
  </what-built>
  <how-to-verify>
    Pré-condições (uma vez): aplicar 041/042/043 (via Supabase Studio SQL Editor — ver Tech Debt do STATE sobre `supabase db push`); `supabase functions deploy notify-sla-evento`; gravar o Vault secret `webhook_sla_secret` com o MESMO valor do Edge secret `WEBHOOK_SLA_SECRET`.
    Smoke do AVISO (não precisa do Google):
    1. Escolha um lead de teste cujo responsável tenha `perfis.slack_user_id` setado e em fase ativa (ex.: `classificacao`).
    2. No SQL Editor, force o prazo para dentro da janela de 2h: `UPDATE lead_sla SET deadline_at = now() + interval '90 minutes', warning_sent_at = NULL, resolved_at = NULL WHERE lead_id = '<id>';`
    3. Dispare o scan manualmente: `SELECT public.cron_sla_scan();`
    4. Esperado: o responsável recebe UMA DM no Slack "⏰ SLA de follow-up vencendo — <lead>" com botão Abrir no CRM. Rode `SELECT public.cron_sla_scan();` de novo e confirme que NÃO chega segunda DM (CAS/idempotência).
    Smoke da ESCALAÇÃO (fail-safe): se `SLACK_GERENCIA_CHANNEL_ID` ainda não estiver setado (deferido — D-04), force `deadline_at = now() - interval '1 minute', escalated_at = NULL` e rode o scan; esperado: NADA quebra, escalação é pulada (fail-safe). Se o canal já estiver setado, confirme a mensagem "🚨 SLA VENCIDO" no canal da gerência.
    5. Registre uma interação no lead (ou `UPDATE lead_sla SET resolved_at = now()`) e confirme que novos scans não disparam nada.
  </how-to-verify>
  <resume-signal>Digite "approved" se a DM chegou uma única vez e a escalação se comportou (posta OU fail-safe), ou descreva o que falhou</resume-signal>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Função/Cron SQL:** `cron_sla_scan()` (SECURITY DEFINER, dois loops fail-closed) + job pg_cron `sla-scan` (`*/15 * * * *`)
- **Migration:** `supabase/migrations/043_cron_sla_scan.sql`
- **Secret manual (user_setup):** Vault `webhook_sla_secret` (== Edge `WEBHOOK_SLA_SECRET`)
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pg_cron → Vault | Lê o secret Bearer só via vault.decrypted_secrets (nunca em git/log SQL) |
| cron (pg_net) → notify-sla-evento | Injeta Authorization: Bearer; a edge function valida (fail-closed) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-999.1-13 | Spoofing | cron dispara sem secret configurado | high | mitigate | RAISE WARNING + RETURN se webhook_sla_secret ausente no Vault (fail-closed, não fail-open) — padrão 038/040 |
| T-999.1-14 | Information Disclosure | secret Bearer vazando em git/log | high | mitigate | Secret só no Vault (vault.decrypted_secrets); nunca no arquivo de migration |
| T-999.1-05 | Denial of Service (noise) | ticks de 15min sobrepostos disparando duplicado | high | mitigate | Filtros WHERE ... IS NULL nos dois loops + CAS no edge function (defesa em profundidade) |
| T-999.1-SC | Tampering | npm/pip/cargo installs | low | accept | Zero novos pacotes (RESEARCH: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- Grep estrutural da migration 043 (dois loops + Vault fail-closed + schedule 15min)
- Checkpoint humano: DM única no aviso; escalação posta ou fail-safe; resolve interrompe disparos
</verification>

<success_criteria>
- Cron autônomo varrendo a cada 15min; aviso e escalação disparados aos candidatos corretos
- Fail-closed sem o secret; sem duplicação sob re-scan
</success_criteria>

<output>
Create `.planning/phases/999.1-sla-follow-up-automation-slack-google-calendar/999.1-04-SUMMARY.md` when done
</output>
