---
phase: 999.1-sla-follow-up-automation-slack-google-calendar
plan: 06
type: execute
wave: 2
depends_on: [999.1-01]
files_modified:
  - src/components/configuracoes/SlaFollowupConfig.tsx
  - src/pages/ConfiguracoesPage.tsx
autonomous: true
requirements: [SLA-09]
must_haves:
  truths:
    - "Um gerente+ consegue editar, nas Configurações, o prazo de SLA (horas) de cada fase ativa do funil e um default global"
    - "Salvar grava em configuracoes.metas.sla_followup_horas_por_stage e sla_followup_horas_default"
    - "Fases terminais e stand_by NÃO aparecem como configuráveis (não têm SLA)"
    - "Os valores salvos são exatamente os que o trigger upsert_lead_sla lê ao calcular deadline_at"
  artifacts:
    - src/components/configuracoes/SlaFollowupConfig.tsx
  key_links:
    - "SlaFollowupConfig lê/escreve via useConfiguracoes/useUpdateConfiguracoes (metas JSONB)"
    - "as fases exibidas vêm de PIPELINE_STAGES menos SLA_EXCLUDED_STAGES (sla-followup.ts do Plan 01)"
---

<objective>
Entregar a UI de CONFIGURAÇÃO dos prazos de SLA por fase (D-02): um componente `SlaFollowupConfig` na página de Configurações onde gerente+ define, para cada fase ativa do funil, o prazo em horas até o próximo contato, além de um default global. Grava em `configuracoes.metas.sla_followup_horas_por_stage` e `sla_followup_horas_default` — exatamente as chaves que o trigger `upsert_lead_sla` (Plan 01) lê.

Purpose: fecha o requisito "SLA POR FASE, configurável" (D-02). Sem esta tela, os prazos ficam presos no default de 24h e o time não consegue ajustar por fase.
Output: `SlaFollowupConfig.tsx` + integração na ConfiguracoesPage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-sla-follow-up-automation-slack-google-calendar/999.1-CONTEXT.md
@.planning/phases/999.1-sla-follow-up-automation-slack-google-calendar/999.1-RESEARCH.md

# Padrões a espelhar:
@src/components/configuracoes/ConfigDiasAprovacao.tsx
@src/hooks/useConfiguracoes.ts
@src/lib/constants.ts
@src/lib/sla-followup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componente SlaFollowupConfig (edição de horas por fase + default)</name>
  <files>src/components/configuracoes/SlaFollowupConfig.tsx</files>
  <read_first>
    - src/components/configuracoes/ConfigDiasAprovacao.tsx (padrão canônico: lê configuracoes.metas, edita um número, salva via useUpdateConfiguracoes, gating de role, toast)
    - src/hooks/useConfiguracoes.ts:226-244 (useUpdateConfiguracoes — recebe Partial<Configuracoes>; ao salvar metas, mande o objeto metas completo mesclado)
    - src/lib/constants.ts:1-29 (PIPELINE_STAGES + ACTIVE_LEAD_STAGES — quais fases exibir)
    - src/lib/sla-followup.ts (SLA_EXCLUDED_STAGES — a lista de exclusão a reusar; NÃO redeclarar)
    - src/components/shared/RequireRole.tsx (gating gerente+; espelhe o que ConfigDiasAprovacao usa)
  </read_first>
  <action>
    Crie `src/components/configuracoes/SlaFollowupConfig.tsx` espelhando `ConfigDiasAprovacao.tsx`. Lê `useConfiguracoes()`; deriva a lista de fases configuráveis a partir de `PIPELINE_STAGES` filtrando as que estão em `SLA_EXCLUDED_STAGES` (importado de `@/lib/sla-followup`, não redeclarar). Estado local: um `Record<stageId, number>` inicializado de `metas.sla_followup_horas_por_stage` (fallback vazio) + um campo `default` de `metas.sla_followup_horas_default` (fallback 24). Renderiza, para cada fase ativa, o label da fase (usar PIPELINE_STAGES.label) e um Input numérico de horas; um Input separado para o default global. Botão "Salvar" chama `useUpdateConfiguracoes().mutate({ metas: { ...metasAtuais, sla_followup_horas_por_stage: <estado>, sla_followup_horas_default: <default> } })` — mesclando com os metas atuais para não sobrescrever outras chaves (meta_leads_mes, dias_para_aprovacao_pendente, etc.). Gating: só gerente+ pode editar/salvar (RequireRole atLeast="gerente", mesmo padrão de ConfigDiasAprovacao). Textos em PT-BR; unidade "horas" explícita; nota curta de que fases terminais/stand_by não têm SLA.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - Componente compila; lista apenas fases NÃO excluídas (reusa SLA_EXCLUDED_STAGES de sla-followup.ts)
    - Salvar preserva as demais chaves de metas (merge, não overwrite)
    - Gating gerente+ para edição/salvamento
  </done>
</task>

<task type="auto">
  <name>Task 2: Montar SlaFollowupConfig na ConfiguracoesPage</name>
  <files>src/pages/ConfiguracoesPage.tsx</files>
  <read_first>
    - src/pages/ConfiguracoesPage.tsx:455-530 (seção de Metas/Gamificação — onde ConfigDiasAprovacao é montado; montar SlaFollowupConfig na mesma vizinhança/aba)
  </read_first>
  <action>
    Importe e monte `<SlaFollowupConfig />` na ConfiguracoesPage, na mesma seção/aba onde vive `ConfigDiasAprovacao` (configurações operacionais/metas). Adicione um subtítulo curto PT-BR "SLA de follow-up" com uma frase explicando que define o prazo do próximo contato por fase do funil. Não altere a lógica de save existente da página — SlaFollowupConfig salva por conta própria via useUpdateConfiguracoes.
  </action>
  <verify>
    <automated>grep -c "SlaFollowupConfig" src/pages/ConfiguracoesPage.tsx</automated>
  </verify>
  <done>
    - SlaFollowupConfig importado e renderizado na ConfiguracoesPage (grep ≥ 2: import + uso)
    - `npm run build` compila
    - A UI aparece na seção de configurações operacionais, junto de ConfigDiasAprovacao
  </done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Componente:** `src/components/configuracoes/SlaFollowupConfig.tsx` (edição de horas de SLA por fase + default global)
- **UI alterada:** `src/pages/ConfiguracoesPage.tsx` (nova seção "SLA de follow-up")
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → configuracoes (RLS) | UPDATE de configuracoes só por gerente+ (policy gerente_update_configuracoes da 040) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-999.1-17 | Elevation of Privilege | consultor alterando prazos de SLA de todo o time | medium | mitigate | Gating de UI gerente+ (RequireRole) + RLS gerente_update_configuracoes (040) — a fronteira real é a RLS, não a UI |
| T-999.1-18 | Tampering | salvar sobrescreve outras chaves de metas | low | mitigate | Merge com metas atuais no update (não overwrite) — mesmo padrão de ConfigDiasAprovacao |
| T-999.1-SC | Tampering | npm/pip/cargo installs | low | accept | Zero novos pacotes (RESEARCH: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `npm run build` compila
- Grep confirma montagem de SlaFollowupConfig na ConfiguracoesPage
- Revisão: valores salvos batem com as chaves lidas pelo trigger (sla_followup_horas_por_stage / _default)
</verification>

<success_criteria>
- Gerente+ configura prazos de SLA por fase; valores persistidos onde o trigger lê
- Fases terminais/stand_by não aparecem como configuráveis
</success_criteria>

<output>
Create `.planning/phases/999.1-sla-follow-up-automation-slack-google-calendar/999.1-06-SUMMARY.md` when done
</output>
