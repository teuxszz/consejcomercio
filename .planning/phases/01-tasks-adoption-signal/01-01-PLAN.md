---
phase: 01-tasks-adoption-signal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/033_rls_tarefas_notificar.sql
  - src/types/index.ts
  - src/lib/query-keys.ts
  - src/components/layout/AppLayout.tsx
  - supabase/functions/notify-tarefa/index.ts
autonomous: false
requirements:
  - SEC-01
  - SEC-02
user_setup:
  - service: supabase
    why: "Aplicar migration 033 no projeto cloud antes da UI de tarefas ir para producao"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (necessario para `supabase db push` em modo nao-interativo)"
    dashboard_config:
      - task: "Confirmar que projeto cloud aceita push da CLI"
        location: "Supabase Dashboard -> Project Settings -> Database"

must_haves:
  truths:
    - "Usuario do tipo cliente nao consegue ler nenhuma linha de tarefas (RLS bloqueia)"
    - "Usuario interno autenticado consegue SELECT/INSERT/UPDATE/DELETE em tarefas"
    - "Coluna tarefas.notificar existe com tipo boolean NOT NULL DEFAULT true"
    - "Tipo Tarefa em src/types/index.ts expoe campo notificar opcional"
    - "Edge function notify-tarefa pula DM quando record.notificar = false"
    - "Evento SIGNED_IN do Supabase Auth insere uma linha em audit_logs com acao=login"
    - "QUERY_KEYS expoe chave adocao com sub-chaves logins, atividade, leadsEsquecidos"
  artifacts:
    - path: "supabase/migrations/033_rls_tarefas_notificar.sql"
      provides: "SEC-01 (policies is_interno em tarefas) + SEC-02 (coluna notificar)"
      contains: "is_interno()"
    - path: "src/types/index.ts"
      provides: "Tarefa.notificar?: boolean"
      contains: "notificar"
    - path: "src/lib/query-keys.ts"
      provides: "QUERY_KEYS.adocao com 3 sub-chaves"
      contains: "adocao:"
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Login tracking via audit_logs no evento SIGNED_IN"
      contains: "SIGNED_IN"
    - path: "supabase/functions/notify-tarefa/index.ts"
      provides: "Skip de DM quando notificar=false"
      contains: "notificar"
  key_links:
    - from: "supabase/migrations/033_rls_tarefas_notificar.sql"
      to: "public.is_interno()"
      via: "USING (public.is_interno())"
      pattern: "public\\.is_interno\\(\\)"
    - from: "src/components/layout/AppLayout.tsx"
      to: "supabase.from('audit_logs').insert"
      via: "onAuthStateChange SIGNED_IN handler"
      pattern: "acao.*login"
    - from: "supabase/functions/notify-tarefa/index.ts"
      to: "tarefa.notificar === false"
      via: "early return apos extracao de record"
      pattern: "notificar"
---

<objective>
Fechar a brecha de seguranca da tabela `tarefas` (SEC-01) e habilitar opt-out de notificacao via coluna `notificar` (SEC-02). Tambem prepara a infraestrutura que as fases seguintes (Tasks UI, Realtime Badge, Adocao) precisam: tipo `Tarefa.notificar`, chaves de query `adocao`, login tracking em `audit_logs`, e edge function `notify-tarefa` ciente do flag `notificar`.

Purpose: Sem essa fundacao, deploy de qualquer feature de tarefas vaza dados internos para usuarios `cliente` (autenticados via portal) e o painel de adocao mostra "0 logins" eternamente por falta de tracking. Esta e a primeira camada — todas as outras dependem dela.

Output: Migration 033 aplicada em cloud, tipos e query keys atualizados, login tracking ativo, edge function atualizada. Wave 1 destrava Waves 2-4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tasks-adoption-signal/01-RESEARCH.md
@.planning/phases/01-tasks-adoption-signal/01-PATTERNS.md
@.planning/phases/01-tasks-adoption-signal/01-VALIDATION.md
@supabase/migrations/021_lockdown_rls.sql
@supabase/migrations/014_tarefas.sql
@supabase/functions/notify-tarefa/index.ts
@src/components/layout/AppLayout.tsx
@src/types/index.ts
@src/lib/query-keys.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration 033_rls_tarefas_notificar.sql (SEC-01 + SEC-02)</name>
  <files>supabase/migrations/033_rls_tarefas_notificar.sql</files>
  <read_first>
    - supabase/migrations/014_tarefas.sql (linha 38 confirma policy `authenticated_all` aberta atual)
    - supabase/migrations/021_lockdown_rls.sql (linhas 14-26: helper `public.is_interno()`; linhas 45-49: padrao de lockdown a copiar)
    - supabase/migrations/029_rls_role_aware.sql (padrao de policies separadas por operacao)
    - .planning/phases/01-tasks-adoption-signal/01-PATTERNS.md (secao "supabase/migrations/033_rls_tarefas_notificar.sql" com o SQL completo de referencia)
  </read_first>
  <action>
    Criar arquivo SQL `supabase/migrations/033_rls_tarefas_notificar.sql` por D-SEC-01 e D-SEC-02. Conteudo na ordem exata: (1) cabecalho de comentario referenciando SEC-01 e SEC-02; (2) `ALTER TABLE tarefas ADD COLUMN IF NOT EXISTS notificar boolean NOT NULL DEFAULT true;` (SEC-02); (3) `ALTER TABLE tarefas DISABLE ROW LEVEL SECURITY;`; (4) `DROP POLICY IF EXISTS "authenticated_all" ON tarefas;`; (5) quatro policies separadas usando `public.is_interno()` — `interno_select_tarefas` (FOR SELECT USING), `interno_insert_tarefas` (FOR INSERT WITH CHECK), `interno_update_tarefas` (FOR UPDATE USING + WITH CHECK), `interno_delete_tarefas` (FOR DELETE USING); (6) `ALTER TABLE tarefas ENABLE ROW LEVEL SECURITY;`. Todas as policies sao `TO authenticated`. Granularidade adicional consultor-vs-coordenador fica no hook (decisao confirmada em RESEARCH.md Pattern 1) — NAO incluir nesta migration. Numero da migration eh 033 (apos 032_lixeira_leads.sql confirmado). Nao usar `FOR ALL` — sempre quatro policies separadas para permitir refinamento granular futuro sem recriar tudo.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const sql=fs.readFileSync('supabase/migrations/033_rls_tarefas_notificar.sql','utf8');const checks=[/ADD COLUMN IF NOT EXISTS notificar boolean NOT NULL DEFAULT true/,/DROP POLICY IF EXISTS \"authenticated_all\" ON tarefas/,/CREATE POLICY \"interno_select_tarefas\"/,/CREATE POLICY \"interno_insert_tarefas\"/,/CREATE POLICY \"interno_update_tarefas\"/,/CREATE POLICY \"interno_delete_tarefas\"/,/public\.is_interno\(\)/,/ENABLE ROW LEVEL SECURITY/];const fail=checks.filter(r=>!r.test(sql));if(fail.length){console.error('Missing:',fail.map(String));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/033_rls_tarefas_notificar.sql` exists.
    - File contains literal string `ADD COLUMN IF NOT EXISTS notificar boolean NOT NULL DEFAULT true`.
    - File contains literal string `DROP POLICY IF EXISTS "authenticated_all" ON tarefas`.
    - File contains exactly 4 `CREATE POLICY` statements with names: `interno_select_tarefas`, `interno_insert_tarefas`, `interno_update_tarefas`, `interno_delete_tarefas`.
    - Every CREATE POLICY uses `public.is_interno()` in USING and/or WITH CHECK clause.
    - File ends with `ALTER TABLE tarefas ENABLE ROW LEVEL SECURITY;`.
    - File does NOT contain `FOR ALL` (granularity preserved by separated policies).
  </acceptance_criteria>
  <done>Arquivo de migration criado seguindo padrao de 021_lockdown_rls.sql, com 4 policies separadas e coluna `notificar` adicionada.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar campo notificar ao tipo Tarefa e chave adocao em QUERY_KEYS</name>
  <files>src/types/index.ts, src/lib/query-keys.ts</files>
  <read_first>
    - src/types/index.ts (linhas 266-282: interface `Tarefa` atual)
    - src/lib/query-keys.ts (linhas 47-51: padrao da chave `tarefas`)
    - .planning/phases/01-tasks-adoption-signal/01-PATTERNS.md (secao "src/types/index.ts" e "src/lib/query-keys.ts")
  </read_first>
  <action>
    Em `src/types/index.ts`, na interface `Tarefa` (linha ~266), adicionar campo `notificar?: boolean` imediatamente antes de `created_at: string` por D-SEC-02. Manter optional para retrocompatibilidade com codigo existente que constroi `Omit<Tarefa, 'id' | 'created_at' | 'updated_at'>` sem mencionar `notificar` (default DB cuida do valor). Nao alterar nenhum outro campo ou tipo. Em `src/lib/query-keys.ts`, adicionar nova chave `adocao` apos a chave `tarefas` (apos linha 51) com tres sub-chaves: `logins: ['adocao', 'logins'] as const`, `atividade: ['adocao', 'atividade'] as const`, `leadsEsquecidos: ['adocao', 'leads-esquecidos'] as const`. Manter `as const` no padrao das chaves existentes para tipos literais.
  </action>
  <verify>
    <automated>npx tsc -b && node -e "const t=require('fs').readFileSync('src/types/index.ts','utf8');const q=require('fs').readFileSync('src/lib/query-keys.ts','utf8');if(!/notificar\?\s*:\s*boolean/.test(t)){console.error('Tarefa.notificar missing');process.exit(1)}if(!/adocao:\s*\{[^}]*logins:[^}]*atividade:[^}]*leadsEsquecidos:/s.test(q)){console.error('adocao key missing');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/index.ts` contains literal `notificar?: boolean` inside the `Tarefa` interface block.
    - `src/lib/query-keys.ts` contains the literal `adocao:` block with sub-keys `logins`, `atividade`, `leadsEsquecidos` each declared with `as const`.
    - `npx tsc -b` exits 0 with no errors.
    - Interface `Tarefa` still contains all 15 pre-existing fields (no field removed).
  </acceptance_criteria>
  <done>Tipo `Tarefa` aceita `notificar` opcional; `QUERY_KEYS.adocao` disponivel para hooks da Phase 4.</done>
</task>

<task type="auto">
  <name>Task 3: Login tracking em AppLayout via audit_logs no SIGNED_IN</name>
  <files>src/components/layout/AppLayout.tsx</files>
  <read_first>
    - src/components/layout/AppLayout.tsx (linhas 1-46: estado atual completo do componente)
    - .planning/phases/01-tasks-adoption-signal/01-RESEARCH.md (Pattern 4 "Login tracking via `audit_logs`" + Pitfall 3 "Login tracking duplicado no StrictMode")
    - .planning/phases/01-tasks-adoption-signal/01-PATTERNS.md (secao "src/components/layout/AppLayout.tsx")
  </read_first>
  <action>
    Modificar `src/components/layout/AppLayout.tsx` para registrar login em `audit_logs` quando o `onAuthStateChange` recebe `SIGNED_IN`. Pre-requisito de ADOPT-01/02. Mudancas concretas: (1) adicionar `useRef` ao import do react (atual: `import { useEffect, useState } from 'react'` → `import { useEffect, useRef, useState } from 'react'`); (2) dentro do `AppLayout`, antes do `useEffect`, declarar `const loggedSessionRef = useRef<string | null>(null)`; (3) no callback do `onAuthStateChange`, apos o bloco existente `if (event === 'SIGNED_OUT' || !session)`, adicionar bloco `if (event === 'SIGNED_IN' && session?.user)` que: compara `loggedSessionRef.current` com `session.access_token`; se diferente, atualiza o ref e dispara `supabase.from('audit_logs').insert({ tabela: 'perfis', registro_id: session.user.id, acao: 'login', usuario: session.user.email ?? null }).then(({ error }) => { if (error) console.error('audit login:', error) })` fire-and-forget (nao await). Razao da comparacao por access_token: React 19 StrictMode double-invoca effects (Pitfall 3) — o ref evita registro duplicado. Nao alterar logica de redirect, getSession, ou cleanup.
  </action>
  <verify>
    <automated>npx tsc -b && node -e "const s=require('fs').readFileSync('src/components/layout/AppLayout.tsx','utf8');const ck=[/useRef/,/loggedSessionRef/,/SIGNED_IN/,/audit_logs/,/acao:\s*['\"]login['\"]/,/session\.access_token/];const fail=ck.filter(r=>!r.test(s));if(fail.length){console.error('Missing:',fail.map(String));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/AppLayout.tsx` imports `useRef` from `react`.
    - File contains literal `loggedSessionRef`.
    - File contains literal `SIGNED_IN` (inside an `if (event === 'SIGNED_IN'` check).
    - File contains literal `audit_logs` and `acao: 'login'` (or `acao: "login"`).
    - File compares `session.access_token` against `loggedSessionRef.current` before inserting.
    - `npx tsc -b` exits 0.
    - Existing `SIGNED_OUT` handler still present (regression check: search for `SIGNED_OUT`).
  </acceptance_criteria>
  <done>Eventos SIGNED_IN gravam linha em `audit_logs` com `acao='login'`; double-invoke do StrictMode nao gera duplicatas.</done>
</task>

<task type="auto">
  <name>Task 4: Edge function notify-tarefa respeita flag notificar (SEC-02)</name>
  <files>supabase/functions/notify-tarefa/index.ts</files>
  <read_first>
    - supabase/functions/notify-tarefa/index.ts (linhas 18-37: interface `TarefaRow` e `WebhookPayload`; linhas 162-180: logica de skip atual)
    - .planning/phases/01-tasks-adoption-signal/01-PATTERNS.md (secao "supabase/functions/notify-tarefa/index.ts")
  </read_first>
  <action>
    Atualizar `supabase/functions/notify-tarefa/index.ts` para respeitar `record.notificar=false` por D-SEC-02. Mudancas concretas: (1) na interface `TarefaRow` (linha ~18), adicionar campo `notificar: boolean | null` apos `data_vencimento: string | null`; (2) na funcao `serve` (linha ~141), apos o bloco existente `if (!tarefa) return json({ ok: true, skipped: 'no record' })` (linha ~167), inserir novo bloco `if (tarefa.notificar === false) return json({ ok: true, skipped: 'notificar=false' })`. Razao de `=== false` (nao `!tarefa.notificar`): coluna eh `NOT NULL DEFAULT true` no DB, mas record pode chegar como `null`/`undefined` em edge cases de webhook payload — so suprimir DM quando explicitamente `false`. Nao alterar nenhuma outra logica (auth check, Slack DM, retry, etc.).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/functions/notify-tarefa/index.ts','utf8');if(!/notificar:\s*boolean\s*\|\s*null/.test(s)){console.error('TarefaRow.notificar missing');process.exit(1)}if(!/tarefa\.notificar\s*===\s*false/.test(s)){console.error('skip check missing');process.exit(1)}if(!/skipped:\s*['\"]notificar=false['\"]/.test(s)){console.error('skipped marker missing');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Interface `TarefaRow` in `supabase/functions/notify-tarefa/index.ts` contains field declaration `notificar: boolean | null`.
    - File contains literal `tarefa.notificar === false`.
    - File contains literal `skipped: 'notificar=false'` (string used in skip response).
    - Pre-existing skip checks (`'not tarefas'`, `'no record'`, `'sem destinatário'`, `'atribuição não mudou'`) all still present.
  </acceptance_criteria>
  <done>Edge function pula DM Slack quando tarefa tem `notificar=false`; comportamento default (true) preservado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [BLOCKING] Aplicar migration 033 no Supabase cloud via `supabase db push`</name>
  <files>supabase/migrations/033_rls_tarefas_notificar.sql</files>
  <read_first>
    - supabase/migrations/033_rls_tarefas_notificar.sql (arquivo recem-criado na Task 1)
    - .planning/phases/01-tasks-adoption-signal/01-RESEARCH.md (Pitfall 5: "Migracoes sequenciais — nao pular o numero 033")
  </read_first>
  <what-built>
    Migration 033 criada localmente (Task 1) adicionando coluna `notificar` e fechando RLS de `tarefas`. Tipos TS e edge function ja foram atualizados em paralelo (Tasks 2-4). Falta aplicar o SQL no banco cloud — sem isso, a coluna `notificar` nao existe em producao, a RLS continua aberta, e qualquer Insert/Select da UI nova (Wave 2) vai bater na policy antiga ou falhar com coluna desconhecida.
  </what-built>
  <action>
    Aplicar a migration 033 no projeto Supabase cloud usando a CLI. Comando: `supabase db push` (na raiz do repo). Se necessario em modo nao-interativo, exportar `SUPABASE_ACCESS_TOKEN` antes. Apos push bem-sucedido: redeployar a edge function `notify-tarefa` para que a versao atualizada (Task 4) entre em vigor — comando: `supabase functions deploy notify-tarefa`. Verificar no Supabase Dashboard que: (a) tabela `tarefas` tem coluna `notificar boolean NOT NULL DEFAULT true`; (b) policies `interno_select_tarefas`, `interno_insert_tarefas`, `interno_update_tarefas`, `interno_delete_tarefas` aparecem em "Authentication -> Policies -> tarefas"; (c) policy `authenticated_all` em `tarefas` nao existe mais.
  </action>
  <how-to-verify>
    1. Rodar `supabase db push` no terminal — saida deve incluir referencia a `033_rls_tarefas_notificar.sql` como aplicada.
    2. Rodar `supabase functions deploy notify-tarefa` — deploy bem-sucedido.
    3. Abrir Supabase Dashboard -> Project -> Database -> Tables -> `tarefas` -> Columns: confirmar `notificar` (boolean, default true, not null).
    4. Dashboard -> Authentication -> Policies -> `tarefas`: confirmar 4 policies novas, nenhuma `authenticated_all`.
    5. (Opcional) Rodar `npm run test:rls` se QA users estiverem provisionados — deve passar a verificacao de "cliente nao le tarefas".
  </how-to-verify>
  <resume-signal>Digite "approved" quando migration aplicada e edge function redeployada, ou descreva problemas encontrados</resume-signal>
  <done>Migration 033 aplicada em cloud, RLS de `tarefas` fechada, edge function notify-tarefa redeployada com check de `notificar`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portal cliente -> API Supabase | Usuarios `cliente` autenticam via portal com anon key; podem tentar SELECT em qualquer tabela exposta ao role `authenticated` |
| client browser -> Supabase Realtime | Subscriptions futuras (Wave 3) usam JWT do cliente; RLS aplicada server-side |
| Database Webhook -> Edge Function notify-tarefa | Webhook envia payload sem autenticacao Supabase nativa; protegido por `WEBHOOK_TAREFA_SECRET` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Tabela `tarefas` (policy `authenticated_all`) | mitigate | SEC-01: migration 033 substitui `authenticated_all` por quatro policies `is_interno()` — cliente perde acesso de leitura, insert, update, delete. Validado por `tests/rls/tarefas.test.ts` (Wave 0) |
| T-01-02 | Tampering | Edge function `notify-tarefa` (DM Slack indesejado) | mitigate | SEC-02: coluna `notificar boolean DEFAULT true` + check `tarefa.notificar === false` na funcao. Tarefas de cadencia futuras (Phase 2/3) podem suprimir DM setando `notificar=false` no insert |
| T-01-03 | Spoofing | `audit_logs` insert de login (Task 3) | accept | Risco baixo: cliente nao tem `is_interno()` entao a policy `interno_insert_audit_logs` (021) bloqueia inserts dele. Usuario interno autenticado pode inserir registros falsos de login propio, mas isso e auto-auditoria (sem impacto cross-user). Mitigacao adicional fica para Phase 3 |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote npm novo instalado nesta fase (confirmado em RESEARCH.md "Package Legitimacy Audit"). Sem superficie de supply chain. |
</threat_model>

<verification>
- [ ] Migration 033 SQL valido (verify Task 1)
- [ ] Tipo Tarefa.notificar e QUERY_KEYS.adocao presentes (verify Task 2)
- [ ] AppLayout grava login em audit_logs (verify Task 3)
- [ ] Edge function checa notificar=false (verify Task 4)
- [ ] [BLOCKING] supabase db push aplicado e edge function redeployada (Task 5)
- [ ] `npm test` passa (regressao)
- [ ] `npx tsc -b` passa
</verification>

<success_criteria>
1. Cliente autenticado via portal nao consegue ler nenhuma tarefa (RLS retorna 0 linhas).
2. Coluna `tarefas.notificar` existe em producao com tipo boolean NOT NULL DEFAULT true.
3. Apos login no CRM, uma linha aparece em `audit_logs` com `acao='login'` e o email do usuario.
4. Edge function `notify-tarefa` redeployada nao envia DM para tarefa com `notificar=false`.
5. Codigo compila (`npx tsc -b`) e testes existentes continuam verdes (`npm test`).
</success_criteria>

<output>
Criar `.planning/phases/01-tasks-adoption-signal/01-01-SUMMARY.md` ao concluir, com: (a) SHA da migration aplicada, (b) timestamp do redeploy da edge function, (c) confirmacao das 4 policies criadas, (d) qualquer issue de aplicacao da migration.
</output>
