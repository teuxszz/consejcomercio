---
phase: 01-tasks-adoption-signal
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/tarefas/TarefaModal.tsx
  - src/components/tarefas/TarefaCard.tsx
  - src/components/tarefas/TarefaStatusBadge.tsx
  - src/pages/TarefasPage.tsx
  - src/router.tsx
  - src/hooks/__tests__/useTarefas.test.tsx
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04

must_haves:
  truths:
    - "Usuario abre /tarefas e ve seu inbox de tarefas ordenado por vencimento ascendente"
    - "Usuario clica em 'Nova tarefa' e o TarefaModal abre com campos titulo/descricao/tipo/prioridade/vencimento/responsavel"
    - "Usuario cria tarefa associada a lead (entidade_tipo='lead', entidade_id=lead.id) e ela aparece no inbox do responsavel"
    - "Usuario cria tarefa interna (entidade_tipo=null, entidade_id=null) e ela aparece no inbox do responsavel"
    - "Usuario filtra inbox por status (todos / abertas / em andamento / concluidas) via Select; filtro persiste na URL"
    - "Usuario clica no Circle de uma tarefa propria e ela passa para status=concluida com toast de sucesso"
    - "Botoes de editar/remover tarefa so aparecem se a tarefa foi criada pelo usuario, atribuida ao usuario, OU usuario eh coordenador+"
    - "Validacao zod bloqueia submit do modal quando titulo < 3 chars; mensagem PT-BR aparece em vermelho"
  artifacts:
    - path: "src/components/tarefas/TarefaModal.tsx"
      provides: "Modal de criacao/edicao de tarefa por lead ou interna"
      exports: ["TarefaModal"]
    - path: "src/components/tarefas/TarefaCard.tsx"
      provides: "Row de tarefa no inbox com botoes de concluir/editar/remover"
      exports: ["TarefaCard"]
    - path: "src/components/tarefas/TarefaStatusBadge.tsx"
      provides: "Badge inline de status (aberta/em_andamento/concluida)"
      exports: ["TarefaStatusBadge"]
    - path: "src/pages/TarefasPage.tsx"
      provides: "Pagina /tarefas com filtros, CTA 'Nova tarefa' e lista"
      exports: ["TarefasPage"]
    - path: "src/router.tsx"
      provides: "Rota /tarefas registrada como child de AppLayout"
      contains: "path: 'tarefas'"
    - path: "src/hooks/__tests__/useTarefas.test.tsx"
      provides: "Cobertura unit para TASK-01..04 (cobre Wave 0 gap)"
      contains: "useCreateTarefa"
  key_links:
    - from: "src/pages/TarefasPage.tsx"
      to: "useMinhasTarefas / useCreateTarefa / useConcluirTarefa / useDeleteTarefa"
      via: "import de @/hooks/useTarefas"
      pattern: "@/hooks/useTarefas"
    - from: "src/components/tarefas/TarefaModal.tsx"
      to: "react-hook-form + zod schema"
      via: "useForm({ resolver: zodResolver(tarefaSchema) })"
      pattern: "zodResolver"
    - from: "src/pages/TarefasPage.tsx"
      to: "TarefaModal e TarefaCard"
      via: "imports + render"
      pattern: "TarefaModal|TarefaCard"
    - from: "src/router.tsx"
      to: "TarefasPage"
      via: "element={<TarefasPage />}"
      pattern: "TarefasPage"
---

<objective>
Entregar a feature ponta-a-ponta de tarefas que cobre TASK-01 a TASK-04 como slice vertical: usuario consegue criar (lead ou interna), ver inbox proprio, concluir, editar e remover tarefas — com gate de permissao por role no UI (consultor so gerencia tarefas proprias/atribuidas; coordenador+ gerencia qualquer). Reaproveita 100% dos hooks ja existentes em `src/hooks/useTarefas.ts` — esta plan e UI + rota + testes.

Purpose: O time precisa de um lugar canonico (`/tarefas`) para registrar e acompanhar trabalho — sem isso o CRM continua sendo "lugar onde deveria reportar" em vez de "lugar onde o trabalho acontece" (core value do Milestone 2).

Output: Rota `/tarefas` navegavel, modal de criacao/edicao funcional com validacao zod, inbox filtravel, e suite de testes unit cobrindo as 4 mutations principais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-tasks-adoption-signal/01-RESEARCH.md
@.planning/phases/01-tasks-adoption-signal/01-PATTERNS.md
@.planning/phases/01-tasks-adoption-signal/01-UI-SPEC.md
@.planning/phases/01-tasks-adoption-signal/01-VALIDATION.md
@src/components/me/TarefasPanel.tsx
@src/hooks/useTarefas.ts
@src/hooks/useCurrentRole.ts
@src/components/leads/NewLeadModal.tsx
@src/pages/AuditoriaPage.tsx
@src/router.tsx
@src/types/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar suite de testes unit useTarefas (Wave 0 — TDD)</name>
  <files>src/hooks/__tests__/useTarefas.test.tsx, src/test/supabase-mock.ts</files>
  <read_first>
    - src/hooks/useTarefas.ts (todos os hooks existentes: useTarefas, useMinhasTarefas, useTarefasByEntidade, useCreateTarefa, useUpdateTarefa, useConcluirTarefa, useDeleteTarefa)
    - src/hooks/__tests__ (verificar se existem analogos como useLeads.test.tsx para padrao de mock)
    - src/test/supabase-mock.ts (utilitario existente de mock segundo CLAUDE.md de Conventions)
    - .planning/phases/01-tasks-adoption-signal/01-VALIDATION.md (linhas 56-63: Wave 0 Requirements + comandos)
  </read_first>
  <behavior>
    - Test 1 (TASK-01): `useCreateTarefa.mutateAsync({ titulo:'X', tipo:'generica', prioridade:'media', status:'aberta', atribuido_a_id:'u1', criado_por_id:'u1', entidade_tipo:'lead', entidade_id:'lead-123', notificar:true })` envia o input ao mock do supabase com `entidade_tipo='lead'` preservado.
    - Test 2 (TASK-02): mesmo hook chamado com `entidade_tipo:null, entidade_id:null` envia tarefa interna sem entidade — mock recebe null em ambos.
    - Test 3 (TASK-03): `useMinhasTarefas('user-1')` chama supabase com `.eq('atribuido_a_id','user-1')` e `.in('status', ['aberta','em_andamento'])`; retorna apenas tarefas do mock que batem.
    - Test 4 (TASK-04): `useConcluirTarefa.mutateAsync('t-1')` chama supabase `.update({ status:'concluida', data_conclusao: <ISO> })` em `.eq('id','t-1')`.
    - Test 5 (TASK-04 gate): teste de UNIDADE de logica de permissao — funcao pura helper (criar `src/lib/tarefas-permissions.ts`? NAO — manter logica inline) — neste arquivo de teste, validar que o componente TarefaCard renderiza botao "Remover" apenas quando `tarefa.criado_por_id === userId || tarefa.atribuido_a_id === userId || isCoordenadorOrAcima`; este teste vai ficar pending ate Task 4 criar o componente. Marcar com `describe.skip` se necessario; senao usar `it.todo`.
  </behavior>
  <action>
    Criar `src/hooks/__tests__/useTarefas.test.tsx` cobrindo TASK-01, 02, 03, 04 conforme behavior acima. Usar vitest com `vi.mock('@/lib/supabase')` ou pattern existente do projeto. Padrao: instanciar QueryClient novo por teste, envolver hook com `renderHook` + wrapper de QueryClientProvider, mockar resposta do supabase via spy/stub na chamada `.from('tarefas')`. Para Test 5 (gate de permissao), usar `it.todo('TarefaCard esconde botao remover para tarefa alheia quando usuario nao eh coordenador+')` — sera implementado no Task 4. Garantir que `npm test -- useTarefas` retorna pelo menos 4 testes passando (Test 1-4) e 1 todo. Se `src/test/supabase-mock.ts` ja existe e expoe builder utility, usar; senao criar mock local minimo no proprio arquivo de teste com `vi.spyOn(supabase, 'from')`. Usar `vi.spyOn(supabase, 'from').mockReturnValue({ insert: vi.fn().mockReturnThis(), select: vi.fn().mockReturnThis(), single: vi.fn().mockResolvedValue({ data: {...}, error: null }) })` (pattern de chain).
  </action>
  <verify>
    <automated>npm test -- useTarefas --run 2>&1 | tee /tmp/useTarefas-test.log; grep -E "(passed|todo)" /tmp/useTarefas-test.log</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/__tests__/useTarefas.test.tsx` exists.
    - File contains at least one `describe` block referencing `useCreateTarefa`.
    - File contains test referencing `entidade_tipo: 'lead'` AND test referencing `entidade_tipo: null` (TASK-01 + TASK-02).
    - File contains test invoking `useMinhasTarefas` and asserting filter on `atribuido_a_id` (TASK-03).
    - File contains test invoking `useConcluirTarefa` asserting `status: 'concluida'` (TASK-04).
    - `npm test -- useTarefas --run` exits 0 with at least 4 passing tests.
  </acceptance_criteria>
  <done>Suite cobre 4 hooks de tarefas com testes verdes; gate de permissao reservado como `todo` ate Task 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar TarefaModal, TarefaCard e TarefaStatusBadge</name>
  <files>src/components/tarefas/TarefaModal.tsx, src/components/tarefas/TarefaCard.tsx, src/components/tarefas/TarefaStatusBadge.tsx</files>
  <read_first>
    - src/components/me/TarefasPanel.tsx (linhas 28-51: TIPO_ICONS, PRIORIDADE_LABEL, PRIORIDADE_STYLE; linhas 183-268: estrutura da row; linhas 291-393: NovaTarefaModal — modelo de modal a refatorar)
    - src/components/leads/NewLeadModal.tsx (padrao zod + react-hook-form do projeto — unico modal com zod, modelo canonico)
    - .planning/phases/01-tasks-adoption-signal/01-UI-SPEC.md (secao "1. TarefaModal", "2. TarefaCard", "3. TarefaStatusBadge")
    - .planning/phases/01-tasks-adoption-signal/01-PATTERNS.md (secoes correspondentes para imports, schema zod, layout)
    - src/types/index.ts (linhas 250-282: TarefaTipo, TarefaPrioridade, TarefaStatus, TarefaEntidade, interface Tarefa)
    - src/hooks/usePerfis.ts (assinatura de `usePerfis()` e `useMeuPerfil()` para preencher Select de responsavel)
  </read_first>
  <behavior>
    - TarefaModal cria tarefa com titulo>=3 chars valido e bloqueia submit quando invalido com mensagem `text-xs text-red-500`.
    - TarefaModal em mode='edit' pre-preenche campos com valores da prop `tarefa`.
    - TarefaCard renderiza titulo, badge de prioridade, badge de status, data de vencimento; data vencida (data_vencimento < now) usa cor `#fca5a5`.
    - TarefaCard mostra botao "Remover" apenas se `tarefa.criado_por_id === userId || tarefa.atribuido_a_id === userId || isCoordenadorOrAcima` (gate de TASK-04).
    - TarefaStatusBadge renderiza tres variantes (aberta/em_andamento/concluida) com cores conforme UI-SPEC.
  </behavior>
  <action>
    Criar tres componentes com named exports.

    `src/components/tarefas/TarefaStatusBadge.tsx`: componente puro `function TarefaStatusBadge({ status }: { status: TarefaStatus })` que retorna `<span>` com classe `text-[10px] px-1.5 py-0.5 rounded-full font-semibold border` e style inline lendo do map `STATUS_STYLE` definido conforme UI-SPEC tabela "Status Colors". Quatro entradas: aberta (bg `rgba(251,191,36,0.12)` color `var(--amber-hi)` border `rgba(251,191,36,0.30)` label "Aberta"), em_andamento (bg `rgba(59,130,246,0.12)` color `#93c5fd` border `rgba(59,130,246,0.30)` label "Em andamento"), concluida (bg `rgba(16,185,129,0.12)` color `var(--emerald-hi)` border `rgba(16,185,129,0.30)` label "Concluida"), cancelada (bg `var(--alpha-bg-xs)` color `var(--text-soft-a)` border `var(--alpha-border)` label "Cancelada"). Importar `TarefaStatus` de `@/types`.

    `src/components/tarefas/TarefaCard.tsx`: extrair logica do row de TarefasPanel.tsx (linhas 183-268) para componente reutilizavel. Props: `{ tarefa: Tarefa; userId: string; onConcluir: (id: string) => void; onEditar: (t: Tarefa) => void; onRemover: (id: string) => void }`. Importar `TIPO_ICONS`, `PRIORIDADE_LABEL`, `PRIORIDADE_STYLE` (duplicar do TarefasPanel — manter colocation com componente novo, dont exportar do TarefasPanel). Renderizar: botao Circle (concluir), conteudo principal (icone tipo + titulo truncate + badge prioridade + TarefaStatusBadge), linha 2 com Calendar/vencimento (color `#fca5a5` se vencida), linha 3 com avatar+nome se `atribuido_a_id !== userId`, botoes de acao (ExternalLink se `entidade_tipo+entidade_id`, Pencil para editar, Trash2 para remover). GATE DE PERMISSAO (TASK-04): usar `useCurrentRole` para obter `isCoordenadorOrAcima`; calcular `canManage = tarefa.criado_por_id === userId || tarefa.atribuido_a_id === userId || isCoordenadorOrAcima`; botoes Editar e Remover so renderizam se `canManage`. Concluir e sempre permitido para qualquer tarefa atribuida ao usuario (`tarefa.atribuido_a_id === userId`). Hover: `hover:bg-[var(--alpha-bg-xs)] transition-colors group`. Tooltips com `title=`.

    `src/components/tarefas/TarefaModal.tsx`: modal de criacao + edicao. Props: `{ open: boolean; onClose: () => void; mode: 'create' | 'edit'; tarefa?: Tarefa; entidadeTipo?: TarefaEntidade | null; entidadeId?: string | null }`. Usar react-hook-form + zod, schema `tarefaSchema = z.object({ titulo: z.string().min(3, 'Titulo obrigatorio (min. 3 caracteres)'), descricao: z.string().optional(), tipo: z.enum(['generica','followup','reuniao_prep','renovacao','upsell','diagnostico','proposta','cobranca']), prioridade: z.enum(['baixa','media','alta','critica']), atribuido_a_id: z.string().uuid('Responsavel obrigatorio'), data_vencimento: z.string().optional().nullable(), notas: z.string().optional().nullable() })`. Layout: Dialog `max-w-lg`, header "Nova tarefa"/"Editar tarefa", chip read-only `Lead: {nome}` quando `entidadeTipo='lead'` (passar como prop opcional), space-y-3, grid-cols-3 para Tipo/Prioridade/Vencimento, Select de responsavel populado por `usePerfis()` (default = `useMeuPerfil().id`). Botoes footer: outline "Descartar tarefa"/"Descartar alteracoes" + primary `style={{ backgroundColor:'#0089ac' }}` "Criar tarefa"/"Salvar alteracoes". Mode='create' usa `useCreateTarefa`; mode='edit' usa `useUpdateTarefa`. Submit popula campos automaticos: `status: 'aberta'` (apenas create), `criado_por_id: meuId` (apenas create), `entidade_tipo`/`entidade_id` (apenas create, das props), `notificar: true` (apenas create). Erros em `text-xs text-red-500 mt-1`. Labels `text-xs font-semibold text-fg2 mb-1 block` (font-semibold, nao font-medium — alinhado a UI-SPEC revisao).
  </action>
  <verify>
    <automated>npx tsc -b && node -e "const fs=require('fs');const m=fs.readFileSync('src/components/tarefas/TarefaModal.tsx','utf8');const c=fs.readFileSync('src/components/tarefas/TarefaCard.tsx','utf8');const b=fs.readFileSync('src/components/tarefas/TarefaStatusBadge.tsx','utf8');const ck=[[m,/zodResolver/],[m,/useForm/],[m,/min\(3/],[m,/export function TarefaModal/],[c,/useCurrentRole/],[c,/isCoordenadorOrAcima/],[c,/export function TarefaCard/],[b,/export function TarefaStatusBadge/],[b,/em_andamento/]];const f=ck.filter(([s,r])=>!r.test(s));if(f.length){console.error('Missing:',f.map(([,r])=>String(r)));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tarefas/TarefaModal.tsx` exports `TarefaModal` via named export; imports `zodResolver`, `useForm`, declares zod schema with `titulo: z.string().min(3, ...)`.
    - `src/components/tarefas/TarefaCard.tsx` exports `TarefaCard`; imports `useCurrentRole`; contains literal `isCoordenadorOrAcima` used in conditional rendering of edit/remove buttons.
    - `src/components/tarefas/TarefaStatusBadge.tsx` exports `TarefaStatusBadge`; handles 4 statuses including literal `em_andamento`.
    - All three files use named exports (no `export default`).
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>Tres componentes criados, tipo-checks passam, gate de permissao implementado no TarefaCard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar TarefasPage e registrar rota /tarefas</name>
  <files>src/pages/TarefasPage.tsx, src/router.tsx</files>
  <read_first>
    - src/pages/AuditoriaPage.tsx (estrutura completa de pagina com Card + h1 + RequireRole)
    - src/components/me/TarefasPanel.tsx (linhas 104-159: filter bar + botao "Nova tarefa"; linhas 161-269: lista com divide-y; linhas 161-169: empty state)
    - src/router.tsx (linhas 40-78: lista de children de AppLayout)
    - .planning/phases/01-tasks-adoption-signal/01-UI-SPEC.md (secao "4. TarefasPage (/tarefas)")
    - src/hooks/useTarefas.ts (useMinhasTarefas, useTarefas, useConcluirTarefa, useDeleteTarefa)
    - src/components/tarefas/TarefaModal.tsx (criado na Task 2)
    - src/components/tarefas/TarefaCard.tsx (criado na Task 2)
  </read_first>
  <behavior>
    - Acessar `/tarefas` renderiza pagina com h1 "Tarefas".
    - Pagina carrega tarefas do usuario logado via `useMinhasTarefas(perfil.id)` por default (escopo 'mine').
    - Coordenador+ vê ScopeToggle e pode alternar para 'all' que usa `useTarefas()`.
    - Filtro Status (Select h-8 w-36) com 4 opcoes: "Todos os status", "Abertas", "Em andamento", "Concluidas" — persistido em `useSearchParams` chave `status`.
    - Filtro Data (Select h-8 w-40) com 4 opcoes: "Qualquer data", "Vencendo hoje", "Esta semana", "Atrasadas" — persistido em `useSearchParams` chave `data`.
    - CTA "Nova tarefa" (`Button h-8 bg #0089ac`) abre TarefaModal em mode='create' sem `entidadeTipo`.
    - Lista vazia (zero tarefas) mostra empty state com CheckCircle2 verde, "Nenhuma tarefa pendente", "Voce esta em dia! Crie uma nova tarefa ou aguarde atribuicoes."
    - Lista com filtros sem resultado mostra "Nenhuma tarefa com esses filtros" / "Tente remover alguns filtros para ver mais tarefas."
    - Ordenacao: `data_vencimento ASC` com nulls last.
  </behavior>
  <action>
    Criar `src/pages/TarefasPage.tsx` com named export `function TarefasPage()`. Imports: hooks `useMinhasTarefas`, `useTarefas`, `useConcluirTarefa`, `useDeleteTarefa` de `@/hooks/useTarefas`; `useMeuPerfil` de `@/hooks/usePerfis`; `useCurrentRole`; `useSearchParams` de react-router-dom; componentes `TarefaCard`, `TarefaModal` de `@/components/tarefas/*`; `ScopeToggle` de `@/components/shared/ScopeToggle`; primitivos Card/CardContent/Select/Button/Input. Estrutura: (1) hooks de estado: `searchParams`/`setSearchParams`, `statusFilter` (default 'todas'), `dataFilter` (default 'qualquer'), `scope` (default 'mine'), `modalOpen` (default false), `editingTarefa` (default null); (2) hook de role para condicionar `ScopeToggle`; (3) query baseada em `scope` — `useMinhasTarefas(perfil?.id)` quando 'mine', `useTarefas()` quando 'all' (so visivel para `isCoordenadorOrAcima`); (4) memo `filtradas` aplicando statusFilter e dataFilter; (5) sort por data_vencimento asc nulls last; (6) JSX: `<h1 className="text-xl font-bold text-foreground mb-6">Tarefas</h1>`, filter bar `<div className="flex items-center gap-3 flex-wrap mb-4">`, Card com `<CardContent className="p-0"><div className="divide-y">` mapeando `<TarefaCard tarefa={t} userId={perfil.id} onConcluir={...} onEditar={t => { setEditingTarefa(t); setModalOpen(true) }} onRemover={...} />`, empty state condicional, modal renderizado no fim. Helpers de filtro: `dataFilter='hoje'` filtra `tarefas.data_vencimento` entre `startOfDay(now)` e `endOfDay(now)` (date-fns); `'semana'` entre `startOfWeek` e `endOfWeek`; `'atrasadas'` < `now()`. Usar `useSearchParams` para sync URL <-> filtros (set/get).

    Em `src/router.tsx`: (1) adicionar import `import { TarefasPage } from '@/pages/TarefasPage'` ao topo apos os outros imports de pages; (2) dentro do array `children` de AppLayout (apos linha 77 antes do bloco do portal), adicionar `{ path: 'tarefas', element: <TarefasPage /> },`.
  </action>
  <verify>
    <automated>npx tsc -b && node -e "const p=require('fs').readFileSync('src/pages/TarefasPage.tsx','utf8');const r=require('fs').readFileSync('src/router.tsx','utf8');const ck=[[p,/export function TarefasPage/],[p,/useSearchParams/],[p,/Nova tarefa/],[p,/Nenhuma tarefa pendente/],[p,/useMinhasTarefas/],[r,/TarefasPage/],[r,/path:\s*['\"]tarefas['\"]/]];const f=ck.filter(([s,re])=>!re.test(s));if(f.length){console.error('Missing:',f.map(([,re])=>String(re)));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/TarefasPage.tsx` exports `TarefasPage` (named).
    - File imports `useSearchParams` from `react-router-dom` and uses it to control filters.
    - File renders h1 with literal text "Tarefas".
    - File renders button containing text "Nova tarefa".
    - File renders empty-state text "Nenhuma tarefa pendente".
    - File calls `useMinhasTarefas`.
    - `src/router.tsx` imports `TarefasPage` and registers `{ path: 'tarefas', element: <TarefasPage /> }` inside AppLayout children.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>Rota /tarefas operacional; usuario navega, ve inbox, abre modal e cria/conclui/remove tarefas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (consultor logado) -> tabela `tarefas` via Supabase JS | Hooks fazem INSERT/UPDATE/DELETE direto com anon key; RLS da Wave 1 (policies `interno_*`) garante isolamento de clientes |
| Browser (consultor logado) -> botoes editar/remover de tarefa alheia | UI gate via `useCurrentRole.isCoordenadorOrAcima` — RLS aceita qualquer interno (TASK-04 fica como gate de UX) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | TarefaCard (botoes editar/remover) | mitigate | TASK-04 gate: TarefaCard so renderiza botoes Editar/Remover quando `criado_por_id===userId || atribuido_a_id===userId || isCoordenadorOrAcima`. Consultor que tentar burlar via console/devtools cai na RLS (Wave 1) — qualquer interno passa `is_interno()`, mas o cenario realista que importa eh UX, nao seguridade hard. Validado por test stub `it.todo` em useTarefas.test.tsx (preenchimento posterior caso necessario) |
| T-01-05 | Input Validation | TarefaModal (campo titulo/descricao/notas) | mitigate | zod schema obriga titulo >=3 chars; demais campos sao texto sem renderizacao html (Radix Input sanitiza). Sem injecao XSS por estes campos |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado (confirmado RESEARCH.md). react-hook-form, zod, @hookform/resolvers ja em package.json |
</threat_model>

<verification>
- [ ] `npm test -- useTarefas --run` passa (Wave 0 gap fechado)
- [ ] `npx tsc -b` passa
- [ ] Navegar para /tarefas em dev (`npm run dev`) renderiza pagina sem erros de console
- [ ] Criar tarefa interna via modal funciona (tarefa aparece no inbox)
- [ ] Concluir tarefa via Circle muda status e remove do inbox (apos refetch)
</verification>

<success_criteria>
1. Usuario abre `/tarefas` e ve apenas suas tarefas abertas/em_andamento ordenadas por vencimento.
2. Usuario cria tarefa interna (sem lead) via "Nova tarefa" e ela aparece no inbox imediatamente apos onSuccess.
3. Usuario conclui tarefa propria via Circle; status muda para concluida e linha some do inbox.
4. Consultor nao vê botoes Editar/Remover em tarefas que nao criou nem foi atribuido a ele; coordenador vê em todas.
5. Filtros Status e Data persistem na URL (refresh mantem filtro).
6. Empty state correto renderiza quando nao ha tarefas.
</success_criteria>

<output>
Criar `.planning/phases/01-tasks-adoption-signal/01-02-SUMMARY.md` com: (a) arquivos criados + LOC aproximado, (b) decisao sobre TarefaModal mode='edit' (implementacao completa ou stub), (c) resultado de `npm test -- useTarefas`, (d) screenshots ou descricao da pagina /tarefas em dev (se aplicavel).
</output>
