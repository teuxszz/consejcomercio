---
phase: 08-individual-performance-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/types/index.ts
  - src/lib/slug.ts
  - src/lib/desempenho.ts
  - src/lib/desempenho-period.ts
  - src/components/mensagens/BlocoEditorModal.tsx
  - src/lib/__tests__/desempenho.test.ts
  - src/lib/__tests__/slug.test.ts
  - src/lib/__tests__/csv-export.test.ts
  - src/components/desempenho/__tests__/DesempenhoReport.test.tsx
  - src/components/desempenho/__tests__/ExportarPDFButton.test.tsx
  - src/test/pdf-mocks.ts
autonomous: true
requirements:
  - REP-01
  - REP-02
  - REP-03
  - REP-04
tags:
  - phase-08
  - pdf-export
  - csv-export
  - pure-helper

must_haves:
  truths:
    - "4 libs export (jspdf, html2canvas, papaparse, jszip) instaladas e tipadas"
    - "DesempenhoMetricas e DesempenhoConsultorTeam interfaces em src/types/index.ts"
    - "slug helper extraído de BlocoEditorModal para src/lib/slug.ts (com regex hex)"
    - "calcularDesempenho calcula 8 métricas determinísticas + null-safety quando denominador é zero"
    - "Wave 0 scaffolds criados (5 test files + pdf-mocks) ANTES de tasks downstream"
    - "BlocoEditorModal refatorado para importar slugify de lib/slug com maxLen=32 preservado"
  artifacts:
    - path: "src/types/index.ts"
      provides: "DesempenhoMetricas + DesempenhoConsultorTeam interfaces"
      contains: "export interface DesempenhoMetricas"
    - path: "src/lib/slug.ts"
      provides: "slugify(input, maxLen=48) reutilizável"
      exports: ["slugify"]
    - path: "src/lib/desempenho.ts"
      provides: "calcularDesempenho({leads,tarefas,clientes,perfilId,perfilNome,periodo,servicosConfig}): DesempenhoMetricas"
      exports: ["calcularDesempenho"]
      contains: "TERMINAL_WON_STAGES"
    - path: "src/lib/desempenho-period.ts"
      provides: "loadPeriod()/savePeriod() via localStorage 'consej_desempenho_period'"
      exports: ["loadPeriod", "savePeriod"]
    - path: "src/test/pdf-mocks.ts"
      provides: "mockJsPdf, mockHtml2canvas, mockJSZip, mockPapaparse, mockBlobUrlAndAnchor"
      contains: "vi.fn"
  key_links:
    - from: "src/lib/desempenho.ts"
      to: "src/lib/icp-dinamico.ts"
      via: "calcularIcpDinamico + buildIcpFitContext + isLeadIcpFit (icp_fit_medio)"
      pattern: "isLeadIcpFit"
    - from: "src/lib/desempenho.ts"
      to: "src/lib/periods.ts"
      via: "getPeriodRange + isInRange (filtro temporal)"
      pattern: "getPeriodRange"
    - from: "src/lib/desempenho.ts"
      to: "src/lib/constants.ts"
      via: "TERMINAL_WON_STAGES + TERMINAL_LOST_STAGES"
      pattern: "TERMINAL_WON_STAGES"
    - from: "src/components/mensagens/BlocoEditorModal.tsx"
      to: "src/lib/slug.ts"
      via: "import { slugify }"
      pattern: "from '@/lib/slug'"
---

<objective>
Fase 8 Slice 1 — Foundation. Instalar libs de export, estender types com DesempenhoMetricas/DesempenhoConsultorTeam (D-10), extrair slug helper de BlocoEditorModal, criar helper puro calcularDesempenho (D-03) que agrega as 8 métricas REP-01, criar localStorage hydration (D-06), e scaffolds de teste do Wave 0 (6 files de VALIDATION.md) para tasks downstream rodarem TDD-friendly.

Purpose: REP-01 helper (8 métricas) + dependências de export (REP-02/03/04 dependem das libs + slug). Sem libs e sem helper, slices 2-4 ficam bloqueados. Sem Wave 0 scaffolds, slices 2-4 violariam Nyquist (gates `<automated>` apontariam para arquivos inexistentes).

Output: 4 packages npm instalados, 2 interfaces novas em types/index.ts, 2 libs novas (slug + desempenho), 1 lib pequena de localStorage, 5 test files com 1 teste verde cada (helper REP-01 entregue funcional), 1 file de mocks, refactor de BlocoEditorModal para consumir slug.

REP-01 helper, REP-02/03/04 foundation (per D-01, D-03, D-04, D-06, D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-individual-performance-reports/08-CONTEXT.md
@.planning/phases/08-individual-performance-reports/08-RESEARCH.md
@.planning/phases/08-individual-performance-reports/08-PATTERNS.md
@.planning/phases/08-individual-performance-reports/08-VALIDATION.md
@src/lib/icp-dinamico.ts
@src/lib/periods.ts
@src/lib/constants.ts
@src/lib/__tests__/icp-dinamico.test.ts
@src/types/index.ts
@src/components/mensagens/BlocoEditorModal.tsx
@src/test/storage-mocks.ts
@src/contexts/ThemeContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar libs export + adicionar interfaces D-10 em src/types/index.ts</name>
  <files>
    package.json,
    package-lock.json,
    src/types/index.ts
  </files>
  <read_first>
    - `src/types/index.ts` (linhas 1-130) — ver style de export interface, Lead/Cliente/Contrato existentes
    - `.planning/phases/08-individual-performance-reports/08-PATTERNS.md` §16 (linha 553-583) — shape exato das interfaces D-10
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` §Installation (linhas 132-139) — comando npm + nota sobre @types/jszip não necessário
    - `CLAUDE.md` — "package-lock.json nunca lido com Read" (~92k tokens)
  </read_first>
  <action>
    Per D-10 e RESEARCH Installation:

    1. Instalar libs em paralelo: `rtk npm install jspdf@^2.5 html2canvas@^1.4 papaparse@^5.4 jszip@^3.10 && rtk npm install -D @types/papaparse`. Nota: `@types/jszip` NÃO necessário (jszip 3.10 ships types). `legacy-peer-deps=true` já está em `.npmrc` (React 19 vs peers React 18).

    2. Adicionar em `src/types/index.ts` (após interfaces existentes Cliente/Contrato; manter 2-space indent, single quotes, sem semicolons):
       - `import type { PeriodValue } from '@/lib/periods'` no topo se não existir
       - `export interface DesempenhoMetricas` com 11 campos exatos: `perfilId: string`, `perfilNome: string`, `periodo: PeriodValue`, `leads_criados: number`, `convertidos: number`, `perdidos: number`, `ciclo_medio_dias: number | null`, `win_rate: number` (0-100), `icp_fit_medio: number | null`, `tarefas_concluidas: number`, `nps_medio: number | null`
       - `export interface DesempenhoConsultorTeam` com `perfilId: string`, `perfilNome: string`, `metricas: DesempenhoMetricas`

    NÃO duplicar Lead/Tarefa/Cliente (CLAUDE.md anti-pattern). Names em snake_case domínio PT-BR consistentes com schema.
  </action>
  <acceptance_criteria>
    - `rtk npm ls jspdf html2canvas papaparse jszip @types/papaparse` lista as 5 packages sem WARN/ERR
    - `grep -n "export interface DesempenhoMetricas" src/types/index.ts` retorna 1 linha
    - `grep -n "export interface DesempenhoConsultorTeam" src/types/index.ts` retorna 1 linha
    - `rtk npm run lint` passa
    - `rtk npx tsc --noEmit` passa
  </acceptance_criteria>
  <verify>
    <automated>rtk npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>4 libs instaladas + @types/papaparse devDep + 2 interfaces D-10 em types/index.ts. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extrair slugify para src/lib/slug.ts + refactor BlocoEditorModal + tests slug</name>
  <files>
    src/lib/slug.ts,
    src/lib/__tests__/slug.test.ts,
    src/components/mensagens/BlocoEditorModal.tsx
  </files>
  <read_first>
    - `src/components/mensagens/BlocoEditorModal.tsx` linhas 30-45 — função privada slugify atual
    - `.planning/phases/08-individual-performance-reports/08-PATTERNS.md` §2 (linhas 91-126) — CRITICAL fix: regex hex `̀-ͯ` em vez de range literal
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` §Pattern 7 (linhas 703-718)
    - `src/lib/__tests__/icp-dinamico.test.ts` linhas 1-30 — style de import + describe/it
  </read_first>
  <action>
    Per RESEARCH Pattern 7 + PATTERNS §2 CRITICAL fix:

    1. Criar `src/lib/slug.ts` com export nomeado `slugify(input: string, maxLen = 48): string`:
       - `.normalize('NFD')` para decompor diacritics
       - `.replace(/[̀-ͯ]/g, '')` — usar ESCAPE HEX (não range literal `̀-ͯ`); regex literal quebra em editors sem UTF-8 e em diffs
       - `.toLowerCase()`
       - `.replace(/[^a-z0-9]+/g, '-')`
       - `.replace(/^-+|-+$/g, '')`
       - `.slice(0, maxLen)`

    2. Criar `src/lib/__tests__/slug.test.ts` cobrindo ≥6 casos (PATTERNS §14 linhas 526-535):
       - acentos pt-BR: `slugify('Conceição')` === `'conceicao'`
       - espaços → dashes: `slugify('João Silva')` === `'joao-silva'`
       - lowercase: `slugify('GABRIEL ARAUJO')` === `'gabriel-araujo'`
       - special chars: `slugify('Maria, & "Eduarda"!')` === `'maria-eduarda'`
       - truncate maxLen: `slugify('a'.repeat(100), 10).length` === 10
       - trim leading/trailing: `slugify('-foo-')` === `'foo'`

    3. Refactor `src/components/mensagens/BlocoEditorModal.tsx`:
       - Remover função privada `slugify` (linhas ~32-40)
       - Adicionar `import { slugify } from '@/lib/slug'` no bloco de imports `@/lib/*`
       - Trocar call sites para `slugify(input, 32)` (PRESERVAR maxLen=32 comportamento original — default da lib agora é 48, callers do BlocoEditorModal devem explicitar 32)
  </action>
  <acceptance_criteria>
    - `rtk npm test -- src/lib/__tests__/slug.test.ts` passa com ≥6 testes verdes
    - `grep -n "function slugify" src/components/mensagens/BlocoEditorModal.tsx | grep -v "^#" | grep -vc "import"` retorna 0 (função privada removida)
    - `grep -n "from '@/lib/slug'" src/components/mensagens/BlocoEditorModal.tsx` retorna ≥1 linha
    - `grep -n "slugify(.*, 32)" src/components/mensagens/BlocoEditorModal.tsx` retorna ≥1 linha (maxLen=32 preservado)
    - `rtk npx tsc --noEmit` passa
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- src/lib/__tests__/slug.test.ts --run</automated>
  </verify>
  <done>slug.ts exporta slugify; 6 tests verdes; BlocoEditorModal consome lib preservando maxLen=32; nenhuma duplicação de função.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Helper puro calcularDesempenho + desempenho-period + tests (REP-01)</name>
  <files>
    src/lib/desempenho.ts,
    src/lib/desempenho-period.ts,
    src/lib/__tests__/desempenho.test.ts
  </files>
  <read_first>
    - `src/lib/icp-dinamico.ts` (todo) — pattern de pure helper + interfaces locais + null-handling
    - `src/lib/__tests__/icp-dinamico.test.ts` — pattern fixture builders + describe + edge cases
    - `src/lib/periods.ts` — assinatura de `getPeriodRange(period: PeriodValue)` + `isInRange(iso, range)` + `getCurrentYear()`
    - `src/lib/constants.ts` — TERMINAL_WON_STAGES (linha 18) + TERMINAL_LOST_STAGES (linha 20)
    - `src/types/index.ts` (após Task 1) — DesempenhoMetricas
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` §Pattern 1 (linhas 281-380) — skeleton completo de calcularDesempenho
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` Open Question 1 + Pitfall 6 (linha 875) — clientes do consultor via contratos.responsavel_id
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` §Pattern 8 (linhas 720-741) — loadPeriod/savePeriod
  </read_first>
  <behavior>
    - Test 1: lista vazia (sem leads/tarefas/clientes) → todas métricas zero ou null (leads_criados=0, convertidos=0, perdidos=0, win_rate=0, tarefas_concluidas=0, ciclo_medio_dias=null, icp_fit_medio=null, nps_medio=null)
    - Test 2: filtra por responsavel_id — leads de outro perfil são ignorados
    - Test 3: filtra por período — leads created_at fora do range são ignorados
    - Test 4: win_rate = round(ganhos/(ganhos+perdidos) * 100) — fixture 3 ganhos + 1 perdido → 75
    - Test 5: ciclo_medio_dias = média de (updated_at - created_at) em dias somente para leads em TERMINAL_WON_STAGES; sem ganhos → null
    - Test 6: nps_medio agrega clientes via contratos.responsavel_id === perfilId (per Pitfall 6 decisão); 0 clientes → null
    - Test 7: tarefas_concluidas conta tarefas com atribuido_a_id=perfilId + status='concluida' + data_conclusao IN range
    - Test 8: icp_fit_medio = round((leads com isLeadIcpFit / leads_criados) * 100); 0 leads → null
    - Test 9 (slug-period): loadPeriod() retorna default {year: currentYear, granularity: 'total'} quando localStorage vazio
    - Test 10 (slug-period): savePeriod() persiste JSON em 'consej_desempenho_period' e loadPeriod() recupera
    - Test 11 (slug-period): loadPeriod() retorna default se JSON inválido (T-08-07 — tampering)
  </behavior>
  <action>
    Per D-03 + RESEARCH §Pattern 1 + Pitfall 6 (clientes via contratos.responsavel_id):

    1. Criar `src/lib/desempenho.ts` (2-space indent, single quotes, sem semicolons):
       - Imports: `type { Lead, Tarefa, Cliente, Contrato, DesempenhoMetricas } from '@/types'`; `type { PeriodValue } from './periods'`; `{ getPeriodRange, isInRange } from './periods'`; `{ TERMINAL_WON_STAGES, TERMINAL_LOST_STAGES } from './constants'`; `{ calcularIcpDinamico, buildIcpFitContext, isLeadIcpFit } from './icp-dinamico'`
       - Header comment explicando regras de negócio: "leads_criados = filter responsavel_id + created_at IN periodo; convertidos/perdidos = subset por status terminal; ciclo_medio_dias = AVG(updated_at - created_at) só ganhos; nps_medio derivado de clientes via contratos.responsavel_id===perfilId (Cliente não tem responsavel_id direto)"
       - Export interface `CalcularDesempenhoInput`: `leads: Lead[]`, `tarefas: Tarefa[]`, `clientes: Cliente[]`, `contratos: Contrato[]`, `perfilId: string`, `perfilNome: string`, `periodo: PeriodValue`, `servicosConfig: Array<{ id: string; segmentos_icp?: string[]; investimento_icp?: string[] }>`
       - Export function `calcularDesempenho(input: CalcularDesempenhoInput): DesempenhoMetricas`:
         - Filtrar leadsPerfil por `l.responsavel_id === perfilId && isInRange(l.created_at, range)`
         - Cast `(TERMINAL_WON_STAGES as readonly string[]).includes(l.status)` para ganhos; análogo perdidos
         - ciclo_medio_dias: filtrar ganhos com diff >= 1 dia → round(média); null se vazio
         - win_rate: round((ganhos.length / (ganhos.length + perdidos.length)) * 100); 0 se denominador zero
         - icp_fit_medio: calcularIcpDinamico(leads completos, periodo, servicoIds) → buildIcpFitContext → isLeadIcpFit per lead; round((fitCount/leadsPerfil.length)*100); null se leadsPerfil vazio
         - tarefas_concluidas: filter atribuido_a_id===perfilId && status==='concluida' && data_conclusao && isInRange(data_conclusao, range)
         - nps_medio: clientesDoPerfil = clientes filtrados onde EXISTE contrato em `contratos` com `c.cliente_id === cli.id && c.responsavel_id === perfilId` (per Pitfall 6); filtrar `typeof cli.nps_score === 'number'`; round((soma/count)*10)/10; null se vazio
         - Retornar shape DesempenhoMetricas

    2. Criar `src/lib/desempenho-period.ts` per RESEARCH §Pattern 8:
       - `const KEY = 'consej_desempenho_period'`
       - `export function loadPeriod(): PeriodValue` com try/catch + typeof validation + fallback `{ year: getCurrentYear(), granularity: 'total' }`
       - `export function savePeriod(v: PeriodValue): void` com try/catch silent

    3. Criar `src/lib/__tests__/desempenho.test.ts` espelhando icp-dinamico.test.ts:
       - Builder helpers: `leadFix({...overrides})`, `tarefaFix({...})`, `clienteFix({...})`, `contratoFix({...})` no topo
       - Constantes: `PERFIL_ID = 'p1'`, `PERIODO_2026: PeriodValue = { year: 2026, granularity: 'total' }`
       - `describe('calcularDesempenho')` com 8 testes (behavior 1-8)
       - `describe('desempenho-period')` com 3 testes (behavior 9-11), usando `beforeEach(() => localStorage.clear())`
  </action>
  <acceptance_criteria>
    - `rtk npm test -- src/lib/__tests__/desempenho.test.ts` passa com ≥11 testes verdes
    - `grep -n "export function calcularDesempenho" src/lib/desempenho.ts` retorna 1 linha
    - `grep -n "TERMINAL_WON_STAGES" src/lib/desempenho.ts` retorna ≥1 linha
    - `grep -n "isLeadIcpFit" src/lib/desempenho.ts` retorna ≥1 linha
    - `grep -n "consej_desempenho_period" src/lib/desempenho-period.ts` retorna 1 linha
    - `rtk npx tsc --noEmit` passa
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- src/lib/__tests__/desempenho.test.ts --run</automated>
  </verify>
  <done>calcularDesempenho retorna 8 métricas determinísticas com null-safety; loadPeriod/savePeriod com tampering guard; 11 tests verdes.</done>
</task>

<task type="auto">
  <name>Task 4: Wave 0 scaffolds — pdf-mocks + 3 test stubs faltantes (csv/DesempenhoReport/ExportarPDFButton)</name>
  <files>
    src/test/pdf-mocks.ts,
    src/lib/__tests__/csv-export.test.ts,
    src/components/desempenho/__tests__/DesempenhoReport.test.tsx,
    src/components/desempenho/__tests__/ExportarPDFButton.test.tsx
  </files>
  <read_first>
    - `src/test/storage-mocks.ts` (todo) — style canônico Phase 7 (factory + interface MockX + Restore type)
    - `.planning/phases/08-individual-performance-reports/08-PATTERNS.md` §12 (linhas 438-477) — pdf-mocks shape esperado
    - `.planning/phases/08-individual-performance-reports/08-VALIDATION.md` §Wave 0 Requirements — 6 files (5 já criados nos Tasks 2-3 + este task fecha)
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` Pitfall (linha 93-99) — jsdom não suporta canvas; mocks obrigatórios
  </read_first>
  <action>
    Per VALIDATION Wave 0 + PATTERNS §12 (espelha storage-mocks.ts):

    1. Criar `src/test/pdf-mocks.ts`:
       - Header comment: "Mock helpers para PDF/CSV export usados em Phase 8. Style espelhado de `src/test/storage-mocks.ts` — factory functions vi.fn-based, sem efeitos colaterais até helper ser chamado por teste. jsdom não suporta canvas (RESEARCH Pitfall)."
       - `import { vi } from 'vitest'`
       - `type Restore = () => void`
       - `export interface MockJsPdfOptions { shouldFail?: boolean }`
       - `export interface MockJsPdfResult { jsPDFClass: ReturnType<typeof vi.fn>; savedFiles: string[]; addImageFn: ReturnType<typeof vi.fn>; addPageFn: ReturnType<typeof vi.fn>; saveFn: ReturnType<typeof vi.fn>; restore: Restore }`
       - `export function mockJsPdf(opts: MockJsPdfOptions = {}): MockJsPdfResult` — vi.mock('jspdf') com class fake { addImage, addPage, save(filename) { savedFiles.push(filename) } }
       - Análogo: `mockHtml2canvas({ canvas? })` — retorna fake canvas com toDataURL() === 'data:image/png;base64,FAKE'
       - `mockJSZip({ shouldFail? })` — fake class com file() acumulando em addedFiles[]; generateAsync retorna Blob fake
       - `mockPapaparse({ unparseImpl? })` — vi.mock('papaparse') com Papa.unparse spy
       - `mockBlobUrlAndAnchor()` — instala URL.createObjectURL/revokeObjectURL e document.createElement('a').click spy (jsdom não tem)
       - Cada result expõe `restore: () => fn.mockReset()`

    2. Criar `src/lib/__tests__/csv-export.test.ts` como STUB (csv-export.ts será criado no Slice 3):
       - Imports: `vi`, `describe`, `it`, `expect`, helpers de `@/test/pdf-mocks`
       - `describe.skip('csv-export', () => { it.skip('TODO: implementar em Slice 3 (Plan 03)', () => {}) })` — describe.skip evita falha mas mantém arquivo presente
       - Comentário no topo: "Stub criado em Plan 01 Wave 0. Tests implementados em Plan 03 (slice export). Per VALIDATION.md Wave 0 Requirements."

    3. Criar `src/components/desempenho/__tests__/DesempenhoReport.test.tsx` STUB análogo:
       - `describe.skip('DesempenhoReport', () => { it.skip('TODO: implementar em Slice 2 (Plan 02)', () => {}) })`

    4. Criar `src/components/desempenho/__tests__/ExportarPDFButton.test.tsx` STUB:
       - `describe.skip('ExportarPDFButton', () => { it.skip('TODO: implementar em Slice 3 (Plan 03)', () => {}) })`

    Importante: stubs com `describe.skip` cumprem requisito Wave 0 (arquivos existem) sem falhar suite quando production code ainda não existe. Plans 02-03 substituem os describe.skip por testes reais.
  </action>
  <acceptance_criteria>
    - `ls src/test/pdf-mocks.ts src/lib/__tests__/csv-export.test.ts src/components/desempenho/__tests__/DesempenhoReport.test.tsx src/components/desempenho/__tests__/ExportarPDFButton.test.tsx` lista 4 arquivos
    - `grep -c "export function mock" src/test/pdf-mocks.ts` retorna ≥4 (jsPdf, html2canvas, JSZip, papaparse, BlobUrlAndAnchor)
    - `grep -c "describe.skip" src/lib/__tests__/csv-export.test.ts src/components/desempenho/__tests__/*.test.tsx` retorna ≥3
    - `rtk npm test` passa (skipped não falha)
    - `rtk npx tsc --noEmit` passa
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test --run 2>&1 | tail -30</automated>
  </verify>
  <done>pdf-mocks com 5 helpers + 3 test stubs com describe.skip + suite global green; Wave 0 (6 files) completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (perfilNome) → filename | Slice 1 só estabelece slug helper (T-08-02 mitigação preparada); consumido em Plan 03 |
| localStorage → app state | loadPeriod lê JSON externo (T-08-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | `src/lib/slug.ts::slugify` | mitigate | `.replace(/[^a-z0-9]+/g, '-')` strip de todo char não-alfanumérico antes de virar filename; testes cobrem injeção de `/` `\` `:` |
| T-08-07 | Tampering | `src/lib/desempenho-period.ts::loadPeriod` | mitigate | `typeof parsed.year === 'number' && typeof parsed.granularity === 'string'` validação antes de retornar; fallback para default em qualquer falha (try/catch + early return) |
| T-08-SC | Tampering | npm installs (jspdf, html2canvas, papaparse, jszip, @types/papaparse) | mitigate | RESEARCH §Package Legitimacy Audit linha 152-167 — todos 5 marcados [OK] pelo slopcheck 2026-05-29; postinstall scripts ausentes em todos; sem cross-ecosystem confusion |
</threat_model>

<verification>
- Suite global: `rtk npm test` ≤ 60s, full green
- Lint: `rtk npm run lint` clean
- Types: `rtk npx tsc --noEmit` clean
- Coverage Wave 0: 6 arquivos criados conforme VALIDATION.md
</verification>

<success_criteria>
- 4 packages instalados sem WARN
- DesempenhoMetricas + DesempenhoConsultorTeam em types/index.ts (D-10)
- slug.ts + 6 tests verdes; BlocoEditorModal refactored preservando maxLen=32
- calcularDesempenho com 8 métricas + 8 tests verdes
- desempenho-period com 3 tests verdes
- pdf-mocks com 5 mock factories
- 3 test stubs (describe.skip) presentes para Plan 02/03 consumirem
- npm test full green; tsc clean; lint clean
</success_criteria>

<output>
Create `.planning/phases/08-individual-performance-reports/08-01-SUMMARY.md` ao final descrevendo libs instaladas, tipos adicionados, helpers criados e o que ficou disponível para Slice 2 (página + render).
</output>
