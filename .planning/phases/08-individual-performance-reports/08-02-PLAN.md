---
phase: 08-individual-performance-reports
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/desempenho/KPICard.tsx
  - src/components/desempenho/DesempenhoKpiGrid.tsx
  - src/components/desempenho/DesempenhoReport.tsx
  - src/components/desempenho/DesempenhoFunilChart.tsx
  - src/components/desempenho/DesempenhoTimelineChart.tsx
  - src/components/desempenho/DesempenhoTarefasChart.tsx
  - src/pages/MeDesempenhoPage.tsx
  - src/router.tsx
  - src/components/me/PerfilPanel.tsx
  - src/components/desempenho/__tests__/DesempenhoReport.test.tsx
autonomous: true
requirements:
  - REP-01
tags:
  - phase-08
  - ui
  - off-screen-render
  - role-aware

must_haves:
  truths:
    - "Consultor visita /me/desempenho e vê 8 KPIs + 4 charts no período selecionado"
    - "Coordenador+ visita /me/desempenho/:perfilId e vê dados de outro consultor; consultor regular é bloqueado por RequireRole quando perfilId != self"
    - "PeriodSelector trocado persiste em localStorage 'consej_desempenho_period' (D-06)"
    - "PerfilPanel mostra link 'Ver meu desempenho' apontando para /me/desempenho (D-09)"
    - "DesempenhoReport renderiza em portal off-screen com 3 data-pdf-page divs e isAnimationActive={false} em todos os charts"
    - "Charts forçam fundo branco e cores HSL paleta CONSEJ (--cyan-hi, --emerald-mid, --amber-mid) para captura PDF futura"
  artifacts:
    - path: "src/components/desempenho/KPICard.tsx"
      provides: "KPI card com null-safe value (renderiza '—' quando null)"
      exports: ["KPICard"]
    - path: "src/components/desempenho/DesempenhoKpiGrid.tsx"
      provides: "Grid 4x2 com as 8 métricas DesempenhoMetricas"
      exports: ["DesempenhoKpiGrid"]
    - path: "src/components/desempenho/DesempenhoReport.tsx"
      provides: "Off-screen renderer 3 páginas A4 794px width com data-pdf-page=1|2|3"
      exports: ["DesempenhoReport"]
      contains: "createPortal"
    - path: "src/pages/MeDesempenhoPage.tsx"
      provides: "Página orchestradora consumindo calcularDesempenho + PeriodSelector"
      exports: ["MeDesempenhoPage"]
    - path: "src/router.tsx"
      provides: "2 rotas: /me/desempenho e /me/desempenho/:perfilId"
      contains: "MeDesempenhoPage"
  key_links:
    - from: "src/pages/MeDesempenhoPage.tsx"
      to: "src/lib/desempenho.ts"
      via: "calcularDesempenho(input)"
      pattern: "calcularDesempenho"
    - from: "src/pages/MeDesempenhoPage.tsx"
      to: "src/components/shared/RequireRole.tsx"
      via: "wrap condicional quando params.perfilId !== meuPerfil.id"
      pattern: "RequireRole atLeast=\"coordenador\""
    - from: "src/router.tsx"
      to: "src/pages/MeDesempenhoPage.tsx"
      via: "imported route element"
      pattern: "MeDesempenhoPage"
    - from: "src/components/desempenho/DesempenhoReport.tsx"
      to: "src/components/desempenho/DesempenhoKpiGrid.tsx"
      via: "import + render inside data-pdf-page=1"
      pattern: "DesempenhoKpiGrid"
    - from: "src/components/me/PerfilPanel.tsx"
      to: "/me/desempenho"
      via: "<Link to>"
      pattern: "Link to=\"/me/desempenho\""
---

<objective>
Fase 8 Slice 2 — Página visível + off-screen renderer. Consumir calcularDesempenho (Plan 01) para renderizar /me/desempenho com PeriodSelector + 8 KPIs + 4 charts. Criar componente off-screen DesempenhoReport pronto para captura PDF futura (Slice 3). Wire routing (D-02), RequireRole condicional (Pitfall 4), localStorage hydration (D-06), e link discovery no PerfilPanel (D-09). Sem exportação ainda — apenas a view.

Purpose: REP-01 entrega valor end-to-end aqui — consultor vê seus dados de performance no app. Charts off-screen ficam pré-instalados; Slice 3 só adiciona o botão de export. Plano isola toda complexidade de UI/routing/role para Slice 3 só lidar com pdf-export.ts + buttons.

Output: 1 página, 6 componentes (KPICard, KpiGrid, Report, 3 charts), 2 rotas, link no PerfilPanel.

REP-01 view (per D-02, D-05, D-06, D-09, D-11; Pitfall 1 isAnimationActive=false; Pitfall 4 RequireRole condicional).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-individual-performance-reports/08-CONTEXT.md
@.planning/phases/08-individual-performance-reports/08-RESEARCH.md
@.planning/phases/08-individual-performance-reports/08-PATTERNS.md
@.planning/phases/08-individual-performance-reports/08-01-SUMMARY.md
@src/pages/ReceitaPage.tsx
@src/pages/DashboardPage.tsx
@src/pages/AnalyticsPage.tsx
@src/components/shared/PeriodSelector.tsx
@src/components/shared/RequireRole.tsx
@src/hooks/usePerfis.ts
@src/hooks/useLeads.ts
@src/hooks/useTarefas.ts
@src/hooks/useClientes.ts
@src/hooks/useConfiguracoes.ts
@src/router.tsx
@src/components/me/PerfilPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KPICard + DesempenhoKpiGrid + 3 chart components (off-screen capable)</name>
  <files>
    src/components/desempenho/KPICard.tsx,
    src/components/desempenho/DesempenhoKpiGrid.tsx,
    src/components/desempenho/DesempenhoFunilChart.tsx,
    src/components/desempenho/DesempenhoTimelineChart.tsx,
    src/components/desempenho/DesempenhoTarefasChart.tsx
  </files>
  <read_first>
    - `src/pages/ReceitaPage.tsx` linhas 80-106 — `KpiCard` analog (extrair para Phase 8)
    - `src/pages/DashboardPage.tsx` linhas 249-268 (KPI grid 4x2) + linhas 395-407 (BarChart funil)
    - `src/pages/ReceitaPage.tsx` linhas 305-335 — LineChart pattern com `isAnimationActive={false}`
    - `.planning/phases/08-individual-performance-reports/08-PATTERNS.md` §6 (linhas 195-252) — deltas Phase 8 vs analogs (fundo branco + isAnimationActive=false + cores HSL)
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` Pitfall 1 (linhas 846-850) — animation must be off
    - `src/types/index.ts` — DesempenhoMetricas (após Plan 01)
  </read_first>
  <behavior>
    - Test 1 (KPICard): renderiza title + value
    - Test 2 (KPICard): value=null → renderiza '—' (fallback NPS undefined)
    - Test 3 (KpiGrid): com 8 métricas renderiza 8 cards numa grid CSS class `grid-cols-2 lg:grid-cols-4`
    - Test 4 (KpiGrid): nps_medio=null + ciclo_medio_dias=null + icp_fit_medio=null → 3 cards mostram '—' sem crash
  </behavior>
  <action>
    Per PATTERNS §6 + §8 + D-11:

    1. Criar `src/components/desempenho/KPICard.tsx`:
       - Props: `{ title: string; value: string | number | null; icon: React.FC<{ className?: string }>; tone?: 'cyan' | 'emerald' | 'amber' | 'destructive' }`
       - `value === null` → render `—` (Claude's Discretion + NPS fallback documentado)
       - Para numbers, render diretamente; strings inalterado
       - Style: Card + CardContent p-4 com tabular-nums; espelha ReceitaPage::KpiCard mas removendo onClick (PDF estático) e adicionando null-safety
       - Cores tone via classe Tailwind cyan-500 / emerald-500 / amber-500 / destructive (paleta CONSEJ D-11)

    2. Criar `src/components/desempenho/DesempenhoKpiGrid.tsx`:
       - Props: `{ metrics: DesempenhoMetricas }`
       - Renderiza grid `grid grid-cols-2 lg:grid-cols-4 gap-4` com 8 KPICards na ordem D-11:
         * Leads criados (lucide Users / cyan)
         * Convertidos (lucide CheckCircle2 / emerald)
         * Perdidos (lucide XCircle / destructive)
         * Win Rate (lucide Trophy / amber) — render `${value}%`
         * Ciclo médio (lucide Clock / cyan) — render `${value}d` quando number, `—` quando null
         * ICP fit médio (lucide Target / emerald) — render `${value}%` ou `—`
         * Tarefas concluídas (lucide CheckSquare / cyan)
         * NPS médio (lucide Star / amber) — render `${value}` (1 casa) ou `—`

    3. Criar `src/components/desempenho/DesempenhoFunilChart.tsx`:
       - Props: `{ metrics: DesempenhoMetricas }`
       - ResponsiveContainer + BarChart layout="vertical" com 3 barras (Criados / Convertidos / Perdidos)
       - `<Bar dataKey="value" isAnimationActive={false} fill="#0089ac" radius={[0, 4, 4, 0]} />` — Pitfall 1
       - XAxis/YAxis com tick fill cores light-mode visible (`#0d1929` para PDF capture)

    4. Criar `src/components/desempenho/DesempenhoTimelineChart.tsx`:
       - Props: `{ leads: Lead[]; periodo: PeriodValue; perfilId: string }`
       - Computar série mensal: 12 buckets (Jan-Dez do ano do período), `leads.filter(l => l.responsavel_id===perfilId && month(l.created_at)===i).length`
       - LineChart com `<Line type="monotone" dataKey="count" stroke="#0089ac" strokeWidth={2} dot={{r:3,fill:'#0089ac'}} isAnimationActive={false} />`
       - XAxis dataKey="month" tick PT-BR (Jan/Fev/.../Dez via date-fns ptBR)

    5. Criar `src/components/desempenho/DesempenhoTarefasChart.tsx`:
       - Props: `{ tarefas: Tarefa[]; periodo: PeriodValue; perfilId: string }`
       - BarChart vertical com 3 status (pendente, em_andamento, concluida) filtrados por atribuido_a_id+período (use isInRange em data_vencimento)
       - `isAnimationActive={false}` em Bar

    6. Atualizar `src/components/desempenho/__tests__/DesempenhoReport.test.tsx` (era describe.skip do Plan 01):
       - Trocar describe.skip por describe ativo
       - Adicionar testes para KPICard (behavior 1-2) e DesempenhoKpiGrid (behavior 3-4)
       - Usar @testing-library/react render + screen

    CRITICAL: Todos os charts recebem `isAnimationActive={false}` por default (não é prop dinâmica). Cores HSL/RGB fixas — NÃO usar CSS vars como `var(--cyan-hi)` em fill (html2canvas não resolve CSS vars confiavelmente). Hex literals.
  </action>
  <acceptance_criteria>
    - `rtk npm test -- src/components/desempenho/__tests__/DesempenhoReport.test.tsx --run` passa com ≥4 testes verdes
    - `grep -c "isAnimationActive={false}" src/components/desempenho/DesempenhoFunilChart.tsx src/components/desempenho/DesempenhoTimelineChart.tsx src/components/desempenho/DesempenhoTarefasChart.tsx | grep -v "^#"` retorna 3 (uma por chart)
    - `grep -n "value === null" src/components/desempenho/KPICard.tsx` retorna ≥1 linha
    - `grep -n "grid-cols-2 lg:grid-cols-4" src/components/desempenho/DesempenhoKpiGrid.tsx` retorna 1 linha
    - `rtk npx tsc --noEmit` passa
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- src/components/desempenho/__tests__/DesempenhoReport.test.tsx --run</automated>
  </verify>
  <done>5 componentes UI criados; null-safety em KPICard; isAnimationActive=false em 3 charts; 4 tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: DesempenhoReport off-screen renderer (portal 794px 3 páginas)</name>
  <files>
    src/components/desempenho/DesempenhoReport.tsx
  </files>
  <read_first>
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` §Pattern 2 (linhas 386-441) — portal off-screen skeleton completo
    - `.planning/phases/08-individual-performance-reports/08-PATTERNS.md` §6 (linhas 245-251) — deltas Phase 8: container + data-pdf-page
    - `.planning/phases/08-individual-performance-reports/08-CONTEXT.md` D-11 — layout exato 3 páginas A4
    - `src/types/index.ts` — DesempenhoMetricas + Perfil
  </read_first>
  <action>
    Per D-11 + RESEARCH §Pattern 2:

    Criar `src/components/desempenho/DesempenhoReport.tsx`:

    - Imports: `createPortal` de `'react-dom'`; types `Lead`, `Tarefa`, `DesempenhoMetricas`, `Perfil` de `@/types`; PeriodValue de `@/lib/periods`; date-fns + ptBR; componentes KPI + 3 charts
    - Props: `{ metrics: DesempenhoMetricas; perfilNome: string; leads: Lead[]; tarefas: Tarefa[]; geradoEm: Date }`
    - Constantes: `A4_PT_WIDTH = 794`, `A4_PT_HEIGHT = 1123`
    - Render: `createPortal(<div data-pdf-root style={{position:'fixed', top:'-9999px', left:'-9999px', width:A4_PT_WIDTH, pointerEvents:'none', background:'white', color:'#0d1929'}}>...</div>, document.body)`
    - 3 sub-divs filhos com `data-pdf-page="1" | "2" | "3"`, cada um com inline style `{width:A4_PT_WIDTH, height:A4_PT_HEIGHT, padding:40, background:'white', color:'#0d1929'}`:
      * Página 1: header `<div>` (logo CONSEJ por enquanto texto "CONSEJ" + título "Relatório de Desempenho" + nome consultor + período formatado via formatPeriodLabel + grid 4x2 com `<DesempenhoKpiGrid />` + footer com `Gerado em ${format(geradoEm, 'PPP', {locale: ptBR})}`
      * Página 2: title "Funil de Conversão" + `<DesempenhoFunilChart metrics={metrics}/>` + title "Leads criados ao longo do tempo" + `<DesempenhoTimelineChart leads={leads} periodo={metrics.periodo} perfilId={metrics.perfilId}/>`
      * Página 3: title "Tarefas" + `<DesempenhoTarefasChart tarefas={tarefas} periodo={metrics.periodo} perfilId={metrics.perfilId}/>` + 2 big KPI cards lado-a-lado (ICP fit + NPS médio) usando `<KPICard tone="emerald">` e `<KPICard tone="amber">`

    Cores fixas hex (NÃO CSS vars) — html2canvas precisa de cores resolvidas. Background sempre `white`. Color text sempre `#0d1929` (cinza-azulado dark legível em paper).

    Importante: componente NÃO controla theme — apenas força cores light explicitamente em inline style. Toggle dark/light global é responsabilidade de `pdf-export.ts` em Plan 03.
  </action>
  <acceptance_criteria>
    - `ls src/components/desempenho/DesempenhoReport.tsx`
    - `grep -n "createPortal" src/components/desempenho/DesempenhoReport.tsx` retorna ≥1
    - `grep -c "data-pdf-page=" src/components/desempenho/DesempenhoReport.tsx` retorna 3
    - `grep -n "data-pdf-root" src/components/desempenho/DesempenhoReport.tsx` retorna 1
    - `grep -n "top: '-9999px'" src/components/desempenho/DesempenhoReport.tsx` retorna 1
    - `grep -n "background: 'white'" src/components/desempenho/DesempenhoReport.tsx` retorna ≥1
    - `rtk npx tsc --noEmit` passa
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- src/components/desempenho/__tests__/DesempenhoReport.test.tsx --run</automated>
  </verify>
  <done>DesempenhoReport portal off-screen 794px, 3 páginas data-pdf-page, cores fixas hex; pronto para captura pdf-export.ts em Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: MeDesempenhoPage + routing + PerfilPanel link (discovery D-09)</name>
  <files>
    src/pages/MeDesempenhoPage.tsx,
    src/router.tsx,
    src/components/me/PerfilPanel.tsx
  </files>
  <read_first>
    - `src/router.tsx` linhas 80-91 — grupo /me/* existente (analog exact)
    - `src/pages/ReceitaPage.tsx` linhas 172-476 — RequireRole + PeriodSelector + content pattern
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` §Code Examples (linhas 905-967) — DesempenhoPage skeleton completo
    - `.planning/phases/08-individual-performance-reports/08-PATTERNS.md` §11 + §17 + §19
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` Pitfall 4 (linhas 863-867) — RequireRole condicional
    - `src/hooks/usePerfis.ts` — useMeuPerfil + usePerfis
    - `src/components/me/PerfilPanel.tsx` (todo) — estrutura atual
  </read_first>
  <action>
    Per D-02 + D-09 + RESEARCH Code Examples + Pitfall 4:

    1. Criar `src/pages/MeDesempenhoPage.tsx`:
       - Imports: useParams, useEffect, useState, useMemo de react + react-router-dom
       - Hooks: useMeuPerfil, usePerfis, useLeads, useTarefas, useClientes, useConfiguracoes (que hoje retorna Configuracoes com servicos: Servico[])
       - Adicionar `useContratos` se existe ou inline via supabase (para nps_medio Pitfall 6). Verificar `src/hooks/useContratos.ts` — se existir, usar; se não, importar `useClientes` que já popula contratos via select '*, contratos(*)'
       - `const params = useParams<{ perfilId?: string }>()`
       - `const { data: meuPerfil } = useMeuPerfil()`
       - `const targetId = params.perfilId ?? meuPerfil?.id`
       - `const isViewingOther = !!params.perfilId && params.perfilId !== meuPerfil?.id`
       - `const [period, setPeriod] = useState(loadPeriod)` + `useEffect(() => savePeriod(period), [period])`
       - `const targetPerfil = perfis.find(p => p.id === targetId) ?? meuPerfil`
       - `const metrics = useMemo(() => calcularDesempenho({ leads, tarefas, clientes, contratos: contratosAchatados, perfilId: targetId!, perfilNome: targetPerfil?.nome ?? '—', periodo: period, servicosConfig: config?.servicos ?? [] }), [...])` — contratosAchatados via `clientes.flatMap(c => c.contratos ?? [])` se hook embute contratos; caso contrário usar hook dedicado
       - Render: header (h1 "Desempenho — {nome}", subtítulo "Visualização: coord+" se isViewingOther, PeriodSelector(value=period, onChange=setPeriod, derivedYearsFrom=leads)) + DesempenhoKpiGrid + section com DesempenhoFunilChart + DesempenhoTimelineChart + DesempenhoTarefasChart
       - Pattern conditional wrap:
         ```
         const content = (/* ... JSX ... */)
         return isViewingOther
           ? <RequireRole atLeast="coordenador">{content}</RequireRole>
           : content
         ```
       - Early return null se !targetId || !meuPerfil

    2. Editar `src/router.tsx`:
       - Importar `MeDesempenhoPage` perto do import de MeEspacoPage (~linha 19)
       - Adicionar dentro dos children de AppLayout, após linha ~91:
         ```
         { path: 'me/desempenho', element: <MeDesempenhoPage /> },
         { path: 'me/desempenho/:perfilId', element: <MeDesempenhoPage /> },
         ```

    3. Editar `src/components/me/PerfilPanel.tsx`:
       - Adicionar `import { Link } from 'react-router-dom'` se não existe
       - Importar `BarChart3` de `lucide-react` (já usado em DashboardPage:15)
       - No final do form (após botão "Salvar perfil"), adicionar:
         ```
         <Link to="/me/desempenho" className="inline-flex items-center gap-2 text-sm text-cyan-500 hover:text-cyan-400 mt-4">
           <BarChart3 className="w-4 h-4" />
           Ver meu desempenho →
         </Link>
         ```

    Sem mudança em Sidebar (D-09 — sem nav pollution).
  </action>
  <acceptance_criteria>
    - `grep -n "MeDesempenhoPage" src/router.tsx | grep -v "^#"` retorna ≥3 linhas (import + 2 rotas)
    - `grep -c "path: 'me/desempenho" src/router.tsx` retorna 2
    - `grep -n "RequireRole atLeast=\"coordenador\"" src/pages/MeDesempenhoPage.tsx` retorna ≥1
    - `grep -n "isViewingOther" src/pages/MeDesempenhoPage.tsx` retorna ≥2 (declaração + uso)
    - `grep -n "to=\"/me/desempenho\"" src/components/me/PerfilPanel.tsx` retorna 1
    - `grep -n "loadPeriod" src/pages/MeDesempenhoPage.tsx` retorna ≥1
    - `rtk npm run lint` passa
    - `rtk npx tsc --noEmit` passa
    - `rtk npm run build` passa (verifica route resolution)
  </acceptance_criteria>
  <verify>
    <automated>rtk npx tsc --noEmit && rtk npm run lint</automated>
  </verify>
  <done>Rotas /me/desempenho [/:perfilId] funcionais; page consome calcularDesempenho + 3 charts + KpiGrid; RequireRole condicional via Pitfall 4; link em PerfilPanel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param :perfilId → page query | Untrusted input pode tentar acessar perfil de outro user |
| localStorage → page state | period load/save (já mitigado em Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Elevation of Privilege | `src/pages/MeDesempenhoPage.tsx` | mitigate | `RequireRole atLeast="coordenador"` wrap quando `params.perfilId !== meuPerfil.id` (Pitfall 4); defesa em profundidade — RLS em leads/tarefas/clientes (Phase 6 migration 029) já filtra no backend. Plan-checker valida que `useLeads`/`useTarefas`/`useClientes` retornam apenas dados visíveis ao role corrente |
| T-08-05a | Information Disclosure | Charts inline | accept | Charts mostram apenas agregados (counts), não dados de leads individuais; risco baixo |
| T-08-06 | Tampering (race) | Snapshot in time | accept | Dados podem mudar mid-render mas snapshot é coerente dentro do mesmo render do TanStack cache; footer no Plan 03 documenta `geradoEm` |
</threat_model>

<verification>
- `rtk npm test` ≤ 60s, full green
- `rtk npm run lint` clean
- `rtk npx tsc --noEmit` clean
- `rtk npm run build` clean (validates route registration)
- Manual smoke (Slice 3 fará UAT formal): visitar `/me/desempenho` mostra header + KPIs + 3 charts; trocar período persiste; PerfilPanel mostra link
</verification>

<success_criteria>
- /me/desempenho renderiza KpiGrid + 3 charts para auth user
- /me/desempenho/:perfilId bloqueia consultor regular via RequireRole quando perfilId != self
- PeriodSelector trocado persiste em localStorage
- DesempenhoReport portal off-screen criado mas não montado ainda (Plan 03 monta)
- PerfilPanel link "Ver meu desempenho"
- npm test full green; tsc + lint + build clean
</success_criteria>

<output>
Create `.planning/phases/08-individual-performance-reports/08-02-SUMMARY.md` ao final descrevendo página + rotas + componentes off-screen, e que Plan 03 precisa apenas adicionar pdf-export.ts + csv-export.ts + 2 botões.
</output>
