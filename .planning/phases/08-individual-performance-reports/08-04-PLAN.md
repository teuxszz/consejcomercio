---
phase: 08-individual-performance-reports
plan: 04
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - src/lib/desempenho-team.ts
  - src/lib/__tests__/desempenho-team.test.ts
  - src/lib/pdf-export.ts
  - src/components/desempenho/DesempenhoTeamReport.tsx
  - src/components/desempenho/ProgressTeamReportModal.tsx
  - src/components/desempenho/ExportarPDFEquipeButton.tsx
  - src/pages/AdocaoPage.tsx
autonomous: false
requirements:
  - REP-04
tags:
  - phase-08
  - team-report
  - abort-controller
  - mobile-fallback
  - uat

must_haves:
  truths:
    - "Coordenador+ vê botão 'Exportar PDF equipe' no header de /adocao (RequireRole gate)"
    - "Click abre modal com progress bar X/N consultores + botão Cancelar funcional via AbortController"
    - "PDF gerado tem capa (totais equipe + período + data) + 1 página por consultor ativo + página final com ranking sorted by leads_convertidos"
    - "'Consultor ativo' = role='consultor' AND criou >=1 lead no período (D-07)"
    - "Cancel mid-export restaura tema dark e não deixa estado inconsistente (Pitfall 5)"
    - "iOS ou Android Chrome com >=5 consultores ativos mostra toast.warning recomendando desktop (Pattern 10)"
    - "Row de consultor em /adocao é clickable + navega para /me/desempenho/<perfilId> (D-08)"
    - "UAT 7-item passa antes de marcar phase complete"
  artifacts:
    - path: "src/lib/desempenho-team.ts"
      provides: "calcularDesempenhoEquipe + rankConsultores helpers puros"
      exports: ["calcularDesempenhoEquipe", "rankConsultores", "filtrarConsultoresAtivos"]
    - path: "src/components/desempenho/DesempenhoTeamReport.tsx"
      provides: "Off-screen renderer multi-modo (capa | consultor | ranking) controlado por prop variant"
      exports: ["DesempenhoTeamReport"]
    - path: "src/components/desempenho/ProgressTeamReportModal.tsx"
      provides: "Dialog progress bar + Cancelar"
      exports: ["ProgressTeamReportModal"]
    - path: "src/components/desempenho/ExportarPDFEquipeButton.tsx"
      provides: "Botão coord+ com AbortController + iOS warn"
      exports: ["ExportarPDFEquipeButton"]
    - path: "src/pages/AdocaoPage.tsx"
      provides: "Header com botão equipe + rows clickable drill"
      contains: "ExportarPDFEquipeButton"
  key_links:
    - from: "src/components/desempenho/ExportarPDFEquipeButton.tsx"
      to: "src/lib/pdf-export.ts::gerarRelatorioEquipe"
      via: "AbortController.signal + onProgress callback"
      pattern: "gerarRelatorioEquipe"
    - from: "src/lib/pdf-export.ts::gerarRelatorioEquipe"
      to: "src/components/desempenho/DesempenhoTeamReport.tsx"
      via: "renderEm callback monta off-screen + RAF + html2canvas"
      pattern: "renderEm"
    - from: "src/pages/AdocaoPage.tsx"
      to: "/me/desempenho/:perfilId"
      via: "useNavigate em onClick de <tr>"
      pattern: "/me/desempenho/"
---

<objective>
Fase 8 Slice 4 — Team report + UAT close-out. Adicionar gerarRelatorioEquipe a pdf-export.ts (capa + N consultores + ranking, sequencial com AbortController), criar DesempenhoTeamReport multi-modo, modal de progress + cancelar, botão coord+ no /adocao header com iOS warn, e drill nas rows da tabela /adocao. Encerrar phase 8 com 7-item UAT checkpoint manual (per VALIDATION.md §Manual-Only).

Purpose: REP-04 entregue end-to-end. Coordenador agora exporta relatório consolidado para reuniões mensais — última peça do MVP slice. Fase 8 fechada.

Output: 1 lib (desempenho-team), 3 componentes (TeamReport, ProgressModal, ExportarEquipeButton), 1 page edit (AdocaoPage com botão + drill), gerarRelatorioEquipe em pdf-export.ts, UAT manual checkpoint.

REP-04 (per D-07, D-08, Pattern 4, Pattern 10, Pitfall 5, Pitfall 7; T-08-04, T-08-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-individual-performance-reports/08-CONTEXT.md
@.planning/phases/08-individual-performance-reports/08-RESEARCH.md
@.planning/phases/08-individual-performance-reports/08-PATTERNS.md
@.planning/phases/08-individual-performance-reports/08-VALIDATION.md
@.planning/phases/08-individual-performance-reports/08-01-SUMMARY.md
@.planning/phases/08-individual-performance-reports/08-02-SUMMARY.md
@.planning/phases/08-individual-performance-reports/08-03-SUMMARY.md
@src/lib/desempenho.ts
@src/lib/pdf-export.ts
@src/components/desempenho/DesempenhoReport.tsx
@src/pages/AdocaoPage.tsx
@src/components/shared/RequireRole.tsx
@src/hooks/usePerfis.ts
@src/components/ui/dialog.tsx
@src/components/ui/progress.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: desempenho-team.ts helpers (filtrarConsultoresAtivos + rankConsultores + calcularDesempenhoEquipe) + tests</name>
  <files>
    src/lib/desempenho-team.ts,
    src/lib/__tests__/desempenho-team.test.ts
  </files>
  <read_first>
    - `src/lib/desempenho.ts` (Plan 01) — calcularDesempenho signature
    - `src/lib/__tests__/desempenho.test.ts` (Plan 01) — fixture builders pattern
    - `.planning/phases/08-individual-performance-reports/08-CONTEXT.md` D-07 — definição "consultor ativo"
    - `.planning/phases/08-individual-performance-reports/08-VALIDATION.md` §REP-04 (linha 1124) — test "consultor ativo" filtro
    - `src/types/index.ts` — Perfil + DesempenhoConsultorTeam (Plan 01)
  </read_first>
  <behavior>
    - Test 1 (filtrarConsultoresAtivos): perfil com role='consultor' + criou >=1 lead no período → incluído
    - Test 2: perfil com role='consultor' SEM lead no período → excluído
    - Test 3: perfil com role='coordenador' (mesmo com leads) → excluído (D-07 só consultor)
    - Test 4: perfil sem role definido → excluído
    - Test 5 (rankConsultores): ordena por convertidos desc; empate mantém ordem original
    - Test 6 (calcularDesempenhoEquipe): retorna {totais: DesempenhoMetricas, consultores: DesempenhoConsultorTeam[]}; totais agregam leads_criados/convertidos/perdidos somando per-consultor; ciclo_medio_dias e nps_medio são médias ponderadas (ou null se denominador zero)
    - Test 7: equipe vazia (sem consultores ativos) → consultores=[] e totais com zeros + nulls
  </behavior>
  <action>
    Per D-07 + RESEARCH §Pattern 4 (linhas 510-568) — separar agregação (pura, testável) da iteração de captura (em pdf-export):

    1. Criar `src/lib/desempenho-team.ts`:
       - Imports: types `Perfil, Lead, Tarefa, Cliente, Contrato, DesempenhoMetricas, DesempenhoConsultorTeam` de `@/types`; `PeriodValue` de `./periods`; `calcularDesempenho` de `./desempenho`; `getPeriodRange, isInRange` de `./periods`
       - `export function filtrarConsultoresAtivos(perfis: Perfil[], leads: Lead[], periodo: PeriodValue): Perfil[]`:
         - `const range = getPeriodRange(periodo)`
         - `return perfis.filter(p => p.role === 'consultor' && leads.some(l => l.responsavel_id === p.id && isInRange(l.created_at, range)))`
       - `export function rankConsultores(consultores: DesempenhoConsultorTeam[]): DesempenhoConsultorTeam[]`:
         - `return [...consultores].sort((a, b) => b.metricas.convertidos - a.metricas.convertidos)`
       - `export interface DesempenhoEquipeResult { totais: DesempenhoMetricas; consultores: DesempenhoConsultorTeam[] }`
       - `export function calcularDesempenhoEquipe(input: { perfis: Perfil[], leads: Lead[], tarefas: Tarefa[], clientes: Cliente[], contratos: Contrato[], periodo: PeriodValue, servicosConfig })`:
         - `const ativos = filtrarConsultoresAtivos(input.perfis, input.leads, input.periodo)`
         - `const consultores: DesempenhoConsultorTeam[] = ativos.map(p => ({ perfilId: p.id, perfilNome: p.nome, metricas: calcularDesempenho({ ...input, perfilId: p.id, perfilNome: p.nome }) }))`
         - Computar totais: leads_criados = sum, convertidos = sum, perdidos = sum, tarefas_concluidas = sum, win_rate = round(sumGanhos/(sumGanhos+sumPerdidos)*100), ciclo_medio_dias = média ponderada (sum(c.ciclo*c.convertidos) / sum(c.convertidos)) ou null se sum convertidos = 0, icp_fit_medio = média simples dos não-null ou null, nps_medio = média ponderada por clientes ou null
         - perfilId='_equipe_', perfilNome='Equipe CONSEJ' nos totais
         - Retornar `{ totais, consultores }`

    2. Criar `src/lib/__tests__/desempenho-team.test.ts`:
       - Importar `filtrarConsultoresAtivos, rankConsultores, calcularDesempenhoEquipe`
       - Reusar builder helpers (leadFix, etc.) de desempenho.test.ts OU duplicar com nomes distintos
       - 7 testes (behavior 1-7)
  </action>
  <acceptance_criteria>
    - `rtk npm test -- src/lib/__tests__/desempenho-team.test.ts --run` passa com ≥7 testes verdes
    - `grep -n "export function filtrarConsultoresAtivos" src/lib/desempenho-team.ts` retorna 1
    - `grep -n "export function rankConsultores" src/lib/desempenho-team.ts` retorna 1
    - `grep -n "export function calcularDesempenhoEquipe" src/lib/desempenho-team.ts` retorna 1
    - `grep -n "role === 'consultor'" src/lib/desempenho-team.ts` retorna ≥1 (D-07 filtro)
    - `rtk npx tsc --noEmit` passa
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- src/lib/__tests__/desempenho-team.test.ts --run</automated>
  </verify>
  <done>3 helpers de agregação team puros + 7 tests verdes; filtro D-07 correto; ranking por convertidos.</done>
</task>

<task type="auto">
  <name>Task 2: DesempenhoTeamReport multi-variant + gerarRelatorioEquipe em pdf-export.ts + ProgressModal</name>
  <files>
    src/components/desempenho/DesempenhoTeamReport.tsx,
    src/components/desempenho/ProgressTeamReportModal.tsx,
    src/lib/pdf-export.ts
  </files>
  <read_first>
    - `src/components/desempenho/DesempenhoReport.tsx` (Plan 02) — pattern off-screen portal
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` §Pattern 4 (linhas 510-568) — gerarRelatorioEquipe skeleton + capturarPagina helper
    - `.planning/phases/08-individual-performance-reports/08-PATTERNS.md` §7 + §10
    - `src/components/ui/dialog.tsx` + `src/components/ui/progress.tsx` (shadcn primitives)
    - `src/lib/pdf-export.ts` (Plan 03) — ProgressInfo já exportada
  </read_first>
  <action>
    Per D-07 + Pattern 4 + T-08-05:

    1. Criar `src/components/desempenho/DesempenhoTeamReport.tsx`:
       - Props: `{ variant: 'capa' | 'consultor' | 'ranking'; totais?: DesempenhoMetricas; consultor?: DesempenhoConsultorTeam & { leads: Lead[]; tarefas: Tarefa[] }; ranking?: DesempenhoConsultorTeam[]; periodoLabel: string; geradoEm: Date }`
       - Render condicional baseado em variant via createPortal com mesmo container off-screen (data-pdf-root, 794px, top:-9999px, background:white):
         * `variant='capa'`: 1 div `data-pdf-page="1"` com título "Relatório de Desempenho da Equipe", subtítulo período, KpiGrid com totais, footer geradoEm
         * `variant='consultor'`: 1 div `data-pdf-page="1"` com header (nome consultor + período) + KpiGrid + chart funil simplificado
         * `variant='ranking'`: 1 div `data-pdf-page="1"` com tabela "Nome | Leads | Convertidos | Win Rate | NPS" sorted (rank já vem ordenado de rankConsultores)
       - Cores hex fixas (não CSS vars)
       - `isAnimationActive={false}` em qualquer chart embutido

    2. Criar `src/components/desempenho/ProgressTeamReportModal.tsx`:
       - Props: `{ open: boolean; current: number; total: number; consultorNome: string; onCancel: () => void }`
       - Render: `<Dialog open={open}><DialogContent className="max-w-md"><DialogTitle>Gerando relatório da equipe</DialogTitle><Progress value={(current/Math.max(total,1))*100} /><p className="text-sm text-muted-foreground">{current} / {total} consultores · {consultorNome}</p><Button variant="outline" onClick={onCancel}>Cancelar</Button></DialogContent></Dialog>`

    3. Estender `src/lib/pdf-export.ts` adicionando função `gerarRelatorioEquipe`:
       - Signature: `export async function gerarRelatorioEquipe(input: { totais: DesempenhoMetricas; consultoresAtivos: Array<DesempenhoConsultorTeam & { leads: Lead[]; tarefas: Tarefa[] }>; ranking: DesempenhoConsultorTeam[]; periodoLabel: string; mountAndCapture: (variant: 'capa'|'consultor'|'ranking', data: any) => Promise<HTMLElement>; onProgress: (info: ProgressInfo) => void; signal: AbortSignal }): Promise<void>`
       - Lazy import jspdf + html2canvas (mesmo pattern de gerarRelatorioIndividual)
       - Theme toggle wasDark com try/finally (Pitfall 5 — finally restaura mesmo após AbortError)
       - `pdf.setProperties({...vazio...})` (T-08-01)
       - Capa: `const capaEl = await input.mountAndCapture('capa', { totais, periodoLabel })`; capturar via helper interno `capturarPagina(html2canvas, pdf, capaEl, false)`
       - Loop sequencial sobre `input.consultoresAtivos`:
         - `if (signal.aborted) throw new DOMException('Aborted', 'AbortError')` no início de cada iteração
         - `input.onProgress({ current: i+1, total, consultorNome })`
         - `const el = await input.mountAndCapture('consultor', { consultor })`
         - `await capturarPagina(html2canvas, pdf, el, true /* addPage */)`
       - Ranking: `const rankingEl = await input.mountAndCapture('ranking', { ranking })`; `await capturarPagina(html2canvas, pdf, rankingEl, true)`
       - `pdf.save(\`desempenho_equipe_\${input.periodoLabel}.pdf\`)` — sem perfilNome (relatório agregado)
       - Helper interno `async function capturarPagina(html2canvas, pdf, rootEl, addPage)`:
         - `await new Promise(r => requestAnimationFrame(() => r(null)))` — garantir paint
         - `const canvas = await html2canvas(rootEl, { scale: 2, backgroundColor: '#fff', useCORS: true, logging: false })`
         - `if (addPage) pdf.addPage('a4', 'portrait')`
         - `pdf.addImage(canvas.toDataURL('image/png'), 'PNG', 0, 0, 210, 297, undefined, 'FAST')`
         - `canvas.width = 0; canvas.height = 0`
  </action>
  <acceptance_criteria>
    - `grep -n "export async function gerarRelatorioEquipe" src/lib/pdf-export.ts` retorna 1
    - `grep -n "signal.aborted" src/lib/pdf-export.ts` retorna ≥1
    - `grep -n "DOMException.*AbortError" src/lib/pdf-export.ts` retorna ≥1
    - `grep -c "variant: 'capa' | 'consultor' | 'ranking'" src/components/desempenho/DesempenhoTeamReport.tsx` retorna ≥1
    - `grep -n "Cancelar" src/components/desempenho/ProgressTeamReportModal.tsx` retorna ≥1
    - `rtk npx tsc --noEmit` passa
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test --run 2>&1 | tail -20</automated>
  </verify>
  <done>3 artefatos: TeamReport multi-variant, ProgressModal, gerarRelatorioEquipe lazy+abortable+theme-safe.</done>
</task>

<task type="auto">
  <name>Task 3: ExportarPDFEquipeButton (AbortController + iOS warn) + AdocaoPage header + row drill</name>
  <files>
    src/components/desempenho/ExportarPDFEquipeButton.tsx,
    src/pages/AdocaoPage.tsx
  </files>
  <read_first>
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` §Pattern 10 (linhas 766-783) — isIOS detection + toast.warning
    - `.planning/phases/08-individual-performance-reports/08-RESEARCH.md` §Pattern 4 (linhas 597-608) — AbortController consumer
    - `.planning/phases/08-individual-performance-reports/08-PATTERNS.md` §10 + §18
    - `.planning/phases/08-individual-performance-reports/08-CONTEXT.md` D-07 (warn ≥5 consultores) + D-08 (drill)
    - `src/pages/AdocaoPage.tsx` (todo) — estrutura RequireRole + tabela
    - `src/components/shared/RequireRole.tsx`
  </read_first>
  <action>
    Per D-07 + D-08 + Pattern 10 + T-08-04 defesa em camadas:

    1. Criar `src/components/desempenho/ExportarPDFEquipeButton.tsx`:
       - Hooks: `useState<ProgressInfo | null>(null)` para progress; `useRef<AbortController | null>(null)` para abort; useState para mountedReport (data passada ao TeamReport)
       - useHooks: useLeads, useTarefas, useClientes, usePerfis, useContratos (ou derivar de clientes), useConfiguracoes; receber `periodo: PeriodValue` via prop (vem do PeriodSelector da AdocaoPage)
       - Detector: `function isMobile(): boolean { return /iPad|iPhone|iPod|Android/.test(navigator.userAgent) && !('MSStream' in window) }`
       - Handler `async function handleExportTeam()`:
         - Compor `equipeResult = calcularDesempenhoEquipe({ perfis, leads, tarefas, clientes, contratos, periodo, servicosConfig })`
         - Se `isMobile() && equipeResult.consultores.length >= 5`:
           - `toast.warning('Relatório da equipe é pesado em mobile. Recomendamos usar desktop.', { duration: 6000, action: { label: 'Tentar mesmo assim', onClick: () => prosseguir(equipeResult) } })`
           - return
         - else `prosseguir(equipeResult)`
       - `async function prosseguir(result)`:
         - `const controller = new AbortController(); controllerRef.current = controller`
         - try await gerarRelatorioEquipe com mountAndCapture callback que renderiza TeamReport per variant em portal + espera RAF + retorna element
         - catch DOMException AbortError → `toast('Geração cancelada')`
         - catch outras → `toast.error(e instanceof Error ? e.message : 'Erro ao gerar PDF da equipe')`
         - finally: setProgress(null), reset controllerRef
       - `function handleCancel() { controllerRef.current?.abort() }`
       - Render: `<Button onClick={handleExportTeam} disabled={!!progress}>Exportar PDF equipe</Button>` + `<ProgressTeamReportModal open={!!progress} current={progress?.current ?? 0} total={progress?.total ?? 0} consultorNome={progress?.consultorNome ?? ''} onCancel={handleCancel} />` + montagem condicional do TeamReport quando renderTarget está set

    2. Editar `src/pages/AdocaoPage.tsx`:
       - Adicionar `import { useNavigate } from 'react-router-dom'` + `const navigate = useNavigate()` em AdocaoContent
       - Adicionar `import { ExportarPDFEquipeButton } from '@/components/desempenho/ExportarPDFEquipeButton'`
       - No header (próximo a `<h1>Adoção</h1>`):
         ```
         <RequireRole atLeast="coordenador" fallback={null}>
           <ExportarPDFEquipeButton periodo={period} />
         </RequireRole>
         ```
       - Em cada `<tr>` de consultor na tabela (D-08):
         ```
         <tr
           key={p.id}
           className="hover:bg-[var(--alpha-bg-xs)] cursor-pointer transition-colors"
           onClick={() => navigate(`/me/desempenho/${p.id}`)}
         >
         ```

    NOTA: AdocaoPage já wraps em `RequireRole atLeast="coordenador"` na linha 104 — duplicar gate no botão é defesa em camadas (CLAUDE.md `## Auth & Role Gating`), aceitável.
  </action>
  <acceptance_criteria>
    - `grep -n "AbortController" src/components/desempenho/ExportarPDFEquipeButton.tsx` retorna ≥1
    - `grep -n "isMobile" src/components/desempenho/ExportarPDFEquipeButton.tsx` retorna ≥2 (def + uso)
    - `grep -n "iPad|iPhone\|iPod|Android" src/components/desempenho/ExportarPDFEquipeButton.tsx` retorna ≥1
    - `grep -n "toast.warning" src/components/desempenho/ExportarPDFEquipeButton.tsx` retorna ≥1
    - `grep -n "AbortError" src/components/desempenho/ExportarPDFEquipeButton.tsx` retorna ≥1
    - `grep -n "ExportarPDFEquipeButton" src/pages/AdocaoPage.tsx` retorna ≥2 (import + uso)
    - `grep -n "navigate.*me/desempenho" src/pages/AdocaoPage.tsx` retorna ≥1 (D-08 drill)
    - `grep -n "cursor-pointer" src/pages/AdocaoPage.tsx` retorna ≥1 (visual affordance row clickable)
    - `rtk npm run build` passa
    - `rtk npm test --run` passa full green
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Botão coord+ no header /adocao com AbortController + mobile warn ≥5; rows tabela clickable drill para /me/desempenho/<perfilId>.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT 7-item manual checkpoint (Phase 8 close-out)</name>
  <files>none (manual verification — sem mudanças de código)</files>
  <action>
    Pausar execução. Apresentar checklist ao usuário (conteúdo de what-built + how-to-verify abaixo). Aguardar resposta no resume-signal antes de prosseguir para o commit final / marcação da phase como completa em ROADMAP/STATE.
  </action>
  <verify>
    <human-check>UAT roteiro 7-item (mais bonus #8) executado pelo usuário em ambiente real (Chrome desktop + iPhone Safari + Excel desktop). Todos itens passam OU gaps documentados para /gsd-plan-phase 8 --gaps.</human-check>
  </verify>
  <done>Usuário responde "aprovado" no resume-signal. Phase 8 fechável.</done>
  <what-built>
    Fase 8 completa:
    - Plan 01: libs export + helper calcularDesempenho + slug + Wave 0 scaffolds
    - Plan 02: /me/desempenho página + 3 charts + DesempenhoReport off-screen + RequireRole condicional + GlobalSearch indexing setup
    - Plan 03: PDF + CSV export individual + 2 botões + lazy chunks + sanitizeCell + theme toggle
    - Plan 04: Team report + AbortController + mobile warn + AdocaoPage drill + ranking
  </what-built>
  <how-to-verify>
    Per `.planning/phases/08-individual-performance-reports/08-VALIDATION.md` §Manual-Only Verifications (7 itens):

    1. **PDF individual visual** (REP-02): Login como consultor (qualquer) → `/me/desempenho` → escolher período "2026 — total" → Click "Exportar PDF" → spinner 2-3s → download arquivo `desempenho_<nome>_2026-total.pdf`. Abrir PDF: 3 páginas A4 portrait com header CONSEJ + nome + período + 8 KPIs + 4 charts. Confirmar dados batem com a tela.

    2. **PDF team report** (REP-04): Login como diretor/coordenador → `/adocao` → "Exportar PDF equipe" no header → modal "Gerando relatório da equipe — X / N consultores · <nome>" aparece → progress bar avança → download `desempenho_equipe_2026-total.pdf`. Abrir: capa com totais + N páginas (1 por consultor ativo) + página final ranking sorted by leads_convertidos desc.

    3. **CSV opens in Excel PT-BR** (REP-03): /me/desempenho → "Exportar CSV" → download `desempenho_<nome>_2026-total.zip` → unzip → abrir leads.csv no Excel desktop (Windows): acentos PT-BR (ção, ã, é) NÃO aparecem corrompidos. Mesma checagem tarefas.csv e contratos.csv.

    4. **CSV injection mitigation** (T-08-03): Criar (ou achar existente) lead com nome contendo `=cmd|...` ou começando com `+` → Exportar CSV → abrir leads.csv no Excel → célula do nome aparece prefixada com `'` (apóstrofo) → fórmula NÃO executa.

    5. **Light theme forçado durante capture** (D-11): App em dark mode (default) → click "Exportar PDF" → brief flash light theme (~2s) durante captura → após download tema volta para dark sozinho. PDF gerado é light theme (legível).

    6. **iOS Safari graceful fallback** (Pattern 10): iPhone Safari (PWA instalado ou navegador) → /me/desempenho → "Exportar PDF" individual → funciona OK (3 páginas). Como coord+ → /adocao → "Exportar PDF equipe" com ≥5 consultores ativos → toast.warning "Relatório da equipe é pesado em mobile..." aparece com botão "Tentar mesmo assim".

    7. **AdocaoPage drill + role gate** (D-08, T-08-04):
       - Coord+ em /adocao → click row de consultor X → navega para /me/desempenho/<X-perfilId> → vê dados de X.
       - Consultor regular tenta abrir manualmente URL `/me/desempenho/<outroPerfilId>` → RequireRole atLeast="coordenador" bloqueia (mensagem "Acesso restrito" ou redirect).

    Plus check de Cancel:
    8. (Bonus) Team report em desktop → click cancelar mid-export → modal fecha + toast "Geração cancelada" → tema permanece consistente (não fica em light permanente — Pitfall 5).
  </how-to-verify>
  <resume-signal>Digite "aprovado" para fechar phase 8 OU descreva quais itens falharam (com print/screenshot se possível). Gaps abertos voltam para `/gsd-plan-phase 8 --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coord+ vê dados de todos consultores | RLS no backend deve permitir |
| AbortController.signal → loop captura | Cancel deve ser respeitado e finally executar |
| Mobile UA → render path | Detecção pode ser spoofed mas é defensiva (UX), não security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Elevation of Privilege | AdocaoPage drill + ExportarPDFEquipeButton | mitigate | Defesa em camadas: (a) AdocaoPage wraps em RequireRole coordenador (linha 104 existente); (b) Botão equipe duplica RequireRole; (c) MeDesempenhoPage usa RequireRole condicional via Pitfall 4; (d) RLS backend (Phase 6 migration 029) filtra leads/tarefas/clientes por role |
| T-08-05 | Denial of Service (self) | Team report 10+ consultores em iOS | mitigate | Sequencial (não paralelo) + AbortController + iOS warn ≥5 consultores + memory release canvas.width=0 entre páginas |
| T-08-FX | Tampering (theme stuck) | Cancel mid-export deixar light permanente | mitigate | `try { ... } finally { restoreTheme() }` em gerarRelatorioEquipe — Pitfall 5; UAT item 8 (bonus) valida |
| T-08-01 | Information Disclosure | PDF team metadata | mitigate | Mesmo `pdf.setProperties({...vazio...})` do Plan 03 aplicado em gerarRelatorioEquipe |
</threat_model>

<verification>
- `rtk npm test` ≤ 60s full green (Plans 01+02+03+04)
- `rtk npm run lint` clean
- `rtk npx tsc --noEmit` clean
- `rtk npm run build` clean
- UAT 7-item manual passa (Task 4 checkpoint blocking)
- Decision coverage: D-07 (Task 1+2+3) + D-08 (Task 3) + REP-04 (toda phase 4)
</verification>

<success_criteria>
- calcularDesempenhoEquipe + filtrarConsultoresAtivos + rankConsultores helpers puros com 7 tests verdes
- DesempenhoTeamReport multi-variant (capa | consultor | ranking)
- ProgressTeamReportModal com Cancelar funcional
- gerarRelatorioEquipe em pdf-export.ts com AbortController + theme safe + metadata strip
- ExportarPDFEquipeButton coord+ com mobile warn ≥5 consultores
- AdocaoPage header com botão + rows clickable drill (D-08)
- UAT 7-item passa (Task 4 blocking checkpoint)
- Phase 8 closeable: ROADMAP marca [x] Phase 8 + STATE.md atualizado para current_phase=09
</success_criteria>

<output>
Create `.planning/phases/08-individual-performance-reports/08-04-SUMMARY.md` documentando team report end-to-end + UAT outcome + métricas finais da phase (libs adicionadas, bundle delta, test count final, ROADMAP marcado).
</output>
