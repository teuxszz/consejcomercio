---
phase: 04-revenue-dashboard
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/receita.ts
  - src/lib/__tests__/receita.test.ts
  - src/pages/RenovacoesPage.tsx
autonomous: true
requirements: [REV-01, REV-02, REV-03]
tags: [revenue, mrr, forecast, helpers, tdd]

must_haves:
  truths:
    - "calcularMrr retorna 0 para lista vazia e separa total/assessoria/consultoriaProRata (per D-01)"
    - "calcularReceitaPontualPeriodo soma valor_total de contratos tipo='consultoria' com data_inicio dentro do range; ignora fora (per D-01 / Row 2 / Open Q2 RESOLVED)"
    - "calcularMrrHistorico retorna N pontos cronológicos (default 6) considerando contratos ativos em cada mês"
    - "calcularForecast retorna N pontos (default 3) usando MRR atual + entradas - saídas; contratos sem data_fim NÃO entram em saídas (per D-03 + Open Q1 RESOLVED)"
    - "classificarRenovacoes divide contratos status='ativo' em 4 buckets (ate30/de31a60/de61a90/semDataFim), ordenados por daysLeft asc (per D-05)"
    - "valor_protegido NÃO contribui ao MRR (per D-01)"
    - "RenovacoesPage importa classifyUrgency de @/lib/receita (DRY) sem mudar comportamento"
  artifacts:
    - path: "src/lib/receita.ts"
      provides: "helpers puros calcularMrr / calcularReceitaPontualPeriodo / calcularMrrHistorico / calcularForecast / classificarRenovacoes / classifyUrgency / contribuicaoMensal"
      exports: ["calcularMrr", "calcularReceitaPontualPeriodo", "calcularMrrHistorico", "calcularForecast", "classificarRenovacoes", "classifyUrgency", "contribuicaoMensal", "MrrBreakdown", "MrrHistoryPoint", "ForecastPoint", "RenovacoesBuckets", "Urgencia"]
      min_lines: 130
    - path: "src/lib/__tests__/receita.test.ts"
      provides: "cobertura unitária dos 5 helpers + edge cases (vazio, null data_fim, valor_protegido, divisão por zero, fora do range)"
      min_lines: 170
  key_links:
    - from: "src/lib/receita.ts"
      to: "@/types Contrato + date-fns + @/lib/utils.getDaysUntilExpiry"
      via: "import"
      pattern: "import.*Contrato.*from '@/types'"
    - from: "src/pages/RenovacoesPage.tsx"
      to: "@/lib/receita.classifyUrgency"
      via: "import"
      pattern: "import.*classifyUrgency.*from '@/lib/receita'"
---

<objective>
TDD foundation da Phase 4 — entrega os helpers puros de cálculo financeiro (MRR pro-rata, receita pontual no período, histórico 6m, forecast 3m, classificação de renovações) com cobertura unitária completa, ANTES de qualquer UI. Refatora `RenovacoesPage` para reusar `classifyUrgency` extraído (DRY com Plan 03).

**Nota sobre smoke E2E (`/receita`):** A edição de `tests/e2e/smoke.spec.ts` foi movida para o Plan 04-03 (Wave 2) para evitar deixar o smoke vermelho durante toda a Wave 1. Plan 01 entrega APENAS unit tests + implementação dos helpers. RED→GREEN do smoke acontece dentro da mesma wave em que `/receita` é registrado.

Por que TDD aqui: cálculos determinísticos com fórmulas explícitas no CONTEXT (D-01 pro-rata; D-01 pontual; D-03 forecast; D-05 buckets renovação) — escreve-se `expect(calcularMrr(fixtures)).toEqual({total, assessoria, consultoriaProRata})` antes do código. Bug em fórmula = decisão de negócio errada (gestor vê receita errada) — risco mitigado por fixtures.

Purpose: Estabelecer contratos testáveis que Plan 03 (UI) consome sem ambiguidade. Plan 02 (URL filter) é independente desses helpers e roda em paralelo (mesma wave).
Output: `src/lib/receita.ts` (helpers + tipos) + `src/lib/__tests__/receita.test.ts` (verde) + RenovacoesPage refatorada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-revenue-dashboard/04-CONTEXT.md
@.planning/phases/04-revenue-dashboard/04-RESEARCH.md
@.planning/phases/04-revenue-dashboard/04-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/types/index.ts
@src/lib/utils.ts
@src/lib/periods.ts
@src/lib/projecao.ts
@src/lib/__tests__/projecao.test.ts
@src/pages/RenovacoesPage.tsx
@src/hooks/useContratos.ts
</context>

## Phase Goal

**As a** gestor coordenador+, **I want to** ver MRR, ARR, forecast 3 meses e renovações pendentes em `/receita`, **so that** consigo decidir sobre crescimento e churn em < 30 segundos sem planilha.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing unit tests para os helpers de receita</name>
  <files>src/lib/__tests__/receita.test.ts</files>
  <read_first>
    - src/lib/__tests__/projecao.test.ts (padrão de teste com TODAY fixo + fixtures inline)
    - src/types/index.ts:72-94 (interface Contrato — campos exatos)
    - .planning/phases/04-revenue-dashboard/04-CONTEXT.md §1, §3, §5 (fórmulas locked)
    - .planning/phases/04-revenue-dashboard/04-RESEARCH.md "Code Examples" (assinaturas de calcularMrr/calcularForecast/calcularMrrHistorico/classificarRenovacoes)
    - .planning/phases/04-revenue-dashboard/04-VALIDATION.md "Coverage Matrix" (Phase Requirements → Test Map)
    - src/lib/periods.ts (PeriodValue, getPeriodRange — referência para fixtures de calcularReceitaPontualPeriodo)
  </read_first>
  <behavior>
    Cobertura mínima em `receita.test.ts` (TODAY fixo = `new Date(2026, 0, 15)`; fixtures inline tipadas como `Contrato`):
    - describe('calcularMrr'):
      * lista vazia → `{ total: 0, assessoria: 0, consultoriaProRata: 0 }`
      * soma valor_mensal direto para contratos `tipo='assessoria' status='ativo'`
      * pro-rata: contrato `tipo='consultoria'` com `valor_total=12000` e `data_inicio`/`data_fim` cobrindo 6 meses → contribui 2000
      * default 12m: contrato com `valor_total=12000` sem `data_fim` → contribui 1000
      * ignora `status != 'ativo'`
      * "breakdown por tipo": separa assessoria vs consultoriaProRata corretamente
      * "valor_protegido nao entra": contrato sem `valor_mensal` e sem `valor_total` mas com `valor_protegido=99999` → contribui 0
      * divisão por zero: `data_inicio == data_fim` → mínimo 1 mês, NÃO retorna Infinity/NaN
    - describe('calcularReceitaPontualPeriodo') (per D-01 Row 2 + Open Q2 RESOLVED):
      * Caso 1 — dentro do range: contrato `tipo='consultoria'` com `valor_total=15000` e `data_inicio=2026-02-10` num range `{ start: 2026-01-01, end: 2026-03-31 }` → retorna 15000
      * Caso 2 — fora do range: mesmo contrato com `data_inicio=2026-05-10` no mesmo range → retorna 0
      * Caso 3 — misto (ambos): dois contratos `tipo='consultoria'`, um com `data_inicio=2026-02-10 valor_total=15000` (dentro) e outro com `data_inicio=2026-05-10 valor_total=8000` (fora) no range Q1 → retorna 15000
      * Ignora contratos `tipo='assessoria'` mesmo se `data_inicio` está no range
      * Ignora `status != 'ativo'` mesmo se for consultoria no range
      * Lista vazia → retorna 0
    - describe('calcularMrrHistorico'):
      * retorna 6 pontos por default; cada ponto = `{ monthKey, monthLabel, mrr }`
      * pontos em ordem cronológica crescente (mais antigo → mais novo)
      * contrato que iniciou após o mês N → não conta em N
      * contrato encerrado antes do mês N → não conta em N
      * contrato com `data_fim=null` ativo desde antes → conta em todos os meses
    - describe('calcularForecast'):
      * retorna 3 pontos por default; ponto = `{ monthKey, monthLabel, baseline }`
      * baseline mês 1 = MRR_atual + entradas - saídas para esse mês
      * "null data_fim": contratos sem `data_fim` NUNCA aparecem como saída
      * acumulativo: baseline[N] usa runningMrr de [N-1]
    - describe('classificarRenovacoes'):
      * 4 buckets retornados (`ate30`, `de31a60`, `de61a90`, `semDataFim`)
      * status != 'ativo' → ignorado em todos os buckets
      * `data_fim=null` + status ativo → vai para semDataFim
      * `data_fim` vencido (daysLeft < 0) → ignorado (Renovação só para futuro ≤90d, conforme D-05)
      * `data_fim` em 95d → ignorado
      * ordenação dentro de cada bucket: daysLeft asc (mais urgente primeiro)
    - describe('classifyUrgency'): cobre -1→'vencido', 0/30→'critico', 31/60→'alto', 61/90→'medio', 91→'baixo'.
  </behavior>
  <action>
    Criar `src/lib/__tests__/receita.test.ts` seguindo o padrão exato de `src/lib/__tests__/projecao.test.ts`: imports `describe/it/expect` de `vitest`, constante `TODAY = new Date(2026, 0, 15)`, helper fabricador de fixtures (ex: `contratoAtivo({ tipo, valor_mensal, valor_total, data_inicio, data_fim })`) que retorna objeto satisfazendo `Contrato` com campos obrigatórios preenchidos (`id`, `cliente_id`, `tipo`, `modelo_precificacao='mensal'`, `areas_direito=[]`, `status='ativo'`, `rm_status='em_dia'`, `created_at`, `updated_at`).

    Para cada describe acima, escrever os `it(...)` cobrindo cada caso. Importar `calcularMrr`, `calcularReceitaPontualPeriodo`, `calcularMrrHistorico`, `calcularForecast`, `classificarRenovacoes`, `classifyUrgency` de `../receita` — todos os imports vão FALHAR (arquivo não existe ainda) ⇒ RED.

    Para `calcularReceitaPontualPeriodo`: usar o tipo de range simples `{ start: Date; end: Date }` (não importar `PeriodValue` aqui — manter o helper desacoplado do componente PeriodSelector; conversão `PeriodValue → range` fica na página).

    Rodar `npm run test -- src/lib/__tests__/receita.test.ts` → deve falhar com "Cannot find module '../receita'" ou similar. Commit com mensagem `test(04-01): add failing tests for receita helpers`. NÃO implementar o módulo ainda. NÃO tocar em `tests/e2e/smoke.spec.ts` (edição movida para Plan 04-03 — ver objective).
  </action>
  <verify>
    <automated>npm run test -- src/lib/__tests__/receita.test.ts 2>&1 | grep -E "FAIL|Cannot find module|receita"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/lib/__tests__/receita.test.ts` existe com no mínimo 1 `describe` por helper (6 describes) e ≥15 casos `it(...)` no total
    - `npm run test -- src/lib/__tests__/receita.test.ts` falha com erro de import (módulo `../receita` não existe) — RED esperado
    - `grep -c "calcularReceitaPontualPeriodo" src/lib/__tests__/receita.test.ts` ≥ 3 (1 import + 1 describe + ≥3 it blocks)
    - `tests/e2e/smoke.spec.ts` NÃO é tocado nesta task (filename ausente de `git status --short`)
    - Nenhum código de produção alterado nesta task (apenas tests)
    - Commit `test(04-01): add failing tests for receita helpers` criado
  </acceptance_criteria>
  <done>RED state estabelecido: todos os testes do `receita.test.ts` falham por módulo ausente; commit dedicado de teste registrado; smoke E2E intocado (preserva CI signal durante Wave 1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implementar src/lib/receita.ts com helpers puros</name>
  <files>src/lib/receita.ts</files>
  <read_first>
    - src/lib/__tests__/receita.test.ts (criado em Task 1 — fonte da verdade do contrato)
    - src/lib/projecao.ts (padrão de helper puro com `today` injetável)
    - src/lib/utils.ts:36-39 (getDaysUntilExpiry — reutilizar em classificarRenovacoes)
    - .planning/phases/04-revenue-dashboard/04-RESEARCH.md "Code Examples" (referência das fórmulas — usar como ponto de partida, ajustar para passar os testes)
    - .planning/phases/04-revenue-dashboard/04-RESEARCH.md "Common Pitfalls" §1, §3, §4 (valor_protegido, null data_fim, divisão por zero)
  </read_first>
  <action>
    Criar `src/lib/receita.ts` seguindo o padrão de `src/lib/projecao.ts` (sem semicolons, single quotes, named exports). Imports necessários: `differenceInDays`, `startOfMonth`, `endOfMonth`, `addMonths`, `subMonths`, `isWithinInterval` de `date-fns`; tipo `Contrato` de `@/types`; `getDaysUntilExpiry` de `@/lib/utils` para classificarRenovacoes.

    Exportar (em ordem, com tipos PT-BR/EN mistos conforme convenção do projeto):
    - `type Urgencia = 'vencido' | 'critico' | 'alto' | 'medio' | 'baixo'`
    - `interface MrrBreakdown { total: number; assessoria: number; consultoriaProRata: number }`
    - `interface MrrHistoryPoint { monthKey: string; monthLabel: string; mrr: number }`
    - `interface ForecastPoint { monthKey: string; monthLabel: string; baseline: number }`
    - `interface RenovacoesBuckets { ate30: (Contrato & { daysLeft: number })[]; de31a60: (Contrato & { daysLeft: number })[]; de61a90: (Contrato & { daysLeft: number })[]; semDataFim: Contrato[] }`
    - `interface MrrOptions { defaultPontualMonths?: number }` (default 12)
    - função `contribuicaoMensal(c: Contrato, opt?: MrrOptions): number` — exporta para Plan 03 usar isolado se precisar; usa `Math.max(1, Math.ceil(days/30))` para evitar divisão por zero (D-01 / Pitfall 4)
    - `calcularMrr(contratos, opt)` — implementa fórmula D-01 (per D-01 do CONTEXT)
    - **`calcularReceitaPontualPeriodo(contratos: Contrato[], range: { start: Date; end: Date }): number`** — implementa Row 2 (per D-01 + Open Q2 RESOLVED): soma `c.valor_total ?? 0` para cada contrato onde `c.tipo === 'consultoria'`, `c.status === 'ativo'` E `c.data_inicio` está dentro de `range` (usar `isWithinInterval(new Date(c.data_inicio), { start: range.start, end: range.end })`). Retorna 0 para lista vazia ou nenhum match. Ignora contratos sem `data_inicio` (defensive — não deveria acontecer mas evita NaN).
    - `calcularMrrHistorico(contratos, { today?, months?=6, ...MrrOptions })` — implementa janela retroativa
    - `calcularForecast(contratos, { today?, months?=3, ...MrrOptions })` — implementa D-03 (per D-03); filtrar `c.data_fim != null` ANTES de incluir em saídas (Pitfall 3)
    - `classifyUrgency(daysLeft: number): Urgencia` — extrair de `RenovacoesPage.tsx:27-33`
    - `classificarRenovacoes(contratos): RenovacoesBuckets` — implementa D-05 (per D-05); ordena cada bucket por daysLeft asc

    NÃO referenciar `valor_protegido` em nenhum helper (per D-01 — provisão de risco, não receita). NÃO instalar pacotes (date-fns 4.x já presente).

    Rodar `npm run test -- src/lib/__tests__/receita.test.ts` até ficar VERDE. Iterar se algum teste falhar — fórmula é a verdade. Commit `feat(04-01): implement receita helpers (MRR/forecast/renovacoes/pontual)`.
  </action>
  <verify>
    <automated>npm run test -- src/lib/__tests__/receita.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/lib/receita.ts` existe com ≥130 linhas
    - `npm run test -- src/lib/__tests__/receita.test.ts` passa 100% (zero falhas, zero skips)
    - `grep -c "^export" src/lib/receita.ts` ≥ 11 (5 tipos + 7 funções: contribuicaoMensal, calcularMrr, calcularReceitaPontualPeriodo, calcularMrrHistorico, calcularForecast, classifyUrgency, classificarRenovacoes)
    - `grep -c "export function calcularReceitaPontualPeriodo" src/lib/receita.ts` ≥ 1 (BLOCKER 4 — Row 2 third facet)
    - `grep -v '^#' src/lib/receita.ts | grep -c "valor_protegido"` == 0 (per D-01 — exclui comentários)
    - `grep -c "Math.max(1," src/lib/receita.ts` ≥ 1 (proteção contra divisão por zero)
    - `npm run lint -- src/lib/receita.ts` retorna 0 erros
    - Commit `feat(04-01): implement receita helpers (MRR/forecast/renovacoes/pontual)` criado
  </acceptance_criteria>
  <done>Suite `receita.test.ts` 100% verde; helpers (incluindo `calcularReceitaPontualPeriodo`) exportados e lintados; commit GREEN registrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — RenovacoesPage importa classifyUrgency de @/lib/receita (DRY)</name>
  <files>src/pages/RenovacoesPage.tsx</files>
  <read_first>
    - src/pages/RenovacoesPage.tsx:1-58 (declaração inline atual de `Urgencia`, `URGENCIA_STYLE`, `classifyUrgency`)
    - src/lib/receita.ts (criado em Task 2 — fonte do `classifyUrgency` extraído)
    - .planning/phases/04-revenue-dashboard/04-RESEARCH.md "Anti-Patterns to Avoid" (duplicar URGENCIA_STYLE/classifyUrgency)
  </read_first>
  <action>
    Em `src/pages/RenovacoesPage.tsx`:
    1. Adicionar import: `import { classifyUrgency, type Urgencia } from '@/lib/receita'`
    2. Remover a declaração local `type Urgencia = ...` (linha ~12) — agora vem do receita
    3. Remover a função local `function classifyUrgency(...)` (linhas ~27-33)
    4. MANTER `URGENCIA_STYLE` inline na página (é cosmético, específico do design dessa página — extrair seria over-engineering)
    5. MANTER `ContratoBucket extends Contrato { daysLeft, urgencia }` (específico desta página)

    Verificar visualmente após salvar: tipo `Urgencia` continua válido nos usos existentes (`urgencia: Urgencia` no interface, `URGENCIA_STYLE[Urgencia]`). NÃO alterar nenhum comportamento visual ou lógico — refactor puro.

    Rodar smoke E2E só para essa rota: `npm run test:e2e -- --grep "/renovacoes"` se possível (ou full smoke). Lint e tipos devem passar.

    Commit `refactor(04-01): RenovacoesPage importa classifyUrgency de @/lib/receita (DRY com Plan 03)`.
  </action>
  <verify>
    <automated>npm run lint -- src/pages/RenovacoesPage.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/RenovacoesPage.tsx` contém `import { classifyUrgency, type Urgencia } from '@/lib/receita'` (grep ≥1)
    - Arquivo NÃO contém mais `function classifyUrgency(` local (grep -c "function classifyUrgency" == 0)
    - Arquivo NÃO contém mais `type Urgencia =` local (grep -c "^type Urgencia" == 0)
    - `URGENCIA_STYLE` constante permanece (grep ≥1) — cosmético da página
    - `npm run lint` zero erros nesse arquivo
    - `npx tsc --noEmit -p tsconfig.app.json` zero erros
    - Commit `refactor(04-01): RenovacoesPage importa classifyUrgency` criado
  </acceptance_criteria>
  <done>Refactor DRY aplicado; RenovacoesPage segue funcionalmente idêntica; tipos+lint verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → helpers | Dados sintéticos in-memory; sem persistência |
| (futuro) browser → helpers puros | Helpers consomem `Contrato[]` já filtrados por RLS no Plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | classifyUrgency / calcularMrr / calcularReceitaPontualPeriodo (lógica de negócio) | mitigate | Cobertura unitária ≥15 casos com fixtures determinísticas (Task 1); fórmulas trancadas em D-01/D-03/D-05 do CONTEXT |
| T-04-01-02 | Tampering | calcularForecast com `data_fim` null | mitigate | Filtrar `c.data_fim != null` antes de subtrair (Pitfall 3); teste explícito "null data_fim" em Task 1 |
| T-04-01-03 | Information Disclosure | helpers puros vazarem PII em logs | accept | Helpers puros não emitem logs; sem `console.*` na codebase (CONVENTIONS) |
| T-04-01-04 | DoS | divisão por zero com data_inicio==data_fim | mitigate | `Math.max(1, Math.ceil(days/30))` no `contribuicaoMensal` (Pitfall 4); teste explícito em Task 1 |
| T-04-01-05 | Tampering | calcularReceitaPontualPeriodo recebendo range inválido (start > end) | accept | `isWithinInterval` lança em range inválido; callers em Plan 03 sempre derivam range de `getPeriodRange(period)` que garante start ≤ end |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | Não há instalação de pacotes nesta fase (Package Legitimacy Audit N/A — RESEARCH §"Package Legitimacy Audit") |
</threat_model>

<verification>
- `npm run test -- src/lib/__tests__/receita.test.ts` 100% verde (zero falhas)
- `npm run lint` zero erros novos
- `npx tsc --noEmit -p tsconfig.app.json` zero erros
- `npm run test` (suite completa unitária) zero regressões — RenovacoesPage continua funcional
- `tests/e2e/smoke.spec.ts` NÃO foi tocado por este plan (preserva CI signal — edição vive no Plan 04-03)
- Três commits separados (test → feat → refactor) seguindo TDD discipline
</verification>

<success_criteria>
- Helpers puros `calcularMrr`, `calcularReceitaPontualPeriodo`, `calcularMrrHistorico`, `calcularForecast`, `classificarRenovacoes`, `classifyUrgency` exportados e testados
- Cobertura mínima de cada fórmula locked do CONTEXT (D-01 MRR, D-01 pontual Row 2, D-03 forecast, D-05 buckets) com fixtures explícitas
- RenovacoesPage refatorada importando classifyUrgency (zero duplicação)
- Smoke E2E permanece intocado nesta wave (Plan 03 cuida da edição)
- Plan 03 pode importar todos os helpers sem ambiguidade de assinatura
</success_criteria>

<output>
Create `.planning/phases/04-revenue-dashboard/04-01-SUMMARY.md` when done — registrar:
- Helpers criados com assinaturas exatas (para Plan 03 consultar) — em especial `calcularReceitaPontualPeriodo(contratos, range)`
- Casos de teste cobertos (lista numerada, incluindo os 3 do calcularReceitaPontualPeriodo)
- Decisão sobre `defaultPontualMonths` (manteve 12 do CONTEXT)
- Próximo passo: Plan 03 implementa ReceitaPage usando os helpers; Plan 03 também adiciona `/receita` ao smoke E2E
</output>
