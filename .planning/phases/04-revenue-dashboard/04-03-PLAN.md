---
phase: 04-revenue-dashboard
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - tests/e2e/smoke.spec.ts
  - src/pages/ReceitaPage.tsx
  - src/router.tsx
  - src/components/layout/Sidebar.tsx
autonomous: false
requirements: [REV-01, REV-02, REV-03]
tags: [revenue, ui, dashboard, mvp-slice, recharts]

must_haves:
  truths:
    - "Gestor coordenador+ acessa /receita e vê MRR, ARR projetado, Forecast 3m e contagem de renovações ≤30d em 4 cards (REV-01)"
    - "Row 2 breakdown mostra MRR(assessoria), MRR(consultoria pro-rata) e Receita pontual no período (via calcularReceitaPontualPeriodo do Plan 01) (REV-01 / D-01)"
    - "Gráfico LineChart com últimos 6 meses de MRR + faixa pontilhada dos próximos 3 meses (forecast) renderiza com PeriodSelector no header (REV-02)"
    - "Lista de renovações pendentes agrupada em 3 buckets (≤30d / 31-60d / 61-90d); ≤30d tem Badge variant=destructive + ícone AlertTriangle (REV-03)"
    - "Seção colapsada secundária mostra contratos status='ativo' sem data_fim (D-02 — 'sem renovação agendada')"
    - "Click em cada card navega para /contratos com filtros via URL (D-08): MRR→?status=ativo; ARR→?status=ativo; Forecast→?status=ativo&vencendo_em_dias=90; Renovações→?status=ativo&vencendo_em_dias=30"
    - "Consultor (role abaixo de coordenador) acessando /receita vê fallback do RequireRole (não vê dados)"
    - "Rota /receita registrada no router; nav item gated visível na sidebar grupo CLIENTES para coordenador+"
    - "tests/e2e/smoke.spec.ts inclui '/receita' no array ROTAS (RED → GREEN dentro desta mesma wave: vermelho enquanto Task 1 não terminou; verde após rota registrada)"
  artifacts:
    - path: "tests/e2e/smoke.spec.ts"
      provides: "rota /receita walked como diretor e bloqueada como consultor"
      contains: "/receita"
    - path: "src/pages/ReceitaPage.tsx"
      provides: "página /receita end-to-end usando helpers de @/lib/receita"
      exports: ["ReceitaPage"]
      min_lines: 200
    - path: "src/router.tsx"
      provides: "rota /receita registrada"
      contains: "path: 'receita'"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "nav item Receita gated coordenador+ no grupo CLIENTES"
      contains: "/receita"
  key_links:
    - from: "src/pages/ReceitaPage.tsx"
      to: "@/lib/receita (calcularMrr/calcularReceitaPontualPeriodo/calcularMrrHistorico/calcularForecast/classificarRenovacoes)"
      via: "import"
      pattern: "import.*calcularReceitaPontualPeriodo.*from '@/lib/receita'"
    - from: "src/pages/ReceitaPage.tsx"
      to: "@/hooks/useContratos"
      via: "useContratos()"
      pattern: "useContratos\\("
    - from: "src/pages/ReceitaPage.tsx"
      to: "@/components/shared/RequireRole"
      via: "wrap atLeast='coordenador'"
      pattern: "RequireRole\\s+atLeast=\"coordenador\""
    - from: "src/pages/ReceitaPage.tsx"
      to: "recharts (LineChart/Line/Area)"
      via: "import + render"
      pattern: "from 'recharts'"
    - from: "src/pages/ReceitaPage.tsx"
      to: "/contratos?vencendo_em_dias= (Plan 02)"
      via: "useNavigate"
      pattern: "navigate\\('/contratos\\?"
    - from: "src/router.tsx"
      to: "ReceitaPage"
      via: "import + path 'receita'"
      pattern: "path:\\s*'receita'"
    - from: "src/components/layout/Sidebar.tsx"
      to: "/receita"
      via: "NavItem"
      pattern: "/receita"
    - from: "tests/e2e/smoke.spec.ts"
      to: "rota /receita"
      via: "array ROTAS"
      pattern: "'/receita'"
---

<objective>
Entregar o slice vertical end-to-end da Phase 4: página `/receita` funcional do clique no sidebar até o cálculo financeiro renderizado, usando os helpers do Plan 01 e o drill-down do Plan 02. Esta é a slice mínima que satisfaz a user story completa.

Por que tudo num único plano (e não dividir em "rota" + "cards" + "gráfico" + "lista"): horizontal layers viola MVP_MODE. Após este plano, gestor coordenador+ vai do login → clique "Receita" no sidebar → vê todas as 5 rows (cards + breakdown + gráfico + renovações + sem data_fim) → clica num card → navega para /contratos filtrada. Cada row anterior depende da próxima existir para a story funcionar.

**Smoke E2E:** Task 0 adiciona `/receita` ao array ROTAS de `tests/e2e/smoke.spec.ts` ANTES da implementação (RED) e Task 1 fecha o ciclo (GREEN) com a rota registrada. Esta ordenação mantém o ciclo RED→GREEN do smoke dentro de uma única wave, sem deixar CI signal quebrado em wave anterior.

Helpers já existem (Plan 01); URL filter já existe (Plan 02). Este plano é composição + render + wiring de auth gate + nav.

Purpose: Slice usável end-to-end.
Output: `/receita` operacional em produção (após deploy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-revenue-dashboard/04-CONTEXT.md
@.planning/phases/04-revenue-dashboard/04-RESEARCH.md
@.planning/phases/04-revenue-dashboard/04-VALIDATION.md
@.planning/phases/04-revenue-dashboard/04-01-SUMMARY.md
@.planning/phases/04-revenue-dashboard/04-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
@src/lib/receita.ts
@src/lib/utils.ts
@src/lib/periods.ts
@src/hooks/useContratos.ts
@src/components/shared/RequireRole.tsx
@src/components/shared/PeriodSelector.tsx
@src/components/shared/ResponsavelBadge.tsx
@src/pages/AdocaoPage.tsx
@src/pages/DashboardPage.tsx
@src/pages/AnalyticsPage.tsx
@src/pages/RenovacoesPage.tsx
@src/router.tsx
@src/components/layout/Sidebar.tsx
@tests/e2e/smoke.spec.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Adicionar /receita ao smoke E2E (RED gate de regressão)</name>
  <files>tests/e2e/smoke.spec.ts</files>
  <read_first>
    - tests/e2e/smoke.spec.ts (array ROTAS atual — verificar agrupamento por área CLIENTES, posição após '/renovacoes')
  </read_first>
  <action>
    Editar `tests/e2e/smoke.spec.ts` adicionando a string `'/receita'` ao array `ROTAS`, posicionada logo após `'/renovacoes'` para manter o agrupamento visual por área (grupo CLIENTES no sidebar). NÃO criar arquivo novo; NÃO mover blocos existentes.

    Após salvar, rodar `npm run test:e2e -- --grep "receita"` localmente. Esperado: FALHA com erro de navegação 404/route-not-found (a rota ainda não existe — Task 1 vai resolver). Esse RED é INTENCIONAL e DEVE ser fechado pela Task 1 ANTES de qualquer commit final desta wave.

    Commit `test(04-03): add /receita to smoke E2E rotas list (RED → GREEN em Task 1)`.
  </action>
  <verify>
    <automated>grep -c "'/receita'" tests/e2e/smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/smoke.spec.ts` contém a string literal `'/receita'` no array `ROTAS` (`grep -c "'/receita'" tests/e2e/smoke.spec.ts` ≥ 1)
    - String aparece logo após `'/renovacoes'` (verificação visual durante edição)
    - Nenhum código de produção alterado nesta task
    - Smoke E2E roda mas falha em `/receita` por route 404 — esperado (RED)
    - Commit `test(04-03): add /receita to smoke E2E rotas list` criado
  </acceptance_criteria>
  <done>Smoke E2E em estado RED esperado; Task 1 vai fechar o ciclo dentro desta mesma wave.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: ReceitaPage.tsx (página completa) + rota + nav sidebar (GREEN do smoke)</name>
  <files>src/pages/ReceitaPage.tsx, src/router.tsx, src/components/layout/Sidebar.tsx</files>
  <read_first>
    - src/lib/receita.ts (Plan 01 — assinaturas exatas dos helpers, incluindo `calcularReceitaPontualPeriodo`)
    - src/lib/utils.ts:10-13 (formatCurrency — usar sempre que mostrar moeda)
    - src/lib/periods.ts (PeriodValue + getPeriodRange + isInRange + availableYears — usar para Row 2 "Receita pontual no período")
    - src/components/shared/PeriodSelector.tsx (props + uso)
    - src/components/shared/RequireRole.tsx:17-31 (padrão exato)
    - src/pages/AdocaoPage.tsx:97-103 (exemplo wrap RequireRole atLeast="coordenador")
    - src/pages/DashboardPage.tsx (padrão de layout: cards no topo + gráfico abaixo)
    - src/pages/AnalyticsPage.tsx:11-14, 196-199, 279, 403-432 (uso de recharts + Tooltip styling + PeriodSelector derivedYearsFrom)
    - src/pages/RenovacoesPage.tsx:80-180 (padrão da lista agrupada — referência para Row 4)
    - src/hooks/useContratos.ts:8-20 (assinatura — sem args, retorna `{data, isLoading, isError}`)
    - src/router.tsx:42-95 (onde adicionar rota — depois de '/adocao', linha ~81)
    - src/components/layout/Sidebar.tsx:42-50 (grupo CLIENTES — onde adicionar nav item)
    - src/components/layout/Sidebar.tsx (procurar `useCurrentRole` e `isCoordenadorOrAcima` — padrão de gate por role no sidebar)
    - .planning/phases/04-revenue-dashboard/04-CONTEXT.md §1-§8 (decisões locked completas)
    - .planning/phases/04-revenue-dashboard/04-RESEARCH.md "Pattern 4: Recharts LineChart + Area" (configuração exata + paleta `#0089ac`)
    - .planning/phases/04-revenue-dashboard/04-RESEARCH.md "Common Pitfalls" §1, §2, §5, §8 (valor_protegido, staleTime, connectNulls, PeriodSelector ambiguity)
    - .planning/phases/04-revenue-dashboard/04-RESEARCH.md "Open Questions (RESOLVED)" Q2 (PeriodSelector afeta APENAS Row 2)
  </read_first>
  <action>
    Criar `src/pages/ReceitaPage.tsx` (componente único — meta ≤400 linhas; se passar, extrair sub-componentes em `src/components/receita/` numa segunda passada). Estrutura interna obrigatória (per D-07 layout tela única scroll vertical):

    **Imports principais:**
    - React: `useMemo, useState`
    - `useNavigate` de react-router-dom
    - `DollarSign, TrendingUp, AlertTriangle, ChevronRight, ChevronDown, BarChart3` de lucide-react
    - `useContratos` de `@/hooks/useContratos`
    - `RequireRole` de `@/components/shared/RequireRole`
    - `PeriodSelector` + `getPeriodRange, availableYears, type PeriodValue` de `@/lib/periods` (verificar API; ajustar se necessário)
    - `Card, CardContent, CardHeader, CardTitle` de `@/components/ui/card`
    - `Badge` de `@/components/ui/badge`
    - `EmptyState` de `@/components/ui/empty-state`
    - `ResponsavelBadge` de `@/components/shared/ResponsavelBadge`
    - `formatCurrency, formatDate, cn` de `@/lib/utils`
    - **`calcularMrr, calcularReceitaPontualPeriodo, calcularMrrHistorico, calcularForecast, classificarRenovacoes, contribuicaoMensal` de `@/lib/receita`** (BLOCKER 4 — usar o helper extraído em vez de inline)
    - recharts: `LineChart, Line, Area, XAxis, YAxis, Tooltip, ResponsiveContainer, ReferenceLine`
    - `type Contrato` de `@/types`

    **Export padrão `ReceitaPage`** wrap RequireRole (exatamente como AdocaoPage):
    ```
    export function ReceitaPage() {
      return (
        <RequireRole atLeast="coordenador">
          <ReceitaPageContent />
        </RequireRole>
      )
    }
    ```

    **`ReceitaPageContent`** orquestra tudo:
    1. `const { data: contratos = [], isLoading } = useContratos()`
    2. `const navigate = useNavigate()`
    3. `const [period, setPeriod] = useState<PeriodValue>({ year: new Date().getFullYear(), granularity: 'total' })`
    4. `const [showSemDataFim, setShowSemDataFim] = useState(false)`
    5. Calcular tudo via `useMemo` (per Pitfall 5 — proteger performance):
       - `mrr = calcularMrr(contratos)` (per D-01)
       - `historico = calcularMrrHistorico(contratos, { months: 6 })` (per REV-02)
       - `forecast = calcularForecast(contratos, { months: 3 })` (per D-03)
       - `buckets = classificarRenovacoes(contratos)` (per D-05)
       - `periodRange = getPeriodRange(period)` (memoizar para Row 2)
       - **`receitaPontualPeriodo = calcularReceitaPontualPeriodo(contratos, periodRange)`** (BLOCKER 4 — usar helper testado, NÃO computar inline). Documentar no comentário inline: `// per D-01 Row 2 + Open Q2 RESOLVED — PeriodSelector afeta APENAS este número`
       - `chartData` = merge de `historico` + `forecast` em formato `{ month, mrr, forecastHigh, forecastLow }`. Para pontos do histórico: `mrr=valor`, `forecastHigh=null`, `forecastLow=null`. Para pontos do forecast: `mrr=null` (exceto o último ponto do histórico que duplica como primeira "ponte" para o forecast — implementar via duplicação do último ponto histórico com `forecastHigh=baseline*1.1` e `forecastLow=baseline*0.9` no primeiro mês de forecast, OU mais simples: deixar `forecastHigh=baseline` e `forecastLow=baseline*0.85` representando "se ~15% não renovar"). Documentar a escolha como decisão de cosmético no commit.

    **Render — 5 rows na ordem D-07:**

    Row Header: `<div>` com `<h1>Receita</h1>` + `<PeriodSelector value={period} onChange={setPeriod} derivedYearsFrom={contratos} />`. Texto curto de ajuda (PT-BR): "Período filtra apenas 'Receita pontual no período' abaixo. Gráfico mostra sempre últimos 6m + próximos 3m." (per Pitfall 8 + Open Q2 RESOLVED — desambiguar para o usuário).

    Row 1 — 4 Cards clicáveis (grid responsivo `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4`):
    - Card 1: "MRR" = `formatCurrency(mrr.total)`; ícone DollarSign; onClick → `navigate('/contratos?status=ativo')` (per D-08)
    - Card 2: "ARR projetado" = `formatCurrency(mrr.total * 12)`; ícone TrendingUp; onClick → `navigate('/contratos?status=ativo')`
    - Card 3: "Forecast 3m" = `formatCurrency(forecast[forecast.length - 1]?.baseline ?? mrr.total)`; ícone BarChart3; onClick → `navigate('/contratos?status=ativo&vencendo_em_dias=90')`
    - Card 4: "Renovações ≤30d" = `buckets.ate30.length` (número, não moeda); ícone AlertTriangle; onClick → `navigate('/contratos?status=ativo&vencendo_em_dias=30')`
    - Padronizar visual seguindo `DashboardPage` cards; cursor pointer; `role="button"` + `onKeyDown` Enter/Space para a11y.

    Row 2 — Breakdown (per D-01 + BLOCKER 4): grid `md:grid-cols-3` com 3 sub-cards menores:
    - "MRR Assessoria" = `formatCurrency(mrr.assessoria)`
    - "MRR Consultoria (pro-rata)" = `formatCurrency(mrr.consultoriaProRata)`
    - "Receita pontual no período" = `formatCurrency(receitaPontualPeriodo)` (vindo de `calcularReceitaPontualPeriodo` do Plan 01 — NÃO computar inline)

    Row 3 — Gráfico (per REV-02 + D-04):
    ```
    <Card><CardHeader><CardTitle>Evolução MRR — 6 meses + 3m forecast</CardTitle></CardHeader>
      <CardContent style={{ height: 280 }}>
        <ResponsiveContainer><LineChart data={chartData}>...
    ```
    Configuração exata do recharts: ver RESEARCH "Pattern 4". Pontos chave:
    - `<XAxis dataKey="month" tick={{ fontSize: 10, fill: 'rgba(150,165,180,0.60)' }} />`
    - `<YAxis tickFormatter={(v) => 'R$' + (v/1000).toFixed(0) + 'k'} tick={{ fontSize: 10, fill: 'rgba(150,165,180,0.60)' }} />`
    - `<Tooltip formatter={(v: number) => formatCurrency(v)} contentStyle={{ background: '#0d1929', border: '1px solid var(--alpha-border-md)', color: 'rgba(220,230,240,0.90)', borderRadius: 8 }} />`
    - `<ReferenceLine x="Hoje" ... />` opcional se houver ponto "Hoje"; alternativa: marcar o último ponto histórico como divisor visual
    - `<Line type="monotone" dataKey="mrr" stroke="#0089ac" strokeWidth={2} dot={{ r: 3 }} connectNulls />` (per Pitfall 5: connectNulls APENAS no `<Line>`)
    - `<Area type="monotone" dataKey="forecastHigh" stroke="#0089ac" strokeDasharray="5 5" fill="#0089ac" fillOpacity={0.15} />` (per D-04)
    - **NÃO** usar `connectNulls` nas séries `forecastHigh`/`forecastLow` (per Pitfall 5)
    - Cor primária: `#0089ac` (cyan CONSEJ — RESEARCH Pattern 4)

    Row 4 — Lista de renovações pendentes agrupada (per REV-03 + D-05 + D-06):
    - 3 sub-seções na ordem: `≤ 30 dias` (badge destructive), `31-60 dias`, `61-90 dias`
    - Cada item: nome cliente (`c.cliente?.nome`), valor mensal (`formatCurrency(contribuicaoMensal(c))`), `formatDate(c.data_fim)`, `daysLeft` (badge), `ResponsavelBadge` com `c.responsavel_id`
    - Para itens `≤30d`: `<Badge variant="destructive">{daysLeft} dias</Badge>` + ícone `<AlertTriangle className="w-4 h-4" />` (per D-06)
    - Click no item → `navigate(\`/clientes/\${c.cliente_id}\`)` (drill ao cliente — alinhado com padrão RenovacoesPage)
    - Empty state em cada bucket: `<EmptyState icon={...} title="Nenhuma renovação neste intervalo" />` quando array vazio

    Row 5 — Sem data_fim (per D-02): seção colapsada
    - Header clicável: `<button onClick={() => setShowSemDataFim(s => !s)}>Contratos ativos sem renovação agendada ({buckets.semDataFim.length}) <ChevronDown/ChevronRight /></button>`
    - Quando `showSemDataFim`, renderiza lista simples (nome cliente + tipo + `formatCurrency(contribuicaoMensal(c))`)
    - Se array vazio, omitir seção inteira

    **Loading state:** `if (isLoading) return <div>Carregando...</div>` (segue padrão de outras pages — pode incrementar visual depois)

    **Empty state geral:** se `contratos.length === 0`, mostrar `<EmptyState icon={DollarSign} title="Sem contratos para calcular receita" description="Cadastre contratos em /contratos para popular este dashboard." />`

    **NÃO IMPLEMENTAR** (per Deferred + Open Questions RESOLVED): tooltips on-hover ricos nos cards, tabs separadoras, drill no gráfico, forecast com regressão linear, export PDF, atualizar DashboardPage/AnalyticsPage (escopo creep — Open Q4 RESOLVED: "NÃO nesta fase").

    **Em `src/router.tsx`:**
    1. Import: `import { ReceitaPage } from '@/pages/ReceitaPage'` (manter ordem alfabética/lógica com outras pages)
    2. Adicionar na children do AppLayout após `{ path: 'adocao', element: <AdocaoPage /> }` (linha ~81): `{ path: 'receita', element: <ReceitaPage /> },`

    **Em `src/components/layout/Sidebar.tsx`:**
    1. No grupo `CLIENTES` (linhas ~42-50), adicionar item após `{ to: '/contratos', label: 'Contratos', icon: FileText }`: `{ to: '/receita', label: 'Receita', icon: DollarSign }`. **Atenção:** verificar se o NAV_GROUPS atual suporta gate por role inline. Se sim, marcar como `coordenador+`. Se a estrutura atual é puramente declarativa sem gate por item, render condicional via `useCurrentRole().isCoordenadorOrAcima` no map (padrão sugerido em RESEARCH "Rota + Sidebar"). Implementar o gate de visibilidade — usuários sem role coordenador+ NÃO veem o nav item (defense-in-depth do RequireRole no componente).
    2. Importar `DollarSign` de `lucide-react` se ainda não importado nesse arquivo.

    Após edits, rodar:
    - `npm run lint`
    - `npx tsc --noEmit -p tsconfig.app.json`
    - `npm run test` (suite unitária — zero regressão; `receita.test.ts` do Plan 01 deve seguir verde)
    - `npm run test:e2e -- --grep "receita"` (smoke do Task 0 deve agora PASSAR para diretor — gate do consultor verificado em Task 2 checkpoint)

    Commit `feat(04-03): ReceitaPage end-to-end + rota + nav sidebar (REV-01/02/03)`.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit -p tsconfig.app.json && npm run test && npm run test:e2e -- --grep "receita"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/ReceitaPage.tsx` existe com ≥200 linhas
    - `grep -c "RequireRole" src/pages/ReceitaPage.tsx` ≥ 2 (1 import + 1 uso `atLeast="coordenador"`)
    - `grep -c "from 'recharts'" src/pages/ReceitaPage.tsx` ≥ 1
    - `grep -c "calcularMrr\|calcularMrrHistorico\|calcularForecast\|classificarRenovacoes\|calcularReceitaPontualPeriodo" src/pages/ReceitaPage.tsx` ≥ 5 (todos os 5 helpers usados; BLOCKER 4 exige `calcularReceitaPontualPeriodo`)
    - `grep -c "calcularReceitaPontualPeriodo" src/pages/ReceitaPage.tsx` ≥ 1 (BLOCKER 4 — Row 2 usa o helper extraído, não inline)
    - `grep -c "navigate('/contratos?status=ativo" src/pages/ReceitaPage.tsx` ≥ 3 (3 dos 4 cards têm drill-down per D-08; o card MRR e ARR podem compartilhar mesma URL, então mínimo 3 ocorrências distintas)
    - `grep -c "vencendo_em_dias=90" src/pages/ReceitaPage.tsx` ≥ 1 (drill Forecast per D-08)
    - `grep -c "vencendo_em_dias=30" src/pages/ReceitaPage.tsx` ≥ 1 (drill Renovações ≤30d per D-08)
    - `grep -c "variant=\"destructive\"" src/pages/ReceitaPage.tsx` ≥ 1 (badge alerta ≤30d per D-06)
    - `grep -c "AlertTriangle" src/pages/ReceitaPage.tsx` ≥ 1 (ícone alerta per D-06)
    - `grep -c "PeriodSelector" src/pages/ReceitaPage.tsx` ≥ 1 (per D-07 header)
    - `grep -v '^//' src/pages/ReceitaPage.tsx | grep -c "valor_protegido"` == 0 (per D-01 — exclui comentários explicativos)
    - `grep -c "connectNulls" src/pages/ReceitaPage.tsx` ≤ 1 (apenas no `<Line dataKey="mrr">` per Pitfall 5)
    - `src/router.tsx` contém `path: 'receita'` (grep ≥1) e `import.*ReceitaPage` (grep ≥1)
    - `src/components/layout/Sidebar.tsx` contém `/receita` (grep ≥1) e gate de role coordenador+ aplicado ao item Receita
    - `npm run lint` zero erros
    - `npx tsc --noEmit -p tsconfig.app.json` zero erros
    - `npm run test` zero regressões (suite unitária + receita.test.ts segue verde)
    - `npm run test:e2e` smoke `/receita` PASSA como projeto `diretor` (rota walked sem erro — fecha o RED do Task 0)
    - Commit `feat(04-03): ReceitaPage end-to-end + rota + nav sidebar` criado
  </acceptance_criteria>
  <done>/receita acessível para coordenador+ com 5 rows (cards + breakdown via `calcularReceitaPontualPeriodo` + gráfico + renovações + sem data_fim); drill-down para /contratos funcional; smoke E2E /receita VERDE (RED do Task 0 fechado); lint+types+suite limpos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint humano — UAT manual de /receita (gestor)</name>
  <what-built>Página `/receita` completa com 4 cards drillables, breakdown por tipo (incluindo "Receita pontual no período" via helper testado), gráfico LineChart + faixa de forecast 3m, lista agrupada de renovações pendentes e seção colapsada de contratos sem data_fim. Gated coordenador+ via RequireRole + nav item gated.</what-built>
  <how-to-verify>
    Rodar `npm run dev`. Validar cenários abaixo manualmente. Comparar contra success criteria do ROADMAP (gestor decide em <30s).

    **Como diretor (perfil com mais permissão):**

    1. Clicar em "Receita" no sidebar (grupo CLIENTES) → URL muda para `/receita` → página renderiza sem tela branca em <2s.

    2. **Row 1 — 4 Cards:**
       - MRR mostra valor em R$ (não `R$ NaN`, não `R$ Infinity`)
       - ARR mostra ~ MRR × 12
       - Forecast 3m mostra valor em R$
       - "Renovações ≤30d" mostra um número (contagem) — pode ser 0 se nenhum contrato vence em 30d
       - Cards são clicáveis (cursor pointer, hover indica clicabilidade)

    3. **Drill-down cards (per D-08):**
       - Click no MRR → navega para `/contratos?status=ativo`
       - Click no Forecast → navega para `/contratos?status=ativo&vencendo_em_dias=90` e LISTA é filtrada (esse é o teste integrado com Plan 02)
       - Click em Renovações ≤30d → `/contratos?status=ativo&vencendo_em_dias=30`
       - Voltar para `/receita` via sidebar — estado preservado (PeriodSelector continua no período escolhido se ainda na sessão)

    4. **Row 2 — Breakdown:**
       - 3 sub-cards: MRR Assessoria, MRR Consultoria (pro-rata), Receita pontual no período
       - Mudar o PeriodSelector (ex: trimestre T2) — "Receita pontual no período" recalcula via `calcularReceitaPontualPeriodo`; "MRR Assessoria"/"MRR Consultoria" NÃO mudam (per D-07 + Pitfall 8 + Open Q2 RESOLVED — gráfico e MRR fixos)
       - Texto de ajuda mostra que PeriodSelector afeta apenas Row 2 (visualmente claro?)

    5. **Row 3 — Gráfico:**
       - LineChart renderiza com 6 pontos histórico + 3 pontos forecast
       - Linha sólida cyan (#0089ac) nos 6 meses passados
       - Faixa pontilhada/sombreada nos 3 meses futuros (per D-04)
       - Tooltip on-hover mostra valor em formatCurrency
       - Eixo Y em formato "R$Xk"
       - **CRÍTICO — Pitfall 5:** a faixa pontilhada NÃO "vaza" para o histórico (sombra deve aparecer somente nos 3 últimos meses)

    6. **Row 4 — Renovações pendentes:**
       - 3 buckets visíveis (≤30 / 31-60 / 61-90)
       - Itens em ≤30d têm Badge destructive (vermelho) + ícone AlertTriangle (per D-06)
       - Ordenação dentro do bucket: mais urgente no topo
       - Click num item navega para `/clientes/<id>`
       - Empty state em buckets vazios é amigável

    7. **Row 5 — Sem data_fim:**
       - Seção colapsada por default; clique expande
       - Lista contratos status='ativo' sem data_fim (per D-02)
       - Se nenhum contrato sem data_fim, seção pode estar oculta

    **Como consultor (sem role coordenador+):**

    8. Logar como consultor (Playwright tem fixture; ou trocar de conta manual)
       - Nav item "Receita" NÃO aparece no sidebar
       - Acessar `/receita` direto na URL: vê fallback do RequireRole (não vê os dados)

    **Console / Errors:**

    9. DevTools console aberto durante toda a navegação:
       - Zero erros de console
       - Zero requests 4xx/5xx para Supabase
       - Nenhum aviso de "NaN" ou "Infinity" nos cards

    **Performance:**

    10. Page load < 2s (per CONTEXT constraint). Cards aparecem imediatamente após o load (helpers são síncronos).

    Se algum cenário falhar, listar qual número (1-10) e o comportamento observado vs esperado.
  </how-to-verify>
  <resume-signal>Type "approved" ou listar discrepâncias (cenário N: observado X, esperado Y)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase (PostgREST via useContratos) | RLS role-aware migration 029 ativa |
| Browser → React Router (drill URL) | Inputs internos confiáveis |
| URL `?vencendo_em_dias=N` | Tratado em Plan 02 com parse defensivo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Elevation of Privilege | Consultor acessando /receita direto na URL | mitigate | `<RequireRole atLeast="coordenador">` no componente + nav item escondido via `useCurrentRole().isCoordenadorOrAcima`. Defense-in-depth: RLS role-aware na tabela `contratos` (migration 029) é a barreira real (verificada em smoke E2E) |
| T-04-03-02 | Information Disclosure | Receita exposta em DevTools/logs | mitigate | Nenhum `console.log(contrato)`; React escapa `{cliente.nome}` automaticamente; sem `dangerouslySetInnerHTML` |
| T-04-03-03 | Tampering | Cliente manipula PeriodSelector ou URL para inflar números | accept | Cálculos são client-side derivados de dados RLS-filtered. Manipulação de UI apenas afeta visão local; servidor (Postgres) é fonte da verdade |
| T-04-03-04 | DoS | Render lento com >500 contratos | mitigate | `useMemo` em todos os cálculos (per Pitfall 2 mitigation); volume esperado <500 (Open Q3 RESOLVED); cap em 6 meses histórico + 3m forecast |
| T-04-03-05 | XSS | `cliente.nome` ou `cliente.empresa` com payload malicioso | accept | React JSX escapa por default; nunca usar innerHTML; padrão já adotado em ContratosPage/RenovacoesPage |
| T-04-03-06 | Tampering | Drill-down URL `?vencendo_em_dias` aceita valor inválido | mitigate | Plan 02 implementou parse defensivo (Number.isFinite + range [0,365]) |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo (recharts, date-fns, react-router já em package.json — Package Legitimacy Audit N/A per RESEARCH) |
</threat_model>

<verification>
- `npm run lint` zero erros
- `npx tsc --noEmit -p tsconfig.app.json` zero erros
- `npm run test` zero regressões (incluindo receita.test.ts do Plan 01)
- `npm run test:e2e` smoke `/receita` VERDE como diretor (rota walked sem erro/4xx/5xx — fecha o RED do Task 0)
- Checkpoint humano UAT aprovado (10 cenários)
- Drill-down de cards `/receita` → `/contratos?vencendo_em_dias=N` integra com Plan 02 corretamente
- `calcularReceitaPontualPeriodo` é importado e usado em Row 2 (não há cálculo inline duplicado)
</verification>

<success_criteria>
- Gestor coordenador+ acessa `/receita` em <2s e vê MRR, ARR, Forecast 3m, Renovações ≤30d em cards (REV-01)
- Breakdown por tipo (assessoria / consultoria / pontual via helper testado) visível em row 2 (REV-01 + BLOCKER 4)
- Gráfico LineChart com 6m histórico + 3m forecast com PeriodSelector no header (REV-02)
- Lista de renovações agrupada (30/60/90) com alerta visual destructive + AlertTriangle para ≤30d (REV-03)
- Drill-down funcional dos cards para `/contratos` com filtros via URL (D-08)
- Consultor não acessa página (RequireRole + nav escondido)
- Zero regressão em /contratos, /renovacoes, /dashboard
- Smoke E2E `/receita` verde (RED do Task 0 fechado)
- Phase 4 goal atingido: gestor decide receita em <30s sem planilha
</success_criteria>

<output>
Create `.planning/phases/04-revenue-dashboard/04-03-SUMMARY.md` when done — registrar:
- Linhas de código de ReceitaPage (referência para futura decisão de extrair sub-componentes)
- Mapeamento exato dos cards → drill-down URLs (per D-08)
- Resultado dos 10 cenários do checkpoint humano
- Volume real de contratos em produção observado (Open Q3 RESOLVED — registrar count; se >500, abrir issue de memoização)
- Decisões de cosmético do recharts (`forecastHigh`/`forecastLow` proporções escolhidas)
- Confirmação de smoke E2E /receita verde
- Confirmação de que `calcularReceitaPontualPeriodo` foi importado e usado em Row 2 (sem cálculo inline)
- Fechar Phase 4 — todos REV-01/02/03 satisfeitos
</output>
