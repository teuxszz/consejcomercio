---
phase: 04-revenue-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ContratosPage.tsx
autonomous: true
requirements: [REV-01]
tags: [revenue, contratos, drill-down, url-filter]

must_haves:
  truths:
    - "ContratosPage aceita parâmetro de URL `?vencendo_em_dias=N` (N = inteiro 0..365)"
    - "Quando presente, filtra a lista para contratos com `getDaysUntilExpiry(c.data_fim)` entre 0 e N (inclusive)"
    - "Quando ausente, comportamento existente da página é 100% preservado (zero regressão)"
    - "Filtro `vencendo_em_dias` compõe AND com filtros existentes (scope, search, tipoFilter, statusFilter, rmFilter)"
    - "Parsing defensivo: valores não-numéricos, negativos ou > 365 são IGNORADOS (NaN-safe per ASVS V5)"
    - "Drill-down de Plan 03 (`/contratos?status=ativo&vencendo_em_dias=30`) navega e filtra corretamente"
  artifacts:
    - path: "src/pages/ContratosPage.tsx"
      provides: "filtro adicional vencendo_em_dias lido via useSearchParams"
      contains: "useSearchParams"
  key_links:
    - from: "src/pages/ContratosPage.tsx"
      to: "react-router-dom.useSearchParams"
      via: "import"
      pattern: "useSearchParams"
    - from: "src/pages/ContratosPage.tsx"
      to: "@/lib/utils.getDaysUntilExpiry"
      via: "import (já existente)"
      pattern: "getDaysUntilExpiry"
---

<objective>
Estender `ContratosPage` para aceitar `?vencendo_em_dias=N` via `useSearchParams`, habilitando o drill-down dos cards de `/receita` (Plan 03) sem quebrar nenhum filtro existente. Independente do Plan 01 (zero overlap de arquivos) — roda em paralelo na mesma wave.

Por que separado de Plan 03: Plan 03 já é uma slice cheia (página + rota + sidebar + integração de 4 helpers). Manter a extensão do filtro como plano dedicado isola o blast radius — qualquer regressão em `/contratos` (rota crítica em produção, lista hoje atualizada várias vezes ao dia) é detectável imediatamente sem confundir com a UI nova de `/receita`.

Purpose: Drill-down bookmarkable e composicional (per D-08).
Output: `ContratosPage.tsx` com 1 novo predicate em `useMemo filtered` + leitura de URL via `useSearchParams`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-revenue-dashboard/04-CONTEXT.md
@.planning/phases/04-revenue-dashboard/04-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@src/pages/ContratosPage.tsx
@src/pages/LeadsPage.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar leitura de ?vencendo_em_dias=N + predicate composicional</name>
  <files>src/pages/ContratosPage.tsx</files>
  <read_first>
    - src/pages/ContratosPage.tsx (arquivo completo — atenção em `useMemo filtered` linhas 335-361 e nos imports do topo)
    - src/pages/LeadsPage.tsx:40-59 (padrão canônico de leitura `useSearchParams` + helper `setFilter` do projeto)
    - src/lib/utils.ts:36-39 (getDaysUntilExpiry — comportamento com null)
    - .planning/phases/04-revenue-dashboard/04-CONTEXT.md §8 (mapping de URLs do drill-down)
    - .planning/phases/04-revenue-dashboard/04-RESEARCH.md "Common Pitfalls" §6 (orientação exata sobre como NÃO quebrar filtros existentes)
    - .planning/phases/04-revenue-dashboard/04-RESEARCH.md "Security Domain" ASVS V5 (parsing defensivo de NaN/negative/overflow)
  </read_first>
  <action>
    Em `src/pages/ContratosPage.tsx`:

    1. **Import:** adicionar `useSearchParams` ao import de `react-router-dom` (se já houver import dessa lib, anexar — verificar; se não, criar `import { useSearchParams } from 'react-router-dom'`).

    2. **Dentro do componente `ContratosPage`**, após `const [selected, setSelected] = useState<Contrato | null>(null)`:
       Adicionar `const [searchParams] = useSearchParams()`.
       Calcular `const vencendoEmDias` como número validado:
       - Ler `searchParams.get('vencendo_em_dias')` (string | null)
       - Se null → `null`
       - Senão parsear `Number(raw)` e validar com `Number.isFinite(n) && Number.isInteger(n) && n >= 0 && n <= 365` — se inválido, tratar como `null` (per ASVS V5 — parse defensivo contra NaN/overflow/string-injection)

    3. **No `useMemo` filtered** (linhas ~335-361): adicionar EXATAMENTE UM novo predicate APÓS o predicate de `rmFilter` (mantendo todos os existentes intactos):
       ```
       Se vencendoEmDias !== null:
         list = list.filter(c => {
           const d = getDaysUntilExpiry(c.data_fim)
           return d !== null && d >= 0 && d <= vencendoEmDias
         })
       ```
       Adicionar `vencendoEmDias` ao array de dependências do `useMemo`.

    4. **NÃO** adicionar UI nova (sem badge "filtrado por receita", sem chip de filtro) — escopo deliberadamente mínimo (per RESEARCH Pitfall 6 / A4). A página continua se comportando idêntica para o usuário que abre `/contratos` direto.

    5. **NÃO** tocar em `tipoFilter`, `statusFilter`, `rmFilter`, `scope`, `search`, `sortBy` — apenas adicionar o predicate.

    6. Verificação manual rápida pós-edit:
       - `/contratos` (sem param) → comportamento idêntico ao anterior
       - `/contratos?vencendo_em_dias=30` → mostra só contratos com data_fim em [hoje, hoje+30]
       - `/contratos?status=ativo&vencendo_em_dias=30` → AND dos dois filtros
       - `/contratos?vencendo_em_dias=abc` → comportamento idêntico a sem param (NaN-safe)
       - `/contratos?vencendo_em_dias=-5` → comportamento idêntico a sem param
       - `/contratos?vencendo_em_dias=9999` → comportamento idêntico a sem param

    Lint + typecheck devem passar. Commit `feat(04-02): ContratosPage aceita ?vencendo_em_dias=N para drill-down`.
  </action>
  <verify>
    <automated>npm run lint -- src/pages/ContratosPage.tsx && npx tsc --noEmit -p tsconfig.app.json && npm run test -- src/lib/__tests__/utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useSearchParams" src/pages/ContratosPage.tsx` ≥ 2 (1 import + 1 uso)
    - `grep -c "vencendo_em_dias" src/pages/ContratosPage.tsx` ≥ 2 (1 leitura do param + 1 comentário/uso)
    - `grep -c "Number.isFinite" src/pages/ContratosPage.tsx` ≥ 1 (parse defensivo presente)
    - O array de dependências do `useMemo` filtered contém `vencendoEmDias` (grep `vencendoEmDias` no contexto do useMemo)
    - `npm run lint` zero erros novos no arquivo
    - `npx tsc --noEmit -p tsconfig.app.json` zero erros
    - Nenhum filtro UI existente removido ou renomeado (diff git mostra apenas adições)
    - Commit `feat(04-02): ContratosPage aceita ?vencendo_em_dias=N` criado
  </acceptance_criteria>
  <done>Drill-down via URL funcional; comportamento sem parâmetro 100% preservado; parsing NaN-safe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint humano — smoke visual em /contratos sem regressão</name>
  <what-built>Extensão de `ContratosPage` para aceitar `?vencendo_em_dias=N` (composição AND, NaN-safe).</what-built>
  <how-to-verify>
    Rodar `npm run dev` e validar 4 cenários no navegador (logado como diretor ou coordenador):

    1. Visitar `http://localhost:5173/contratos`
       - Esperado: lista IDÊNTICA ao comportamento antes desta wave (mesmo número de contratos, mesmos filtros UI funcionando)
       - Aplicar filtros UI (status=Ativos, tipo=Assessoria, busca por nome) — confirmar que respondem normal

    2. Visitar `http://localhost:5173/contratos?vencendo_em_dias=30`
       - Esperado: lista apenas contratos com `data_fim` em [hoje, hoje+30 dias]. Conferir 1-2 itens manualmente vendo a data_fim na linha
       - O cabeçalho/UI permanece IDÊNTICO (sem novo chip nem badge — escopo mínimo intencional)

    3. Visitar `http://localhost:5173/contratos?status=ativo&vencendo_em_dias=30`
       - Esperado: subset de #2 com apenas status=ativo (zero encerrados/suspensos)

    4. Visitar URLs inválidas — todas devem se comportar como sem parâmetro:
       - `/contratos?vencendo_em_dias=abc` → lista completa
       - `/contratos?vencendo_em_dias=-5` → lista completa
       - `/contratos?vencendo_em_dias=9999` → lista completa

    Se algum cenário falhar, descrever qual e qual era o esperado.
  </how-to-verify>
  <resume-signal>Type "approved" ou descrever discrepância encontrada</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string (untrusted) → predicate de filtro | Único input externo desta task |
| Browser → Supabase via supabase-js | Inalterado; RLS continua aplicando |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | URL ?vencendo_em_dias param | mitigate | Parse defensivo: `Number.isFinite + Number.isInteger + range [0,365]`; valores inválidos descartados silenciosamente (per ASVS V5) |
| T-04-02-02 | DoS | range muito grande causando processamento excessivo | mitigate | Cap em 365 dias; `getDaysUntilExpiry` é O(1); filter é O(n) sobre ~100-500 contratos — trivial |
| T-04-02-03 | Information Disclosure | URL bookmarkable expondo filtros | accept | Padrão já adotado em `/leads?status=ganho` (`LeadsPage.tsx:40`); RLS continua filtrando por role no DB |
| T-04-02-04 | Elevation of Privilege | bypass de filtros pré-existentes via novo param | mitigate | Predicate é APPEND-ONLY no chain de `useMemo filtered`; tipoFilter/statusFilter/rmFilter intactos (verificação manual no Task 2) |
| T-04-02-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- `npm run lint` zero novos erros
- `npx tsc --noEmit -p tsconfig.app.json` zero erros
- `npm run test` suite completa unitária — zero regressões
- `/contratos` sem param: comportamento idêntico (checkpoint manual)
- `/contratos?vencendo_em_dias=30`: filtra corretamente (checkpoint manual)
- Valores inválidos do param: comportam-se como sem param (checkpoint manual)
</verification>

<success_criteria>
- ContratosPage aceita `?vencendo_em_dias=N` e compõe AND com filtros existentes
- Parsing 100% NaN-safe (ASVS V5)
- Zero regressão visual ou funcional em `/contratos` sem parâmetro
- Drill-down de Plan 03 funcionará out-of-the-box
</success_criteria>

<output>
Create `.planning/phases/04-revenue-dashboard/04-02-SUMMARY.md` when done — registrar:
- Exato diff aplicado em ContratosPage (1 import + 1 leitura param + 1 predicate)
- Resultado do checkpoint humano (4 cenários)
- Confirmação de zero regressão em filtros UI existentes
- Drill-down semântico documentado para Plan 03 (`?status=ativo&vencendo_em_dias=30`)
</output>
