---
phase: 07-client-portal-expansion
plan: 03
type: execute
wave: 3
depends_on: [07-01a, 07-01b]
files_modified:
  - src/lib/storage-helpers.ts
  - src/hooks/useClienteDocs.ts
  - src/components/clientes/UploadDropzone.tsx
  - src/components/clientes/ClienteDocsList.tsx
  - src/pages/portal/PortalDocumentosPage.tsx
  - src/pages/portal/PortalLayout.tsx
  - src/pages/ClienteDetailPage.tsx
  - src/router.tsx
  - package.json
  - src/hooks/__tests__/useClienteDocs.test.tsx
  - src/components/clientes/__tests__/UploadDropzone.test.tsx
autonomous: false
requirements: [PORTAL-01, PORTAL-02]
user_setup: []

must_haves:
  truths:
    - "Cliente faz drag-and-drop em /portal/documentos, arquivo cai em cliente-docs/{cliente_id}/{doc_id}.{ext} (D-04)"
    - "Cliente vê lista própria com download via signed URL 60min (D-04)"
    - "Consultor abre /clientes/:id → tab 'Documentos' → faz upload com tag (proposta/contrato/relatorio/outro) + checkbox 'Pedir aprovação' (D-01, D-03)"
    - "Cliente uploads nunca exibem botão de aprovação (D-03 — autor_tipo='cliente' descarta)"
    - "Histórico de versões: badge 'v2 (atual)' + dropdown 'Ver versões anteriores' (D-02)"
    - "Validação client-side: extensão whitelist + 10MB max + file.type tolerante (D-05, Pitfall §2)"
    - "Upload pré-cria row em cliente_docs (gera doc_id) → upload storage → UPDATE storage_path; rollback DELETE row se upload falhar (RESEARCH §1.C)"
    - "Path scheme imutável: {cliente_id}/{doc_id}.{ext}; nome original em coluna nome_arquivo (T-07-01 mitigado)"
  artifacts:
    - path: "src/lib/storage-helpers.ts"
      provides: "uploadClienteDoc + getSignedDownloadUrl + SIGNED_URL_EXPIRY_SECONDS constant"
      exports: ["uploadClienteDoc", "getSignedDownloadUrl", "SIGNED_URL_EXPIRY_SECONDS"]
    - path: "src/hooks/useClienteDocs.ts"
      provides: "useClienteDocs query + useUploadClienteDoc + useDownloadDoc mutations"
      contains: "useMutation"
      min_lines: 100
    - path: "src/components/clientes/UploadDropzone.tsx"
      provides: "Drag-and-drop UI com react-dropzone, validação inline, toast errors PT-BR"
      contains: "useDropzone"
    - path: "src/components/clientes/ClienteDocsList.tsx"
      provides: "Lista de docs do cliente + ações (Download, Aprovar/Revisão se interno+pending+aprovacao)"
      contains: "useClienteDocs"
    - path: "src/pages/portal/PortalDocumentosPage.tsx"
      provides: "Página cliente /portal/documentos com Dropzone + lista"
      contains: "UploadDropzone"
    - path: "src/pages/ClienteDetailPage.tsx"
      provides: "Tab nova 'Documentos' integrada em /clientes/:id"
      contains: "TabsTrigger.*documentos"
  key_links:
    - from: "src/pages/portal/PortalDocumentosPage.tsx"
      to: "src/hooks/useClienteDocs.ts.useUploadClienteDoc"
      via: "drag-drop → onFiles → mutation"
      pattern: "useUploadClienteDoc"
    - from: "src/hooks/useClienteDocs.ts.useUploadClienteDoc"
      to: "supabase.storage.from('cliente-docs').upload"
      via: "INSERT row → upload → UPDATE storage_path"
      pattern: "from\\('cliente-docs'\\)"
    - from: "src/pages/portal/PortalLayout.tsx NAV"
      to: "/portal/documentos route"
      via: "nav entry com icon FileText"
      pattern: "/portal/documentos"
---

<objective>
**Slice 3 — UI bidirecional upload + lista de documentos**

Implementa o fluxo bidirecional UX: cliente sobe em `/portal/documentos`, consultor sobe na
tab "Documentos" de `/clientes/:id`. Lista mostra docs (com versões D-02), download via signed
URL 60min, badges de status (pending/aprovado/revisao_solicitada/superseded), histórico de
versões em dropdown inline.

**Não inclui:** botões Aprovar/Solicitar Revisão (Slice 4), dashboard pendentes (Slice 4),
NotificacoesPanel matriz 5×3 (Slice 4), bucket usage banner (Slice 4). Slice 3 é "subir e ver";
Slice 4 é "aprovar e gerenciar".

**Autonomous: false** porque inclui 1 checkpoint humano: `npm install react-dropzone` precisa
de confirmação visual antes do install (legitimacy gate ASSUMED em RESEARCH).

Purpose: a partir do final do Slice 3, Gabriel pode em mãos:
1. Logar como cliente teste → /portal/documentos → arrastar PDF → ver na lista própria
2. Logar como consultor → /clientes/:id → tab Documentos → upload com checkbox "Pedir aprovação"
3. Verificar que doc do consultor aparece para o cliente (RLS + Storage RLS funcionando)
4. Tudo SEM os botões de aprovação (Slice 4) — fluxo de "upload e visualizar" completo

Output:
- react-dropzone instalado (1 nova dep)
- storage-helpers.ts (upload + signed URL puros)
- useClienteDocs hook (query + 2 mutations)
- UploadDropzone componente
- ClienteDocsList componente
- PortalDocumentosPage + nav entry no PortalLayout
- Tab "Documentos" em ClienteDetailPage
- Rota /portal/documentos
- Tests populando it.todo de useClienteDocs + UploadDropzone
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-client-portal-expansion/07-01a-SUMMARY.md
@.planning/phases/07-client-portal-expansion/07-01b-SUMMARY.md
@.planning/phases/07-client-portal-expansion/07-CONTEXT.md
@.planning/phases/07-client-portal-expansion/07-RESEARCH.md
@.planning/phases/07-client-portal-expansion/07-PATTERNS.md

# Analogs do projeto
@src/hooks/usePerfis.ts
@src/hooks/useLeads.ts
@src/lib/cnpj.ts
@src/lib/file-validation.ts
@src/lib/query-keys.ts
@src/lib/utils.ts
@src/pages/portal/PortalLayout.tsx
@src/pages/portal/PortalHistoricoPage.tsx
@src/pages/ClienteDetailPage.tsx
@src/components/shared/ActivityTimeline.tsx
@src/router.tsx
@src/types/index.ts
@package.json
@.npmrc
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser drag-drop event → File object | Browser-provided; file.type pode mentir (T-07-04) |
| Browser → Supabase Storage upload | JWT-authenticated; bucket RLS valida path |
| Browser → Supabase REST INSERT/UPDATE cliente_docs | JWT + RLS aplicadas |
| Browser → createSignedUrl request | RLS aplica via JWT antes de gerar signed URL (T-07-08 mitigated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Storage path | mitigate | useUploadClienteDoc gera path `${clienteId}/${doc_id}.${ext}` onde doc_id é UUID server-generated via INSERT pre-upload; file.name vai apenas em coluna nome_arquivo |
| T-07-03 | Tampering | XSS via filename | mitigate | ClienteDocsList renderiza `{doc.nome_arquivo}` via interpolação React (escape automático); NUNCA dangerouslySetInnerHTML |
| T-07-04 | Tampering | MIME spoofing | accept | file-validation.ts (Slice 1) faz check extensão + file.type. Backend MIME sniffing real é tech-debt aceito (D-05) |
| T-07-06 | Information Disclosure | Signed URL leak | mitigate | SIGNED_URL_EXPIRY_SECONDS = 3600 (60min D-04); URL gerada on-click via useDownloadDoc mutation (não em useEffect que poderia logar) |
| T-07-07 | Tampering | Race condition concurrent uploads | mitigate | doc_id é uuid v4 server-generated em INSERT (PRIMARY KEY) — colision-free; path imutável |
| T-07-08 | Information Disclosure | Storage enumeration | mitigate | storage.objects RLS SELECT policy (Slice 1) filtra por foldername (cliente vê só própria pasta) |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3.1 [Legitimacy Gate]: Gabriel verifica react-dropzone antes de install</name>
  <what-built>
    Antes do `npm install`, confirmar visualmente que o pacote react-dropzone é legítimo
    (slopcheck não rodou em RESEARCH — ASSUMED status).
  </what-built>
  <how-to-verify>
    Comando no terminal:
    ```bash
    rtk npm view react-dropzone repository.url
    rtk npm view react-dropzone maintainers
    rtk npm view react-dropzone version
    rtk npm view react-dropzone time.modified
    ```

    Esperado:
    - repository.url: github.com/react-dropzone/react-dropzone
    - maintainers: lista com nomes conhecidos da comunidade React (ex: rolandjitsu, lookfirst)
    - version: 15.0.0
    - time.modified: 2026-02-10 (ou posterior, sem regression)

    Reference:
    - npm package: https://www.npmjs.com/package/react-dropzone
    - GitHub: https://github.com/react-dropzone/react-dropzone (10+ anos, 10k+ stars)

    Se algo estranho aparecer (mantenedor desconhecido, URL fake, version 0.x recém-publicada),
    PARAR e re-avaliar — fallback é HTML5 nativo (Pattern 3 alternative em RESEARCH §3).
  </how-to-verify>
  <files>(manual checkpoint — see <how-to-verify> for exact commands and files touched outside repo)</files>
  <action>Execute every numbered step in <how-to-verify> in order; do not skip Studio SQL Editor / CLI / Dashboard tasks. After each step run the verification SQL/curl shown inline.</action>
  <verify><human-check>All inline verification queries / curl invocations in <how-to-verify> return the expected values; capture outputs in the SUMMARY.</human-check></verify>
  <done>Every step in <how-to-verify> completed with the expected output; any anomaly logged in SUMMARY and either fixed before resume or escalated to a follow-up gap-closure plan.</done>
    <resume-signal>Type "approved" para liberar npm install OU "fallback-native" para mudar para HTML5 puro (planner ajusta UploadDropzone para skeleton nativo de RESEARCH §3 alternative).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3.2: npm install react-dropzone + import smoke check</name>
  <files>package.json</files>
  <read_first>
    @package.json
    @.npmrc
  </read_first>
  <action>
    1. Rodar `rtk npm install react-dropzone` (usa legacy-peer-deps=true do .npmrc — peer dep React 18 vs runtime React 19 não bloqueia).

    2. Verificar package.json contém `react-dropzone: ^15.0.0` em dependencies.

    3. Smoke import test num arquivo temporário (ou inline no UploadDropzone do Task 3.4) — apenas confirmar resolução:
       ```typescript
       import { useDropzone } from 'react-dropzone'
       ```

    4. Não fazer outras mudanças neste task — instalação isolada.
  </action>
  <verify>
    <automated>grep -c "react-dropzone" package.json | awk '$1 >= 1 { print "OK" }'</automated>
  </verify>
  <done>
    react-dropzone@^15.0.0 em package.json dependencies. `npm test` continua verde (nenhum import quebrado em arquivos existentes).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.3: Implementar src/lib/storage-helpers.ts (upload pre-row + signed URL)</name>
  <files>src/lib/storage-helpers.ts</files>
  <read_first>
    @src/hooks/usePerfis.ts
    @src/lib/file-validation.ts
    @.planning/phases/07-client-portal-expansion/07-RESEARCH.md
  </read_first>
  <action>
    Extrair lib pura espelhando `usePerfis.ts:82-93` (PATTERNS §6) com as 4 divergências exigidas
    por D-04:
    - bucket `cliente-docs` privado
    - upsert: false (path único por doc_id)
    - getSignedUrl ao invés de getPublicUrl
    - validation pre-upload via validateDocOrThrow

    ```typescript
    // src/lib/storage-helpers.ts
    import { supabase } from '@/lib/supabase'
    import { validateDocOrThrow } from '@/lib/file-validation'

    export const SIGNED_URL_EXPIRY_SECONDS = 3600  // 60min (D-04)
    export const BUCKET = 'cliente-docs'

    export function buildDocPath(clienteId: string, docId: string, filename: string): string {
      const ext = filename.split('.').pop()?.toLowerCase() ?? 'bin'
      return `${clienteId}/${docId}.${ext}`
    }

    export interface UploadParams {
      path: string
      file: File
      onProgress?: (pct: number) => void
    }

    export async function uploadClienteDocFile({ path, file, onProgress }: UploadParams): Promise<void> {
      validateDocOrThrow(file)

      const { error } = await supabase.storage
        .from(BUCKET)
        .upload(path, file, {
          contentType: file.type || 'application/octet-stream',
          upsert: false,
          // @ts-expect-error onUploadProgress: types em @supabase/supabase-js@2.99.x não expõem (Pitfall §1 RESEARCH)
          onUploadProgress: onProgress
            ? (p: { loaded: number; total: number }) => onProgress((p.loaded / p.total) * 100)
            : undefined,
        })
      if (error) throw error
    }

    export async function getSignedDownloadUrl(storagePath: string): Promise<string> {
      const { data, error } = await supabase.storage
        .from(BUCKET)
        .createSignedUrl(storagePath, SIGNED_URL_EXPIRY_SECONDS)
      if (error) throw error
      return data.signedUrl
    }
    ```

    Sem hooks aqui — pura wrapper sobre supabase-js. `useUploadClienteDoc` em Task 3.4 orquestra
    INSERT row + uploadClienteDocFile + UPDATE storage_path + rollback DELETE on error.
  </action>
  <verify>
    <automated>grep -E "uploadClienteDocFile|getSignedDownloadUrl|SIGNED_URL_EXPIRY_SECONDS|buildDocPath" src/lib/storage-helpers.ts | wc -l | awk '$1 >= 4 { print "OK" }'</automated>
  </verify>
  <done>
    storage-helpers.ts exporta uploadClienteDocFile + getSignedDownloadUrl + buildDocPath + SIGNED_URL_EXPIRY_SECONDS + BUCKET. Pitfall §1 documentado com @ts-expect-error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.4: Implementar src/hooks/useClienteDocs.ts (useClienteDocs + useUploadClienteDoc + useDownloadDoc)</name>
  <files>
    src/hooks/useClienteDocs.ts,
    src/hooks/__tests__/useClienteDocs.test.tsx
  </files>
  <read_first>
    @src/hooks/useLeads.ts
    @src/hooks/usePerfis.ts
    @src/lib/storage-helpers.ts
    @src/lib/query-keys.ts
    @src/lib/file-validation.ts
    @src/test/storage-mocks.ts
    @src/hooks/__tests__/useClienteDocs.test.tsx
  </read_first>
  <behavior>
    Test cases para popular os it.todo do Slice 1:
    - useClienteDocs(clienteId): SELECT cliente_docs WHERE cliente_id=X AND deleted_at IS NULL ORDER BY created_at desc
    - useClienteDocs(null): enabled=false (não query)
    - useUploadClienteDoc happy path: validateDocOrThrow ok → INSERT row (gera doc_id) → uploadClienteDocFile → UPDATE storage_path → return docId → invalidate queries
    - useUploadClienteDoc validation fail (size > 10MB): toast.error 'Arquivo maior que 10 MB'; sem INSERT
    - useUploadClienteDoc upload fail: row INSERT ok mas upload throws → DELETE row de rollback → throw original error
    - useUploadClienteDoc cliente passes autorTipo='cliente' + requerAprovacao=false (RLS rejeitaria cliente com requer_aprovacao=true — D-01)
    - useDownloadDoc: chama getSignedDownloadUrl → cria <a download> → click → remove (DOM manipulation testable via document.body.lastChild)
    - **D-02 versionamento (BLOCKER #2 revision iter 2):** useUploadClienteDoc com parentDocId definido:
      - SELECT versao FROM parent → INSERT com versao=parent.versao+1 + parent_doc_id=parent.id
      - UPDATE parent SET status='superseded' (idempotente .neq('status','superseded'))
      - Sem parentDocId: versao=1, parent_doc_id=null, parent UNTOUCHED
      - Test: mock parent.versao=1 + parentDocId=parent.id → assert insert recebeu versao:2 + parent_doc_id:parent.id + update foi chamado em parent.id com status='superseded'
      - Test: useUploadClienteDoc sem parentDocId → assert insert recebeu versao:1 + parent_doc_id:null + nenhum update em outra row
  </behavior>
  <action>
    Espelhar `useLeads.ts` (PATTERNS §7) + RESEARCH §1.C linhas 357-424:

    ```typescript
    import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
    import { supabase } from '@/lib/supabase'
    import { QUERY_KEYS } from '@/lib/query-keys'
    import {
      uploadClienteDocFile, getSignedDownloadUrl, buildDocPath
    } from '@/lib/storage-helpers'
    import type { ClienteDoc, AutorDoc, TagDoc } from '@/types'
    import { toast } from 'sonner'

    export function useClienteDocs(clienteId: string | null) {
      return useQuery<ClienteDoc[]>({
        queryKey: clienteId ? QUERY_KEYS.clienteDocs.byCliente(clienteId) : ['clienteDocs', 'disabled'],
        enabled: !!clienteId,
        queryFn: async () => {
          const { data, error } = await supabase
            .from('cliente_docs')
            .select('*')
            .eq('cliente_id', clienteId!)
            .is('deleted_at', null)
            .order('created_at', { ascending: false })
          if (error) throw error
          return (data ?? []) as ClienteDoc[]
        },
      })
    }

    export interface UploadClienteDocInput {
      clienteId: string
      file: File
      tag: TagDoc
      requerAprovacao: boolean
      autorId: string
      autorTipo: AutorDoc
      parentDocId?: string | null
      onProgress?: (pct: number) => void
    }

    export function useUploadClienteDoc() {
      const qc = useQueryClient()
      return useMutation({
        mutationFn: async (input: UploadClienteDocInput): Promise<string> => {
          // D-02 versionamento (BLOCKER #2 revision iter 2):
          //   - Quando input.parentDocId !== undefined/null → estamos subindo v(N+1).
          //     1. SELECT versao FROM cliente_docs WHERE id = parentDocId → parentVersao
          //     2. INSERT nova row com versao = parentVersao + 1, parent_doc_id = parentDocId
          //     3. UPDATE parent SET status='superseded' (idempotente: WHERE status != 'superseded')
          //   - Quando input.parentDocId é undefined/null → v1 normal (versao=1, parent_doc_id=null)

          let novaVersao = 1
          if (input.parentDocId) {
            const { data: parent, error: parentErr } = await supabase
              .from('cliente_docs')
              .select('versao')
              .eq('id', input.parentDocId)
              .single()
            if (parentErr || !parent) throw parentErr ?? new Error('Versão anterior não encontrada')
            novaVersao = (parent.versao ?? 1) + 1
          }

          // 1. Pre-insert row para gerar doc_id (path scheme {cliente_id}/{doc_id}.{ext})
          const { data: row, error: rowErr } = await supabase
            .from('cliente_docs')
            .insert({
              cliente_id: input.clienteId,
              autor_id: input.autorId,
              autor_tipo: input.autorTipo,
              tag: input.tag,
              nome_arquivo: input.file.name,
              mime_type: input.file.type || 'application/octet-stream',
              tamanho_bytes: input.file.size,
              storage_path: '',
              requer_aprovacao: input.autorTipo === 'cliente' ? false : input.requerAprovacao,  // D-01 enforce
              status: input.autorTipo === 'interno' && input.requerAprovacao ? 'pending' : null,
              parent_doc_id: input.parentDocId ?? null,
              versao: novaVersao,  // D-02 — parent_doc_id ? parent.versao+1 : 1
            })
            .select('id')
            .single()
          if (rowErr || !row) throw rowErr ?? new Error('Falha ao registrar documento')

          const path = buildDocPath(input.clienteId, row.id, input.file.name)

          // 2. Upload + rollback row on failure
          try {
            await uploadClienteDocFile({ path, file: input.file, onProgress: input.onProgress })
          } catch (uploadErr) {
            await supabase.from('cliente_docs').delete().eq('id', row.id)
            throw uploadErr
          }

          // 3. UPDATE storage_path
          await supabase.from('cliente_docs').update({ storage_path: path }).eq('id', row.id)

          // 4. D-02: marcar parent como superseded (idempotente)
          if (input.parentDocId) {
            await supabase
              .from('cliente_docs')
              .update({ status: 'superseded' })
              .eq('id', input.parentDocId)
              .neq('status', 'superseded')  // idempotente — evita UPDATE noop
          }

          return row.id
        },
        onSuccess: (_, input) => {
          qc.invalidateQueries({ queryKey: QUERY_KEYS.clienteDocs.byCliente(input.clienteId) })
          toast.success('Documento enviado')
        },
        onError: (e: unknown) =>
          toast.error(e instanceof Error ? e.message : 'Erro ao enviar documento'),
      })
    }

    export function useDownloadDoc() {
      return useMutation({
        mutationFn: async (doc: ClienteDoc) => {
          const url = await getSignedDownloadUrl(doc.storage_path)
          const link = document.createElement('a')
          link.href = url
          link.download = doc.nome_arquivo
          document.body.appendChild(link)
          link.click()
          link.remove()
        },
        onError: (e: unknown) =>
          toast.error(e instanceof Error ? e.message : 'Erro ao baixar documento'),
      })
    }
    ```

    **Tests:** popular `src/hooks/__tests__/useClienteDocs.test.tsx` com 7 casos acima usando
    `mockSupabaseStorageUpload`/`mockSupabaseStorageSignedUrl`/`mockFileBuilder` de
    `src/test/storage-mocks.ts` + mock supabase via `src/test/supabase-mock.ts` existente. Para
    `useUploadClienteDoc`, mock o INSERT/SELECT/UPDATE/DELETE chains do supabase.

    **D-01 enforce client-side:** `requer_aprovacao: input.autorTipo === 'cliente' ? false : input.requerAprovacao`
    — mesmo que UI permita, useUploadClienteDoc força false para uploads de cliente. RLS é a 2ª linha de defesa.
  </action>
  <verify>
    <automated>npm test -- --run src/hooks/__tests__/useClienteDocs.test.tsx 2>&1 | grep -E "Tests.*passed"</automated>
  </verify>
  <done>
    useClienteDocs.ts exporta 3 hooks. 7+2 testes passando (rollback path + 2 cases D-02 versionamento). D-01 enforce client-side documentado.

    **Acceptance criteria D-02 (BLOCKER #2 revision iter 2):**
    - Mutation com `parentDocId` definido faz SELECT versao do parent, INSERT v=parent+1 com parent_doc_id, e UPDATE parent SET status='superseded' (idempotente via `.neq('status', 'superseded')`).
    - Mutation sem `parentDocId` preserva legado (versao=1, parent_doc_id=null, sem UPDATE em outras rows).
    - SELECT versao FROM cliente_docs WHERE parent_doc_id = $1 retorna `parentVersao + 1`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.5: UploadDropzone componente (react-dropzone) + popular tests</name>
  <files>
    src/components/clientes/UploadDropzone.tsx,
    src/components/clientes/__tests__/UploadDropzone.test.tsx
  </files>
  <read_first>
    @src/lib/utils.ts
    @src/lib/file-validation.ts
    @src/components/clientes/__tests__/UploadDropzone.test.tsx
    @.planning/phases/07-client-portal-expansion/07-RESEARCH.md
  </read_first>
  <behavior>
    Tests popular it.todo:
    - render: dropzone com texto PT-BR "Arraste arquivos ou clique para selecionar"
    - render: lista MIME types em hint "PDF, DOCX, DOC, ODT, JPG, PNG, WebP — até 10 MB"
    - onDropAccepted: chama onFiles com array de Files
    - onDropRejected (size): toast.error 'Arquivo maior que 10 MB'
    - onDropRejected (mime): toast.error 'Tipo não permitido (use PDF, DOCX, DOC, ODT ou imagem)'
    - disabled=true: input aria-disabled + opacity-50 class
  </behavior>
  <action>
    Implementar conforme RESEARCH §3 linhas 471-537 + PATTERNS §10.

    Skeleton key parts:
    ```tsx
    import { useDropzone } from 'react-dropzone'
    import { Upload } from 'lucide-react'
    import { toast } from 'sonner'
    import { cn } from '@/lib/utils'
    import { ALLOWED_EXTENSIONS } from '@/lib/file-validation'

    // ACCEPT com extension fallback (Pitfall §8 RESEARCH — Windows .docx pode reportar octet-stream)
    const ACCEPT = {
      'application/pdf': ['.pdf'],
      'application/vnd.openxmlformats-officedocument.wordprocessingml.document': ['.docx'],
      'application/msword': ['.doc'],
      'application/vnd.oasis.opendocument.text': ['.odt'],
      'image/jpeg': ['.jpg', '.jpeg'],
      'image/png': ['.png'],
      'image/webp': ['.webp'],
    }
    const MAX_SIZE = 10 * 1024 * 1024

    interface Props {
      onFiles: (files: File[]) => void
      multiple?: boolean
      disabled?: boolean
    }

    export function UploadDropzone({ onFiles, multiple = true, disabled = false }: Props) {
      const { getRootProps, getInputProps, isDragActive, isDragReject } = useDropzone({
        accept: ACCEPT,
        maxSize: MAX_SIZE,
        multiple,
        disabled,
        onDropRejected: (rejections) => {
          const first = rejections[0]?.errors[0]
          if (first?.code === 'file-too-large') toast.error('Arquivo maior que 10 MB')
          else if (first?.code === 'file-invalid-type') toast.error('Tipo não permitido (use PDF, DOCX, DOC, ODT ou imagem)')
          else toast.error(first?.message ?? 'Arquivo rejeitado')
        },
        onDropAccepted: (files) => onFiles(files),
      })

      return (
        <div
          {...getRootProps()}
          className={cn(
            'rounded-xl border-2 border-dashed p-8 text-center transition-colors cursor-pointer',
            isDragActive && !isDragReject && 'border-primary bg-primary/5',
            isDragReject && 'border-destructive bg-destructive/5',
            !isDragActive && 'border-muted hover:border-primary/50',
            disabled && 'opacity-50 cursor-not-allowed',
          )}
        >
          <input {...getInputProps()} aria-label="Selecionar documentos" />
          <Upload className="w-8 h-8 mx-auto mb-2 text-muted-foreground" />
          <p className="text-sm text-foreground">
            {isDragActive ? 'Solte para subir' : 'Arraste arquivos ou clique para selecionar'}
          </p>
          <p className="text-xs text-muted-foreground mt-1">
            PDF, DOCX, DOC, ODT, JPG, PNG, WebP — até 10 MB
          </p>
        </div>
      )
    }
    ```

    Tests usando vi.mock para `react-dropzone.useDropzone` retornando `getRootProps`/`getInputProps`
    + callbacks accessíveis via spies. 6 casos.
  </action>
  <verify>
    <automated>npm test -- --run src/components/clientes/__tests__/UploadDropzone.test.tsx 2>&1 | grep -E "Tests.*passed"</automated>
  </verify>
  <done>
    UploadDropzone renderiza texto PT-BR, fallback extension no ACCEPT (Pitfall §8), 6 testes passando.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.6: ClienteDocsList componente (lista + download; SEM aprovação, fica Slice 4)</name>
  <files>src/components/clientes/ClienteDocsList.tsx</files>
  <read_first>
    @src/components/me/NotificacoesPanel.tsx
    @src/pages/AdocaoPage.tsx
    @src/components/shared/ActivityTimeline.tsx
    @src/hooks/useClienteDocs.ts
    @src/types/index.ts
  </read_first>
  <action>
    Componente puro de visualização — recebe `clienteId` + opcional `mode: 'portal' | 'crm'`.

    Funcionalidade:
    - Usa `useClienteDocs(clienteId)` + `useDownloadDoc()`
    - Loading skeleton (PATTERNS §Page-level loading)
    - Empty state ("Nenhum documento ainda" PT-BR)
    - Lista renderizada por agrupamento `parent_doc_id IS NULL` (raízes) com sub-versões inline
    - Por raiz: mostra apenas a versão mais nova (`MAX(versao)`); versões anteriores em dropdown inline
    - Cada doc mostra: ícone por extensão (FileText), nome_arquivo, tag (Badge), tamanho formatado, autor_tipo (badge "Cliente" vs "Interno"), status (badge color), data relativa ptBR
    - Botão Download → `downloadDoc.mutateAsync(doc)` (usa useDownloadDoc)
    - Slot vazio onde Slice 4 plugará AprovacaoButtons + DocVersionTimeline

    Skeleton de estrutura:
    ```tsx
    export function ClienteDocsList({ clienteId, mode = 'crm' }: { clienteId: string; mode?: 'portal' | 'crm' }) {
      const { data: docs = [], isLoading } = useClienteDocs(clienteId)
      const downloadDoc = useDownloadDoc()

      // Agrupar por raiz: docs sem parent_doc_id, com versões em sub-array
      const roots = useMemo(() => groupByRoot(docs), [docs])

      if (isLoading) return <ListSkeleton />
      if (roots.length === 0) return <EmptyState text="Nenhum documento ainda" />

      return (
        <ul className="space-y-2">
          {roots.map(({ current, history }) => (
            <DocCard
              key={current.id}
              doc={current}
              history={history}
              mode={mode}
              onDownload={() => downloadDoc.mutate(current)}
            />
          ))}
        </ul>
      )
    }
    ```

    `DocCard` interno mostra metadados + botão Download. `history.length > 0` mostra texto "v{N} (atual)" + dropdown "Ver versões anteriores".

    **Mode awareness:**
    - `mode='portal'`: usa inline styles cyan/dark do portal (PATTERNS §13 — `#6bd0e7`, `#0089ac`)
    - `mode='crm'`: usa Tailwind tokens (`bg-card`, `text-foreground`, etc.)

    **Slot Slice 4:** deixar prop opcional `actionsSlot?: (doc: ClienteDoc) => ReactNode` que Slice 4 plugará com AprovacaoButtons. Hoje passa undefined → sem botões de aprovação ainda.

    Helper `groupByRoot(docs: ClienteDoc[])` em `src/lib/doc-grouping.ts` (lib pura, sem teste obrigatório Slice 3 — mas adicionar `src/lib/__tests__/doc-grouping.test.ts` com 2 cases é boa prática).
  </action>
  <verify>
    <automated>grep -E "useClienteDocs|useDownloadDoc|actionsSlot|groupByRoot" src/components/clientes/ClienteDocsList.tsx | wc -l | awk '$1 >= 3 { print "OK" }'</automated>
  </verify>
  <done>
    ClienteDocsList renderiza, agrupa por raiz, mostra apenas versão mais nova + dropdown histórico. Suporta mode='portal'|'crm'. Slot actionsSlot? deixado para Slice 4. Loading/empty states cobertos.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.7: PortalDocumentosPage + nav entry no PortalLayout + rota /portal/documentos</name>
  <files>
    src/pages/portal/PortalDocumentosPage.tsx,
    src/pages/portal/PortalLayout.tsx,
    src/router.tsx
  </files>
  <read_first>
    @src/pages/portal/PortalLayout.tsx
    @src/pages/portal/PortalHistoricoPage.tsx
    @src/router.tsx
    @src/hooks/usePortal.ts
    @src/components/clientes/UploadDropzone.tsx
    @src/components/clientes/ClienteDocsList.tsx
  </read_first>
  <action>
    **PortalDocumentosPage.tsx** (PATTERNS §13 espelhando PortalHistoricoPage):

    ```tsx
    import { FileText, Upload } from 'lucide-react'
    import { usePortalPerfil } from '@/hooks/usePortal'
    import { useUploadClienteDoc } from '@/hooks/useClienteDocs'
    import { UploadDropzone } from '@/components/clientes/UploadDropzone'
    import { ClienteDocsList } from '@/components/clientes/ClienteDocsList'

    export function PortalDocumentosPage() {
      const { data: perfil } = usePortalPerfil()
      const upload = useUploadClienteDoc()

      if (!perfil?.cliente_id) {
        return <div style={{ color: '#fff' }}>Carregando…</div>
      }

      const handleFiles = (files: File[]) => {
        files.forEach(file => {
          upload.mutate({
            clienteId: perfil.cliente_id!,
            file,
            tag: 'outro',  // cliente sobe sempre como 'outro'; consultor escolhe tag
            requerAprovacao: false,  // D-01: cliente nunca marca próprio doc como requer_aprovacao
            autorId: perfil.id,
            autorTipo: 'cliente',
          })
        })
      }

      return (
        <div>
          <div className="flex items-center gap-3 mb-6">
            <div className="w-10 h-10 rounded-xl flex items-center justify-center"
              style={{ background: 'rgba(0,137,172,0.15)', border: '1px solid rgba(0,137,172,0.3)' }}>
              <FileText className="w-5 h-5" style={{ color: '#6bd0e7' }} />
            </div>
            <h1 style={{ fontSize: 22, fontWeight: 700, color: '#fff' }}>Documentos</h1>
          </div>

          <div className="mb-6">
            <UploadDropzone onFiles={handleFiles} disabled={upload.isPending} />
          </div>

          <ClienteDocsList clienteId={perfil.cliente_id} mode="portal" />
        </div>
      )
    }
    ```

    **PortalLayout.tsx NAV** (PATTERNS §Nav linhas 961-967): adicionar entry entre Histórico e Preferências:
    ```typescript
    const NAV = [
      { to: '/portal',           label: 'Carteira',  icon: Wallet,        end: true },
      { to: '/portal/indicar',   label: 'Indicar',   icon: UserPlus       },
      { to: '/portal/catalogo',  label: 'Catálogo',  icon: Gift           },
      { to: '/portal/historico', label: 'Histórico', icon: ClipboardList  },
      { to: '/portal/documentos', label: 'Documentos', icon: FileText },  // NOVO Slice 3
      { to: '/portal/preferencias', label: 'Preferências', icon: BellRing  },
    ]
    ```

    **router.tsx:** adicionar rota `{ path: 'documentos', element: <PortalDocumentosPage /> }` como child do PortalLayout (mesma posição das outras portal routes).
  </action>
  <verify>
    <automated>grep -c "/portal/documentos\|PortalDocumentosPage" src/router.tsx src/pages/portal/PortalLayout.tsx | awk -F: '{sum+=$2} END { if (sum >= 3) print "OK"; else print "MISS " sum }'</automated>
  </verify>
  <done>
    `/portal/documentos` route registrada. NAV portal mostra entry "Documentos" entre Histórico e Preferências. PortalDocumentosPage carrega perfil, faz upload com autor_tipo='cliente' + requer_aprovacao=false, lista docs em mode='portal'.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.8: Tab "Documentos" em ClienteDetailPage (consultor uploads com tag + checkbox requer_aprovacao)</name>
  <files>src/pages/ClienteDetailPage.tsx</files>
  <read_first>
    @src/pages/ClienteDetailPage.tsx
    @src/components/clientes/UploadDropzone.tsx
    @src/components/clientes/ClienteDocsList.tsx
    @src/hooks/useClienteDocs.ts
    @src/hooks/usePerfis.ts
  </read_first>
  <action>
    Localizar `<Tabs defaultValue="contratos">` em `ClienteDetailPage.tsx:224-230` (PATTERNS §Tabs structure).

    1. Adicionar `<TabsTrigger value="documentos">Documentos ({docs?.length || 0})</TabsTrigger>` entre "oportunidades" e "historico":

    ```tsx
    <TabsList className="mb-4">
      <TabsTrigger value="dados">Dados</TabsTrigger>
      <TabsTrigger value="contratos">Contratos ({contratos?.length || 0})</TabsTrigger>
      <TabsTrigger value="indicacoes">Indicações ({indicacoes.length})</TabsTrigger>
      <TabsTrigger value="oportunidades">Oportunidades ({oportunidades.length})</TabsTrigger>
      <TabsTrigger value="documentos">Documentos ({docs?.length || 0})</TabsTrigger>  {/* NOVO Slice 3 */}
      <TabsTrigger value="historico">Histórico</TabsTrigger>
    </TabsList>
    ```

    2. Adicionar `<TabsContent value="documentos">`:
    ```tsx
    <TabsContent value="documentos">
      <DocsTabConsultor clienteId={cliente.id} />
    </TabsContent>
    ```

    3. Subcomponente local `DocsTabConsultor` (no mesmo arquivo OU split em `src/components/clientes/DocsTabConsultor.tsx` — escolha do executor):

    ```tsx
    function DocsTabConsultor({ clienteId }: { clienteId: string }) {
      const { data: perfil } = useMeuPerfil()
      const upload = useUploadClienteDoc()
      const [tag, setTag] = useState<TagDoc>('proposta')
      const [requerAprovacao, setRequerAprovacao] = useState(true)

      if (!perfil) return null

      const handleFiles = (files: File[]) => {
        files.forEach(file => {
          upload.mutate({
            clienteId,
            file,
            tag,
            requerAprovacao,
            autorId: perfil.id,
            autorTipo: 'interno',
          })
        })
      }

      return (
        <div className="space-y-4">
          <div className="flex flex-wrap items-center gap-3 p-4 rounded-xl border border-border bg-card">
            <Label className="text-sm">Tag</Label>
            <Select value={tag} onValueChange={v => setTag(v as TagDoc)}>
              <SelectTrigger className="w-40"><SelectValue /></SelectTrigger>
              <SelectContent>
                <SelectItem value="proposta">Proposta</SelectItem>
                <SelectItem value="contrato">Contrato</SelectItem>
                <SelectItem value="relatorio">Relatório</SelectItem>
                <SelectItem value="outro">Outro</SelectItem>
              </SelectContent>
            </Select>

            <label className="flex items-center gap-2 text-sm">
              <Checkbox checked={requerAprovacao} onCheckedChange={(v) => setRequerAprovacao(!!v)} />
              Pedir aprovação do cliente
            </label>
          </div>

          <UploadDropzone onFiles={handleFiles} disabled={upload.isPending} />

          <ClienteDocsList clienteId={clienteId} mode="crm" />
        </div>
      )
    }
    ```

    4. Atualizar count `docs?.length` no `<TabsTrigger>`: passar `useClienteDocs(cliente.id)` no nível pai OU acessar via subcomponente (pode usar 0 inicial e atualizar via parent prop drilling se preferir — escolha do executor).

    **Justificativa D-01/D-03 no código:** comentário acima de `requerAprovacao` no setState explicando "consultor escolhe; cliente NÃO escolhe (UI do portal nem mostra esse checkbox)".
  </action>
  <verify>
    <automated>grep -E "value=\"documentos\"|DocsTabConsultor|Pedir aprovação" src/pages/ClienteDetailPage.tsx | wc -l | awk '$1 >= 3 { print "OK" }'</automated>
  </verify>
  <done>
    Tab "Documentos" presente em ClienteDetailPage. Consultor escolhe tag + checkbox requer_aprovacao + dropzone faz upload com autor_tipo='interno'. ClienteDocsList renderiza no mode='crm'.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.9: Smoke UX manual cliente + consultor (Slice 3 end-to-end)</name>
  <what-built>
    Wave 3 completa: react-dropzone instalado, storage-helpers + useClienteDocs + UploadDropzone + ClienteDocsList implementados, PortalDocumentosPage live, tab "Documentos" em ClienteDetailPage. Sem botões de aprovação ainda (Slice 4).
  </what-built>
  <how-to-verify>
    1. `rtk npm run dev` → abrir http://localhost:5173

    **Cenário cliente:**
    1. Logar com conta cliente (perfis.tipo='cliente' com cliente_id setado)
    2. Navegar para `/portal/documentos` (via nav portal "Documentos")
    3. Header "Documentos" com ícone FileText cyan visível
    4. Dropzone visível: "Arraste arquivos ou clique para selecionar"
    5. Arrastar um PDF do desktop (qualquer < 10MB) → ver toast "Documento enviado"
    6. Doc aparece na lista logo abaixo (badge tag "Outro", autor "Cliente", tamanho formatado)
    7. Clicar botão Download → arquivo baixa com nome original
    8. Tentar drag de .exe → toast "Tipo não permitido"
    9. Tentar drag de PDF > 10MB → toast "Arquivo maior que 10 MB"

    **Cenário consultor:**
    1. Logar com conta interna (consultor responsável pelo mesmo cliente)
    2. Navegar para `/clientes/<cliente-id>` → clicar tab "Documentos"
    3. Ver doc subido pelo cliente no passo anterior (RLS funciona)
    4. Subir um PDF marcado como tag=Proposta + checkbox "Pedir aprovação"=true → toast OK
    5. Doc aparece na lista com badge "Proposta", status "pending"
    6. (Cliente reentra em /portal/documentos): vê novo doc do consultor (cross-RLS check)
    7. SEM botões "Aprovar"/"Solicitar Revisão" ainda — comportamento esperado (Slice 4)

    **Cenário negativo (RLS):**
    1. Logar com OUTRO cliente (cliente_id diferente)
    2. Navegar /portal/documentos
    3. NÃO ver docs do cliente A — só os próprios (storage.objects RLS + cliente_docs RLS funcionam)
  </how-to-verify>
  <files>(manual checkpoint — see <how-to-verify> for exact commands and files touched outside repo)</files>
  <action>Execute every numbered step in <how-to-verify> in order; do not skip Studio SQL Editor / CLI / Dashboard tasks. After each step run the verification SQL/curl shown inline.</action>
  <verify><human-check>All inline verification queries / curl invocations in <how-to-verify> return the expected values; capture outputs in the SUMMARY.</human-check></verify>
  <done>Every step in <how-to-verify> completed with the expected output; any anomaly logged in SUMMARY and either fixed before resume or escalated to a follow-up gap-closure plan.</done>
    <resume-signal>Type "approved" se todos os 3 cenários funcionaram. Descreva qualquer falha (e.g. "upload trava em 50%", "doc não aparece para cliente", "tab vazia") para diagnóstico.</resume-signal>
</task>

</tasks>

<verification>
```bash
# Tests unit verdes
rtk npm test -- --run src/hooks/__tests__/useClienteDocs.test.tsx src/components/clientes/__tests__/UploadDropzone.test.tsx src/lib/__tests__/file-validation.test.ts

# TypeScript compila
rtk npx tsc -b

# ESLint clean
rtk npm run lint -- src/hooks/useClienteDocs.ts src/lib/storage-helpers.ts src/components/clientes/

# Rotas presentes
rtk grep -c "/portal/documentos" src/router.tsx   # >= 1
```
</verification>

<success_criteria>
- [ ] react-dropzone@^15.0.0 instalado (após legitimacy gate)
- [ ] storage-helpers.ts: uploadClienteDocFile + getSignedDownloadUrl + buildDocPath + SIGNED_URL_EXPIRY_SECONDS
- [ ] useClienteDocs.ts: useClienteDocs (query) + useUploadClienteDoc (mutation) + useDownloadDoc (mutation)
- [ ] D-01 enforce: useUploadClienteDoc força `requer_aprovacao: false` quando autor_tipo='cliente'
- [ ] UploadDropzone: react-dropzone wrapper com ACCEPT + MAX_SIZE + toast PT-BR (Pitfall §8 cobertos)
- [ ] ClienteDocsList: lista agrupada por raiz, mostra versão mais nova, slot actionsSlot? para Slice 4
- [ ] PortalDocumentosPage + nav portal + rota /portal/documentos
- [ ] Tab "Documentos" em ClienteDetailPage com Select tag + Checkbox requer_aprovacao
- [ ] Smoke UX: cliente sobe, consultor sobe, cliente vê do consultor, OUTRO cliente NÃO vê (RLS validada manualmente)
- [ ] PORTAL-01 implementado (drag-drop upload cliente + storage RLS)
- [ ] PORTAL-02 implementado (consultor upload com tag na ficha cliente)
- [ ] 13+ tests passando (file-validation 8, useClienteDocs 7, UploadDropzone 6)
</success_criteria>

<output>
Create `.planning/phases/07-client-portal-expansion/07-03-SUMMARY.md`:
- react-dropzone version instalada
- Lista de arquivos criados/modificados
- Smoke UX results (cliente / consultor / cross-RLS — sucesso ou issues)
- Tests passando count
- Slot Slice 4 documentado: ClienteDocsList aceita actionsSlot?
</output>
