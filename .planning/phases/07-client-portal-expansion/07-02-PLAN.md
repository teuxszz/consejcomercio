---
phase: 07-client-portal-expansion
plan: 02
type: execute
wave: 2
depends_on: [07-01a, 07-01b]
files_modified:
  - supabase/functions/_shared/aprovacoes.ts
  - supabase/functions/notify-aprovacao-evento/index.ts
  - supabase/functions/notify-aprovacoes-stale/index.ts
  - supabase/migrations/038_cron_aprovacoes_stale.sql
  - supabase/functions/_shared/__tests__/aprovacoes.test.ts
autonomous: false
requirements: [PORTAL-03, PORTAL-04]
user_setup:
  - service: supabase-edge-functions
    why: "Edge functions notify-aprovacao-evento + notify-aprovacoes-stale precisam ser deployadas; migration 038 (pg_cron schedule) aplicada via Studio SQL Editor; smoke test via curl"
    cli_commands:
      - "supabase functions deploy notify-aprovacao-evento"
      - "supabase functions deploy notify-aprovacoes-stale"
    dashboard_config:
      - task: "Aplicar migration 038_cron_aprovacoes_stale.sql via SQL Editor"
        location: "https://supabase.com/dashboard/project/wfnriqwkzdazdbuzbyug/sql/new"

must_haves:
  truths:
    - "Helper _shared/aprovacoes.ts exporta sendNotificacaoAprovacao com Promise.allSettled paralelo de Slack+Email+Push respeitando prefs.documentos"
    - "Helper aceita skipSlack=true para variante D-12 (reenviar lembrete dispara só email + push pro cliente)"
    - "Idempotência via UNIQUE notificacoes_envios (perfil_id, tipo='documentos', canal, dia, entidade_id=doc_id) — pattern Phase 5"
    - "Trigger Postgres em cliente_docs (INSERT autor_tipo='cliente' OR UPDATE status mudou) dispara notify-aprovacao-evento via pg_net.http_post — 2 triggers separadas (Pitfall §4)"
    - "Cron pg_cron diário 11:00 UTC chama notify-aprovacoes-stale para docs requer_aprovacao=true AND status='pending' AND created_at < now() - dias_para_aprovacao_pendente"
    - "Threshold dias_para_aprovacao_pendente lido de configuracoes.metas (default 5, configurável em Slice 4)"
    - "Edge functions usam constantTimeAuthCheck contra WEBHOOK_APROVACAO_SECRET (Vault em Slice 1)"
    - "Smoke test inline confirma POST 401 sem Bearer e 200 com Bearer correto"
  artifacts:
    - path: "supabase/functions/_shared/aprovacoes.ts"
      provides: "sendNotificacaoAprovacao + EventoAprovacao type + SendNotificacaoAprovacaoResult shape"
      contains: "Promise.allSettled"
      min_lines: 140
    - path: "supabase/functions/notify-aprovacao-evento/index.ts"
      provides: "Edge function trigger-driven (eventos a/b/c)"
      contains: "constantTimeAuthCheck"
      exports: ["default serve handler"]
    - path: "supabase/functions/notify-aprovacoes-stale/index.ts"
      provides: "Edge function cron-driven (evento d)"
      contains: "sendNotificacaoAprovacao"
    - path: "supabase/migrations/038_cron_aprovacoes_stale.sql"
      provides: "cron_disparar_aprovacoes_stale() SQL function + cron.schedule + cliente_docs_after_change() trigger function + 2 CREATE TRIGGER"
      contains: "cron.schedule"
      min_lines: 100
  key_links:
    - from: "Postgres trigger cliente_docs_after_change"
      to: "edge function notify-aprovacao-evento"
      via: "pg_net.http_post com Bearer webhook_aprovacao_secret"
      pattern: "net.http_post"
    - from: "pg_cron schedule disparar-aprovacoes-stale"
      to: "edge function notify-aprovacoes-stale"
      via: "pg_net.http_post per-doc no loop"
      pattern: "FOR.*LOOP.*http_post"
    - from: "edge function notify-aprovacao-evento"
      to: "_shared/aprovacoes.ts.sendNotificacaoAprovacao"
      via: "import + call por evento"
      pattern: "sendNotificacaoAprovacao"
---

<objective>
**Slice 2 — Backend de notificação multi-canal (helper + 2 edge functions + cron)**

Com a foundation pronta (Slice 1: types + RLS + Vault secret + JSONB extend), agora ligo o
backend de notificação dos eventos de aprovação:

- (a) cliente aprovou doc → notif consultor
- (b) cliente pediu revisão → notif consultor (com comentario_cliente)
- (c) cliente subiu doc no portal → notif consultor
- (d) 5+ dias sem resposta em aprovação pending → notif consultor (cron-driven)

Eventos a/b/c via Postgres triggers em UPDATE/INSERT de `cliente_docs` (RESEARCH §8, Pitfall
§4: 2 triggers separadas porque OLD não existe em INSERT). Evento d via pg_cron (RESEARCH §9).
Helper `_shared/aprovacoes.ts` espelha exatamente email.ts + push.ts (PATTERNS §2) com
Promise.allSettled paralelo de 3 canais respeitando `prefs.documentos.{slack,email,push}` (D-08, D-09).

Esta plan termina com smoke test via curl confirmando deploys e auth funcionando — UI vem
nos Slices 3/4.

**Autonomous: false** porque exige Gabriel: (1) aplicar migration 038 via Studio (mesmo
tech-debt do Slice 1), (2) deploy de 2 edge functions via CLI, (3) smoke curl test.

Output:
- `_shared/aprovacoes.ts`: sendNotificacaoAprovacao helper
- `notify-aprovacao-evento` edge function (trigger-driven, eventos a/b/c)
- `notify-aprovacoes-stale` edge function (cron-driven, evento d)
- Migration 038: trigger functions + 2 triggers + cron schedule + cron_disparar fn
- Tests aprovacoes.ts (5+ casos populando os it.todo do Slice 1)
- Smoke verificação 401/200
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-client-portal-expansion/07-01a-SUMMARY.md
@.planning/phases/07-client-portal-expansion/07-01b-SUMMARY.md
@.planning/phases/07-client-portal-expansion/07-CONTEXT.md
@.planning/phases/07-client-portal-expansion/07-RESEARCH.md
@.planning/phases/07-client-portal-expansion/07-PATTERNS.md

# Analogs Phase 5/6 que esta plan espelha
@supabase/functions/_shared/email.ts
@supabase/functions/_shared/push.ts
@supabase/functions/_shared/perfis.ts
@supabase/functions/_shared/auth.ts
@supabase/functions/_shared/slack.ts
@supabase/functions/notify-tarefa/index.ts
@supabase/functions/notify-resumo-diario/index.ts
@supabase/migrations/031_cron_renovacoes.sql
@supabase/migrations/034_cron_resumo_diario.sql
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres trigger → Edge Function | pg_net.http_post com Bearer Vault secret |
| pg_cron → Edge Function | Mesma via, 1 chamada por doc stale |
| Edge Function → External APIs | sendEmail (Resend), postDm (Slack), sendPush (web-push VAPID) |
| Cliente comentario_cliente → Trigger payload | Texto livre vai como JSONB string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Injection | comentario_cliente em trigger SQL | mitigate | `jsonb_build_object('comentario_cliente', NEW.comentario_cliente)` — parametrizado, não concat string (Postgres-safe) |
| T-07-11 | Information Disclosure | webhook_aprovacao_secret em logs | mitigate | aprovacoes.ts segue Phase 5 pattern (email.ts:18 comment): "RESEND_API_KEY nunca em error_msg ou logs" — aplicar mesma regra para Authorization header |
| T-07-12 | Information Disclosure | Vault secret cleartext em trigger | mitigate | `SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'webhook_aprovacao_secret'` — pattern migration 031, secret nunca aparece em log SQL |
| T-07-06 | Information Disclosure | Idempotência day-level falha → notif duplicada | mitigate | UNIQUE notificacoes_envios (perfil_id, tipo='documentos', canal, dia, entidade_id=doc_id) — pattern Phase 5. Trigger UPDATE usa OLD.status IS DISTINCT FROM NEW.status (RESEARCH Open Q4) para evitar fire duplo |
| T-07-13 (novo) | Tampering | Trigger fire em status='superseded' dispara notif errada | mitigate | Trigger only fires em `aprovado` ou `revisao_solicitada` (CASE WHEN explícito no v_evento decision); superseded ignora |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: Implementar helper _shared/aprovacoes.ts (D-09 — espelha email.ts + push.ts)</name>
  <files>
    supabase/functions/_shared/aprovacoes.ts,
    supabase/functions/_shared/__tests__/aprovacoes.test.ts
  </files>
  <read_first>
    @supabase/functions/_shared/email.ts
    @supabase/functions/_shared/push.ts
    @supabase/functions/_shared/perfis.ts
    @supabase/functions/_shared/slack.ts
    @supabase/functions/_shared/__tests__/aprovacoes.test.ts
    @.planning/phases/07-client-portal-expansion/07-RESEARCH.md
  </read_first>
  <behavior>
    Test cases para popular it.todo de Slice 1:
    - sendNotificacaoAprovacao com prefs.documentos.email=true E perfil.email definido → emailP é Promise real (sendEmail chamada); email.ok=true
    - prefs.documentos.email=false → emailP retorna {ok:true, skipped:'email_off'}; sendEmail NUNCA chamada
    - prefs.documentos.slack=true E findSlackUserId retorna ID → slackP postDm chamada
    - skipSlack=true (variante D-12 reenviar-lembrete) → slackP retorna {skipped:'slack_off'} mesmo com prefs.slack=true
    - sendEmail retorna {ok:false, status:'failed'} → result.ok=false; result.email.ok=false; mas result.slack/push continuam (Promise.allSettled isola falhas)
    - Idempotência: sendEmail mock retornar {ok:true, status:'skipped_idempotent'} → result.ok=true (skipped é sucesso)
    - Evento subjectFor maps corretamente (4 eventos × 4 subjects PT-BR)
  </behavior>
  <action>
    Implementar `supabase/functions/_shared/aprovacoes.ts` espelhando RESEARCH §7 (linhas 685-792)
    e PATTERNS §2.

    **Imports + env** (PATTERNS §2.1):
    ```typescript
    // @ts-nocheck (URL imports + Deno.env não resolvem no TS Node — Deno resolve em prod)
    import type { SupabaseClient } from 'https://esm.sh/@supabase/supabase-js@2'
    import { sendEmail } from './email.ts'
    import { sendPush } from './push.ts'
    import { postDm } from './slack.ts'
    import { findSlackUserId, loadPrefs } from './perfis.ts'

    const SLACK_BOT_TOKEN = (typeof Deno !== 'undefined' ? Deno.env.get('SLACK_BOT_TOKEN') : process.env.SLACK_BOT_TOKEN) ?? ''
    const APP_URL = (
      (typeof Deno !== 'undefined' ? Deno.env.get('APP_URL') : process.env.APP_URL)
      ?? 'https://localhost:5173'
    ).replace(/\/$/, '')
    ```

    **Tipos exportados** (RESEARCH §7 linhas 693-716):
    ```typescript
    export type EventoAprovacao =
      | 'cliente_aprovou'
      | 'cliente_pediu_revisao'
      | 'cliente_subiu_doc'
      | 'aprovacao_stale'

    export interface SendNotificacaoAprovacaoParams {
      perfilId: string  // destinatário (normalmente consultor; para D-12 reenviar = cliente)
      evento: EventoAprovacao
      docId: string
      clienteNome: string
      docNomeArquivo: string
      comentarioCliente?: string | null
      skipSlack?: boolean  // D-12: reenviar-lembrete dispara só email+push pro cliente
    }

    export interface SendNotificacaoAprovacaoResult {
      ok: boolean
      slack: { ok: boolean; skipped?: string; error?: string }
      email: { ok: boolean; skipped?: string; status?: string; errorMsg?: string }
      push:  { ok: boolean; skipped?: string }
    }
    ```

    **subjectFor helper** (PT-BR per D-09 + RESEARCH §7 linhas 720-727):
    ```typescript
    function subjectFor(e: EventoAprovacao, clienteNome: string, docNome: string): string {
      switch (e) {
        case 'cliente_aprovou':       return `${clienteNome} aprovou ${docNome}`
        case 'cliente_pediu_revisao': return `${clienteNome} pediu revisão em ${docNome}`
        case 'cliente_subiu_doc':     return `${clienteNome} enviou ${docNome}`
        case 'aprovacao_stale':       return `Aprovação parada há +5 dias — ${docNome}`
      }
    }
    ```

    **sendNotificacaoAprovacao** (linha-base RESEARCH §7 linhas 729-792):
    1. `loadPrefs(supabase, perfilId)` → prefs
    2. `findSlackUserId(supabase, perfilId)` → slackUserId
    3. SELECT email, nome FROM perfis WHERE id = perfilId → perfil
    4. `subject = subjectFor(evento, clienteNome, docNomeArquivo)`
    5. Decisões por canal:
       - `wantSlack = !skipSlack && prefs?.documentos?.slack === true && !!slackUserId && !!SLACK_BOT_TOKEN`
       - `wantEmail = prefs?.documentos?.email === true && !!perfil?.email`
       - `wantPush  = prefs?.documentos?.push  === true`
    6. Construir 3 promises (slack via postDm, email via sendEmail com tipo='documentos' e entidadeId=docId, push via sendPush com title/body/data.deepLink)
    7. `await Promise.allSettled([slackP, emailP, pushP])`
    8. Unwrap helper:
       ```typescript
       const unwrap = (s: PromiseSettledResult<any>) =>
         s.status === 'fulfilled' ? s.value : { ok: false, error: String(s.reason) }
       ```
    9. Result agregado com `ok` = AND dos 3, ou OR — usar AND igual RESEARCH §7 linha 786-791 (cada canal é responsável por seu sucesso; result.ok refletindo "todos sucessos OU skipped" é o desejado)

    **Tests:** popular `supabase/functions/_shared/__tests__/aprovacoes.test.ts` com 7 cases acima.
    Mock `email.ts`/`push.ts`/`slack.ts`/`perfis.ts` via vitest `vi.mock`. Supabase client passado
    via `supabase: {} as any` (helpers mockados não usam).

    **Segurança (T-07-11):** comentário no topo do arquivo: `// Segurança: WEBHOOK_APROVACAO_SECRET nunca aparece em error_msg ou logs (espelhando email.ts:18)`.
  </action>
  <verify>
    <automated>npm test -- --run supabase/functions/_shared/__tests__/aprovacoes.test.ts 2>&1 | grep -E "Tests.*passed"</automated>
  </verify>
  <done>
    aprovacoes.ts exporta sendNotificacaoAprovacao + 2 tipos + EventoAprovacao. 7 tests passando. Promise.allSettled comprovadamente isola falhas (test "sendEmail falha mas Slack/Push continuam"). skipSlack=true variant funciona.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.2: Edge function notify-aprovacao-evento/index.ts (trigger-driven a/b/c)</name>
  <files>supabase/functions/notify-aprovacao-evento/index.ts</files>
  <read_first>
    @supabase/functions/notify-tarefa/index.ts
    @supabase/functions/_shared/auth.ts
    @supabase/functions/_shared/aprovacoes.ts
    @.planning/phases/07-client-portal-expansion/07-RESEARCH.md
  </read_first>
  <action>
    Espelhar exatamente `supabase/functions/notify-tarefa/index.ts` (analog exact PATTERNS §3).

    **Imports + env setup** (notify-tarefa:21-64):
    ```typescript
    import { serve } from 'https://deno.land/std@0.224.0/http/server.ts'
    import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
    import { constantTimeAuthCheck } from '../_shared/auth.ts'
    import { sendNotificacaoAprovacao, type EventoAprovacao } from '../_shared/aprovacoes.ts'

    const WEBHOOK_SECRET = Deno.env.get('WEBHOOK_APROVACAO_SECRET')
    const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!
    const SERVICE_KEY = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!

    const supabase = createClient(SUPABASE_URL, SERVICE_KEY, { auth: { persistSession: false } })
    ```

    **Payload shape (trigger SQL define — RESEARCH §8 linhas 850-856; Slice 4 ReenviarLembreteButton estende com `skip_slack`):**
    ```typescript
    interface AprovacaoEventoPayload {
      evento: EventoAprovacao  // 'cliente_aprovou' | 'cliente_pediu_revisao' | 'cliente_subiu_doc' | 'aprovacao_stale'
      doc_id: string
      cliente_id: string
      destinatario_perfil_id: string
      comentario_cliente: string | null
      skip_slack?: boolean  // D-12: reenviar-lembrete pro cliente dispara só email+push (Slack OFF)
    }
    ```

    **Serve handler:**
    1. Auth check (notify-tarefa:117-120):
       ```typescript
       if (WEBHOOK_SECRET) {
         const auth = req.headers.get('Authorization') ?? ''
         if (!constantTimeAuthCheck(auth, WEBHOOK_SECRET)) {
           return json({ ok: false, error: 'unauthorized' }, 401)
         }
       }
       ```
    2. Parse JSON body
    3. Fetch cliente_nome + doc_nome_arquivo:
       ```typescript
       const { data: doc } = await supabase
         .from('cliente_docs')
         .select('nome_arquivo, clientes!inner(nome)')
         .eq('id', payload.doc_id)
         .single()
       ```
    4. Call `sendNotificacaoAprovacao(supabase, { perfilId: payload.destinatario_perfil_id, evento: payload.evento, docId: payload.doc_id, clienteNome, docNomeArquivo, comentarioCliente: payload.comentario_cliente, skipSlack: payload.skip_slack ?? false })`
       - **Importante (BLOCKER #3 revision iter 2):** o handler MUST ler `payload.skip_slack` (snake_case, vindo do JSON do trigger / Slice 4 ReenviarLembreteButton) e propagar como `skipSlack: payload.skip_slack ?? false` (camelCase, contrato do helper). Sem essa propagação, o D-12 (reenviar-lembrete sem Slack) quebra silenciosamente.
    5. Return `json({ ok: result.ok, slack: result.slack, email: result.email, push: result.push }, result.ok ? 200 : 500)`

    **CORS:** copiar exato do notify-tarefa header (OPTIONS preflight).
  </action>
  <verify>
    <automated>grep -E "constantTimeAuthCheck|sendNotificacaoAprovacao|WEBHOOK_APROVACAO_SECRET" supabase/functions/notify-aprovacao-evento/index.ts | wc -l | awk '$1 >= 3 { print "OK" }'</automated>
  </verify>
  <done>
    Edge function compila (lint pass), faz auth check com WEBHOOK_APROVACAO_SECRET, parseia payload, chama sendNotificacaoAprovacao com evento + dados do doc, retorna JSON com slack/email/push results.

    **Acceptance criteria (BLOCKER #3 revision iter 2):**
    - Payload com `skip_slack: true` NÃO dispara `sendSlack` no helper (verificar via grep do código OU test mock — `result.slack.skipped === 'slack_off'` quando `skip_slack=true` no body, mesmo com `prefs.documentos.slack=true`).
    - `sendNotificacaoAprovacao` é chamada com `skipSlack: true` quando `payload.skip_slack === true`.
    - Quando `payload.skip_slack` é undefined ou false, fluxo normal (Slack dispara se prefs permitirem).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.3: Edge function notify-aprovacoes-stale/index.ts (cron-driven d)</name>
  <files>supabase/functions/notify-aprovacoes-stale/index.ts</files>
  <read_first>
    @supabase/functions/notify-resumo-diario/index.ts
    @supabase/functions/_shared/aprovacoes.ts
    @.planning/phases/07-client-portal-expansion/07-RESEARCH.md
  </read_first>
  <action>
    Espelhar `notify-resumo-diario/index.ts` (analog exact PATTERNS §4).

    **Header comment** (notify-resumo-diario:1-15 pattern):
    ```typescript
    // Edge Function: notify-aprovacoes-stale
    // Gatilho: chamada explícita do pg_cron (migration 038) diariamente às 08:00 BRT (11:00 UTC).
    // Para cada doc pending com requer_aprovacao=true E (now - created_at) > dias_para_aprovacao_pendente,
    // dispara sendNotificacaoAprovacao(evento='aprovacao_stale') para o consultor responsável.
    // Idempotência via UNIQUE notificacoes_envios (já em prod desde Phase 5).
    ```

    **Payload (cron envia 1 chamada por doc — RESEARCH §9 linhas 922-929):**
    ```typescript
    interface StalePayload {
      doc_id: string
      cliente_id: string
      destinatario_perfil_id: string
      cliente_nome: string
      doc_nome_arquivo: string
    }
    ```

    **Handler:**
    1. Auth check com WEBHOOK_APROVACAO_SECRET (mesmo de Task 2.2)
    2. Parse body
    3. Call `sendNotificacaoAprovacao(supabase, { perfilId, evento: 'aprovacao_stale', docId, clienteNome, docNomeArquivo })`
    4. Return JSON

    Não precisa SELECT extra — o cron SQL já passou todos os dados no body (mais leve, menos round-trips, igual notify-resumo-diario faz com o resumo).
  </action>
  <verify>
    <automated>grep -E "aprovacao_stale|WEBHOOK_APROVACAO_SECRET|sendNotificacaoAprovacao" supabase/functions/notify-aprovacoes-stale/index.ts | wc -l | awk '$1 >= 3 { print "OK" }'</automated>
  </verify>
  <done>
    Edge function deployable, valida auth, chama helper com evento='aprovacao_stale', retorna JSON.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.4: Migration 038 — trigger function cliente_docs_after_change + 2 triggers + cron_disparar_aprovacoes_stale + cron.schedule</name>
  <files>supabase/migrations/038_cron_aprovacoes_stale.sql</files>
  <read_first>
    @supabase/migrations/031_cron_renovacoes.sql
    @supabase/migrations/034_cron_resumo_diario.sql
    @.planning/phases/07-client-portal-expansion/07-RESEARCH.md
  </read_first>
  <action>
    Espelhar migrations 031 (renovações via pg_net + Vault) + 034 (cron schedule).

    **Header:**
    ```sql
    -- Migration 038: pg_cron + trigger functions para aprovações de cliente_docs.
    --
    -- Conteúdo:
    --   1. Function public.cliente_docs_after_change() — chamada por 2 triggers (INSERT + UPDATE)
    --      para eventos a (cliente_aprovou), b (cliente_pediu_revisao), c (cliente_subiu_doc)
    --   2. 2 triggers separadas em cliente_docs (Pitfall §4 RESEARCH: INSERT vs UPDATE precisam
    --      ser triggers diferentes porque OLD não existe em INSERT)
    --   3. Function public.cron_disparar_aprovacoes_stale() — loop docs pending > N dias
    --   4. cron.schedule diário 11:00 UTC = 08:00 BRT (depois do notify-resumo-diario 07h)
    ```

    **Bloco 1 — cliente_docs_after_change()** (RESEARCH §8 linhas 803-865):
    ```sql
    CREATE OR REPLACE FUNCTION public.cliente_docs_after_change()
    RETURNS trigger
    LANGUAGE plpgsql
    SECURITY DEFINER
    SET search_path = public
    AS $$
    DECLARE
      v_secret      TEXT;
      v_url         TEXT := 'https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-aprovacao-evento';
      v_evento      TEXT;
      v_responsavel UUID;
      v_cliente_nome TEXT;
    BEGIN
      -- Decide evento
      IF TG_OP = 'INSERT' AND NEW.autor_tipo = 'cliente' THEN
        v_evento := 'cliente_subiu_doc';
      ELSIF TG_OP = 'UPDATE' AND NEW.status = 'aprovado' AND OLD.status IS DISTINCT FROM 'aprovado' THEN
        v_evento := 'cliente_aprovou';
      ELSIF TG_OP = 'UPDATE' AND NEW.status = 'revisao_solicitada' AND OLD.status IS DISTINCT FROM 'revisao_solicitada' THEN
        v_evento := 'cliente_pediu_revisao';
      ELSE
        RETURN NEW;
      END IF;

      -- Resolve consultor responsável
      SELECT responsavel_id, nome
        INTO v_responsavel, v_cliente_nome
        FROM clientes WHERE id = NEW.cliente_id;

      IF v_responsavel IS NULL THEN
        RETURN NEW;
      END IF;

      -- Vault secret lookup (pattern migration 031)
      SELECT decrypted_secret INTO v_secret
        FROM vault.decrypted_secrets WHERE name = 'webhook_aprovacao_secret';
      IF v_secret IS NULL THEN
        RAISE WARNING 'cliente_docs_after_change: webhook_aprovacao_secret ausente';
        RETURN NEW;
      END IF;

      -- Fire-and-forget HTTP POST (pg_net assíncrono — Pitfall §5 RESEARCH)
      PERFORM net.http_post(
        url     := v_url,
        body    := jsonb_build_object(
          'evento', v_evento,
          'doc_id', NEW.id,
          'cliente_id', NEW.cliente_id,
          'destinatario_perfil_id', v_responsavel,
          'comentario_cliente', NEW.comentario_cliente
        ),
        headers := jsonb_build_object(
          'Content-Type', 'application/json',
          'Authorization', 'Bearer ' || v_secret
        )
      );

      RETURN NEW;
    END;
    $$;
    ```

    **Bloco 2 — 2 triggers (Pitfall §4 RESEARCH):**
    ```sql
    DROP TRIGGER IF EXISTS trg_cliente_docs_after_insert ON cliente_docs;
    CREATE TRIGGER trg_cliente_docs_after_insert
      AFTER INSERT ON cliente_docs
      FOR EACH ROW EXECUTE FUNCTION public.cliente_docs_after_change();

    DROP TRIGGER IF EXISTS trg_cliente_docs_after_update ON cliente_docs;
    CREATE TRIGGER trg_cliente_docs_after_update
      AFTER UPDATE ON cliente_docs
      FOR EACH ROW EXECUTE FUNCTION public.cliente_docs_after_change();
    ```

    **Bloco 3 — cron_disparar_aprovacoes_stale()** (RESEARCH §9 linhas 883-944):
    Lê `configuracoes.metas->>'dias_para_aprovacao_pendente'` (default 5). FOR LOOP em
    cliente_docs WHERE status='pending' AND requer_aprovacao=true AND now() - created_at > N days
    AND NOT EXISTS notif hoje. Chama `notify-aprovacoes-stale` via pg_net.http_post com Bearer
    Vault secret.

    **Bloco 4 — cron.schedule:**
    ```sql
    SELECT cron.schedule(
      'disparar-aprovacoes-stale',
      '0 11 * * *',  -- 11:00 UTC = 08:00 BRT
      $$SELECT public.cron_disparar_aprovacoes_stale()$$
    );
    ```
  </action>
  <verify>
    <automated>grep -cE "^CREATE OR REPLACE FUNCTION|^CREATE TRIGGER|cron.schedule" supabase/migrations/038_cron_aprovacoes_stale.sql | awk '$1 >= 5 { print "OK" }'</automated>
  </verify>
  <done>
    Migration 038 contém 2 SECURITY DEFINER functions + 2 triggers + 1 cron.schedule. Pitfalls §4 (2 triggers separadas) e §5 (pg_net assíncrono) tratados.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2.5 [BLOCKING]: Gabriel aplica migration 038 + deploy 2 edge functions + smoke curl</name>
  <what-built>
    Migration 038 escrita (Task 2.4). Edge functions notify-aprovacao-evento (Task 2.2) e
    notify-aprovacoes-stale (Task 2.3) prontos para deploy. Helper aprovacoes.ts (Task 2.1)
    testado.

    Esta task: subir tudo em prod + smoke test inline.
  </what-built>
  <how-to-verify>
    **Passo 1 — Aplicar migration 038 via Supabase Studio SQL Editor:**
    1. Abrir https://supabase.com/dashboard/project/wfnriqwkzdazdbuzbyug/sql/new
    2. Colar conteúdo de `supabase/migrations/038_cron_aprovacoes_stale.sql`
    3. Run; confirmar 0 erros
    4. Verificação:
       ```sql
       SELECT proname FROM pg_proc WHERE proname IN ('cliente_docs_after_change','cron_disparar_aprovacoes_stale');
       -- esperado: 2 rows
       SELECT tgname FROM pg_trigger WHERE tgname LIKE 'trg_cliente_docs_%';
       -- esperado: 2 rows (after_insert + after_update)
       SELECT jobname, schedule FROM cron.job WHERE jobname = 'disparar-aprovacoes-stale';
       -- esperado: 1 row, schedule '0 11 * * *'
       ```

    **Passo 2 — Deploy edge functions:**
    ```bash
    rtk supabase functions deploy notify-aprovacao-evento
    rtk supabase functions deploy notify-aprovacoes-stale
    ```

    **Passo 3 — Smoke curl test (auth):**

    Sem Bearer → 401:
    ```bash
    curl -i -X POST https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-aprovacao-evento \
      -H "Content-Type: application/json" \
      -d '{"evento":"cliente_aprovou","doc_id":"00000000-0000-0000-0000-000000000000","cliente_id":"00000000-0000-0000-0000-000000000000","destinatario_perfil_id":"00000000-0000-0000-0000-000000000000","comentario_cliente":null}'
    # esperado: HTTP/2 401 + body {"ok":false,"error":"unauthorized"}
    ```

    Com Bearer correto → 200 (ou 500 se doc_id não existe, mas auth ok):
    ```bash
    curl -i -X POST https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-aprovacao-evento \
      -H "Authorization: Bearer <WEBHOOK_APROVACAO_SECRET>" \
      -H "Content-Type: application/json" \
      -d '{"evento":"cliente_aprovou","doc_id":"00000000-0000-0000-0000-000000000000","cliente_id":"00000000-0000-0000-0000-000000000000","destinatario_perfil_id":"00000000-0000-0000-0000-000000000000","comentario_cliente":null}'
    # esperado: HTTP/2 não-401 (200 com result {ok:false} ou 500 — ambos OK; o ponto é confirmar auth passou)
    ```

    Repetir para `notify-aprovacoes-stale` com payload mínimo.

    **Passo 4 (opcional) — End-to-end real:**
    1. Studio → cliente_docs → INSERT manual:
       ```sql
       -- Use IDs reais de cliente + perfil interno (responsavel_id) para um teste vivo
       INSERT INTO cliente_docs (cliente_id, autor_id, autor_tipo, tag, nome_arquivo, mime_type, tamanho_bytes, storage_path, requer_aprovacao, status)
       VALUES ('<cliente-id>', '<perfil-interno-id>', 'interno', 'proposta', 'test.pdf', 'application/pdf', 1000, '', true, 'pending');
       ```
    2. UPDATE status='aprovado' nessa row
    3. Verificar `SELECT * FROM notificacoes_envios WHERE tipo='documentos' ORDER BY created_at DESC LIMIT 3` retorna row
    4. (Opcional) Verificar email recebido (se prefs.documentos.email=true)
  </how-to-verify>
  <files>(manual checkpoint — see <how-to-verify> for exact commands and files touched outside repo)</files>
  <action>Execute every numbered step in <how-to-verify> in order; do not skip Studio SQL Editor / CLI / Dashboard tasks. After each step run the verification SQL/curl shown inline.</action>
  <verify><human-check>All inline verification queries / curl invocations in <how-to-verify> return the expected values; capture outputs in the SUMMARY.</human-check></verify>
  <done>Every step in <how-to-verify> completed with the expected output; any anomaly logged in SUMMARY and either fixed before resume or escalated to a follow-up gap-closure plan.</done>
    <resume-signal>Type "approved" após confirmar: (1) migration 038 aplicada — 2 functions + 2 triggers + 1 cron job; (2) deploy ambas edge functions com sucesso; (3) smoke curl 401 sem Bearer + não-401 com Bearer; (4) opcional: e2e em cliente_docs INSERT/UPDATE gerou row em notificacoes_envios. Descreva qualquer falha.</resume-signal>
</task>

</tasks>

<verification>
**Smoke check Slice 2 (no commit):**

```bash
# Tests unit do helper
rtk npm test -- --run supabase/functions/_shared/__tests__/aprovacoes.test.ts

# Migration 038 sintaxe básica
rtk grep -cE "^(CREATE|DROP|SELECT)" supabase/migrations/038_cron_aprovacoes_stale.sql
# >= 8 linhas

# Edge functions presentes
rtk ls supabase/functions/notify-aprovacao-evento/index.ts
rtk ls supabase/functions/notify-aprovacoes-stale/index.ts

# TypeScript compila (browser side intocado, mas conferir nenhum regression)
rtk npx tsc -b
```

**Verificação live (após Task 2.5):**
- `SELECT * FROM cron.job WHERE jobname = 'disparar-aprovacoes-stale';` retorna 1 row schedule '0 11 * * *'
- `SELECT count(*) FROM pg_trigger WHERE tgrelid = 'cliente_docs'::regclass AND tgname LIKE 'trg_%';` = 2
- curl 401 sem Bearer confirmado
- (Opcional) e2e INSERT/UPDATE manual em cliente_docs gera row em notificacoes_envios
</verification>

<success_criteria>
- [ ] `_shared/aprovacoes.ts` exporta sendNotificacaoAprovacao + EventoAprovacao + Result types
- [ ] aprovacoes.ts respeita prefs.documentos.{slack,email,push} (3 testes provando isto)
- [ ] aprovacoes.ts honra skipSlack=true (variante D-12 reenviar-lembrete pro cliente — preparado para Slice 4)
- [ ] Promise.allSettled isola falha de um canal (test prova outros 2 continuam OK)
- [ ] notify-aprovacao-evento + notify-aprovacoes-stale deployed em prod
- [ ] Migration 038: 2 trigger functions + 2 triggers (INSERT/UPDATE separadas) + 1 cron schedule
- [ ] Smoke curl: 401 sem Bearer, 200/500 com Bearer (auth passa)
- [ ] PORTAL-03 backend implementado (helper + edge function)
- [ ] PORTAL-04 backend stale-detection implementado (cron + edge function)
- [ ] Threat T-07-10/11/12/13 mitigados (jsonb parametrizado, Vault, no log de secret, fire só em status que importa)
</success_criteria>

<output>
Create `.planning/phases/07-client-portal-expansion/07-02-SUMMARY.md`:
- Helper aprovacoes.ts: hash + test coverage % + lista de eventos suportados
- Migration 038: aplicada? + cron job confirmada via cron.job
- Edge functions deployed (URLs)
- Smoke curl results (401/200 status codes observados)
- Opcional: e2e INSERT/UPDATE → notificacoes_envios timestamp
</output>
