---
phase: 05-multi-channel-notifications-email
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/components/ui/switch.tsx
  - src/types/index.ts
  - src/lib/query-keys.ts
  - src/hooks/usePreferenciasNotif.ts
  - src/hooks/useNotificacoesEnvios.ts
  - src/hooks/useReenviarNotificacao.ts
  - src/hooks/useQuotaResend.ts
  - src/hooks/__tests__/usePreferenciasNotif.test.tsx
  - src/hooks/__tests__/useReenviarNotificacao.test.tsx
  - src/components/me/NotificacoesPanel.tsx
  - src/components/me/__tests__/NotificacoesPanel.test.tsx
  - src/pages/__tests__/NotificacoesHistoricoPage.test.tsx
  - src/pages/MeEspacoPage.tsx
  - src/pages/NotificacoesHistoricoPage.tsx
  - src/components/shared/QuotaResendBanner.tsx
  - src/pages/AdocaoPage.tsx
  - src/pages/ConfiguracoesPage.tsx
  - src/router.tsx
  - supabase/functions/reenviar-notificacao/index.ts
autonomous: false
requirements: [EMAIL-01, EMAIL-04]
user_setup: []

must_haves:
  truths:
    - "Consultor abre `/me/preferencias` → vê matriz 4×2 com 8 Switches refletindo `preferencias_notif` atual; togglar + clicar Salvar grava no banco e o próximo disparo respeita a nova preferência."
    - "Coordenador abre `/me/notificacoes-historico` → vê últimos 30 dias de envios (próprios + dropdown 'Filtrar por usuário' para outros membros)."
    - "Consultor abre `/me/notificacoes-historico` → vê só os próprios envios (sem dropdown — RLS bloqueia)."
    - "Botão 'Reenviar' em qualquer linha do histórico chama edge function `reenviar-notificacao` que cria nova linha em `notificacoes_envios` com `reenviado_por_id` + `reenviado_em` preenchidos e dispara novo e-mail."
    - "Banner de quota aparece em `/adocao` e `/configuracoes` APENAS para coordenador+ quando `max(hoje/100, mes/3000) > 0.8` (D-15)."
    - "Tab Notificações em MeEspacoPage acessível via `/me?tab=notificacoes` E redirect `/me/preferencias` → `/me?tab=notificacoes`."
  artifacts:
    - path: "src/components/ui/switch.tsx"
      provides: "shadcn Switch primitive (Radix)"
      contains: "@radix-ui/react-switch"
    - path: "src/types/index.ts"
      provides: "Tipos TipoNotif, CanalNotif, StatusNotif, PreferenciasNotif, NotificacaoEnvio"
      exports: ["TipoNotif", "CanalNotif", "StatusNotif", "PreferenciasNotif", "NotificacaoEnvio"]
    - path: "src/lib/query-keys.ts"
      provides: "QUERY_KEYS.preferenciasNotif, QUERY_KEYS.notificacoesEnvios, QUERY_KEYS.quotaResend"
      contains: "preferenciasNotif"
    - path: "src/hooks/usePreferenciasNotif.ts"
      provides: "useQueryPreferencias + useSalvarPrefs com PATCH atômico"
      exports: ["usePreferenciasNotif", "useSalvarPrefs"]
    - path: "src/hooks/useNotificacoesEnvios.ts"
      provides: "Query 30d com filtro opcional perfilId"
      exports: ["useNotificacoesEnvios"]
    - path: "src/hooks/useReenviarNotificacao.ts"
      provides: "Mutation que invoca edge function reenviar-notificacao"
      exports: ["useReenviarNotificacao"]
    - path: "src/hooks/useQuotaResend.ts"
      provides: "Query RPC quota_resend_atual com 1min stale"
      exports: ["useQuotaResend"]
    - path: "src/components/me/NotificacoesPanel.tsx"
      provides: "Matriz 4×2 com 8 Switches + botão Salvar + link Ver histórico"
      min_lines: 60
    - path: "src/pages/NotificacoesHistoricoPage.tsx"
      provides: "Lista 30d + dropdown filtro user (coord+) + botão Reenviar"
      min_lines: 80
    - path: "src/components/shared/QuotaResendBanner.tsx"
      provides: "Banner warning visível só coord+ quando quota>80%"
      min_lines: 30
    - path: "supabase/functions/reenviar-notificacao/index.ts"
      provides: "Edge function que ressubmete linha como nova com reenviado_por_id/reenviado_em (D-18)"
      contains: "reenviado_por_id"
  key_links:
    - from: "src/components/me/NotificacoesPanel.tsx"
      to: "src/hooks/usePreferenciasNotif.ts"
      via: "useSalvarPrefs().mutateAsync({ perfilId, prefs })"
      pattern: "useSalvarPrefs"
    - from: "src/pages/NotificacoesHistoricoPage.tsx"
      to: "src/hooks/useReenviarNotificacao.ts"
      via: "useReenviarNotificacao().mutate(envioId)"
      pattern: "useReenviarNotificacao"
    - from: "src/components/shared/QuotaResendBanner.tsx"
      to: "src/hooks/useQuotaResend.ts"
      via: "const { data: quota } = useQuotaResend()"
      pattern: "useQuotaResend"
    - from: "src/router.tsx"
      to: "src/pages/NotificacoesHistoricoPage.tsx"
      via: "{ path: 'notificacoes-historico', element: <NotificacoesHistoricoPage /> }"
      pattern: "notificacoes-historico"
---

<objective>
Construir a **UI completa para internos** do sistema de notificações multi-canal: matriz 4×2 de preferências em `/me/preferencias` (tab Notificações em MeEspacoPage), página `/me/notificacoes-historico` com lista 30d + reenvio, banner de quota warning para coord+, e edge function `reenviar-notificacao` que faz o re-send. Tudo já tem backend pronto via Plan 1+2.

**Cobre requirements:** EMAIL-01 (completa — UI matriz interna + smart default já no banco), EMAIL-04 (completa — histórico + reenviar + status visíveis; Portal cliente fica no Plan 4).

**Purpose:** Sem esta UI, o sistema funciona mas é invisível — usuário não consegue desligar e-mail e não vê histórico. Plan 3 é o que torna o sistema **usável**. Coordenador ganha observabilidade da quota Resend antes que estoure.

**Output:** Switch primitive instalado + 4 hooks + 1 panel + 2 páginas + 1 banner + 1 edge function + 2 testes Wave-0 + rotas registradas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md
@.planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md
@.planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md
@.planning/phases/05-multi-channel-notifications-email/05-01-SUMMARY.md
@.planning/phases/05-multi-channel-notifications-email/05-02-SUMMARY.md
@src/pages/MeEspacoPage.tsx
@src/components/me/PerfilPanel.tsx
@src/pages/AuditoriaPage.tsx
@src/hooks/usePerfis.ts
@src/hooks/useAuditLogs.ts
@src/hooks/useLeads.ts
@src/lib/query-keys.ts
@src/types/index.ts
@src/router.tsx
@src/components/shared/RequireRole.tsx
@src/hooks/useCurrentRole.ts
@supabase/functions/_shared/email.ts
@supabase/functions/_shared/perfis.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Instalar shadcn Switch primitive (CLI interativa)</name>
  <what-built>Antes de implementar a matriz 4×2 (NotificacoesPanel), precisamos do primitive `Switch` que ainda não existe em `src/components/ui/`. Confirmado via Glob no RESEARCH Q6. shadcn CLI é interativo no Windows e pode pedir confirmações que não suprimimos via flag.</what-built>
  <how-to-verify>
1. Executar no terminal do projeto: `npx shadcn@latest add switch`
2. Responder confirmações da CLI (geralmente: tema, overwrite=N para arquivos existentes).
3. Verificar que o arquivo `src/components/ui/switch.tsx` foi criado.
4. Verificar que `@radix-ui/react-switch` foi adicionado ao `package.json` em `dependencies`.
5. Rodar `npm install` se necessário (CLI shadcn às vezes pula).
6. Confirmar que `npm run build` passa sem erros novos.
  </how-to-verify>
  <resume-signal>Digite "switch instalado" (com confirmação visual do arquivo criado) ou descreva qualquer erro. Se quiser pular (componente já existe pelo Plan 1 ter rodado o comando), responda "já existe".</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tipos + query-keys + 4 hooks de dados (usePreferenciasNotif, useNotificacoesEnvios, useReenviarNotificacao, useQuotaResend) (per D-08, D-19, R5 PATCH atômico)</name>
  <files>src/types/index.ts, src/lib/query-keys.ts, src/hooks/usePreferenciasNotif.ts, src/hooks/useNotificacoesEnvios.ts, src/hooks/useReenviarNotificacao.ts, src/hooks/useQuotaResend.ts, src/hooks/__tests__/usePreferenciasNotif.test.tsx, src/hooks/__tests__/useReenviarNotificacao.test.tsx</files>
  <read_first>
    - src/types/index.ts (lines 1-80 — pattern de export interface/type, snake_case para DB fields, `| null` para nullable)
    - src/lib/query-keys.ts (lines 1-67 — pattern de objeto literal com nested `.all` + `.byId(x)` `as const`)
    - src/hooks/usePerfis.ts (full file ~80 linhas — pattern useMutation com upsert column + invalidate + toast; useSalvarPerfil é o analog direto para useSalvarPrefs)
    - src/hooks/useAuditLogs.ts (full file ~50 linhas — pattern useQuery com limit + order desc; useAllAuditLogs é o analog para useNotificacoesEnvios)
    - src/hooks/useLeads.ts (lines 118-134 — pattern useMutation com `e instanceof Error` error surfacing; useDeleteLead é o analog para useReenviarNotificacao — porém ele usa `supabase.rpc`, nosso usa `supabase.functions.invoke`)
    - src/test/supabase-mock.ts (se existir — pattern de mock client; verificar como testar hooks com renderHook + QueryClientProvider)
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-08 estrutura JSONB; D-18 colunas reenviado_por_id/reenviado_em; D-15 quota visível só coord+ com 1min stale)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (Q8 RPC quota_resend_atual; R5 PATCH atômico único — não 8 mutations)
    - .planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md (§hooks/usePreferenciasNotif.ts, §useNotificacoesEnvios.ts, §useReenviarNotificacao.ts, §useQuotaResend.ts — código sketch)
  </read_first>
  <behavior>
    - usePreferenciasNotif.test.tsx cenário "query": mock supabase.from('perfis').select('preferencias_notif').eq.maybeSingle → renderHook returns prefs no banco; estrutura JSONB com 4 chaves × 2 booleans.
    - usePreferenciasNotif.test.tsx cenário "useSalvarPrefs PATCH atômico (R5)": mutateAsync({ perfilId, prefs: { tarefa: {slack:true,email:false}, cadencia:..., renovacao:..., indicacao:... } }) → supabase.from('perfis').update({ preferencias_notif: prefs }) chamado UMA VEZ com o objeto inteiro; invalidate em `QUERY_KEYS.preferenciasNotif.all` + `['perfil-meu']`.
    - usePreferenciasNotif.test.tsx cenário "optimistic update": onMutate setData imediatamente; onError rollback se mutation falha; toast.error com mensagem do error.
    - useReenviarNotificacao.test.tsx cenário "happy": mock supabase.functions.invoke('reenviar-notificacao', { body: { id } }) retorna { data, error: null } → mutation resolves; invalidateQueries em notificacoesEnvios.all; toast.success('Notificação reenviada.').
    - useReenviarNotificacao.test.tsx cenário "erro": invoke retorna { data: null, error: new Error('quota') } → toast.error('quota'); mutation rejeita.
  </behavior>
  <action>
**`src/types/index.ts`:** append (não modificar tipos existentes) seguindo style snake_case + `| null` per PATTERNS §types/index.ts:
- `export type TipoNotif = 'tarefa' | 'cadencia' | 'renovacao' | 'indicacao'`
- `export type CanalNotif = 'email' | 'slack'`
- `export type StatusNotif = 'queued' | 'delivered' | 'opened' | 'bounced' | 'complained' | 'dropped_quota' | 'skipped_no_recipient' | 'fallback_diretor' | 'failed'`
- `export interface PreferenciasNotif { tarefa: { slack: boolean; email: boolean }; cadencia: { slack: boolean; email: boolean }; renovacao: { slack: boolean; email: boolean }; indicacao: { slack: boolean; email: boolean } }`
- `export interface NotificacaoEnvio { id: string; perfil_id: string; tipo: TipoNotif; entidade_id: string | null; entidade_tipo: 'lead' | 'cliente' | 'contrato' | 'tarefa' | null; canal: CanalNotif; subject: string | null; status: StatusNotif; resend_id: string | null; slack_ts: string | null; error_msg: string | null; sent_at: string; delivered_at: string | null; opened_at: string | null; bounced_at: string | null; complained_at: string | null; reenviado_por_id: string | null; reenviado_em: string | null; dia: string }`

**`src/lib/query-keys.ts`:** append (após `dashboard` ou onde fizer sentido alfabeticamente):
```
preferenciasNotif: {
  all: ['preferenciasNotif'] as const,
  byPerfil: (perfilId: string) => ['preferenciasNotif', perfilId] as const,
},
notificacoesEnvios: {
  all: ['notificacoesEnvios'] as const,
  byPerfil: (perfilId: string) => ['notificacoesEnvios', perfilId] as const,
},
quotaResend: {
  current: ['quotaResend', 'current'] as const,
},
```

**`src/hooks/usePreferenciasNotif.ts`:** 2 exports (query + mutation), mirror exato PATTERNS §usePreferenciasNotif.ts:
- `usePreferenciasNotif(perfilId)` → useQuery que faz `supabase.from('perfis').select('preferencias_notif').eq('id', perfilId).maybeSingle()` → retorna `data?.preferencias_notif as PreferenciasNotif | null`. queryKey = `QUERY_KEYS.preferenciasNotif.byPerfil(perfilId)`.
- `useSalvarPrefs()` → useMutation com `mutationFn` que faz `supabase.from('perfis').update({ preferencias_notif: input.prefs }).eq('id', input.perfilId).select().single()` (PATCH atômico único — R5). `onMutate` para optimistic update opcional (pattern PerfilPanel é sync-after-save com toast; optimistic com 8 switches togglados rapidamente fica mais sentido). `onSuccess`: `qc.invalidateQueries({ queryKey: QUERY_KEYS.preferenciasNotif.all })` + `qc.invalidateQueries({ queryKey: ['perfil-meu'] })` + `toast.success('Preferências salvas!')`. `onError`: `toast.error(e instanceof Error ? e.message : 'Erro ao salvar preferências')`.

**`src/hooks/useNotificacoesEnvios.ts`:** export único `useNotificacoesEnvios(perfilId?: string | null)` → useQuery seguindo PATTERNS §useNotificacoesEnvios.ts:
- queryKey condicional: `perfilId ? QUERY_KEYS.notificacoesEnvios.byPerfil(perfilId) : QUERY_KEYS.notificacoesEnvios.all`.
- Query: `supabase.from('notificacoes_envios').select('*').gte('dia', dataHa30Dias).order('sent_at', { ascending: false }).limit(200)`; se `perfilId` → adicionar `.eq('perfil_id', perfilId)`.
- `dataHa30Dias = new Date(Date.now() - 30 * 86400_000).toISOString().slice(0, 10)`.
- Retorna `data as NotificacaoEnvio[]`.

**`src/hooks/useReenviarNotificacao.ts`:** export `useReenviarNotificacao()` → useMutation seguindo PATTERNS §useReenviarNotificacao.ts:
- `mutationFn: async (notifId: string) => { const { data, error } = await supabase.functions.invoke('reenviar-notificacao', { body: { id: notifId } }); if (error) throw error; return data }`.
- `onSuccess`: invalidate `QUERY_KEYS.notificacoesEnvios.all` + `toast.success('Notificação reenviada.')`.
- `onError: (e: unknown) => toast.error(e instanceof Error ? e.message : 'Erro ao reenviar notificação')`.

**`src/hooks/useQuotaResend.ts`:** export `useQuotaResend()` → useQuery seguindo PATTERNS §useQuotaResend.ts:
- queryFn chama `supabase.rpc('quota_resend_atual')` (RPC criada na migration 035).
- Retorna `data as { hoje: number; mes: number }`.
- `staleTime: 60_000` (1 min — D-15 + CONTEXT discretion).
- `refetchOnWindowFocus: false`.

**Testes Wave-0** (`src/hooks/__tests__/`):
- `usePreferenciasNotif.test.tsx`: 3 cenários (query, mutation PATCH atômico, optimistic+rollback). Pattern de teste hook com renderHook (@testing-library/react) + wrapper QueryClientProvider; mock supabase client via vi.mock.
- `useReenviarNotificacao.test.tsx`: 2 cenários (happy, erro). Mock `supabase.functions.invoke`.

**NÃO criar `src/hooks/__tests__/useNotificacoesEnvios.test.tsx` nem `useQuotaResend.test.tsx`** — coverage seria trivial (query simples sem lógica). Documentar no SUMMARY que coverage deliberadamente skipped.
  </action>
  <verify>
    <automated>npm test -- src/hooks/__tests__/usePreferenciasNotif.test.tsx src/hooks/__tests__/useReenviarNotificacao.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^export (type|interface) (TipoNotif|CanalNotif|StatusNotif|PreferenciasNotif|NotificacaoEnvio)" src/types/index.ts` retorna 5.
    - `grep -c "preferenciasNotif:" src/lib/query-keys.ts` retorna ≥1.
    - `grep -c "notificacoesEnvios:" src/lib/query-keys.ts` retorna ≥1.
    - `grep -c "quotaResend:" src/lib/query-keys.ts` retorna ≥1.
    - 4 arquivos `src/hooks/{usePreferenciasNotif,useNotificacoesEnvios,useReenviarNotificacao,useQuotaResend}.ts` existem.
    - `grep -c "export function usePreferenciasNotif" src/hooks/usePreferenciasNotif.ts` retorna ≥1.
    - `grep -c "export function useSalvarPrefs" src/hooks/usePreferenciasNotif.ts` retorna ≥1.
    - `grep -c "supabase.functions.invoke" src/hooks/useReenviarNotificacao.ts` retorna ≥1.
    - `grep -c "supabase.rpc('quota_resend_atual')" src/hooks/useQuotaResend.ts` retorna ≥1.
    - `grep -c "staleTime: 60_000" src/hooks/useQuotaResend.ts` retorna ≥1.
    - 2 testes verdes (vitest exit 0).
  </acceptance_criteria>
  <done>Tipos + query-keys + 4 hooks + 2 testes. Próxima task constrói os componentes/páginas consumindo.</done>
</task>

<task type="auto">
  <name>Task 3: NotificacoesPanel (matriz 4×2) + tab em MeEspacoPage + NotificacoesHistoricoPage + QuotaResendBanner + rotas (per D-09, D-11, D-15, D-17, D-18)</name>
  <files>src/components/me/NotificacoesPanel.tsx, src/pages/MeEspacoPage.tsx, src/pages/NotificacoesHistoricoPage.tsx, src/components/shared/QuotaResendBanner.tsx, src/pages/AdocaoPage.tsx, src/pages/ConfiguracoesPage.tsx, src/router.tsx</files>
  <read_first>
    - src/components/me/PerfilPanel.tsx (full file — pattern do panel framing `bg-card rounded-2xl border p-6`, toast pattern PerfilPanel:38-47)
    - src/pages/MeEspacoPage.tsx (full file — TABS const linhas 21-27, TabsContent layout, useSearchParams para `?tab=`)
    - src/pages/AuditoriaPage.tsx (full file — list + role-gated action pattern; status-color map lines 9-17; Card framing CardHeader/CardTitle/CardContent; ClipboardList icon)
    - src/components/shared/RequireRole.tsx (full file — atLeast / roles props; wrapping pattern para QuotaResendBanner e dropdown filtro)
    - src/hooks/useCurrentRole.ts (full file — `isCoordenadorOrAcima` etc; usar para gate inline)
    - src/router.tsx (full file — linhas 49-84 children CRM; linha 77 pattern Navigate redirect; usar mesmo pattern para `/me/preferencias` → `/me?tab=notificacoes`)
    - src/components/ui/switch.tsx (criado Task 1 — confirmar API: `<Switch checked={x} onCheckedChange={fn} />`)
    - src/components/ui/{card,button,select}.tsx (já existentes — primitives a reusar)
    - src/pages/AdocaoPage.tsx (full file — para identificar onde inserir QuotaResendBanner no topo)
    - src/pages/ConfiguracoesPage.tsx (full file — para identificar onde inserir card de status quota)
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-09 matriz 4×2 com 8 Switch; D-11 histórico dedicado + dropdown coord+; D-15 banner só coord+; D-17 RLS role-aware; D-18 Reenviar sempre visível)
    - .planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md (§NotificacoesPanel, §NotificacoesHistoricoPage, §MODIFY MeEspacoPage TABS, §MODIFY router.tsx, §Role-Gated UI)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (R5 PATCH único atômico; A6 volume <30/dia — UI não precisa paginação MVP)
  </read_first>
  <action>
**`src/components/me/NotificacoesPanel.tsx`:** mirror PATTERNS §NotificacoesPanel.tsx, com matriz 4×2 (8 Switches) + botão Salvar + link "Ver histórico":
- Importar: `useMeuPerfil` (de usePerfis), `usePreferenciasNotif`, `useSalvarPrefs`, `Switch` (de @/components/ui/switch), `Button`, `toast` (sonner), `Link` (react-router-dom), `Bell` (lucide-react).
- Estado local: `const [draft, setDraft] = useState<PreferenciasNotif | null>(null)`; useEffect para inicializar com `prefs` quando carrega.
- Grid 4 linhas (Tarefas, Cadência, Renovação, Indicação) × 2 colunas (Slack, Email) — usar `<table>` semântico ou `<div className="grid grid-cols-3 gap-4">` (1 coluna para label + 2 para switches). Cada Switch: `<Switch checked={draft.tarefa.slack} onCheckedChange={v => setDraft(d => ({ ...d, tarefa: { ...d.tarefa, slack: v }}))} />`. Repetir para os 8.
- Texto explicativo em cima: "Escolha onde receber cada tipo de notificação. Smart default: Email ligado, Slack só se você tem usuário Slack configurado."
- Botão "Salvar" no rodapé: `onClick={() => salvar.mutateAsync({ perfilId: perfil.id, prefs: draft }).then(() => toast.success('Preferências salvas!')).catch(e => toast.error(e.message))}`. Desabilitar se `draft === prefs` (sem mudanças) ou `salvar.isPending`.
- Link no rodapé: `<Link to="/me/notificacoes-historico" className="text-sm text-primary underline">Ver histórico de envios</Link>`.
- Loading state seguindo PerfilPanel:`<div className="text-center py-16 text-fg4">Carregando...</div>`.
- Frame externo: `<div className="bg-card rounded-2xl border p-6">...</div>`.

**`src/pages/MeEspacoPage.tsx`:** modificar TABS const conforme PATTERNS §MODIFY MeEspacoPage — adicionar entrada `{ id: 'notificacoes', label: 'Notificações', icon: Bell }` entre tab 'agenda' e 'perfil'. Importar `Bell` de lucide-react. Adicionar `<TabsContent value="notificacoes"><NotificacoesPanel /></TabsContent>` no JSX. Importar `NotificacoesPanel` de `@/components/me/NotificacoesPanel`.

**`src/pages/NotificacoesHistoricoPage.tsx`:** mirror PATTERNS §NotificacoesHistoricoPage:
- Imports: `useNotificacoesEnvios`, `useReenviarNotificacao`, `useCurrentRole`, `useMeuPerfil`, `usePerfis` (para dropdown), `Card`, `CardHeader`, `CardTitle`, `CardContent`, `Button`, `Select` (shadcn), `RotateCcw`, `ClipboardList`, `Mail`, `MessageSquare` (lucide). Importar `Link` para retorno.
- Estado: `const [filtroPerfilId, setFiltroPerfilId] = useState<string | null>(null)` inicializado para `null` (defaultará para perfil próprio via RLS).
- `const { isCoordenadorOrAcima } = useCurrentRole()`.
- `const { data: meu } = useMeuPerfil()`.
- `const { data: envios, isLoading } = useNotificacoesEnvios(filtroPerfilId)`.
- `const reenviar = useReenviarNotificacao()`.
- Layout: h1 "Histórico de notificações". Se `isCoordenadorOrAcima`, renderizar dropdown "Filtrar por usuário" (Select com options de `usePerfis` listando todos perfis interno; default = "Eu mesmo" → null).
- Card com lista divide-y. Cada row: ícone do canal (Mail/MessageSquare) + tipo + subject + status (cor) + sent_at formatado + botão "Reenviar" (sempre visível per D-18; comparar `env.perfil_id === meu?.id || isCoordenadorOrAcima` para decidir disabled).
- Status color map seguindo AuditoriaPage:9-17: queued=gray, delivered=blue, opened=green, bounced=red, complained=red, dropped_quota=amber, fallback_diretor=amber, failed=red.
- Botão Reenviar: `<Button variant="ghost" size="sm" disabled={reenviar.isPending} onClick={() => reenviar.mutate(env.id)}><RotateCcw className="w-4 h-4 mr-1" />Reenviar</Button>`.
- Loading: "Carregando..."; empty: "Sem envios nos últimos 30 dias.".

**`src/components/shared/QuotaResendBanner.tsx`:** novo componente seguindo PATTERNS §Role-Gated UI:
- Imports: `useQuotaResend`, `AlertTriangle` (lucide).
- `const { data: quota } = useQuotaResend()`.
- Computar `pctHoje = (quota?.hoje ?? 0) / 100`, `pctMes = (quota?.mes ?? 0) / 3000`, `pctMax = Math.max(pctHoje, pctMes)`.
- Se `pctMax <= 0.8` → render `null`.
- Senão: `<div className="bg-amber-100 border border-amber-300 rounded-lg p-3 flex items-center gap-3 text-amber-900 text-sm"><AlertTriangle className="w-5 h-5" />Quota Resend: {quota.hoje}/100 hoje · {quota.mes}/3000 mês. Aproximando do limite — novos e-mails podem ser dropados.</div>`.
- Componente sem RequireRole wrap interno — o caller decide (em AdocaoPage/ConfiguracoesPage envolver com `<RequireRole atLeast="coordenador">`).

**`src/pages/AdocaoPage.tsx`:** importar `QuotaResendBanner` + `RequireRole`. No topo do main content (acima do conteúdo atual), adicionar:
```
<RequireRole atLeast="coordenador">
  <QuotaResendBanner />
</RequireRole>
```

**`src/pages/ConfiguracoesPage.tsx`:** importar `QuotaResendBanner` + `RequireRole`. Adicionar em local apropriado (provavelmente um Card "Status do sistema" ou seção dedicada — planner decide; primary location: topo da página após header).

**`src/router.tsx`:** modificar children CRM conforme PATTERNS §MODIFY router.tsx:
- Adicionar `{ path: 'preferencias', element: <Navigate to="/me?tab=notificacoes" replace /> }` no nível de children CRM (não dentro de /me — `/preferencias` direto também redireciona).
- Adicionar `{ path: 'me/preferencias', element: <Navigate to="/me?tab=notificacoes" replace /> }`.
- Adicionar `{ path: 'me/notificacoes-historico', element: <NotificacoesHistoricoPage /> }` (alternativa: `{ path: 'notificacoes-historico', element: <NotificacoesHistoricoPage /> }` — escolher prefixo /me/ para consistência com decisão D-11).
- Importar `NotificacoesHistoricoPage` lazy se project usa lazy (verificar pattern existente) ou direto.

**Testes obrigatórios (sampling continuity Nyquist 8c):**
- `src/components/me/__tests__/NotificacoesPanel.test.tsx`: render com mock de `usePreferenciasNotif` retornando prefs default; assert: exatamente 8 `Switch` presentes (`screen.getAllByRole('switch').length === 8`); clicar o Switch de "tarefa × email" e clicar Salvar → `useSalvarPrefs.mutateAsync` chamado **1 vez** com objeto contendo `tarefa.email === false` (toggle invertido) e os outros 3 tipos inalterados (D-08 atomicidade — atualiza a coluna inteira, não fragmento).
- `src/pages/__tests__/NotificacoesHistoricoPage.test.tsx`: render com `useCurrentRole` mockado retornando `{ isCoordenadorOrAcima: true }` → dropdown "Filtrar por usuário" aparece (`screen.getByRole('combobox', { name: /filtrar por usuário/i })`); render novamente com `{ isCoordenadorOrAcima: false }` → dropdown NÃO aparece (`queryByRole('combobox') === null`). Garante D-17 role-gate observável.
  </action>
  <verify>
    <automated>npm test -- src/components/me/__tests__/NotificacoesPanel.test.tsx src/pages/__tests__/NotificacoesHistoricoPage.test.tsx &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/me/NotificacoesPanel.tsx` existe (≥60 linhas).
    - `grep -c "Switch" src/components/me/NotificacoesPanel.tsx` retorna ≥8 (8 switches).
    - `grep -c "useSalvarPrefs" src/components/me/NotificacoesPanel.tsx` retorna ≥1.
    - `src/pages/MeEspacoPage.tsx` tem entrada `notificacoes` em TABS (`grep -c "id: 'notificacoes'"` ≥1).
    - `src/pages/NotificacoesHistoricoPage.tsx` existe (≥80 linhas).
    - `grep -c "useReenviarNotificacao" src/pages/NotificacoesHistoricoPage.tsx` retorna ≥1.
    - `grep -c "isCoordenadorOrAcima" src/pages/NotificacoesHistoricoPage.tsx` retorna ≥1 (dropdown role-gated).
    - `src/components/shared/QuotaResendBanner.tsx` existe.
    - `grep -c "QuotaResendBanner" src/pages/AdocaoPage.tsx` retorna ≥1.
    - `grep -c "QuotaResendBanner" src/pages/ConfiguracoesPage.tsx` retorna ≥1.
    - `grep -c "atLeast=\"coordenador\"" src/pages/AdocaoPage.tsx` retorna ≥1 (banner gated).
    - `grep -c "notificacoes-historico" src/router.tsx` retorna ≥1.
    - `grep -c "to=\"/me?tab=notificacoes\"" src/router.tsx` retorna ≥1 (redirect).
    - `src/components/me/__tests__/NotificacoesPanel.test.tsx` existe e passa (Vitest exit 0).
    - `src/pages/__tests__/NotificacoesHistoricoPage.test.tsx` existe e passa (Vitest exit 0).
    - `npm run build` exit 0.
    - `npm run lint` exit 0 (warnings aceitáveis em código não-tocado).
  </acceptance_criteria>
  <done>UI completa para internos — matriz prefs + histórico + reenviar + banner quota. Build + lint verdes. Próxima task cria a edge function reenviar-notificacao e faz smoke E2E.</done>
</task>

<task type="auto">
  <name>Task 4: Edge function reenviar-notificacao + deploy + smoke test UI E2E (per D-18, Open Question 2 Opção A)</name>
  <files>supabase/functions/reenviar-notificacao/index.ts</files>
  <read_first>
    - supabase/functions/_shared/email.ts (criado Plan 1 — sendEmail importado para reuso)
    - supabase/functions/_shared/perfis.ts (criado Plan 1 — findPerfilNome)
    - supabase/functions/_shared/templates/render.ts (criado Plan 1+2 — para re-renderizar baseado em tipo)
    - supabase/functions/notify-tarefa/index.ts (pós-Plan 2 — pattern de auth + serve handler)
    - supabase/functions/_shared/auth.ts
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-18 botão Reenviar sempre visível, colunas reenviado_por_id/em, conta no quota)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (§Open Questions #2 Recomendação Opção A: edge function direta; R8 índice parcial libera reenvios da UNIQUE)
  </read_first>
  <action>
**`supabase/functions/reenviar-notificacao/index.ts`:** edge function nova que recebe `{ id: notifId }` no body (chamada via `supabase.functions.invoke` — o cliente envia automaticamente o JWT do usuário no header Authorization). Lógica:

1. Bootstrap padrão (imports serve/createClient + auth helper).
2. Em vez de `constantTimeAuthCheck`, esta função aceita JWT do usuário (chamada do browser via `functions.invoke`). Criar Supabase client COM o JWT do usuário (não service_role) para que RLS valide quem pode reenviar:
   ```
   const authHeader = req.headers.get('Authorization') ?? ''
   const userJwt = authHeader.replace(/^Bearer\\s+/i, '')
   const supabaseUser = createClient(SUPABASE_URL, ANON_KEY, { global: { headers: { Authorization: `Bearer ${userJwt}` } } })
   const { data: { user } } = await supabaseUser.auth.getUser()
   if (!user) return json({ ok: false, error: 'not authenticated' }, 401)
   ```
3. SELECT a linha original via supabaseUser (RLS: consultor só vê próprias; coord+ vê todas):
   ```
   const { data: original } = await supabaseUser.from('notificacoes_envios').select('*').eq('id', body.id).maybeSingle<NotificacaoEnvio>()
   if (!original) return json({ ok: false, error: 'notificacao não encontrada ou sem permissão' }, 404)
   ```
4. Buscar destinatário original (email do perfil_id):
   ```
   const supabaseAdmin = createClient(SUPABASE_URL, SERVICE_KEY, { auth: { persistSession: false } })
   const { data: perfil } = await supabaseAdmin.from('perfis').select('email, nome').eq('id', original.perfil_id).maybeSingle()
   if (!perfil?.email) return json({ ok: false, error: 'destinatário sem email' }, 400)
   ```
5. Renderizar template apropriado por tipo. Para MVP, o reenvio pode reusar o subject original e gerar HTML genérico com link "Abrir no CRM" para a entidade — ou idealmente buscar entidade e re-renderizar com renderTarefa/renderCadencia/etc. Estratégia simples MVP: reusar subject original + HTML minimalista wrapping (CONSEJ assinatura + link Gerenciar prefs). Documentar como decisão (re-render fiel ao original = nice-to-have v2).
   ```
   const html = `<html><body><p>Você está recebendo este reenvio da notificação original.</p><p><strong>${original.subject}</strong></p><p><a href="${APP_URL}/me/notificacoes-historico">Ver no CRM</a></p><p><a href="${APP_URL}/me?tab=notificacoes">Gerenciar preferências</a></p></body></html>`
   ```
6. Inserir nova linha em notificacoes_envios via supabaseAdmin (service_role para escrever) com `reenviado_por_id = user.id` e `reenviado_em = now()` — índice parcial WHERE reenviado_por_id IS NULL libera da UNIQUE (R8):
   ```
   const { data: newRow, error: insertErr } = await supabaseAdmin.from('notificacoes_envios').insert({
     perfil_id: original.perfil_id,
     tipo: original.tipo,
     entidade_id: original.entidade_id,
     entidade_tipo: original.entidade_tipo,
     canal: 'email',
     subject: original.subject,
     status: 'queued',
     reenviado_por_id: user.id,
     reenviado_em: new Date().toISOString(),
   }).select('id').single()
   if (insertErr) return json({ ok: false, error: insertErr.message }, 500)
   ```
7. Chamar Resend API diretamente (não usar sendEmail() do _shared/ porque ele faz INSERT idempotente que entraria em conflito — reenvio já inseriu a linha). Pattern de fetch igual ao de email.ts (Bearer + retry 3x). Após sucesso: UPDATE newRow.id SET resend_id.

   **Alternativa mais limpa:** refatorar `_shared/email.ts` para extrair função `postToResend(toEmail, subject, html, refId): Promise<{ id?, error? }>` separada do INSERT/UPDATE — reusar aqui. Planner decide entre essa refatoração (mais clean) ou copy do snippet (mais isolado). Recomendação: extrair função pura.

8. Return `{ ok: true, newId: newRow.id }`.

**Deploy:**
```
supabase functions deploy reenviar-notificacao
```

**Smoke test E2E (manual):**
1. Login no CRM como Gabriel (interno).
2. Abrir `/me?tab=notificacoes` — confirmar matriz 4×2 com 8 Switches renderizada; togglar tarefa.email OFF; clicar Salvar → toast.success.
3. Reload página → estado persistido (`tarefa.email = false`).
4. Re-togglar para ON, Salvar.
5. Criar tarefa de teste atribuída a Gabriel — confirmar e-mail chega.
6. Abrir `/me/notificacoes-historico` — confirmar linha aparece com status delivered (ou opened se você abriu o email).
7. Clicar "Reenviar" — toast.success "Notificação reenviada"; nova linha aparece no histórico com `reenviado_por_id` preenchido (verificar via SQL); novo e-mail chega na caixa.
8. (Coord+) Trocar para conta diretor → abrir `/me/notificacoes-historico` → confirmar dropdown "Filtrar por usuário" visível; selecionar outro usuário → ver os envios dele.
9. Confirmar banner quota NÃO aparece em `/adocao` (porque quota está bem abaixo de 80% em smoke); para testar visualmente, inserir manualmente 81 rows fake em notificacoes_envios via SQL e refrescar `/adocao` → banner aparece.
  </action>
  <verify>
    <automated>supabase functions list | grep -c "reenviar-notificacao"</automated>
    <human-check>Smoke E2E completo descrito em "Smoke test E2E" do action — Gabriel salva pref, recebe email, abre histórico, reenvia, vê nova linha + recebe email reenviado. Coord vê dropdown e envios de outros. Banner aparece quando quota>80% (simulado via SQL).</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/reenviar-notificacao/index.ts` existe.
    - `grep -c "reenviado_por_id" supabase/functions/reenviar-notificacao/index.ts` retorna ≥1.
    - `grep -c "reenviado_em" supabase/functions/reenviar-notificacao/index.ts` retorna ≥1.
    - `grep -c "supabaseUser.auth.getUser" supabase/functions/reenviar-notificacao/index.ts` retorna ≥1 (RLS via JWT).
    - `supabase functions list` mostra `reenviar-notificacao`.
    - Smoke E2E (8 passos) completou com sucesso.
  </acceptance_criteria>
  <done>Edge function reenviar-notificacao deployada + smoke E2E mostra UI completa funcionando (prefs salvas, histórico lista, reenviar funciona, dropdown coord+, banner quota). EMAIL-01 e EMAIL-04 100% entregues.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → reenviar-notificacao Edge Function | JWT do usuário no Authorization header; função usa supabase client COM JWT (não service_role) para SELECT inicial → RLS valida quem pode ver/reenviar |
| Browser → Supabase DB (preferencias_notif update) | RLS na perfis bloqueia UPDATE de outro perfil; mutation só atualiza `id = auth.uid()` implicitamente |
| Browser → Supabase RPC quota_resend_atual | RPC SECURITY DEFINER, GRANT EXECUTE para authenticated — visualização do número agregado é benigna |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | "Reenviar" pode ser clicado repetidamente para spam quota Resend | mitigate | (a) Hard quota Resend (100/dia, 3k/mês) é o cap final — pre-check no sendEmail/edge function dropa para `dropped_quota`. (b) Banner UI visível a coord+ quando >80% permite intervir. (c) CONTEXT D-18 decisão explícita: "Sem rate-limit interno de re-envio (hard quota Resend já protege)". Anotar como tech-debt se padrão de abuso emergir. (d) Botão Reenviar mostra `isPending` state — feedback visual reduz fricção mas não previne |
| T-05-04 | Information Disclosure | NotificacoesHistoricoPage consultor vendo envios de outros | mitigate | RLS policy `notif_envios_read` (migration 035) bloqueia SELECT de outros para consultor. Dropdown "Filtrar por usuário" só aparece para coord+ via `useCurrentRole().isCoordenadorOrAcima` E mesmo se um consultor manipular URL/state com perfilId de outro, RLS bloqueia no backend. Test em `tests/rls/notificacoes_envios.test.ts` (Plan 1) já valida |
| T-05-REENVIAR-AUTHZ | Elevation of Privilege | Consultor reenvia notificação de outro usuário via API direta | mitigate | reenviar-notificacao usa supabaseUser (client com JWT do usuário) para o SELECT inicial → RLS bloqueia SELECT se não for próprio OU coord+. Apenas após RLS aprovar, função usa supabaseAdmin para INSERT (porque INSERT em notificacoes_envios é service-role only por design — R6) |
| T-05-PREFS-TAMPER | Tampering | Atacante UPDATE direto em perfis.preferencias_notif de outro perfil | mitigate | RLS em perfis (preexistente) restringe UPDATE a `id = auth.uid()`. Test em `tests/rls/preferencias_notif.test.ts` (Plan 1) cenário "UPDATE de outro perfil falha" valida |
| T-05-OPEN-REDIRECT | Tampering | Magic link "Gerenciar preferências" no email → redirectTo manipulado | accept | redirectTo é gerado server-side em sendEmail (Plan 1), sempre apontando para `${APP_URL}/me?tab=notificacoes` — não é input do usuário. Sem vetor de open-redirect |

</threat_model>

<verification>

1. Testes Wave-0 verdes: `npm test -- src/hooks/__tests__/usePreferenciasNotif.test.tsx src/hooks/__tests__/useReenviarNotificacao.test.tsx`
2. Full suite: `npm test && npm run test:rls` (não quebrar nada do Plan 1+2).
3. Lint: `npm run lint`.
4. Type-check + build: `npm run build`.
5. Deploy: `supabase functions list` mostra `reenviar-notificacao`.
6. Smoke E2E manual (8 passos) — todos OK.

</verification>

<success_criteria>

- [ ] `src/components/ui/switch.tsx` instalado via shadcn CLI.
- [ ] 5 tipos + 3 query-keys appended.
- [ ] 4 hooks criados (usePreferenciasNotif, useNotificacoesEnvios, useReenviarNotificacao, useQuotaResend).
- [ ] 2 testes Wave-0 verdes (usePreferenciasNotif, useReenviarNotificacao).
- [ ] NotificacoesPanel com matriz 4×2 funcional (8 Switches + Save + link histórico).
- [ ] Tab "Notificações" em MeEspacoPage + redirect `/me/preferencias` + `/preferencias`.
- [ ] NotificacoesHistoricoPage com lista 30d + dropdown coord+ + botão Reenviar.
- [ ] QuotaResendBanner visível só para coord+ quando quota >80%, integrado em AdocaoPage + ConfiguracoesPage.
- [ ] Edge function reenviar-notificacao deployada + smoke E2E confirma reenvio cria nova linha com reenviado_por_id/em.
- [ ] Build + lint + testes RLS + testes unit todos verdes.

</success_criteria>

<output>
Create `.planning/phases/05-multi-channel-notifications-email/05-03-SUMMARY.md` documentando:
- Decisões discricionárias (optimistic update sim/não; re-render fiel em reenviar-notificacao vs HTML wrapper genérico)
- Lista final de rotas adicionadas
- Padrões de RLS validados em smoke (consultor → dropdown ausente; coord → presente)
- Screenshots ou descrições visuais da matriz 4×2 + histórico + banner
- Eventuais surpresas (Switch shadcn install behavior, conflitos de import)
</output>