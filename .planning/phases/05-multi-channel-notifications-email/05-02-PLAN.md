---
phase: 05-multi-channel-notifications-email
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - supabase/functions/_shared/slack.ts
  - supabase/functions/_shared/templates/cadencia.html.ts
  - supabase/functions/_shared/templates/renovacao.html.ts
  - supabase/functions/_shared/templates/indicacao.html.ts
  - supabase/functions/_shared/templates/render.ts
  - supabase/functions/notify-resumo-diario/index.ts
  - supabase/functions/notify-indicacao/index.ts
  - supabase/functions/notify-renovacao/index.ts
  - supabase/functions/notify-renovacao/__tests__/handoff.test.ts
  - supabase/functions/resend-webhook/index.ts
  - supabase/functions/resend-webhook/verify.ts
  - supabase/functions/resend-webhook/__tests__/verify.test.ts
  - supabase/functions/resend-webhook/__tests__/handler.test.ts
  - supabase/functions/resend-webhook/__tests__/replay.test.ts
autonomous: false
requirements: [EMAIL-02, EMAIL-03, EMAIL-04]
user_setup:
  - service: resend
    why: "Webhook precisa ser registrado no Resend Dashboard apontando para a edge function deployada — Resend gera `WEBHOOK_RESEND_SECRET` (formato `whsec_xxx`) que precisa ser configurado como Supabase Secret antes do webhook funcionar"
    env_vars:
      - name: WEBHOOK_RESEND_SECRET
        source: "Resend Dashboard → Webhooks → Add endpoint (gerado pelo Resend quando webhook é criado)"
    dashboard_config:
      - task: "Após deploy de resend-webhook, registrar endpoint `https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/resend-webhook` no Resend Dashboard com TODOS os eventos: email.sent, email.delivered, email.opened, email.bounced, email.complained, email.delivery_delayed. Copiar o `whsec_xxx` gerado e rodar `supabase secrets set WEBHOOK_RESEND_SECRET=whsec_xxx`. Validar com 'Send test event' do dashboard."
        location: "https://resend.com/webhooks"

must_haves:
  truths:
    - "Cada um dos 4 tipos de notif (tarefa, cadencia, renovacao, indicacao) dispara e-mail real para o destinatário (ou diretores via fallback) quando prefs.{tipo}.email=true."
    - "Canal Slack broadcast (`SLACK_LEADS_CHANNEL_ID`) continua intocado em notify-indicacao e notify-renovacao (D-01) — per-user (DM + e-mail) é ADIÇÃO, não substituição."
    - "Webhook Resend recebe eventos com signature HMAC válida — payload é processado; signature inválida retorna 401."
    - "Webhook `email.delivered`/`email.opened`/`email.bounced`/`email.complained` atualiza `notificacoes_envios.{status,delivered_at,opened_at,bounced_at,complained_at}` via match `data.email_id ↔ resend_id`."
    - "Status nunca regride: `delivered` chegando após `opened` mantém `opened` (Pitfall 2 — STATUS_RANK ordering)."
    - "Webhook replay protection: `svix-id` repetido OU `svix-timestamp` >5min de skew → 400/401."
    - "notify-renovacao lê `contratos.responsavel_id` no momento do disparo (D-07 hand-off) — sem dupla notificação durante transição de responsável."
    - "Helpers `_shared/slack.ts` (postDm + openDmChannel) extraídos — 4 funções importam, sem código duplicado."
  artifacts:
    - path: "supabase/functions/_shared/slack.ts"
      provides: "postDm + openDmChannel extraídos (parâmetro token explícito, não Deno.env interno)"
      exports: ["postDm", "openDmChannel"]
    - path: "supabase/functions/_shared/templates/render.ts"
      provides: "renderTarefa (existe) + renderCadencia + renderRenovacao + renderIndicacao (novos)"
      exports: ["renderTarefa", "renderCadencia", "renderRenovacao", "renderIndicacao", "escapeHtml"]
    - path: "supabase/functions/resend-webhook/index.ts"
      provides: "Edge function que recebe eventos Resend e atualiza notificacoes_envios"
      contains: "serve(async (req)"
    - path: "supabase/functions/resend-webhook/verify.ts"
      provides: "verifySvixSignature — HMAC manual Web Crypto SHA-256 com base64-decoded secret"
      exports: ["verifySvixSignature"]
  key_links:
    - from: "supabase/functions/notify-resumo-diario/index.ts"
      to: "supabase/functions/_shared/email.ts"
      via: "import sendEmail + Promise.all([postDm, sendEmail])"
      pattern: "sendEmail\\(supabase"
    - from: "supabase/functions/notify-indicacao/index.ts"
      to: "supabase/functions/_shared/email.ts"
      via: "lookup leads.responsavel_id após broadcast Slack + sendEmail per-user"
      pattern: "leads.*responsavel_id"
    - from: "supabase/functions/notify-renovacao/index.ts"
      to: "supabase/functions/_shared/email.ts"
      via: "lookup contratos.responsavel_id (D-07 momento do disparo) + sendEmail per-user"
      pattern: "contratos.*responsavel_id"
    - from: "supabase/functions/resend-webhook/index.ts"
      to: "notificacoes_envios"
      via: "UPDATE WHERE resend_id = payload.data.email_id, monotonic STATUS_RANK"
      pattern: "data\\.email_id"
---

<objective>
Propagar a fatia de e-mail para os outros 3 tipos de notificação (cadência, renovação, indicação) e ativar **tracking automático de delivery** via webhook Resend. Plan 1 já entregou Tarefa + helpers + migration; Plan 2 fecha o loop: todos os 4 tipos disparam e-mail, e o status sobe automaticamente para `delivered`/`opened`/`bounced` via webhook.

**Cobre requirements:** EMAIL-02 (completa — agora cobrindo 4 tipos não só tarefa), EMAIL-03 (completa — todas as 4 funções com prefs lookup + D-03 paralelo + D-05 fallback + D-07 hand-off), EMAIL-04 (parcial — backend de webhook + tracking; UI no Plan 3).

**Purpose:** Sem o webhook, `notificacoes_envios.status` fica em `queued` permanentemente — usuário não vê se e-mail foi entregue. Plan 2 é o que torna o histórico do Plan 3 informativo. Refactor oportunista de `_shared/slack.ts` agora porque 4 funções estão sendo tocadas — momento natural de deduplicar (Q5 recomendação).

**Output:** 3 funções refatoradas + 1 nova função `resend-webhook` + helper `_shared/slack.ts` + 3 templates HTML + 4 testes Wave-0 + webhook registrado no Resend Dashboard + secret `WEBHOOK_RESEND_SECRET` provisionado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md
@.planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md
@.planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md
@.planning/phases/05-multi-channel-notifications-email/05-VALIDATION.md
@.planning/phases/05-multi-channel-notifications-email/05-01-SUMMARY.md
@supabase/functions/notify-resumo-diario/index.ts
@supabase/functions/notify-indicacao/index.ts
@supabase/functions/notify-renovacao/index.ts
@supabase/functions/slack-commands/index.ts
@supabase/functions/_shared/auth.ts
@supabase/functions/_shared/perfis.ts
@supabase/functions/_shared/email.ts
@supabase/functions/_shared/templates/render.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extrair _shared/slack.ts + criar 3 templates HTML + extender render.ts (por D-22, Q5)</name>
  <files>supabase/functions/_shared/slack.ts, supabase/functions/_shared/templates/cadencia.html.ts, supabase/functions/_shared/templates/renovacao.html.ts, supabase/functions/_shared/templates/indicacao.html.ts, supabase/functions/_shared/templates/render.ts</files>
  <read_first>
    - supabase/functions/notify-tarefa/index.ts (post-Plan 1 — lines 98-140 originais com openDmChannel/postDm que ficaram inline; agora extrair)
    - supabase/functions/notify-resumo-diario/index.ts (lines 64-106 — postDm duplicado idêntico exceto comments)
    - supabase/functions/_shared/templates/tarefa.html.ts (criado no Plan 1 — pattern base a replicar para os 3 novos)
    - supabase/functions/_shared/templates/render.ts (criado no Plan 1 — pattern de renderTarefa a estender com 3 funções novas)
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-22 templates específicos com header+título+corpo+CTA+footer; PT-BR; cor primary; deep-link Abrir no CRM)
    - .planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md (§_shared/slack.ts — código exato; §templates — render helper pattern)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (Q5 §Duplicação digna de extração; Pitfall 1 sender restrito; §Security escapeHtml)
  </read_first>
  <behavior>
    - `_shared/slack.ts::postDm(token, slackUserId, text, blocks)` exportado, recebe token como PARÂMETRO (não closure module-level — PATTERNS §Pattern divergence).
    - `_shared/slack.ts::openDmChannel(token, slackUserId)` exportado, mesmo princípio.
    - Comportamento idêntico ao original notify-tarefa:114-140 (retry 3x, 500*2^i backoff, return `{ ok, ts?, error? }`).
    - `render.ts::renderCadencia({ nomeConsultor, totalTarefas, totalLeads, deepLink, gerenciarPrefsLink })` substitui placeholders escapando textos.
    - `render.ts::renderRenovacao({ nomeResponsavel, nomeCliente, diasAteRenovacao, valorContrato, deepLink, gerenciarPrefsLink })` substitui placeholders.
    - `render.ts::renderIndicacao({ nomeResponsavel, nomeIndicante, nomeIndicado, segmento, deepLink, gerenciarPrefsLink })` substitui placeholders.
    - Todos os 3 templates HTML compartilham layout base (header + título + corpo + CTA + footer) mas conteúdo customizado por tipo.
  </behavior>
  <action>
**`supabase/functions/_shared/slack.ts`:** copiar EXATAMENTE o bloco openDmChannel + postDm de PATTERNS §_shared/slack.ts (que reflete notify-tarefa:114-140). Divergência crítica: aceitar `token` como primeiro parâmetro de ambas funções (PATTERNS explicitamente: "don't read Deno.env inside helper — keeps the helper portable"). Sem closure sobre SLACK_BOT_TOKEN.

**`supabase/functions/_shared/templates/cadencia.html.ts`:** estrutura idêntica a tarefa.html.ts (header CONSEJ + cor primary + título + corpo + CTA + footer com link Gerenciar preferências). Placeholders: `{{nomeConsultor}}`, `{{totalTarefas}}`, `{{totalLeads}}`, `{{deepLink}}`, `{{gerenciarPrefsLink}}`. Título sugerido: "Resumo diário — Você tem {{totalTarefas}} tarefas e {{totalLeads}} leads ativos". CTA: "Abrir Meu Espaço" linkando para `{{deepLink}}` (= `${APP_URL}/me`).

**`supabase/functions/_shared/templates/renovacao.html.ts`:** placeholders: `{{nomeResponsavel}}`, `{{nomeCliente}}`, `{{diasAteRenovacao}}`, `{{valorContrato}}` (formatado em BRL pelo caller), `{{deepLink}}`, `{{gerenciarPrefsLink}}`. Título: "Renovação aproximando — Cliente {{nomeCliente}} em {{diasAteRenovacao}} dias". CTA: "Abrir contrato" → `{{deepLink}}` (= `${APP_URL}/clientes/{contrato.cliente_id}` ou `/contratos/{contrato.id}` — caller decide).

**`supabase/functions/_shared/templates/indicacao.html.ts`:** placeholders: `{{nomeResponsavel}}`, `{{nomeIndicante}}`, `{{nomeIndicado}}`, `{{segmento}}`, `{{deepLink}}`, `{{gerenciarPrefsLink}}`. Título: "Nova indicação — {{nomeIndicante}} indicou {{nomeIndicado}}". CTA: "Abrir lead" → `{{deepLink}}` (= `${APP_URL}/leads/{lead.id}`).

**`supabase/functions/_shared/templates/render.ts`** (extender — manter `renderTarefa` + `escapeHtml` do Plan 1): adicionar exports:
- `renderCadencia(vars)` importa `cadencia.html.ts` e faz `.replaceAll('{{var}}', escapeHtml(vars.var))` para textos e `.replaceAll('{{deepLink}}', vars.deepLink)` para URLs.
- `renderRenovacao(vars)` mesma estrutura.
- `renderIndicacao(vars)` mesma estrutura.
Mantém o pattern de NÃO escapar URLs (`deepLink`, `gerenciarPrefsLink`).

Adicionar testes em `supabase/functions/_shared/__tests__/templates.test.ts` (estender o arquivo do Plan 1): 1 teste por novo render verificando placeholder substitution + HTML escape em pelo menos um campo de texto.

**NÃO modificar notify-tarefa nesta task** — atualização para importar de `_shared/slack.ts` acontece na Task 2 junto com refactor das outras 3 funções (fica num único PR coeso).
  </action>
  <verify>
    <automated>npm test -- supabase/functions/_shared/__tests__/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/functions/_shared/slack.ts` existe.
    - `grep -cE "export (async )?function (postDm|openDmChannel)" supabase/functions/_shared/slack.ts` retorna 2.
    - `grep -c "token: string" supabase/functions/_shared/slack.ts` retorna ≥1 (token como parâmetro).
    - `grep -c "Deno.env" supabase/functions/_shared/slack.ts` retorna 0 (não lê env diretamente).
    - 3 arquivos `supabase/functions/_shared/templates/{cadencia,renovacao,indicacao}.html.ts` existem.
    - `grep -cE "export (function )?render(Cadencia|Renovacao|Indicacao)" supabase/functions/_shared/templates/render.ts` retorna 3.
    - Teste `templates.test.ts` cobre os 3 novos renders + escape — passa (vitest exit 0).
  </acceptance_criteria>
  <done>Helper `_shared/slack.ts` extraído + 3 templates HTML + 3 funções de render adicionadas. Pronto para Task 2 refatorar as 3 funções edge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refatorar notify-resumo-diario + notify-indicacao + notify-renovacao + notify-tarefa (slack.ts import) (per D-01, D-03, D-05, D-07, Q5)</name>
  <files>supabase/functions/notify-tarefa/index.ts, supabase/functions/notify-resumo-diario/index.ts, supabase/functions/notify-indicacao/index.ts, supabase/functions/notify-renovacao/index.ts, supabase/functions/notify-renovacao/__tests__/handoff.test.ts</files>
  <read_first>
    - supabase/functions/notify-tarefa/index.ts (estado pós-Plan 1 — atualizar imports para usar `_shared/slack.ts`)
    - supabase/functions/notify-resumo-diario/index.ts (full file — já tem `payload.perfil_id`; adicionar companion email após postDm; usar `tipo='cadencia'`, entidade=NULL)
    - supabase/functions/notify-indicacao/index.ts (full file — lines 19-30 auth duplicado; line 218 postToSlack do broadcast; ADICIONAR per-user lookup APÓS broadcast: SELECT lead.responsavel_id + sendEmail per-user; broadcast preservado D-01)
    - supabase/functions/notify-renovacao/index.ts (full file — line 216 postToSlack broadcast; ADICIONAR lookup contratos.responsavel_id via hydrateContrato existente (linhas 75-100) + sendEmail per-user; broadcast preservado D-01)
    - supabase/functions/_shared/slack.ts (criado Task 1)
    - supabase/functions/_shared/email.ts (criado Plan 1)
    - supabase/functions/_shared/perfis.ts (criado Plan 1 — loadPrefs, findSlackUserId, findPerfilNome, findDiretores)
    - supabase/functions/_shared/templates/render.ts (criado Plan 1+Task 1)
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-01 broadcast preservado; D-02 destinatários por entidade; D-03 paralelo; D-05 fallback diretor; D-07 hand-off responsavel atual)
    - .planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md (§MODIFY notify-resumo-diario, §MODIFY notify-indicacao, §MODIFY notify-renovacao)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (Q5 mudanças por função; A6 volume <30/dia válido; R3 generateLink rate-limit mitigation para notify-resumo-diario que pode disparar batch)
  </read_first>
  <behavior>
    - notify-tarefa (já refatorada Plan 1) atualiza imports: linha que tem `function postDm` / `function openDmChannel` inline removidas; substituir por `import { postDm } from '../_shared/slack.ts'` + chamadas passam `SLACK_BOT_TOKEN!` como primeiro arg.
    - notify-resumo-diario: após postDm (que continua intacto), adicionar lookup do email do perfil + loadPrefs(perfil_id) + se `prefs.cadencia.email` → renderCadencia + sendEmail. tipo='cadencia', entidade_id=NULL, entidade_tipo=NULL.
    - notify-indicacao: lines 215-220 onde acontece `postToSlack(blocks)` (broadcast canal) — preservar 100%. APÓS o broadcast: `SELECT id, responsavel_id, nome FROM leads WHERE id = $lead_id`; targets = responsavel_id ? [responsavel_id] : findDiretores ids; loop: loadPrefs + se email ON → renderIndicacao + sendEmail; tipo='indicacao', entidade_id=lead.id, entidade_tipo='lead'.
    - notify-renovacao: estender hydrateContrato existente (linhas 75-100) para incluir `responsavel_id` no SELECT. APÓS postToSlack broadcast: targets = contrato.responsavel_id ? [responsavel_id] : findDiretores; loop sendEmail tipo='renovacao', entidade_id=contrato.id, entidade_tipo='contrato'. **D-07 hand-off:** o SELECT em hydrateContrato é executado no momento do disparo do cron — o `responsavel_id` lido é o ATUAL, não snapshot. Não armazenar/cachear o responsavel_id antes do dispatch.
    - Test handoff.test.ts: simular cenário onde contratos.responsavel_id muda entre cron schedule e dispatch — confirmar que sendEmail é chamado com perfilId == valor atual (não o anterior). Mock do supabase.from('contratos').select retorna o valor corrente no momento da chamada.
  </behavior>
  <action>
**notify-tarefa (cleanup do Plan 1):** apenas atualizar imports:
- Remover funções inline `openDmChannel`/`postDm` deixadas no arquivo.
- Adicionar `import { postDm } from '../_shared/slack.ts'`.
- Atualizar callsites: `postDm(SLACK_BOT_TOKEN!, slackUserId!, text, blocks)`.

**notify-resumo-diario:** aplicar refactor seguindo PATTERNS §MODIFY notify-resumo-diario:
1. Substituir imports: `import { constantTimeAuthCheck } from '../_shared/auth.ts'`, `import { postDm } from '../_shared/slack.ts'`, `import { loadPrefs, findPerfilNome } from '../_shared/perfis.ts'`, `import { sendEmail } from '../_shared/email.ts'`, `import { renderCadencia } from '../_shared/templates/render.ts'`.
2. Remover definições locais duplicadas (constantTimeAuthCheck, findPerfilName/Nome inline, openDmChannel/postDm).
3. Após o postDm bem-sucedido (manter como está), buscar email do perfil + prefs:
   ```
   const { data: perfil } = await supabase.from('perfis').select('email, nome').eq('id', payload.perfil_id).maybeSingle()
   const prefs = await loadPrefs(supabase, payload.perfil_id)
   if (perfil?.email && prefs?.cadencia?.email) {
     // R3 mitigation: para resumo diário que pode disparar batch, gerar magic link sequencial com sleep entre cada (ou usar fallback ${APP_URL}/me?tab=notificacoes direto se volume >25/dia)
     const html = renderCadencia({ nomeConsultor: perfil.nome ?? 'Consultor', totalTarefas: payload.totalTarefas, totalLeads: payload.totalLeads, deepLink: `${APP_URL}/me`, gerenciarPrefsLink: `${APP_URL}/me?tab=notificacoes` })
     await sendEmail(supabase, { perfilId: payload.perfil_id, toEmail: perfil.email, tipo: 'cadencia', entidadeId: null, entidadeTipo: null, subject: `Resumo diário — ${payload.totalTarefas} tarefas hoje`, html })
   }
   ```
   Decisão R3: dado volume CONSEJ <30/dia (STATE assumption A6), gerar magic link via admin.generateLink (Q7 opção a) é seguro. Caso volume cresça >25/dia, downgrade para fallback link direto.

**notify-indicacao:** aplicar refactor seguindo PATTERNS §MODIFY notify-indicacao:
1. Mesmos imports + remover duplicados.
2. **Preservar 100%** o broadcast `postToSlack` existente (D-01) — não tocar nessa parte.
3. APÓS o broadcast retornar:
   ```
   if (ind.lead_id) {
     const { data: lead } = await supabase.from('leads').select('id, responsavel_id, nome, segmento').eq('id', ind.lead_id).maybeSingle()
     const targetIds = lead?.responsavel_id ? [lead.responsavel_id] : (await findDiretores(supabase)).map(d => d.id)
     for (const perfilId of targetIds) {
       const { data: perfil } = await supabase.from('perfis').select('email, nome').eq('id', perfilId).maybeSingle()
       if (!perfil?.email) continue
       const prefs = await loadPrefs(supabase, perfilId)
       if (!prefs?.indicacao?.email) continue
       const html = renderIndicacao({
         nomeResponsavel: perfil.nome ?? 'Consultor',
         nomeIndicante: ind.indicante_nome ?? 'Cliente',
         nomeIndicado: lead.nome,
         segmento: lead.segmento ?? '-',
         deepLink: `${APP_URL}/leads/${lead.id}`,
         gerenciarPrefsLink: `${APP_URL}/me?tab=notificacoes`,
       })
       const status = lead.responsavel_id ? 'queued' : 'fallback_diretor'  // info status quando é fallback
       await sendEmail(supabase, { perfilId, toEmail: perfil.email, tipo: 'indicacao', entidadeId: lead.id, entidadeTipo: 'lead', subject: `Nova indicação: ${lead.nome}`, html })
       // Quando fallback, opcionalmente INSERT extra row com status='fallback_diretor' para auditoria (decisão discricionária)
     }
   }
   ```

**notify-renovacao:** aplicar refactor seguindo PATTERNS §MODIFY notify-renovacao:
1. Mesmos imports + remover duplicados.
2. Estender `hydrateContrato` (linhas 75-100) para incluir `responsavel_id, cliente:cliente_id(nome)` no SELECT.
3. **D-07 hand-off:** documentar com comentário inline `// D-07: lê responsavel_id atual no momento do disparo (cron) — sem snapshot`.
4. **Preservar 100%** o postToSlack broadcast (D-01).
5. APÓS broadcast:
   ```
   const targetIds = contrato.responsavel_id ? [contrato.responsavel_id] : (await findDiretores(supabase)).map(d => d.id)
   for (const perfilId of targetIds) {
     const { data: perfil } = await supabase.from('perfis').select('email, nome').eq('id', perfilId).maybeSingle()
     if (!perfil?.email) continue
     const prefs = await loadPrefs(supabase, perfilId)
     if (!prefs?.renovacao?.email) continue
     const html = renderRenovacao({
       nomeResponsavel: perfil.nome ?? 'Consultor',
       nomeCliente: contrato.cliente?.nome ?? 'Cliente',
       diasAteRenovacao: payload.dias_antes,
       valorContrato: contrato.valor != null ? `R$ ${contrato.valor.toLocaleString('pt-BR')}` : '-',
       deepLink: `${APP_URL}/clientes/${contrato.cliente_id}`,
       gerenciarPrefsLink: `${APP_URL}/me?tab=notificacoes`,
     })
     await sendEmail(supabase, { perfilId, toEmail: perfil.email, tipo: 'renovacao', entidadeId: contrato.id, entidadeTipo: 'contrato', subject: `Renovação em ${payload.dias_antes} dias: ${contrato.cliente?.nome}`, html })
   }
   ```

**Test handoff.test.ts:** mock supabase tal que `from('contratos').select(...).eq('id', X).maybeSingle()` retorna contrato com `responsavel_id='novo_resp'` — verificar que sendEmail é chamado com `perfilId='novo_resp'` (não algum valor cacheado). Pattern de mock: assertion sobre args do `sendEmail` mock.
  </action>
  <verify>
    <automated>npm test -- supabase/functions/notify-renovacao/__tests__/handoff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '../_shared/slack.ts'" supabase/functions/notify-tarefa/index.ts` retorna ≥1.
    - `grep -c "from '../_shared/email.ts'" supabase/functions/notify-resumo-diario/index.ts` retorna ≥1.
    - `grep -c "from '../_shared/email.ts'" supabase/functions/notify-indicacao/index.ts` retorna ≥1.
    - `grep -c "from '../_shared/email.ts'" supabase/functions/notify-renovacao/index.ts` retorna ≥1.
    - `grep -c "renderCadencia" supabase/functions/notify-resumo-diario/index.ts` retorna ≥1.
    - `grep -c "renderIndicacao" supabase/functions/notify-indicacao/index.ts` retorna ≥1.
    - `grep -c "renderRenovacao" supabase/functions/notify-renovacao/index.ts` retorna ≥1.
    - `grep -c "responsavel_id" supabase/functions/notify-indicacao/index.ts` retorna ≥1 (lookup D-02).
    - `grep -c "responsavel_id" supabase/functions/notify-renovacao/index.ts` retorna ≥1 (lookup D-02 + D-07).
    - `grep -c "findDiretores" supabase/functions/notify-indicacao/index.ts` retorna ≥1 (fallback D-05).
    - `grep -c "findDiretores" supabase/functions/notify-renovacao/index.ts` retorna ≥1.
    - `grep -cE "^(function|const) (postDm|openDmChannel|constantTimeAuthCheck)\\b" supabase/functions/notify-resumo-diario/index.ts` retorna 0 (helpers velhos removidos).
    - `grep -cE "^(function|const) (postDm|openDmChannel|constantTimeAuthCheck)\\b" supabase/functions/notify-indicacao/index.ts` retorna 0.
    - `grep -cE "^(function|const) (postDm|openDmChannel|constantTimeAuthCheck)\\b" supabase/functions/notify-renovacao/index.ts` retorna 0.
    - Teste handoff.test.ts passa (vitest exit 0).
  </acceptance_criteria>
  <done>3 funções edge refatoradas — todas as 4 notif funções agora usam helpers `_shared/`, disparam e-mail per-user paralelo a Slack, fazem fallback diretor, e notify-renovacao lê responsavel atual (D-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Edge function resend-webhook + HMAC verify + handler com STATUS_RANK monotonic + dedup svix-id (per D-16, Q2, Pitfalls 2/3)</name>
  <files>supabase/functions/resend-webhook/index.ts, supabase/functions/resend-webhook/verify.ts, supabase/functions/resend-webhook/__tests__/verify.test.ts, supabase/functions/resend-webhook/__tests__/handler.test.ts, supabase/functions/resend-webhook/__tests__/replay.test.ts</files>
  <read_first>
    - supabase/functions/slack-commands/index.ts (full file — lines 84-115 pattern HMAC verify; lines 117-140 replay window 5min; estrutura serve handler completa)
    - supabase/functions/notify-renovacao/index.ts (lines 162-232 — handler shape: auth check → JSON parse → DB UPDATE → return JSON)
    - supabase/functions/_shared/auth.ts (criado Plan 1 — usado APENAS como fallback Bearer; assinatura HMAC primary)
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-16 webhook + secret + tracking completo)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (Q2 HMAC manual completo lines 236-318; Pitfall 2 STATUS_RANK ordering; Pitfall 3 webhook duplicate; payload shape `data.email_id`)
    - .planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md (§resend-webhook — composite analogs slack-commands + notify-renovacao; Resend/Svix divergence base string + base64 decode secret)
  </read_first>
  <behavior>
    - Test verify.test.ts cenário "assinatura válida": secret `whsec_<base64>`, body raw `{"type":"email.delivered","data":{"email_id":"r1"}}`, svix-id `evt_1`, svix-timestamp `1700000000`, signature = `v1,<HMAC SHA-256 base64 do signedContent>` — `verifySvixSignature` retorna true.
    - Test verify.test.ts cenário "assinatura inválida": signature alterada → retorna false.
    - Test verify.test.ts cenário "múltiplas assinaturas com 1 válida": header `"v1,wrong v1,valid"` → retorna true (Q2 suporte a rotação).
    - Test verify.test.ts cenário "timestamp >5min": svix-timestamp <(now-300s) → função `isReplayValid` retorna false.
    - Test handler.test.ts cenário "email.delivered": POST com payload `{ type: 'email.delivered', data: { email_id: 'r1' }, created_at: '2026-05-28T10:00:00Z' }`, sig válida → handler UPDATE notificacoes_envios SET status='delivered', delivered_at='2026-05-28T10:00:00Z' WHERE resend_id='r1'; retorna 200.
    - Test handler.test.ts cenário "email.opened": similar, atualiza status='opened', opened_at.
    - Test handler.test.ts cenário "email.bounced": status='bounced', bounced_at, error_msg=`data.bounce.message`.
    - Test handler.test.ts cenário "STATUS_RANK não regride (Pitfall 2)": linha existente tem status='opened' (rank 2); chega email.delivered (rank 1) → handler NÃO atualiza status; retorna 200.
    - Test replay.test.ts cenário "svix-id repetido": primeiro evento com svix-id='evt_1' aplica; segundo com mesmo svix-id é idempotente (UPDATE com mesmo timestamp é naturalmente idempotente — Pitfall 3); ambos retornam 200.
    - Test handler.test.ts cenário "signature inválida": retorna 401.
  </behavior>
  <action>
**`supabase/functions/resend-webhook/verify.ts`:** implementar conforme RESEARCH Q2 lines 250-278:
```
import { decode as b64decode, encode as b64encode } from 'https://deno.land/std@0.224.0/encoding/base64.ts'

export async function verifySvixSignature(
  body: string,
  svixId: string,
  svixTimestamp: string,
  svixSignatureHeader: string,
  secret: string,  // formato whsec_xxx
): Promise<boolean> {
  const keyB64 = secret.replace(/^whsec_/, '')
  const keyBytes = b64decode(keyB64)
  const signedContent = `${svixId}.${svixTimestamp}.${body}`
  const key = await crypto.subtle.importKey('raw', keyBytes, { name: 'HMAC', hash: 'SHA-256' }, false, ['sign'])
  const sig = await crypto.subtle.sign('HMAC', key, new TextEncoder().encode(signedContent))
  const computed = b64encode(new Uint8Array(sig))

  const provided = svixSignatureHeader.split(' ')
    .filter(s => s.startsWith('v1,'))
    .map(s => s.slice(3))

  return provided.some(p => constantTimeEquals(computed, p))
}

function constantTimeEquals(a: string, b: string): boolean {
  if (a.length !== b.length) return false
  let diff = 0
  for (let i = 0; i < a.length; i++) diff |= a.charCodeAt(i) ^ b.charCodeAt(i)
  return diff === 0
}

export function isReplayValid(svixTimestamp: string, maxSkewSeconds = 300): boolean {
  const ts = Number(svixTimestamp)
  if (!Number.isFinite(ts)) return false
  const now = Math.floor(Date.now() / 1000)
  return Math.abs(now - ts) <= maxSkewSeconds
}
```

**`supabase/functions/resend-webhook/index.ts`:** seguindo PATTERNS §resend-webhook composite:
```
import { serve } from 'https://deno.land/std@0.224.0/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
import { verifySvixSignature, isReplayValid } from './verify.ts'

const WEBHOOK_RESEND_SECRET = Deno.env.get('WEBHOOK_RESEND_SECRET')!
const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!
const SERVICE_KEY = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
const supabase = createClient(SUPABASE_URL, SERVICE_KEY, { auth: { persistSession: false } })

const STATUS_RANK: Record<string, number> = {
  queued: 0,
  delivered: 1,
  opened: 2,
  bounced: 99,
  complained: 99,
  failed: 99,
}

function json(body: unknown, status = 200): Response {
  return new Response(JSON.stringify(body), { status, headers: { 'Content-Type': 'application/json' } })
}

serve(async (req) => {
  if (req.method !== 'POST') return json({ ok: false, error: 'method not allowed' }, 405)

  const svixId = req.headers.get('svix-id') ?? ''
  const svixTs = req.headers.get('svix-timestamp') ?? ''
  const svixSig = req.headers.get('svix-signature') ?? ''
  const body = await req.text()

  if (!isReplayValid(svixTs)) return json({ ok: false, error: 'timestamp skew' }, 401)
  if (!(await verifySvixSignature(body, svixId, svixTs, svixSig, WEBHOOK_RESEND_SECRET))) {
    return json({ ok: false, error: 'invalid signature' }, 401)
  }

  let payload: { type: string; created_at: string; data: { email_id: string; bounce?: { message?: string } } }
  try { payload = JSON.parse(body) } catch { return json({ ok: false, error: 'invalid json' }, 400) }

  const { type, created_at, data } = payload
  if (!data?.email_id) return json({ ok: true, skipped: 'no email_id' })

  // Buscar row atual para enforcement de STATUS_RANK monotonic (Pitfall 2)
  const { data: current } = await supabase
    .from('notificacoes_envios')
    .select('id, status')
    .eq('resend_id', data.email_id)
    .maybeSingle<{ id: string; status: string }>()
  if (!current) return json({ ok: true, skipped: 'unknown resend_id' })

  let newStatus: string | null = null
  const updates: Record<string, unknown> = {}
  switch (type) {
    case 'email.sent':       /* equivale ao queued — ignore */ return json({ ok: true, skipped: 'email.sent' })
    case 'email.delivered':  newStatus = 'delivered';  updates.delivered_at = created_at; break
    case 'email.opened':     newStatus = 'opened';     updates.opened_at = created_at; break
    case 'email.bounced':    newStatus = 'bounced';    updates.bounced_at = created_at; updates.error_msg = data.bounce?.message ?? null; break
    case 'email.complained': newStatus = 'complained'; updates.complained_at = created_at; break
    case 'email.delivery_delayed': return json({ ok: true, skipped: 'delivery_delayed' })
    case 'email.clicked':    return json({ ok: true, skipped: 'clicked' })  // MVP ignora
    default:                 return json({ ok: true, skipped: `unknown type: ${type}` })
  }

  // STATUS_RANK monotonic: só atualizar se novo rank > rank atual
  if (newStatus && STATUS_RANK[newStatus] > (STATUS_RANK[current.status] ?? 0)) {
    updates.status = newStatus
  }

  if (Object.keys(updates).length === 0) {
    return json({ ok: true, skipped: 'status regress prevented' })
  }

  const { error } = await supabase.from('notificacoes_envios').update(updates).eq('id', current.id)
  if (error) return json({ ok: false, error: error.message }, 500)

  return json({ ok: true, applied: updates })
})
```

**Testes:** 3 arquivos em `supabase/functions/resend-webhook/__tests__/`:
- `verify.test.ts`: 4 cenários (válida, inválida, múltiplas com 1 válida, timestamp skew). Gerar assinatura válida em test via mesmo algoritmo (importar verifySvixSignature do próprio módulo seria circular — implementar gerador HMAC paralelo ou pré-computar fixtures).
- `handler.test.ts`: 5 cenários (delivered/opened/bounced/STATUS_RANK regress/signature inválida). Mock supabase client com `.from('notificacoes_envios').select.eq.maybeSingle()` e `.update.eq()`. Mock crypto via `globalThis.crypto.subtle` (Deno suporta nativamente em vitest? — se não, mock o módulo `./verify.ts` inteiro com `vi.mock`).
- `replay.test.ts`: 1 cenário svix-id repetido — confirmar que segundo processamento é idempotente (mesmo updates aplicados não geram erro; UPDATE é naturalmente idempotente per Pitfall 3).

**Não criar tabela auxiliar `notificacoes_webhook_eventos`** nesta fase (RESEARCH Q2 menciona como opcional; MVP confia em idempotência natural do UPDATE — Pitfall 3 confirma que isso é suficiente).
  </action>
  <verify>
    <automated>npm test -- supabase/functions/resend-webhook/__tests__/verify.test.ts supabase/functions/resend-webhook/__tests__/handler.test.ts supabase/functions/resend-webhook/__tests__/replay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/resend-webhook/index.ts` existe.
    - `supabase/functions/resend-webhook/verify.ts` existe.
    - `grep -c "verifySvixSignature" supabase/functions/resend-webhook/verify.ts` retorna ≥1.
    - `grep -c "isReplayValid" supabase/functions/resend-webhook/verify.ts` retorna ≥1.
    - `grep -c "crypto.subtle" supabase/functions/resend-webhook/verify.ts` retorna ≥1 (Web Crypto, não hand-rolled).
    - `grep -c "constantTimeEquals" supabase/functions/resend-webhook/verify.ts` retorna ≥1.
    - `grep -c "STATUS_RANK" supabase/functions/resend-webhook/index.ts` retorna ≥1 (Pitfall 2).
    - `grep -c "data.email_id" supabase/functions/resend-webhook/index.ts` retorna ≥1 (A7 confirmado).
    - `grep -c "email.delivered" supabase/functions/resend-webhook/index.ts` retorna ≥1.
    - `grep -c "email.opened" supabase/functions/resend-webhook/index.ts` retorna ≥1.
    - `grep -c "email.bounced" supabase/functions/resend-webhook/index.ts` retorna ≥1.
    - 3 testes verdes (vitest exit 0).
  </acceptance_criteria>
  <done>Edge function resend-webhook criada com HMAC verify, replay protection, STATUS_RANK monotonic, dedup natural via UPDATE idempotente. 3 testes Wave-0 verdes.</done>
</task>

<task type="auto">
  <name>Task 4: Deploy 4 funções edge + configurar webhook no Resend Dashboard + setar WEBHOOK_RESEND_SECRET</name>
  <files>(nenhum arquivo modificado — apenas operações de deploy/config)</files>
  <read_first>
    - supabase/functions/notify-tarefa/index.ts (verificar refactor pós-Task 2)
    - supabase/functions/notify-resumo-diario/index.ts (verificar refactor)
    - supabase/functions/notify-indicacao/index.ts (verificar refactor)
    - supabase/functions/notify-renovacao/index.ts (verificar refactor)
    - supabase/functions/resend-webhook/index.ts (verificar criação)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (R4 webhook race — registrar APÓS migration 035 estável; Q3 Supabase Secrets)
  </read_first>
  <action>
1. **Deploy todas as funções edge (em ordem — resend-webhook por último para evitar R4 race):**
   ```
   supabase functions deploy notify-tarefa
   supabase functions deploy notify-resumo-diario
   supabase functions deploy notify-indicacao
   supabase functions deploy notify-renovacao
   supabase functions deploy resend-webhook
   ```
   Cada um retorna `Deployed Function <name> on project wfnriqwkzdazdbuzbyug.`. Se algum falhar com erro de import de `_shared/`, verificar que helpers existem nos paths esperados (Supabase Edge Runtime resolve relative imports automaticamente).

2. **Registrar webhook no Resend Dashboard (manual):**
   - Abrir https://resend.com/webhooks
   - Clicar "Add endpoint"
   - URL: `https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/resend-webhook`
   - Events: marcar TODOS — `email.sent`, `email.delivered`, `email.opened`, `email.bounced`, `email.complained`, `email.delivery_delayed`, `email.clicked` (handler ignora os que não interessam; melhor marcar todos para não perder evento futuro)
   - Salvar
   - Copiar o "Signing Secret" gerado (formato `whsec_xxxxxxxx`)

3. **Provisionar secret no Supabase:**
   ```
   supabase secrets set WEBHOOK_RESEND_SECRET=whsec_xxxxxxxx
   ```
   Para verificar: `supabase secrets list` deve mostrar `WEBHOOK_RESEND_SECRET` na lista (valor mascarado).

4. **Redeploy resend-webhook** após setar secret (Supabase requer redeploy para a função ler o novo env):
   ```
   supabase functions deploy resend-webhook
   ```

5. **Smoke test webhook:**
   - No Resend Dashboard, clicar "Send test event" no endpoint registrado.
   - Verificar logs: `supabase functions logs resend-webhook --tail 20` — deve mostrar processamento (response 200 ou 200 com `skipped: 'unknown resend_id'` se o test event não bater com nenhuma row real).
   - Criar tarefa real (atribuir a outro usuário com email pré-cadastrado), aguardar ~30s, conferir via SQL: `SELECT id, status, resend_id, delivered_at FROM notificacoes_envios ORDER BY sent_at DESC LIMIT 1;` — `status` deve ter subido de `queued` para `delivered` (ou `opened` se você abriu o email).

Se webhook falhar com signature inválida nos logs, conferir que `WEBHOOK_RESEND_SECRET` no Supabase é EXATAMENTE o `whsec_xxx` do Resend Dashboard (sem espaços, prefix intacto).
  </action>
  <verify>
    <automated>supabase functions list | grep -E "(notify-tarefa|notify-resumo-diario|notify-indicacao|notify-renovacao|resend-webhook)" | wc -l</automated>
    <human-check>Após Resend Dashboard "Send test event" + criar tarefa real: logs de resend-webhook mostram requests entrando + linha em notificacoes_envios subiu para `status='delivered'` em <30s.</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase functions list` mostra 5 funções: notify-tarefa, notify-resumo-diario, notify-indicacao, notify-renovacao, resend-webhook (todas com `Updated` recente).
    - `supabase secrets list` mostra `WEBHOOK_RESEND_SECRET` (valor mascarado, presente).
    - Resend Dashboard mostra o endpoint registrado com signing secret ativo.
    - Smoke test: criar tarefa real → e-mail chega + `notificacoes_envios.status` sobe para `delivered` em ≤30s (confirmado via SQL).
    - `supabase functions logs resend-webhook --tail 30` mostra ≥1 evento processado com response 200.
  </acceptance_criteria>
  <done>4 funções refatoradas + resend-webhook deployadas; webhook registrado no Resend; secret provisionado; smoke test E2E mostra status delivered automático. EMAIL-02/03 completas no backend; EMAIL-04 backend completo (UI no Plan 3).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resend infra → resend-webhook Edge Function | Resend POST com headers `svix-id`, `svix-timestamp`, `svix-signature`; signature HMAC-SHA256 sobre `${svixId}.${svixTs}.${body}` com secret base64-decoded |
| 4 notify-* Edge Functions → Resend API | Bearer RESEND_API_KEY (mesma de Plan 1) |
| Edge Functions → Supabase DB | service_role bypassa RLS para UPDATE em notificacoes_envios via resend-webhook; INSERT em notify-* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing + Repudiation | Webhook signature bypass / replay | mitigate | (a) HMAC SHA-256 obrigatório via `verifySvixSignature` — sem fallback inseguro, response 401 se header ausente ou inválido. (b) Replay window 5min via `isReplayValid(svixTimestamp)` — payloads antigos rejeitados (Q2 + slack-commands pattern). (c) constant-time compare via `constantTimeEquals` impede timing-attack na assinatura |
| T-05-02 | Tampering | Templates HTML (cadencia, renovacao, indicacao) — placeholders com dados de banco | mitigate | TODOS os campos de texto passam por `escapeHtml` em `render.ts` antes de `replaceAll`. URLs (`deepLink`, `gerenciarPrefsLink`) ficam sem escape mas são construídas por nós (não user input direto). Test `templates.test.ts` valida escape para cada render |
| T-05-04 | Information Disclosure | notify-indicacao lookup de leads.responsavel_id — vazamento de leads para diretor via fallback | accept | Diretores já têm acesso a TODOS os leads via RLS (role-aware). Fallback diretor é dentro do mesmo tenant — não cross-tenant. Status `fallback_diretor` deixa rastro auditável (Plan 3) |
| T-05-06 | Denial of Service | notify-resumo-diario cron pode disparar batch de magic links generateLink → estourar rate limit Supabase (R3 ~30/min) | mitigate | Volume CONSEJ <30/dia validated (STATE A6) — sob o limite. Se volume crescer >25/dia: downgrade para fallback link direto `${APP_URL}/me?tab=notificacoes` (sem token) per Q7 opção (c). Anotar como tech-debt para revisitar em milestone v4.0 |
| T-05-MIG-RACE | Tampering (R4) | Webhook reentrante DURANTE migration 035 → deadlock UPDATE | mitigate | **Ordem garantida pela Task 4:** migration já está aplicada (Plan 1); webhook só é REGISTRADO no Dashboard após deploy de resend-webhook (Task 4 step 2). Antes disso, Resend não tem para onde POSTar. Documentar no runbook |
| T-05-WEBHOOK-INJECT | Tampering | Atacante POSTa para `/functions/v1/resend-webhook` com payload forjado fingindo ser Resend | mitigate | HMAC verify rejeita qualquer request sem assinatura válida; secret nunca expõe (vive só em Supabase Secrets + Resend Dashboard). Atacante teria que comprometer Resend ou Supabase para forjar — fora do threat model do MVP |
| T-05-STATUS-REGRESS | Tampering (Pitfall 2) | Race entre `email.opened` e `email.delivered` regredindo status | mitigate | `STATUS_RANK` ordering no handler: só atualiza status se novo rank > rank atual; opened (rank 2) não é regredido por delivered (rank 1). Test `handler.test.ts` cenário "STATUS_RANK não regride" valida |
| T-05-WEBHOOK-DUP | Tampering (Pitfall 3) | Resend reenvia mesmo evento por network failure → contadores inflam | mitigate | UPDATE é naturalmente idempotente (mesmo timestamp aplicado 2x = mesmo resultado). Sem contador agregado — sem caminho para inflar. Test `replay.test.ts` cenário "svix-id repetido" valida |

</threat_model>

<verification>

1. Testes Wave-0 verdes: `npm test -- supabase/functions/resend-webhook/__tests__/ supabase/functions/notify-renovacao/__tests__/handoff.test.ts supabase/functions/_shared/__tests__/templates.test.ts`
2. Testes existentes ainda verdes (não quebramos nada): `npm test`
3. Lint: `npm run lint` → 0 errors.
4. Type-check: `npm run build` → 0 errors.
5. Deploy: 5 funções listadas em `supabase functions list` com timestamp recente.
6. Secret: `supabase secrets list` mostra `WEBHOOK_RESEND_SECRET`.
7. Webhook registrado no Resend Dashboard com signing secret.
8. Smoke E2E: tarefa nova → e-mail recebido → status sobe para `delivered` automaticamente em ≤30s.

</verification>

<success_criteria>

- [ ] `_shared/slack.ts` extraído (postDm + openDmChannel com token como parâmetro).
- [ ] 3 templates HTML adicionais (cadencia, renovacao, indicacao) + 3 render functions estendendo render.ts.
- [ ] 4 funções notify-* refatoradas para usar `_shared/*` (zero código duplicado de constantTimeAuthCheck/postDm/findSlackUserId entre as 4).
- [ ] notify-indicacao adiciona per-user lookup (leads.responsavel_id) + companion email PRESERVANDO broadcast Slack.
- [ ] notify-renovacao adiciona per-user lookup (contratos.responsavel_id no momento do disparo — D-07) + companion email PRESERVANDO broadcast.
- [ ] notify-resumo-diario adiciona companion email (perfil_id já existe).
- [ ] Edge function resend-webhook implementada com HMAC verify + isReplayValid + STATUS_RANK monotonic.
- [ ] 4 testes Wave-0 verdes: verify.test.ts, handler.test.ts, replay.test.ts, handoff.test.ts.
- [ ] Templates.test.ts cobrindo os 3 novos renders também verde.
- [ ] 5 funções deployadas (`supabase functions list`).
- [ ] Webhook registrado no Resend Dashboard apontando para resend-webhook URL.
- [ ] `WEBHOOK_RESEND_SECRET` provisionado em Supabase Secrets.
- [ ] Smoke E2E: tarefa → e-mail → status `delivered` automático em ≤30s.

</success_criteria>

<output>
Create `.planning/phases/05-multi-channel-notifications-email/05-02-SUMMARY.md` documentando:
- Lista final de funções deployadas + timestamps
- `whsec_xxx` registrado (REDACTED — apenas confirmação de provisionamento; NUNCA commitar o valor)
- Decisões discricionárias (notify-indicacao: status='fallback_diretor' extra row sim/não; magic link sequencial vs fallback link direto em notify-resumo-diario)
- Tempo end-to-end medido (criar tarefa → status delivered)
- Eventuais surpresas (ex: Resend test event UUID não bate com resend_id real — comportamento esperado de skip)
</output>