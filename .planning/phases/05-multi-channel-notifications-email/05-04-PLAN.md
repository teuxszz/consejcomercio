---
phase: 05-multi-channel-notifications-email
plan: 04
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - src/pages/portal/PortalPreferenciasPage.tsx
  - src/pages/portal/PortalLayout.tsx
  - src/router.tsx
autonomous: true
requirements: [EMAIL-01]
user_setup: []

must_haves:
  truths:
    - "Cliente loga no portal e vê item de navegação 'Preferências' no PortalLayout."
    - "Cliente clica 'Preferências' → abre `/portal/preferencias` com placeholder UI explicando que notificações de documentos virão na Phase 7."
    - "Cliente pode togglar Switch de email para tipo `documentos` e salvar — preferência grava em `perfis.preferencias_notif` (mesma coluna do CRM, scope reduzido a 1 linha × 1 coluna)."
    - "RLS bloqueia cliente de ler/editar prefs de outro perfil (preexistente — pattern Plan 1)."
    - "Interno acessando o Portal via account switcher (D-09 / Sidebar dropdown) também vê a rota Preferências (não causa erro)."
  artifacts:
    - path: "src/pages/portal/PortalPreferenciasPage.tsx"
      provides: "Página Portal com Switch único (documentos × email) + nota explicativa Phase 7"
      min_lines: 50
  key_links:
    - from: "src/pages/portal/PortalLayout.tsx"
      to: "/portal/preferencias"
      via: "NAV item { to: '/portal/preferencias', label: 'Preferências', icon: BellRing }"
      pattern: "/portal/preferencias"
    - from: "src/router.tsx"
      to: "src/pages/portal/PortalPreferenciasPage.tsx"
      via: "{ path: 'preferencias', element: <PortalPreferenciasPage /> } no children de /portal"
      pattern: "PortalPreferenciasPage"
---

<objective>
Construir a **infraestrutura de preferências para clientes do Portal** — UI mínima (1 Switch para tipo `documentos` × canal email) que prepara o terreno para Phase 7 (aprovação de documentos) ligar notificações reais. Em Plan 5 ainda não dispara nenhuma notif real ao cliente — esta página é placeholder funcional.

**Cobre requirements:** EMAIL-01 (completa do ponto de vista do Portal — internos foram cobertos no Plan 3; cliente fica preparado aqui).

**Purpose:** Decisão D-10 + Specifics deixaram explícito: "Cria UI de `/portal/preferencias` mesmo sem notif ativa ainda — Phase 5 prepara o terreno; Phase 7 plugará as notifs reais." Sem este plan, Phase 7 vai ter que entregar tanto a feature de docs quanto a UI de preferências — fragmenta o trabalho. Plan 4 é pequeno (1 arquivo principal + 2 modificações) mas isola o trabalho Portal do trabalho CRM em PRs distintos para review.

**Output:** 1 página + 1 link nav + 1 rota.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md
@.planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md
@.planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md
@.planning/phases/05-multi-channel-notifications-email/05-01-SUMMARY.md
@.planning/phases/05-multi-channel-notifications-email/05-03-SUMMARY.md
@src/pages/portal/PortalLayout.tsx
@src/pages/portal/PortalWalletPage.tsx
@src/components/me/NotificacoesPanel.tsx
@src/hooks/usePreferenciasNotif.ts
@src/router.tsx
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PortalPreferenciasPage + nav item PortalLayout + rota router (per D-10)</name>
  <files>src/pages/portal/PortalPreferenciasPage.tsx, src/pages/portal/PortalLayout.tsx, src/router.tsx</files>
  <read_first>
    - src/pages/portal/PortalLayout.tsx (full file — pattern NAV const lines 14-19; estilo dark-theme inline `#00081d` background, `#6bd0e7` accents, `rgba(0,137,172,0.x)` borders)
    - src/pages/portal/PortalWalletPage.tsx (full file — pattern de page Portal com inline-styled dark theme; replicar mesma estética)
    - src/components/me/NotificacoesPanel.tsx (criado Plan 3 — pattern de Switch + Save + useSalvarPrefs; replicar lógica reduzida para 1 toggle)
    - src/hooks/usePreferenciasNotif.ts (criado Plan 3 — useSalvarPrefs aceita objeto inteiro de PreferenciasNotif, preservar outras chaves quando salvar do Portal)
    - src/router.tsx (full file — children portal lines ~89-94; pattern de adição de rota)
    - src/types/index.ts (PreferenciasNotif do Plan 3 — Plan 4 NÃO modifica essa estrutura; cliente usa as 4 chaves existentes; Phase 7 pode estender com chave nova `documentos` se necessário, mas por ora MVP reusa `indicacao` como proxy OU mostra placeholder informativo)
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-10 UI reduzida + scope to define; Specifics "Phase 7 plugará as notifs reais"; Open Question 3 RESEARCH: cliente não recebe e-mail real em Phase 5)
    - .planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md (§PortalPreferenciasPage — portal page conventions sem tokens; reduced matrix; nota Phase 7)
  </read_first>
  <action>
**Decisão estrutural primeiro:** Como a `PreferenciasNotif` interface (Plan 3) tem 4 chaves fixas (tarefa, cadencia, renovacao, indicacao) — todas relativas a fluxos INTERNOS — qual chave o cliente toggleia? Opções:

(a) **Adicionar chave `documentos`** ao interface PreferenciasNotif + ao DEFAULT da migration 035 — porém modificar a migration já aplicada exige NOVA migration. Limpo mas custoso.

(b) **Reusar chave existente como proxy** — confuso, viola decisão D-10 ("tipos relevantes pra cliente").

(c) **Placeholder visual sem persistência** — Switch desabilitado com tooltip "Disponível na Phase 7"; sem mutation.

(d) **Placeholder visual com persistência LOCAL** (localStorage) — Switch funcional mas estado apenas no browser do cliente; quando Phase 7 ligar, migra para coluna.

**Recomendação:** **Opção (c) — Switch DESABILITADO com tooltip explicativo + copy clara**. Razão: o objetivo D-10 + Specifics é "prepara o terreno" — não há requisito de persistir prefs em Phase 5 porque NENHUMA notif real ao cliente é disparada em Phase 5 (Open Question 3 RESEARCH confirma). Persistir teria que ser desfeito ou migrado em Phase 7. Switch visualmente desabilitado documenta a intenção sem criar tech-debt de migração.

Plan 4 portanto **NÃO modifica `src/types/index.ts` nem `src/hooks/usePreferenciasNotif.ts`** — escopo enxuto. Phase 7 adicionará a chave `documentos` (migration 036), atualizará o tipo, e ativará a UI.

**`src/pages/portal/PortalPreferenciasPage.tsx`:** mirror PATTERNS §PortalPreferenciasPage usando estilo inline-dark do Portal (replicar tokens de PortalWalletPage.tsx — `#00081d` bg, `#6bd0e7` accents, `rgba(0,137,172,0.3)` borders).

Estrutura sugerida (~50 linhas):
- Imports: `Switch` (@/components/ui/switch), `Bell` ou `BellRing` (lucide).
- Sem hooks de dados nesta versão MVP.
- Container com mesmo styling do PortalWalletPage (verificar lines 36-50).
- H1: "Preferências de notificação".
- Parágrafo explicativo (PT-BR): "Em breve você poderá escolher como receber notificações sobre documentos enviados, propostas para aprovar e atualizações do seu projeto. Esta seção é preparatória — as notificações ao cliente serão ativadas na Phase 7 (Portal do Cliente — Documentos)."
- Card/box estilo Portal com Switch desabilitado:
  - Label: "Receber e-mails sobre documentos pendentes de aprovação"
  - `<Switch disabled checked={false} />` + texto opacidade reduzida.
  - Tooltip ou texto pequeno: "Disponível na próxima fase."
- Nota inferior: "Outros canais (Push, WhatsApp) chegarão em fases futuras conforme cronograma."

**`src/pages/portal/PortalLayout.tsx`:** modificar NAV const conforme PATTERNS §MODIFY PortalLayout — adicionar:
```
{ to: '/portal/preferencias', label: 'Preferências', icon: BellRing },
```
Importar `BellRing` de lucide-react se ausente.

**`src/router.tsx`:** adicionar dentro de children `/portal`:
```
{ path: 'preferencias', element: <PortalPreferenciasPage /> },
```
Importar `PortalPreferenciasPage`.

**Comentário de tech-debt:** adicionar comentário no topo de `PortalPreferenciasPage.tsx`:
```
// Phase 5 (Plan 4): UI placeholder. Phase 7 (Portal Docs) habilitará:
//  - Migration 036 adiciona chave 'documentos' em perfis.preferencias_notif
//  - Switch passa a ser controlado via usePreferenciasNotif + useSalvarPrefs (mesmo pattern de NotificacoesPanel)
//  - Notify-cliente-aprovacao edge function ativa o disparo real
```
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run lint</automated>
    <human-check>Login como cliente no Portal → navegação mostra "Preferências"; clicar → página carrega com Switch desabilitado e copy explicativa em PT-BR. Login como interno via account switcher → também vê a página sem erro.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/pages/portal/PortalPreferenciasPage.tsx` existe (≥50 linhas).
    - `grep -c "Phase 7" src/pages/portal/PortalPreferenciasPage.tsx` retorna ≥1 (copy ou comentário).
    - `grep -c "Switch" src/pages/portal/PortalPreferenciasPage.tsx` retorna ≥1.
    - `grep -c "disabled" src/pages/portal/PortalPreferenciasPage.tsx` retorna ≥1 (Switch desabilitado per decisão (c)).
    - `grep -c "/portal/preferencias" src/pages/portal/PortalLayout.tsx` retorna ≥1.
    - `grep -c "BellRing" src/pages/portal/PortalLayout.tsx` retorna ≥1.
    - `grep -c "PortalPreferenciasPage" src/router.tsx` retorna ≥1.
    - `npm run build` exit 0.
    - `npm run lint` exit 0.
    - Smoke manual confirma navegação e copy visíveis.
  </acceptance_criteria>
  <done>Portal Preferências MVP entregue como placeholder funcional + nav item + rota. Tech-debt explícito para Phase 7 documentado em comentário inline. Phase 5 EMAIL-01..04 completos.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente browser → PortalLayout/Router | Cliente autenticado via Supabase Auth (tipo='cliente'); PortalLayout (Plan 4 não modifica guarda) verifica sessão; cliente forçado ao Portal pelo AppLayout do CRM |
| Cliente browser → Supabase DB (read prefs futuro) | Plan 4 não faz query ainda. Quando Phase 7 ativar, RLS em perfis.preferencias_notif (preexistente — Plan 1 cobriu) bloqueia leitura/UPDATE de outro perfil |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Cliente vendo prefs de outro cliente via URL/state manipulation | accept (Plan 4 escopo) | Switch é DESABILITADO + sem mutation nesta fase. Sem query carregando prefs de outro perfil possível. RLS preexistente (validado Plan 1 Task 1) já garante para quando Phase 7 ativar |
| T-05-PORTAL-XSS | Tampering | Página Portal com copy hard-coded estática (sem placeholders de user input) | accept | Sem `dangerouslySetInnerHTML`; sem dados dinâmicos renderizados. Static copy PT-BR sem vetor de injection |
| T-05-PORTAL-AUTH-DRIFT | Elevation of Privilege | Interno navegando para `/portal/preferencias` via account switcher acessando funcionalidade não-cliente | accept | Página é informativa apenas, não revela dados de outros perfis. Account switcher (CRM ↔ Portal) é pattern já estabelecido (Sidebar dropdown D-09) — Plan 4 não introduz nova superfície |

**Nota geral:** Plan 4 é o mais leve da phase em termos de threat surface — UI estática sem persistência nem queries. Reaver threats em Phase 7 quando data flow real for ativado (notif-cliente-aprovacao + migration 036 + mutation real).

</threat_model>

<verification>

1. Build: `npm run build` → exit 0.
2. Lint: `npm run lint` → exit 0.
3. Manual: login como cliente (qualquer cliente ativo, ex.: criar via Supabase Studio se necessário) → ver nav "Preferências" no Portal → página abre com Switch desabilitado.
4. Manual: login como interno → account switcher para Portal → mesma navegação visível, página acessível.
5. Sem regressão em outras páginas Portal: PortalWalletPage, PortalCatalogoPage, etc. continuam funcionando.

</verification>

<success_criteria>

- [ ] `PortalPreferenciasPage.tsx` criada com Switch desabilitado + copy explicativa Phase 7.
- [ ] Nav item "Preferências" adicionado em PortalLayout NAV.
- [ ] Rota `/portal/preferencias` registrada em router.tsx.
- [ ] Comentário tech-debt no topo da página documenta o que Phase 7 fará.
- [ ] Build + lint verdes.
- [ ] Smoke manual confirma navegação + visualização para cliente E interno via account switcher.

</success_criteria>

<output>
Create `.planning/phases/05-multi-channel-notifications-email/05-04-SUMMARY.md` documentando:
- Decisão final entre opções (a/b/c/d) e por quê (esperado: (c) Switch desabilitado)
- Tech-debt explícito para Phase 7 (migration 036 + chave documentos + mutation real + edge function notify-cliente-aprovacao)
- Confirmação de smoke manual (cliente vs interno)
- Phase 5 milestone: 4 requirements EMAIL-01..04 entregues, pronto para `/gsd-verify-work` ou `/gsd-ship-milestone`
</output>