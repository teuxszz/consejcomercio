---
phase: 05-multi-channel-notifications-email
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/035_notificacoes_envios.sql
  - supabase/functions/_shared/auth.ts
  - supabase/functions/_shared/perfis.ts
  - supabase/functions/_shared/email.ts
  - supabase/functions/_shared/templates/tarefa.html.ts
  - supabase/functions/_shared/templates/render.ts
  - supabase/functions/_shared/__tests__/email.test.ts
  - supabase/functions/_shared/__tests__/email.quota.test.ts
  - supabase/functions/_shared/__tests__/email.fallback.test.ts
  - supabase/functions/_shared/__tests__/templates.test.ts
  - supabase/functions/notify-tarefa/index.ts
  - supabase/functions/notify-tarefa/__tests__/self-loop.test.ts
  - supabase/functions/notify-tarefa/__tests__/parallel.test.ts
  - tests/rls/preferencias_notif.test.ts
  - tests/rls/notificacoes_envios.test.ts
  - vitest.config.ts
autonomous: false
requirements: [EMAIL-01, EMAIL-02, EMAIL-03]
user_setup:
  - service: resend
    why: "Sender restrito a `onboarding@resend.dev` em sandbox — só envia para e-mails verificados na Audience da conta Resend (R1 do RESEARCH). Sem pré-cadastro, prod falha 403 silencioso."
    env_vars:
      - name: RESEND_API_KEY
        source: "Já configurada como Supabase Secret em 2026-05-27 (ver STATE.md)"
    dashboard_config:
      - task: "Pré-cadastrar e-mails internos CONSEJ ativos em Resend → Audiences → Add test email (mínimo Gabriel + diretores + qualquer interno que será destinatário no MVP). Sem isso, Resend retorna 403 para e-mails não-verificados enquanto o sender for `onboarding@resend.dev`."
        location: "Resend Dashboard → Audiences"

must_haves:
  truths:
    - "Migration 035 está aplicada (schema notificacoes_envios + coluna perfis.preferencias_notif + trigger handle_new_user + backfill smart default + RPC quota_resend_atual)."
    - "Atribuir uma tarefa a outro usuário (criador != atribuido) dispara e-mail via Resend para o atribuído quando prefs.tarefa.email=true."
    - "Atribuir tarefa a si mesmo (criado_por_id == atribuido_a_id) NÃO dispara nem Slack nem e-mail (D-06 self-loop)."
    - "Quando responsavel_id é NULL no destinatário de notify-tarefa, fallback para todos os perfis com role='diretor' (D-05) e linha registrada com status='fallback_diretor'."
    - "Helper sendEmail retorna status='dropped_quota' sem chamar Resend quando quota diária (100) ou mensal (3000) estouradas (D-13/D-14)."
    - "Helper sendEmail retorna status='skipped_idempotent' em segundo INSERT com mesmo (perfil_id, tipo, entidade_id, canal='email', dia) — UNIQUE constraint atômica (D-21 / Pitfall 6)."
    - "RLS em notificacoes_envios: consultor SELECT só vê próprias (perfil_id = auth.uid()); coordenador+ vê todas via is_at_least('coordenador') (D-19)."
    - "RLS em perfis.preferencias_notif: usuário lê/atualiza só a própria; INSERT/UPDATE direto via anon falha sem service_role."
  artifacts:
    - path: "supabase/migrations/035_notificacoes_envios.sql"
      provides: "Schema notificacoes_envios + coluna perfis.preferencias_notif + trigger update + RPC quota_resend_atual + RLS policies"
      contains: "CREATE TABLE notificacoes_envios"
    - path: "supabase/functions/_shared/auth.ts"
      provides: "constantTimeAuthCheck extraído (export)"
      exports: ["constantTimeAuthCheck"]
    - path: "supabase/functions/_shared/perfis.ts"
      provides: "loadPrefs, findSlackUserId, findPerfilNome, findDiretores"
      exports: ["loadPrefs", "findSlackUserId", "findPerfilNome", "findDiretores"]
    - path: "supabase/functions/_shared/email.ts"
      provides: "sendEmail helper completo (quota pre-check + idempotent INSERT + Resend POST + status UPDATE)"
      exports: ["sendEmail", "SendEmailParams", "SendEmailResult"]
    - path: "supabase/functions/_shared/templates/render.ts"
      provides: "renderTarefa + escapeHtml"
      exports: ["renderTarefa", "escapeHtml"]
    - path: "supabase/functions/_shared/templates/tarefa.html.ts"
      provides: "Template HTML PT-BR para notificação de tarefa (header + título + corpo + CTA + footer com magic-link prefs)"
      exports: ["default"]
    - path: "supabase/functions/notify-tarefa/index.ts"
      provides: "Função notify-tarefa refatorada com self-loop suppression + lookup prefs + Promise.all([postDm, sendEmail])"
  key_links:
    - from: "supabase/functions/notify-tarefa/index.ts"
      to: "supabase/functions/_shared/email.ts"
      via: "import { sendEmail } from '../_shared/email.ts'"
      pattern: "sendEmail\\(supabase, \\{"
    - from: "supabase/functions/_shared/email.ts"
      to: "https://api.resend.com/emails"
      via: "fetch with Authorization: Bearer + body { from, to, subject, html, headers: { X-Entity-Ref-ID } }"
      pattern: "api\\.resend\\.com/emails"
    - from: "supabase/functions/_shared/email.ts"
      to: "notificacoes_envios"
      via: "supabase.from('notificacoes_envios').insert(...).select('id').single() then update(resend_id)"
      pattern: "notificacoes_envios"
    - from: "supabase/functions/notify-tarefa/index.ts"
      to: "perfis.preferencias_notif"
      via: "loadPrefs(supabase, perfilId) → prefs.tarefa.{slack,email}"
      pattern: "prefs\\?\\.tarefa\\?\\.(slack|email)"
---

<objective>
Entregar a **primeira fatia vertical end-to-end** de e-mail: quando uma tarefa é atribuída a outro usuário com `prefs.tarefa.email=true`, um e-mail real sai via Resend e é registrado em `notificacoes_envios` com status `queued` (depois sobe via webhook no Plan 2). Implementa migration 035 completa (schema + RPC quota + trigger smart default + backfill), helpers `_shared/` (`auth`, `perfis`, `email`, `templates/`), refatora `notify-tarefa` (self-loop D-06 + lookup prefs D-08 + paralelo D-03 + fallback diretor D-05), instala os 5 arquivos de teste Wave-0 desta fatia, e aplica a migration no Supabase remoto via `supabase db push`.

**Cobre requirements:** EMAIL-01 (parcial — schema e RLS de preferências; UI no Plan 3), EMAIL-02 (helper sendEmail end-to-end via D-20), EMAIL-03 (parcial — notify-tarefa per D-03 + D-05 + D-06; cadência/renovação/indicação no Plan 2).

**Purpose:** Validar a pipeline inteira (DB → helper → Resend → log) com a função mais simples (notify-tarefa) antes de propagar pras outras três no Plan 2. Risco de Plan 1 (primeira API key em prod + primeira migration grande) isolado em PR menor → rollback rápido se algo falhar.

**Output:** Migration aplicada + 7 arquivos `_shared/` criados + `notify-tarefa` refatorada e deployada + 5 testes Wave-0 verdes + ≥1 e-mail real chegando na caixa de Gabriel após task de fumaça.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md
@.planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md
@.planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md
@.planning/phases/05-multi-channel-notifications-email/05-VALIDATION.md
@supabase/migrations/011_auto_profile_trigger.sql
@supabase/migrations/026_renovacao_notif.sql
@supabase/migrations/029_rls_role_aware.sql
@supabase/migrations/034_cron_resumo_diario.sql
@supabase/functions/notify-tarefa/index.ts
@supabase/functions/notify-renovacao/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 035 — schema notificacoes_envios + preferencias_notif + RPC + RLS + trigger smart default + backfill (per D-19, D-23, Q4)</name>
  <files>supabase/migrations/035_notificacoes_envios.sql, tests/rls/preferencias_notif.test.ts, tests/rls/notificacoes_envios.test.ts</files>
  <read_first>
    - supabase/migrations/011_auto_profile_trigger.sql (full file — pattern do trigger `handle_new_user` CREATE OR REPLACE FUNCTION com SECURITY DEFINER + SET search_path)
    - supabase/migrations/026_renovacao_notif.sql (full file — pattern de tabela idempotency + RLS `is_interno()`)
    - supabase/migrations/029_rls_role_aware.sql (full file — declarações de `is_at_least(role text)`, `is_diretor()`, `can_see_perfil(target_id)` que serão consumidas)
    - supabase/migrations/030_perfis_slack.sql (coluna slack_user_id em perfis — referenciada no smart default)
    - supabase/migrations/034_cron_resumo_diario.sql (última migration aplicada — confirmar formato/ordering)
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-04 smart default, D-19 schema, D-23 migration scope)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (Q4 migration completa, Q8 query/index quota, Pitfall 4 NULL em UNIQUE, R8 índice parcial reenvio, R6 service-role-only INSERT)
    - .planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md (§Migration RLS Helpers Already Present, §Idempotent Insert Pattern)
    - tests/rls/ (qualquer arquivo .test.ts existente — pattern de setup vitest + Supabase clientes com `anon` vs `service_role`)
  </read_first>
  <behavior>
    - Test 1 (preferencias_notif RLS): consultor autenticado lê própria coluna `preferencias_notif`; tentativa de UPDATE de outro perfil falha (RLS bloqueia).
    - Test 2 (preferencias_notif default): perfil recém-criado via trigger tem `preferencias_notif.tarefa.email = true`, `tarefa.slack = false` (porque `slack_user_id IS NULL` no INSERT inicial — D-04 smart default).
    - Test 3 (notificacoes_envios RLS read): consultor com 2 envios próprios + 3 de outros vê apenas os 2 próprios (perfil_id = auth.uid()); diretor vê todos os 5 (is_at_least('coordenador') true).
    - Test 4 (notificacoes_envios RLS write): INSERT como cliente `anon` ou `authenticated` (sem service_role) falha (sem policy permissiva); INSERT como service_role passa (R6).
    - Test 5 (notificacoes_envios UNIQUE idempotência): segundo INSERT com mesma (perfil_id, tipo, entidade_id, canal='email', dia) viola constraint com SQLSTATE 23505; mesma sequência com `reenviado_por_id IS NOT NULL` na 2ª linha NÃO viola (índice parcial WHERE reenviado_por_id IS NULL — R8).
    - Test 6 (notificacoes_envios UNIQUE com NULL): duas linhas com `entidade_id=NULL` mas mesmo (perfil_id, tipo='cadencia', canal='email', dia) DEVEM colidir (COALESCE no índice — Pitfall 4).
    - Test 7 (RPC quota_resend_atual): após inserir 3 linhas canal='email' status='queued' hoje + 2 com status='dropped_quota' hoje, `SELECT quota_resend_atual()` retorna `{ hoje: 3, mes: 3 }` (dropped não conta — Q8).
  </behavior>
  <action>
Criar `supabase/migrations/035_notificacoes_envios.sql` numerada como 035 (próxima após 034_cron_resumo_diario.sql, confirmado via Glob no RESEARCH Q4) contendo, na ordem:

1. **`ALTER TABLE perfis ADD COLUMN preferencias_notif jsonb NOT NULL DEFAULT`** com objeto JSON contendo as 4 chaves (`tarefa`, `cadencia`, `renovacao`, `indicacao`) cada uma `{ "slack": false, "email": true }` (default conservador; backfill abaixo recalcula condicional ao slack_user_id por linha existente).

2. **`CREATE TABLE notificacoes_envios`** com EXATAMENTE estas colunas e tipos (per D-19): `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `perfil_id uuid NOT NULL REFERENCES perfis(id) ON DELETE CASCADE`, `tipo text NOT NULL CHECK (tipo IN ('tarefa','cadencia','renovacao','indicacao'))`, `entidade_id uuid`, `entidade_tipo text CHECK (entidade_tipo IN ('lead','cliente','contrato','tarefa') OR entidade_tipo IS NULL)`, `canal text NOT NULL CHECK (canal IN ('email','slack'))`, `subject text`, `status text NOT NULL CHECK (status IN ('queued','delivered','opened','bounced','complained','dropped_quota','skipped_no_recipient','fallback_diretor','failed'))`, `resend_id text`, `slack_ts text`, `error_msg text`, `sent_at timestamptz NOT NULL DEFAULT now()`, `delivered_at timestamptz`, `opened_at timestamptz`, `bounced_at timestamptz`, `complained_at timestamptz`, `reenviado_por_id uuid REFERENCES perfis(id)`, `reenviado_em timestamptz`, `dia date GENERATED ALWAYS AS (sent_at::date) STORED`.

3. **Índice de idempotência (NÃO usar CONSTRAINT UNIQUE direto — usar índice parcial conforme Pitfall 4 + R8):** `CREATE UNIQUE INDEX notif_envios_unique_idempotency ON notificacoes_envios (perfil_id, tipo, canal, dia, COALESCE(entidade_id, '00000000-0000-0000-0000-000000000000'::uuid)) WHERE reenviado_por_id IS NULL;`. Reenvios ficam fora do índice — sempre cria nova linha.

4. **Índices auxiliares (per Q8):**
   - `CREATE INDEX idx_notif_envios_quota ON notificacoes_envios (canal, dia, status);`
   - `CREATE INDEX idx_notif_envios_perfil_sent ON notificacoes_envios (perfil_id, sent_at DESC);`
   - `CREATE INDEX idx_notif_envios_resend ON notificacoes_envios (resend_id) WHERE resend_id IS NOT NULL;`

5. **`ALTER TABLE notificacoes_envios ENABLE ROW LEVEL SECURITY;`** + policy `CREATE POLICY notif_envios_read ON notificacoes_envios FOR SELECT TO authenticated USING (perfil_id = auth.uid() OR public.is_at_least('coordenador'));`. **Comentar explicitamente** no SQL (linha imediatamente acima da policy): `-- INSERT/UPDATE feitos APENAS via service_role (Edge Functions). Sem policy permissiva propositadamente (R6 do RESEARCH).`

6. **RPC `quota_resend_atual()`** SECURITY DEFINER que retorna `TABLE(hoje int, mes int)` executando a query exata de RESEARCH Q8 (filtros `canal='email'` + `dia=CURRENT_DATE` para hoje, `dia >= date_trunc('month', CURRENT_DATE)::date` para mes, sempre com `status NOT IN ('dropped_quota','skipped_no_recipient','fallback_diretor')`). GRANT EXECUTE para `authenticated` (UI lê) e implicitamente para service_role (edge function lê).

7. **Atualizar trigger `handle_new_user`** (CREATE OR REPLACE FUNCTION, mantendo SECURITY DEFINER + SET search_path = public per pattern de 011): após o INSERT INTO public.perfis existente, adicionar `UPDATE perfis SET preferencias_notif = jsonb_build_object('tarefa', jsonb_build_object('slack', slack_user_id IS NOT NULL, 'email', true), 'cadencia', jsonb_build_object('slack', slack_user_id IS NOT NULL, 'email', true), 'renovacao', jsonb_build_object('slack', slack_user_id IS NOT NULL, 'email', true), 'indicacao', jsonb_build_object('slack', slack_user_id IS NOT NULL, 'email', true)) WHERE id = new.id;` (D-04 smart default condicional). Não recriar TRIGGER — apenas a função.

8. **Backfill de perfis existentes** (per RESEARCH Q4 "Observação importante"): no fim da migration, `UPDATE perfis SET preferencias_notif = jsonb_build_object('tarefa', jsonb_build_object('slack', slack_user_id IS NOT NULL, 'email', true), 'cadencia', jsonb_build_object('slack', slack_user_id IS NOT NULL, 'email', true), 'renovacao', jsonb_build_object('slack', slack_user_id IS NOT NULL, 'email', true), 'indicacao', jsonb_build_object('slack', slack_user_id IS NOT NULL, 'email', true));`

Criar `tests/rls/preferencias_notif.test.ts` e `tests/rls/notificacoes_envios.test.ts` (Vitest, `tests/rls/` env) com os 7 testes do bloco `<behavior>`. Reusar helpers existentes de `tests/rls/` (mirror de qualquer .test.ts já presente — provavelmente cria Supabase clients via env QA_*). Para testes que precisam de service_role, ler do env `SUPABASE_SERVICE_ROLE_KEY` (já documentado em `.env.test.example`).
  </action>
  <verify>
    <automated>npm test -- tests/rls/preferencias_notif.test.ts tests/rls/notificacoes_envios.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/035_notificacoes_envios.sql` existe.
    - `grep -c "CREATE TABLE notificacoes_envios" supabase/migrations/035_notificacoes_envios.sql` retorna ≥1.
    - `grep -c "GENERATED ALWAYS AS (sent_at::date) STORED" supabase/migrations/035_notificacoes_envios.sql` retorna ≥1.
    - `grep -c "WHERE reenviado_por_id IS NULL" supabase/migrations/035_notificacoes_envios.sql` retorna ≥1 (índice parcial R8).
    - `grep -c "COALESCE(entidade_id" supabase/migrations/035_notificacoes_envios.sql` retorna ≥1 (Pitfall 4).
    - `grep -c "is_at_least('coordenador')" supabase/migrations/035_notificacoes_envios.sql` retorna ≥1 (RLS D-11/D-19).
    - `grep -c "quota_resend_atual" supabase/migrations/035_notificacoes_envios.sql` retorna ≥1 (RPC Q8).
    - `grep -c "slack_user_id IS NOT NULL" supabase/migrations/035_notificacoes_envios.sql` retorna ≥2 (trigger + backfill, D-04).
    - Arquivos `tests/rls/preferencias_notif.test.ts` e `tests/rls/notificacoes_envios.test.ts` existem.
    - Os 7 testes do bloco `<behavior>` passam (vitest exit 0).
  </acceptance_criteria>
  <done>Migration 035 escrita conforme D-19/D-23/Q4/Pitfall 4/R8 + 7 testes RLS verdes via Vitest. Migration ainda NÃO aplicada no remoto (push acontece no Task 5 — gate humano).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helpers _shared/ (auth + perfis) + templates/ + sendEmail completo (per D-20, D-22, Q1, Q7, Pitfall 6)</name>
  <files>supabase/functions/_shared/auth.ts, supabase/functions/_shared/perfis.ts, supabase/functions/_shared/email.ts, supabase/functions/_shared/templates/tarefa.html.ts, supabase/functions/_shared/templates/render.ts, supabase/functions/_shared/__tests__/email.test.ts, supabase/functions/_shared/__tests__/email.quota.test.ts, supabase/functions/_shared/__tests__/email.fallback.test.ts, supabase/functions/_shared/__tests__/templates.test.ts</files>
  <read_first>
    - supabase/functions/notify-tarefa/index.ts (full file — copiar lines 40-49 `constantTimeAuthCheck` para auth.ts; lines 79-96 `findSlackUserId`/`findPerfilName` para perfis.ts; lines 98-140 `openDmChannel`/`postDm` NÃO migrar nesta task — fica para Plan 2)
    - supabase/functions/notify-renovacao/index.ts (lines 137-159 retry exponencial 500*2^i; lines 187-232 pattern insert-then-API-then-update; lines 22-31 constantTimeAuthCheck duplicado)
    - supabase/functions/notify-resumo-diario/index.ts (lines 23-32, 46-62 — pattern de findPerfilNome com nome divergente; unificar como findPerfilNome em perfis.ts)
    - supabase/functions/notify-indicacao/index.ts (lines 19-30 — constantTimeAuthCheck duplicado)
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-20 helper centralizado; D-22 templates; D-12 deep-link magic link; D-04 smart default)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (Q1 Resend API + esqueleto helper lines 942-1067; Q2 webhook NÃO nesta task; Q7 Supabase admin.generateLink magic link option (a); Pitfall 1 sender restrito; Pitfall 6 atomic INSERT RETURNING + xmax; §Security escapeHtml; R5 PATCH único atômico; R3 rate-limit generateLink mitigation sequencial; §Code Examples helper completo)
    - .planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md (§supabase/functions/_shared/auth.ts; §_shared/perfis.ts com loadPrefs + findDiretores; §_shared/email.ts completo; §templates — escapeHtml + render.ts)
    - src/pages/ClienteDetailPage.tsx (linha ~173 — pattern existente `supabase.auth.signInWithOtp`; helper edge usa `supabase.auth.admin.generateLink` que é a versão server-side equivalente)
  </read_first>
  <behavior>
    - Test email.test.ts cenário 1 (happy path queued): mock global fetch para `api.resend.com/emails` retornar 200 `{ id: 'r_abc' }`; chamar `sendEmail(supabaseMock, { perfilId, toEmail, tipo:'tarefa', entidadeId:'lead-1', entidadeTipo:'lead', subject:'X', html:'<p>X</p>' })`; assert: `result.status==='queued'`, `result.resendId==='r_abc'`; supabaseMock recebeu 1 INSERT em notificacoes_envios com `status='queued'`, depois 1 UPDATE com `resend_id='r_abc'`.
    - Test email.test.ts cenário 2 (idempotent skip): supabaseMock retorna error `{ code: '23505' }` no INSERT; assert `result.status==='skipped_idempotent'`, fetch para Resend NÃO foi chamado.
    - Test email.test.ts cenário 3 (Resend 429 → retry 3x → failed): mock fetch retorna 429 nas 3 tentativas; assert fetch chamada 3 vezes, `result.status==='failed'`, último UPDATE em notificacoes_envios setou `status='failed'` + `error_msg`.
    - Test email.test.ts cenário 4 (Resend 5xx → retry 3x → 200 na 3ª): mock fetch retorna 500, 500, 200; assert `result.status==='queued'`.
    - Test email.quota.test.ts cenário 1 (hoje>=100): supabaseMock.rpc('quota_resend_atual') retorna `{ hoje: 100, mes: 200 }`; `sendEmail()` retorna `result.status==='dropped_quota'`; fetch Resend NÃO chamado; supabaseMock INSERT em notificacoes_envios com `status='dropped_quota'`.
    - Test email.quota.test.ts cenário 2 (mes>=3000): supabaseMock.rpc retorna `{ hoje: 5, mes: 3000 }`; mesmo comportamento de dropped_quota.
    - Test email.quota.test.ts cenário 3 (ambos abaixo): retorna `{ hoje: 50, mes: 1000 }`; sendEmail prossegue com INSERT+fetch normal (mock fetch 200).
    - Test email.fallback.test.ts cenário A (helper findDiretores): `findDiretores(supabaseMock)` retorna array com 2 diretores quando 2 perfis têm `role='diretor'` (mock `supabase.from('perfis').select.eq.returns`); retorna `[]` quando nenhum diretor existe.
    - Test email.fallback.test.ts cenário B (dispatch loop end-to-end — D-05): dado destinatário NULL, chamar `dispatchPerUserEmail({ supabase: supabaseMock, perfilId: null, tipo:'renovacao', entidadeId:'contr-1', entidadeTipo:'contrato', subject:'X', html:'<p>X</p>' })` (ou equivalente wrapper que chama `findDiretores` + loop `sendEmail`); assert: `sendEmail` mock foi chamado **exatamente N vezes** onde N = `findDiretores().length` (2 no fixture), com `perfilId` igual ao id de cada diretor; cada chamada registrou linha em `notificacoes_envios` com `status='fallback_diretor'` (literal, não `queued`). Garante que regressão futura que pular o loop quebra o teste.
    - Test templates.test.ts: `renderTarefa({ nomeAtribuido: '<script>alert(1)</script>', tituloTarefa: 'Ligar João & Maria', deepLink: 'https://app/x', gerenciarPrefsLink: 'https://app/me?tab=notificacoes' })` retorna string contendo `&lt;script&gt;alert(1)&lt;/script&gt;` (escape HTML) e `Ligar João &amp; Maria` (ampersand escapado) e `https://app/x` literal (URL NÃO escapada) e `https://app/me?tab=notificacoes` literal.
  </behavior>
  <action>
**`supabase/functions/_shared/auth.ts`:** extrair `constantTimeAuthCheck` exato de notify-tarefa:40-49 (PATTERNS §_shared/auth.ts mostra o bloco completo). Imports: `timingSafeEqual` de `https://deno.land/std@0.224.0/crypto/timing_safe_equal.ts`. Export named function.

**`supabase/functions/_shared/perfis.ts`:** exportar (mirror exato PATTERNS §_shared/perfis.ts):
- `findSlackUserId(supabase, perfilId) → Promise<string | null>` (de notify-tarefa:79-96).
- `findPerfilNome(supabase, perfilId | null) → Promise<string>` (unificar nomes divergentes findPerfilName/findPerfilNome — escolher `findPerfilNome` consistente com português).
- `loadPrefs(supabase, perfilId) → Promise<Record<string, { slack: boolean; email: boolean }> | null>` (SELECT preferencias_notif, email FROM perfis WHERE id = $1).
- `findDiretores(supabase) → Promise<Array<{ id: string; email: string }>>` (SELECT id, email FROM perfis WHERE role='diretor').
Helper `loadPrefs` retorna apenas `preferencias_notif`. Helper paralelo `loadPerfilDestinatario(supabase, perfilId) → { email, prefs }` opcional para evitar 2 queries — planner decide. **Não importar token Slack aqui** — perfis.ts é purely DB lookup.

**`supabase/functions/_shared/templates/tarefa.html.ts`:** arquivo .ts exportando `export default` com string template HTML (Deno suporta export default string — mais portável que .html arquivo + Deno.readTextFile per RESEARCH §Template helper pattern Alternativa). Estrutura per D-22: header com nome CONSEJ + cor `--primary` (hex: usar `#0089ac` espelhando portal — confirmar lendo `src/pages/portal/PortalWalletPage.tsx`) + título PT-BR (`Olá, {{nomeAtribuido}}, nova tarefa atribuída`) + corpo com `{{tituloTarefa}}` + CTA `<a href="{{deepLink}}">Abrir no CRM</a>` estilizado como botão inline + footer com `<a href="{{gerenciarPrefsLink}}">Gerenciar preferências</a>` + assinatura `CONSEJ — Empresa Júnior de Consultoria Jurídica`. Inline CSS apenas (compat email clients). Sem links externos.

**`supabase/functions/_shared/templates/render.ts`:** exportar `escapeHtml(s: string): string` (5 chars: `&<>"'`) e `renderTarefa(vars: { nomeAtribuido, tituloTarefa, deepLink, gerenciarPrefsLink }): string` que importa `tarefaTemplate` do .html.ts e faz `.replaceAll('{{nomeAtribuido}}', escapeHtml(vars.nomeAtribuido))` para textos, `.replaceAll('{{deepLink}}', vars.deepLink)` para URLs (NÃO escapar URLs). Exatamente conforme PATTERNS §templates.

**`supabase/functions/_shared/email.ts`:** implementar helper completo copiando o esqueleto detalhado de RESEARCH §Code Examples (lines 942-1067) com estas diferenças vs esqueleto:
- Constantes no topo: `RESEND_API_KEY = Deno.env.get('RESEND_API_KEY')!`, `FROM = 'CONSEJ <onboarding@resend.dev>'` (D-24), `APP_URL = (Deno.env.get('APP_URL') ?? 'https://localhost:5173').replace(/\/$/, '')`.
- Tipos exportados: `TipoNotif = 'tarefa' | 'cadencia' | 'renovacao' | 'indicacao'`, `EntidadeTipo = 'lead' | 'cliente' | 'contrato' | 'tarefa'`, `SendEmailParams` (perfilId, toEmail, tipo, entidadeId, entidadeTipo, subject, html), `SendEmailResult` (ok, status: 'queued'|'dropped_quota'|'failed'|'skipped_idempotent', resendId?, errorMsg?, notificacaoId?).
- Função `checkQuota(supabase) → { hoje, mes, estourou }` consulta `supabase.rpc('quota_resend_atual')`; QUOTA_DIARIA=100, QUOTA_MENSAL=3000.
- Função `generateMagicLink(supabase, toEmail, redirectPath)` opcional usando `supabase.auth.admin.generateLink({ type: 'magiclink', email: toEmail, options: { redirectTo: `${APP_URL}${redirectPath}` } })` retornando `linkData?.properties?.action_link ?? `${APP_URL}${redirectPath}`` (fallback graceful per Q7 + R3). **Não chamar dentro de sendEmail por default** — caller passa `html` já renderizado com deepLink + gerenciarPrefsLink. Exportar como utility separado para o caller (notify-tarefa) usar.
- Função principal `sendEmail(supabase: SupabaseClient, p: SendEmailParams): Promise<SendEmailResult>`:
  1. `await checkQuota` → se estourou: INSERT row com status='dropped_quota', retornar `{ ok: true, status: 'dropped_quota', notificacaoId }`.
  2. `INSERT .. .select('id').single()`. Se error.code === '23505' → retornar `{ ok: true, status: 'skipped_idempotent' }` (Pitfall 6 simplificado — sem `RETURNING (xmax = 0)` porque idempotência via índice WHERE reenviado_por_id IS NULL já garante).
  3. Loop retry 3x para POST `https://api.resend.com/emails` com headers Authorization Bearer + Content-Type application/json e body `{ from: FROM, to: [p.toEmail], subject: p.subject, html: p.html, headers: { 'X-Entity-Ref-ID': inserted.id } }`. Para HTTP 429/5xx: `await new Promise(r => setTimeout(r, 500 * 2 ** i))` e continue. Para 4xx (não 429): break sem retry.
  4. Se sucesso (`body.id` presente): `UPDATE notificacoes_envios SET resend_id=body.id WHERE id=inserted.id`, retornar `{ ok: true, status: 'queued', resendId, notificacaoId }`.
  5. Se falha após retries: `UPDATE SET status='failed', error_msg=lastErr WHERE id=inserted.id`, retornar `{ ok: false, status: 'failed', errorMsg, notificacaoId }`.

**Testes Wave-0 (`supabase/functions/_shared/__tests__/`):** Vitest com mock de `globalThis.fetch` (via `vi.spyOn(globalThis, 'fetch').mockResolvedValue(new Response(...))`) e Supabase client mockado conforme `src/test/supabase-mock.ts` (verificar arquivo existente e replicar pattern). Cobrir os 5 cenários do bloco `<behavior>`. Configurar `vitest.config.ts` se necessário para incluir o glob `supabase/functions/**/__tests__/*.test.ts` (verificar `vitest.config.ts:include` atual — já inclui `src/**/*.test.{ts,tsx}` e `tests/rls/**/*.test.ts`; **adicionar** `supabase/functions/**/__tests__/*.test.ts`).
  </action>
  <verify>
    <automated>npm test -- supabase/functions/_shared/__tests__/email.test.ts supabase/functions/_shared/__tests__/email.quota.test.ts supabase/functions/_shared/__tests__/email.fallback.test.ts supabase/functions/_shared/__tests__/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os 5 arquivos `supabase/functions/_shared/{auth,perfis,email,templates/render,templates/tarefa.html}.ts` existem.
    - `grep -c "export function constantTimeAuthCheck" supabase/functions/_shared/auth.ts` retorna ≥1.
    - `grep -cE "export (async )?function (loadPrefs|findSlackUserId|findPerfilNome|findDiretores)" supabase/functions/_shared/perfis.ts` retorna 4.
    - `grep -c "export async function sendEmail" supabase/functions/_shared/email.ts` retorna ≥1.
    - `grep -c "api.resend.com/emails" supabase/functions/_shared/email.ts` retorna ≥1.
    - `grep -c "quota_resend_atual" supabase/functions/_shared/email.ts` retorna ≥1.
    - `grep -c "23505" supabase/functions/_shared/email.ts` retorna ≥1 (idempotência atômica).
    - `grep -c "500 \\* 2 \\*\\*" supabase/functions/_shared/email.ts` retorna ≥1 (retry exponencial).
    - `grep -c "onboarding@resend.dev" supabase/functions/_shared/email.ts` retorna ≥1 (D-24).
    - `grep -c "escapeHtml" supabase/functions/_shared/templates/render.ts` retorna ≥1.
    - `grep -c "renderTarefa" supabase/functions/_shared/templates/render.ts` retorna ≥1.
    - Os 4 arquivos de teste rodam e passam (vitest exit 0).
    - `vitest.config.ts` inclui `supabase/functions/**/__tests__/*.test.ts` em `test.include` (verificável via `grep -c "supabase/functions" vitest.config.ts` ≥1).
  </acceptance_criteria>
  <done>Helpers `_shared/` (auth, perfis, email, templates) escritos seguindo D-20/D-22/Q1/Q7/Pitfall 6 + 4 testes Wave-0 verdes. Próxima task refatora notify-tarefa pra consumir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refatorar notify-tarefa — self-loop D-06 + lookup prefs D-08 + dispatch paralelo D-03 + fallback diretor D-05 (per Q5)</name>
  <files>supabase/functions/notify-tarefa/index.ts, supabase/functions/notify-tarefa/__tests__/self-loop.test.ts, supabase/functions/notify-tarefa/__tests__/parallel.test.ts</files>
  <read_first>
    - supabase/functions/notify-tarefa/index.ts (full file — handler atual ~227 linhas; pontos de mudança: linha 40-49 constantTimeAuthCheck → substituir por import; linhas 79-96 findSlackUserId/findPerfilName → substituir por import; linhas 98-140 openDmChannel/postDm → MANTER inline nesta task (Plan 2 extrai para _shared/slack.ts); linha ~178 após detecção do `novoAtribuido` → inserir self-loop check D-06; linha ~225 substituir único postDm por Promise.all paralelo D-03)
    - supabase/functions/_shared/auth.ts (criado no Task 2 — para import)
    - supabase/functions/_shared/perfis.ts (criado no Task 2 — para import loadPrefs/findSlackUserId/findPerfilNome/findDiretores)
    - supabase/functions/_shared/email.ts (criado no Task 2 — para import sendEmail)
    - supabase/functions/_shared/templates/render.ts (criado no Task 2 — para import renderTarefa)
    - .planning/phases/05-multi-channel-notifications-email/05-CONTEXT.md (D-03 paralelo sem dedup, D-05 fallback diretor, D-06 self-loop, D-12 magic link no rodapé)
    - .planning/phases/05-multi-channel-notifications-email/05-PATTERNS.md (§MODIFY notify-tarefa — código exato de self-loop guard + Promise.all dispatch)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (Q5 mudanças cirúrgicas comuns às 4 funções; Q7 magic link via admin.generateLink)
    - src/test/supabase-mock.ts (se existir — pattern de mock para edge function tests)
  </read_first>
  <behavior>
    - Test self-loop.test.ts: webhook payload `{ tarefa: { id: 't1', atribuido_a_id: 'p1', criado_por_id: 'p1' } }` — handler retorna `{ ok: true, skipped: 'self_loop' }` sem fazer NENHUMA chamada a Slack ou sendEmail (mocks não invocados).
    - Test self-loop.test.ts (variante): payload onde `criado_por_id !== atribuido_a_id` prossegue para dispatch normal (testar que NÃO short-circuita).
    - Test parallel.test.ts cenário "ambos ON": mock loadPrefs retorna `{ tarefa: { slack: true, email: true } }`; mock findSlackUserId retorna 'U123'; mock sendEmail retorna `{ ok: true, status: 'queued' }`; mock postDm retorna `{ ok: true, ts: '1700.0001' }`. Handler chama AMBOS em paralelo (Promise.all) e retorna `{ ok: true, slack: {...}, email: {...} }`.
    - Test parallel.test.ts cenário "só email": prefs `{ tarefa: { slack: false, email: true } }` — postDm NÃO chamado; sendEmail chamado uma vez.
    - Test parallel.test.ts cenário "só slack": prefs `{ tarefa: { slack: true, email: false } }` — sendEmail NÃO chamado; postDm chamado uma vez.
    - Test parallel.test.ts cenário "nenhum": prefs `{ tarefa: { slack: false, email: false } }` — nem postDm nem sendEmail chamados; retorna ok com ambos skipped.
    - Test parallel.test.ts cenário "fallback diretor": `atribuido_a_id` resolvido para perfil cujo `email` é null OU caso de tarefa sem destinatário concreto → handler chama findDiretores e dispara sendEmail para cada diretor. (Nota: se o caso "atribuido NULL" não existir em notify-tarefa porque atribuido_a_id é obrigatório, planner ajusta o teste para um cenário onde lookup do email do destinatário falha; documentar a decisão no commit.)
  </behavior>
  <action>
Modificar `supabase/functions/notify-tarefa/index.ts` aplicando as 4 mudanças cirúrgicas listadas em PATTERNS §MODIFY notify-tarefa:

1. **Imports (topo do arquivo):** substituir blocos inline por:
   ```
   import { constantTimeAuthCheck } from '../_shared/auth.ts'
   import { findSlackUserId, findPerfilNome, loadPrefs, findDiretores } from '../_shared/perfis.ts'
   import { sendEmail } from '../_shared/email.ts'
   import { renderTarefa } from '../_shared/templates/render.ts'
   ```
   Remover as definições locais antigas de `constantTimeAuthCheck`, `findSlackUserId`, `findPerfilName` (renomear callsites para `findPerfilNome`).

2. **Self-loop guard (D-06):** imediatamente após resolver `novoAtribuido` (perfil_id do atribuído) e ANTES do lookup de prefs, inserir:
   ```
   if (tarefa.criado_por_id && tarefa.criado_por_id === novoAtribuido) {
     return json({ ok: true, skipped: 'self_loop' })
   }
   ```

3. **Lookup prefs + buscar email do destinatário** (D-08): `const prefs = await loadPrefs(supabase, novoAtribuido)` + `const { data: destinatario } = await supabase.from('perfis').select('email, nome').eq('id', novoAtribuido).maybeSingle()` (planner pode adicionar este SELECT em `loadPerfilDestinatario` em perfis.ts se preferir).

4. **Fallback diretor quando destinatario.email é NULL (D-05):**
   ```
   let targets: Array<{ perfilId: string; email: string; prefs: ... }>
   if (!destinatario?.email) {
     const diretores = await findDiretores(supabase)
     targets = await Promise.all(diretores.map(async d => ({ perfilId: d.id, email: d.email, prefs: await loadPrefs(supabase, d.id) })))
     // Log fallback_diretor row em notificacoes_envios para observabilidade (status='fallback_diretor', perfil_id=novoAtribuido original)
   } else {
     targets = [{ perfilId: novoAtribuido, email: destinatario.email, prefs }]
   }
   ```

5. **Magic link generation + dispatch paralelo (D-03 + D-12):** para cada target, gerar magic link via `supabase.auth.admin.generateLink({ type: 'magiclink', email: target.email, options: { redirectTo: `${APP_URL}/me?tab=notificacoes` } })` (Q7 opção (a)), com fallback `${APP_URL}/me?tab=notificacoes` se generateLink falhar (Q7 opção (c)). Então renderizar template:
   ```
   const html = renderTarefa({
     nomeAtribuido: await findPerfilNome(supabase, target.perfilId),
     tituloTarefa: tarefa.titulo,
     deepLink: `${APP_URL}/tarefas?highlight=${tarefa.id}`,
     gerenciarPrefsLink: magicLinkOrFallback,
   })
   const wantSlack = target.prefs?.tarefa?.slack === true && slackUserId !== null
   const wantEmail = target.prefs?.tarefa?.email === true
   const [slackRes, emailRes] = await Promise.all([
     wantSlack ? postDm(SLACK_BOT_TOKEN!, slackUserId!, text, blocks) : Promise.resolve({ ok: true, skipped: 'slack_off' }),
     wantEmail ? sendEmail(supabase, { perfilId: target.perfilId, toEmail: target.email, tipo: 'tarefa', entidadeId: tarefa.id, entidadeTipo: 'tarefa', subject: `Nova tarefa: ${tarefa.titulo}`, html }) : Promise.resolve({ ok: true, skipped: 'email_off' }),
   ])
   ```
   Preservar `slackUserId` lookup atual; **manter `openDmChannel`/`postDm` inline** nesta task (extração para `_shared/slack.ts` fica para Plan 2 conforme Q5 recomendação de fazer junto com refactor das outras 3 funções).

6. **Log Slack delivery em notificacoes_envios:** após postDm bem-sucedido, INSERT em notificacoes_envios com `canal='slack'`, `slack_ts=slackRes.ts`, `status='delivered'` (Slack confirma síncronamente per Q5 §3). Usar o mesmo padrão idempotente do sendEmail (ON CONFLICT silencioso via `.insert(...).then(err => err?.code === '23505' ? ignore : throw)`).

7. **Retornar `{ ok: slackRes.ok && emailRes.ok, slack: slackRes, email: emailRes }`** (estrutura uniforme para o caller).

Criar `supabase/functions/notify-tarefa/__tests__/self-loop.test.ts` e `parallel.test.ts` com os cenários do bloco `<behavior>`. Mock `globalThis.fetch` para postDm (Slack endpoints) e Resend; mock o cliente Supabase via `src/test/supabase-mock.ts`. Use `vi.mock('../_shared/email.ts', () => ({ sendEmail: vi.fn() }))` etc para isolar a unidade.

**NÃO fazer:** extrair postDm/openDmChannel para `_shared/slack.ts` (fica para Plan 2 que também toca notify-resumo-diario/indicacao/renovacao — fazer junto reduz risco de drift).
  </action>
  <verify>
    <automated>npm test -- supabase/functions/notify-tarefa/__tests__/self-loop.test.ts supabase/functions/notify-tarefa/__tests__/parallel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '../_shared/auth.ts'" supabase/functions/notify-tarefa/index.ts` retorna ≥1.
    - `grep -c "from '../_shared/email.ts'" supabase/functions/notify-tarefa/index.ts` retorna ≥1.
    - `grep -c "from '../_shared/perfis.ts'" supabase/functions/notify-tarefa/index.ts` retorna ≥1.
    - `grep -c "from '../_shared/templates/render.ts'" supabase/functions/notify-tarefa/index.ts` retorna ≥1.
    - `grep -c "skipped: 'self_loop'" supabase/functions/notify-tarefa/index.ts` retorna ≥1 (D-06).
    - `grep -c "Promise.all" supabase/functions/notify-tarefa/index.ts` retorna ≥1 (D-03).
    - `grep -c "findDiretores" supabase/functions/notify-tarefa/index.ts` retorna ≥1 (D-05).
    - `grep -c "fallback_diretor" supabase/functions/notify-tarefa/index.ts` retorna ≥1.
    - `grep -c "admin.generateLink" supabase/functions/notify-tarefa/index.ts` retorna ≥1 (D-12 Q7).
    - `grep -cE "^(function|const) (constantTimeAuthCheck|findSlackUserId|findPerfilName)\\b" supabase/functions/notify-tarefa/index.ts` retorna 0 (helpers velhos removidos, agora vêm dos imports).
    - Ambos testes verdes (vitest exit 0).
  </acceptance_criteria>
  <done>notify-tarefa refatorada com self-loop suppression + lookup prefs + dispatch paralelo + fallback diretor + magic link, importando dos helpers `_shared/`. 2 testes Wave-0 verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Pre-deploy gate — verificar e-mails CONSEJ pré-cadastrados na Resend Audience (R1 mitigation)</name>
  <what-built>Plan 1 está pronto para deploy: migration 035 escrita, helpers `_shared/` implementados, notify-tarefa refatorada, 9 testes Wave-0 verdes. **Antes** de aplicar a migration no Supabase remoto (Task 5) e deployar a edge function, é CRÍTICO confirmar que os e-mails internos CONSEJ que poderão receber notificação estão pré-cadastrados na Resend Audience — porque `onboarding@resend.dev` (D-24) é o sender sandbox e Resend retorna **403 silencioso** para destinatários não-verificados (Pitfall 1 + R1 do RESEARCH).</what-built>
  <how-to-verify>
1. Abrir https://resend.com/audiences no browser (logar com a conta CONSEJ).
2. Verificar que cada um dos seguintes e-mails (mínimo) está presente:
   - `araujon2000@gmail.com` (Gabriel — para smoke test)
   - Todos os e-mails dos perfis `role='diretor'` (consultar via Supabase Studio SQL Editor: `SELECT email FROM perfis WHERE role='diretor';`)
   - Qualquer interno CONSEJ ativo que será destinatário de tarefas no MVP (`SELECT email FROM perfis WHERE tipo='interno' AND email IS NOT NULL;`)
3. Se algum estiver ausente: clicar "Add contact" no dashboard Resend e adicionar.
4. Anotar a lista exata no `.planning/STATE.md` (seção "Phase 5 pre-start" — adicionar bullet "✅ Resend Audience pré-cadastrada: [lista de e-mails]").
5. (Opcional, mas recomendado) confirmar com `curl -s -X POST https://api.resend.com/emails -H "Authorization: Bearer $RESEND_API_KEY" -H "Content-Type: application/json" -d '{"from":"CONSEJ <onboarding@resend.dev>","to":["araujon2000@gmail.com"],"subject":"Phase 5 smoke","html":"<p>Test</p>"}'` retorna `{"id":"..."}` (status 200) — confirma que API key + sender + 1 destinatário funcionam end-to-end.
  </how-to-verify>
  <resume-signal>Digite "approved" (com lista de e-mails cadastrados) OU descreva o que falta para destravar. Se quiser pular esta verificação porque o ambiente é apenas dev/local sem disparo real, responda "dev-only".</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Aplicar migration 035 no Supabase remoto + deploy notify-tarefa (BLOCKING — schema push obrigatório)</name>
  <files>(nenhum arquivo modificado — apenas operações de deploy/push)</files>
  <read_first>
    - supabase/migrations/035_notificacoes_envios.sql (verificar última versão escrita no Task 1)
    - supabase/functions/notify-tarefa/index.ts (verificar refactor do Task 3)
    - .planning/STATE.md (confirmar `RESEND_API_KEY` está como Supabase Secret)
    - .planning/phases/05-multi-channel-notifications-email/05-RESEARCH.md (§Environment Availability, Q3 Supabase secrets)
  </read_first>
  <action>
[BLOCKING] **Schema push obrigatório.** Sem isso, a verificação do plano dá falso-positivo (tipos vêm de `database.types.ts` regen, não da DB live).

1. **Aplicar migration 035 no Supabase remoto:**
   ```
   supabase db push
   ```
   Se houver prompt interativo de senha que não pode ser suprimido em ambiente non-TTY, definir `SUPABASE_ACCESS_TOKEN` no env primeiro: `export SUPABASE_ACCESS_TOKEN=<token-pessoal>` (ou no Windows PowerShell: `$env:SUPABASE_ACCESS_TOKEN='<token>'`). Token pode ser gerado em https://supabase.com/dashboard/account/tokens.
   
   **Sinais de sucesso esperados na saída:** `Applying migration 035_notificacoes_envios.sql...` + `Finished supabase db push.`. Sem erros de constraint/RLS.

2. **Regenerar tipos TypeScript da DB (se o projeto usa `database.types.ts`):**
   Verificar se `src/types/database.types.ts` ou similar existe via `Glob "**/database.types.ts"`. Se sim: `supabase gen types typescript --project-id wfnriqwkzdazdbuzbyug > src/types/database.types.ts` (project id confirmado em STATE.md / migrations/031).
   Se não existir tipos gerados → skip esta sub-etapa.

3. **Deploy edge function `notify-tarefa`:**
   ```
   supabase functions deploy notify-tarefa
   ```
   Deno bundling vai resolver `../_shared/*.ts` automaticamente (pattern Supabase Edge Runtime).
   
   **Sinal de sucesso:** `Deployed Function notify-tarefa on project wfnriqwkzdazdbuzbyug.` sem erros de import.

4. **Smoke test em prod (manual mas executável via CLI):**
   - Criar tarefa de teste no CRM real atribuída a Gabriel (`araujon2000@gmail.com`) por outro perfil (não auto-atribuição — para não disparar self-loop).
   - Aguardar ~10s.
   - Verificar via Supabase Studio SQL: `SELECT id, perfil_id, tipo, canal, status, resend_id, sent_at FROM notificacoes_envios WHERE tipo='tarefa' ORDER BY sent_at DESC LIMIT 5;` — deve haver linha nova com `canal='email'`, `status='queued'`, `resend_id` preenchido com UUID Resend.
   - Verificar caixa de entrada de `araujon2000@gmail.com` — e-mail "Nova tarefa: ..." deve chegar em <30s (status sobe para `delivered` no Plan 2 via webhook; nesta task fica em `queued` é OK).

Se smoke falhar, ler logs da edge function: `supabase functions logs notify-tarefa --tail 50`.
  </action>
  <verify>
    <automated>supabase db diff --schema public 2>&amp;1 | grep -cE "(notificacoes_envios|preferencias_notif|quota_resend_atual)" | grep -q "^0$" &amp;&amp; echo "schema matches" || echo "schema drift detected"</automated>
    <human-check>Smoke test manual descrito no item 4 do `<action>` — confirma e-mail real chegou + linha em `notificacoes_envios` com `status='queued'` e `resend_id` preenchido.</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completou sem erros (saída contém "Applying migration 035_notificacoes_envios.sql" + "Finished").
    - `supabase functions deploy notify-tarefa` completou sem erros.
    - Query `SELECT * FROM notificacoes_envios WHERE tipo='tarefa' ORDER BY sent_at DESC LIMIT 1` retorna uma linha com `canal='email'`, `status='queued'`, `resend_id` não-nulo (UUID Resend).
    - E-mail "Nova tarefa: ..." chegou na caixa de `araujon2000@gmail.com` após criar tarefa de teste.
    - `supabase functions logs notify-tarefa --tail 20` não mostra `Error` nas últimas execuções.
  </acceptance_criteria>
  <done>Migration 035 aplicada no remoto + notify-tarefa deployada + primeiro e-mail real chegou (status `queued` em notificacoes_envios; subirá para `delivered` quando Plan 2 conectar webhook). Phase 5 tem fundação end-to-end funcionando.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase | Browser autenticado consulta/atualiza `perfis.preferencias_notif` via RLS (próprio perfil apenas) e lê `notificacoes_envios` via policy role-aware |
| Edge Function `notify-tarefa` → Resend API | Bearer token RESEND_API_KEY no header Authorization; nunca exposto ao browser |
| Webhook caller → Edge Function | Pre-existing: cron/trigger Supabase chama notify-tarefa com `WEBHOOK_TAREFA_SECRET` via constantTimeAuthCheck (não alterado nesta phase) |
| Edge Function → Supabase DB | Service role bypassa RLS para INSERT/UPDATE em notificacoes_envios (R6 — sem policy permissiva propositadamente) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | `_shared/templates/render.ts` — placeholders `{{var}}` em HTML | mitigate | `escapeHtml(s)` chamado em TODOS os textos antes do `replaceAll` (PATTERNS §templates); URLs passam direto (sem escape). Test `templates.test.ts` valida `<script>` → `&lt;script&gt;` |
| T-05-04 | Information Disclosure | `notificacoes_envios` RLS (consultor lê outras notifs) | mitigate | RLS policy `notif_envios_read FOR SELECT USING (perfil_id = auth.uid() OR is_at_least('coordenador'))` na migration 035. Test `tests/rls/notificacoes_envios.test.ts` cenário "consultor vê só próprias" valida |
| T-05-05 | Spoofing + Information Disclosure | Magic link em rodapé de e-mail (D-12) | mitigate | Usar `supabase.auth.admin.generateLink` (Q7 opção a) — Supabase audita e expira links (~1h default); fallback graceful `${APP_URL}/me?tab=notificacoes` (sem token) se generateLink falhar (R3 rate limit) — usuário cai no login normal. Sem JWT hand-rolled (don't hand-roll cripto) |
| T-05-06 | Denial of Service | NULL `responsavel_id` → fallback diretor pode inundar diretor | mitigate | Fallback dispara para perfis `role='diretor'` (D-05) com row registrada com `status='fallback_diretor'` — observabilidade via histórico (Plan 3). Idempotência via UNIQUE (perfil_id, tipo, entidade_id, canal, dia) protege duplicatas dentro do mesmo dia. **Volume baixo:** CONSEJ tem 1-2 diretores; risco real é mínimo |
| T-05-07 | Elevation of Privilege | Atacante forja `criado_por_id` em payload de notify-tarefa para inundar outro perfil | accept | Webhook é chamado pelo trigger Postgres com `WEBHOOK_TAREFA_SECRET` (preexistente, constantTimeAuthCheck). Sem caminho público para o atacante chamar a função — accepted como pattern já em vigor no projeto |
| T-05-API-LEAK | Information Disclosure | `RESEND_API_KEY` vazada via log ou exception | mitigate | Helper `sendEmail` nunca loga o header Authorization; `error_msg` armazena apenas `body.message` ou `HTTP ${status}` do Resend, sem incluir headers. Key vive só em `Deno.env` (Edge Function), nunca no browser |
| T-05-QUOTA-DOS | Denial of Service | Atacante interno dispara reenvios para estourar quota Resend | accept (Plan 1 escopo) | Hard quota Resend (100/dia, 3k/mês) é o limite final; pre-check em `sendEmail` (D-13 + D-14) → `dropped_quota`. Plan 3 adiciona warning UI 80% para coord+ visualizar abuso. Sem rate-limit interno per usuário nesta phase (CONTEXT D-18 explicitamente: "Sem rate-limit interno de re-envio"). **Reenviar UI** só chega no Plan 3 — risco isolado no Plan 1 |
| T-05-MIG-RACE | Tampering (R4) | Webhook Resend chegando durante aplicação da migration 035 → potencial deadlock | mitigate | Webhook Resend será registrado **apenas no Plan 2** (após migration 035 aplicada e estável). Plan 1 não registra webhook — não há possibilidade de race nesta fatia. Documentar no commit |

</threat_model>

<verification>

**Checks de phase (executados antes de fechar Plan 1):**

1. Migration aplicada: `supabase db diff --schema public` retorna empty (sem drift entre local e remoto).
2. Testes unitários Wave-0: `npm test -- supabase/functions/_shared/__tests__/ supabase/functions/notify-tarefa/__tests__/` → todos verdes.
3. Testes RLS: `npm run test:rls -- tests/rls/preferencias_notif.test.ts tests/rls/notificacoes_envios.test.ts` → todos verdes.
4. Lint: `npm run lint` → 0 errors (warnings aceitáveis em código existente intocado).
5. Type-check: `npm run build` (executa `tsc -b && vite build`) → 0 errors.
6. Edge function deployada: `supabase functions list` mostra `notify-tarefa` com `Updated` recente.
7. Smoke E2E (Task 5): tarefa criada → linha em notificacoes_envios com `resend_id` preenchido → e-mail real recebido.
8. (Manual, R1) Lista de e-mails Resend Audience documentada em STATE.md.

</verification>

<success_criteria>

- [ ] Migration 035 escrita conforme D-19/D-23/Pitfall 4/R8 com 8 itens (coluna prefs, tabela, índice idempotência parcial, 3 índices auxiliares, RLS, RPC quota, trigger update, backfill).
- [ ] Migration 035 aplicada no Supabase remoto via `supabase db push` (Task 5 — BLOCKING).
- [ ] Helpers `supabase/functions/_shared/{auth,perfis,email,templates/render,templates/tarefa.html}.ts` criados conforme D-20/D-22/Q1/Q7/Pitfall 6.
- [ ] `notify-tarefa/index.ts` refatorada para usar imports `_shared/*` + self-loop guard (D-06) + lookup prefs (D-08) + Promise.all paralelo (D-03) + fallback diretor (D-05) + magic link (D-12).
- [ ] 9 arquivos de teste Wave-0 criados e verdes: 4 em `_shared/__tests__/`, 2 em `notify-tarefa/__tests__/`, 2 em `tests/rls/`, 1 em `_shared/__tests__/templates.test.ts`.
- [ ] Edge function `notify-tarefa` deployada com sucesso (`supabase functions deploy notify-tarefa`).
- [ ] Smoke test em prod: criar tarefa → e-mail real chega em `araujon2000@gmail.com` + linha em `notificacoes_envios` com `status='queued'` e `resend_id` preenchido.
- [ ] R1 mitigation: lista de e-mails pré-cadastrados na Resend Audience documentada em STATE.md.

</success_criteria>

<output>
Create `.planning/phases/05-multi-channel-notifications-email/05-01-SUMMARY.md` when done. Document:
- Migration 035 final SQL (especialmente o trigger update e backfill)
- Decisões discricionárias (e-mail destinatário lookup: helper separado vs inline; loadPerfilDestinatario combinado ou não)
- Lista de e-mails Resend Audience pré-cadastrados
- Tempo de cold-start do helper sendEmail observado (para calibrar Plan 2)
- Eventuais surpresas (ex: nome divergente findPerfilName/Nome; vitest.config include adjustment)
</output>