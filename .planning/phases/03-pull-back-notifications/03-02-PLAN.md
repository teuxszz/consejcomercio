---
phase: 03-pull-back-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/notify-resumo-diario/index.ts
  - supabase/migrations/034_cron_resumo_diario.sql
  - src/lib/__tests__/cadencia.test.ts
  - .planning/phases/03-pull-back-notifications/03-02-CRON-DEPLOY-LOG.md
autonomous: false
requirements:
  - NOTIF-02
  - NOTIF-03
user_setup:
  - service: supabase-vault
    why: "O secret Bearer `webhook_resumo_secret` autentica o cron PL/pgSQL contra a edge function `notify-resumo-diario`. Não pode ser commitado em SQL."
    env_vars:
      - name: webhook_resumo_secret
        source: "Gerado pelo usuário (qualquer string random ≥ 32 chars) e armazenado no Supabase Vault via SQL editor: `SELECT vault.create_secret('<valor>', 'webhook_resumo_secret', 'Bearer p/ notify-resumo-diario');` — o MESMO valor é configurado como secret da edge function `WEBHOOK_RESUMO_SECRET`."
    dashboard_config:
      - task: "Criar o secret no Vault via SQL editor antes de aplicar migration 034"
        location: "Supabase Dashboard → SQL Editor"
      - task: "Configurar `WEBHOOK_RESUMO_SECRET` como secret da edge function notify-resumo-diario"
        location: "Supabase Dashboard → Edge Functions → notify-resumo-diario → Secrets"
tags:
  - notifications
  - slack
  - cron
  - pg_cron
  - edge-function
  - cadencia

must_haves:
  truths:
    - "Todo dia às 07:00 BRT (10:00 UTC), cada consultor interno com `slack_user_id` mapeado e com pelo menos 1 tarefa vencendo hoje OU 1 lead em D-point (D1/D3/D5/D7/D10) hoje recebe UMA única DM no Slack consolidada."
    - "A DM mostra (a) número de tarefas vencendo hoje e (b) lista de leads em D-point com formato `• <nome> (D<n>)`."
    - "Lead em estágio terminal (`ganho_assessoria`, `ganho_consultoria`, `perdido`, `cancelado`) NÃO aparece no alerta de cadência."
    - "Lead novo sem nenhuma interação criado há 0 ou 1 dias aparece como D1 no alerta."
    - "Consultores SEM `slack_user_id` mapeado ou sem nada para fazer hoje NÃO recebem DM."
  artifacts:
    - path: "supabase/migrations/034_cron_resumo_diario.sql"
      provides: "Função PL/pgSQL `cron_resumo_diario()` SECURITY DEFINER + job pg_cron `resumo-diario-consultores` agendado para `0 10 * * *`"
      contains: "cron_resumo_diario"
    - path: "supabase/functions/notify-resumo-diario/index.ts"
      provides: "Edge function Deno que recebe `{ perfil_id, tarefas_hoje, leads_cadencia }` e posta DM Slack consolidada"
      contains: "notify-resumo-diario"
    - path: ".planning/phases/03-pull-back-notifications/03-02-CRON-DEPLOY-LOG.md"
      provides: "Log auditável do deploy: hash do `supabase db push`, hash do `supabase functions deploy`, primeira execução do cron (job_run_details), DMs reais observadas"
      contains: "job_run_details"
  key_links:
    - from: "pg_cron job `resumo-diario-consultores`"
      to: "função `public.cron_resumo_diario()`"
      via: "cron.schedule('resumo-diario-consultores', '0 10 * * *', 'SELECT public.cron_resumo_diario()')"
      pattern: "cron\\.schedule\\('resumo-diario-consultores'"
    - from: "`cron_resumo_diario()`"
      to: "edge function `notify-resumo-diario`"
      via: "net.http_post com Bearer do Vault (`webhook_resumo_secret`)"
      pattern: "net\\.http_post"
    - from: "edge function `notify-resumo-diario`"
      to: "Slack `chat.postMessage`"
      via: "openDmChannel(slack_user_id) → postDm(channel, text, blocks)"
      pattern: "conversations\\.open"
---

## Phase Goal

**As a** consultor CONSEJ que pode esquecer leads e tarefas pendentes, **I want to** receber, todo dia de manhã, UMA única DM no Slack que diga exatamente quantas tarefas vencem hoje e quais leads estão no dia da cadência (D1/D3/D5/D7/D10), **so that** eu seja puxado de volta ao CRM sem o gestor precisar me cobrar manualmente.

<objective>
Entregar um slice vertical end-to-end de notificações pull-back via Slack DM agendadas por pg_cron: (1) edge function `notify-resumo-diario` que recebe payload agregado por consultor e posta DM consolidada; (2) migration 034 com função PL/pgSQL `cron_resumo_diario()` que agrega tarefas + leads em D-point por consultor e chama a edge function via `pg_net`; (3) deploy + schema push + verificação real de execução do cron e DMs entregues.

Purpose: Resolver NOTIF-02 e NOTIF-03 com UM único code path (mesmo cron + mesma edge function + mesma DM consolidada por consultor) — evita N DMs por dia e mantém o time recebendo um lembrete único acionável.
Output: Edge function deployada, migration aplicada, cron rodando, e log auditável (`03-02-CRON-DEPLOY-LOG.md`) com primeira execução do cron + DMs reais observadas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-pull-back-notifications/03-RESEARCH.md
@.planning/phases/03-pull-back-notifications/03-PATTERNS.md
@.planning/phases/03-pull-back-notifications/03-VALIDATION.md
@supabase/functions/notify-tarefa/index.ts
@supabase/functions/notify-renovacao/index.ts
@supabase/migrations/031_cron_renovacoes.sql
@supabase/migrations/030_perfis_slack.sql
@supabase/migrations/033_rls_tarefas_notificar.sql
@src/lib/cadencia.ts
@src/lib/constants.ts
@src/lib/__tests__/cadencia.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender testes unitários de cadência para cobrir lógica do resumo diário (NOTIF-02, NOTIF-03)</name>
  <files>src/lib/__tests__/cadencia.test.ts</files>
  <read_first>
    - src/lib/cadencia.ts (entender exatamente `getNextCadenciaPoint` — assinatura: `(lead, interacoesDoLead, today?) => NextCadenciaResult | null`; `NextCadenciaResult = { point: CadenciaPoint; daysUntil: number }`; `daysUntil === 0` significa "devido hoje", negativo = atrasado, positivo = faltam dias). Também: `daysSinceLastTouch`, `CADENCIA_DIAS`, `TERMINAL_STAGES`.
    - src/lib/__tests__/cadencia.test.ts (entender padrão de testes existente: o arquivo importa `getNextCadenciaPoint` e `CADENCIA_DIAS`; usa helpers `makeDate(daysAgo)`, `makeLead(status, createdDaysAgo)`, `makeInteracao(daysAgo)`; assertions seguem o padrão `expect(result!.point.dia).toBe(N)` e `expect(result!.daysUntil).toBe(0)`)
    - src/lib/constants.ts (confirmar valores exatos de `TERMINAL_WON_STAGES`, `TERMINAL_LOST_STAGES`, `TERMINAL_STAGES`)
    - .planning/phases/03-pull-back-notifications/03-PATTERNS.md (seção "Padrão específico de 034 — iteração por perfil interno" — UNION ALL define a regra de elegibilidade que estes testes devem espelhar)
  </read_first>
  <behavior>
    Adicionar à suíte existente `cadencia.test.ts` um novo `describe('resumo diário — elegibilidade NOTIF-02/NOTIF-03', ...)` com casos. Todos os casos usam `getNextCadenciaPoint` (função já testada no arquivo) e asseguram `daysUntil === 0` para os casos "devido hoje" (que é o que o cron PL/pgSQL replica). NÃO criar referências a `getCadenciaDueToday` — o arquivo de testes usa `getNextCadenciaPoint` como contrato canônico:

    - Test A (NOTIF-03 — lead com interação enviada há 3 dias): lead `status='qualificado'`, `created_at` há 30 dias, com 1 `InteracaoLead` enviada há exatamente 3 dias → `getNextCadenciaPoint()` retorna result com `result!.point.dia === 3` e `result!.daysUntil === 0`. Espelha o branch "leads COM interação" da query SQL (HAVING `MAX(enviada_em)` há 3 dias).
    - Test B (NOTIF-03 — lead sem interação, criado hoje): lead `status='novo'`, `created_at = hoje`, `interacoesDoLead = []` → result com `result!.point.dia === 1` e `result!.daysUntil === 1` (D1 ativo mas ainda não devido hoje — semântica da função; o cron SQL trata D1 quando `(CURRENT_DATE - created_at::date) IN (0,1)`, ou seja, hoje OU ontem são marcados como D1 pelo cron). Documentar essa assimetria como comentário no teste.
    - Test C (NOTIF-03 — lead sem interação, criado há 1 dia): lead `created_at = ontem`, `interacoesDoLead = []` → result com `result!.point.dia === 1` e `result!.daysUntil === 0` (D1 devido hoje).
    - Test D (NOTIF-03 — lead sem interação, criado há 2 dias): lead `created_at` há 2 dias, `interacoesDoLead = []` → retorna `null` (D1 expirado). Espelha exclusão implícita do Branch 2 da query SQL (`(CURRENT_DATE - created_at::date) IN (0,1)`).
    - Test E (NOTIF-03 — exclusão terminal): para cada valor em `TERMINAL_STAGES` (`ganho_assessoria`, `ganho_consultoria`, `perdido`, `cancelado`), lead com última interação há 3 dias → retorna `null`. Espelha o `WHERE l.status NOT IN (...)` da query SQL.
    - Test F (NOTIF-03 — dia sem D-point ainda não atingido): lead com última interação há 2 dias (não está em 1/3/5/7/10, mas D3 está 1 dia à frente) → result com `result!.point.dia === 3` e `result!.daysUntil === 1`. Documenta que o cron SQL NÃO dispara para este caso (apenas `daysUntil === 0` é elegível para a DM); o teste confirma o cálculo correto de "próximo ponto".
    - Test G (NOTIF-03 — D-point exato em todos os marcos): para cada `dia` em `[1, 3, 5, 7, 10]`, lead com última interação há exatamente `dia` dias → result com `result!.point.dia === dia` e `result!.daysUntil === 0`. Confirma que apenas dias da cadência disparam o cron.

    Cada teste é uma asserção pura sobre `getNextCadenciaPoint` (função já existente e testada, NÃO criar nova). Os testes documentam em TypeScript a regra que a função PL/pgSQL `cron_resumo_diario()` (Task 2) deve replicar, e protegem contra regressão futura na lógica de elegibilidade. Para o cron SQL, "elegível para DM" equivale a `daysUntil === 0` no contrato TS.
  </behavior>
  <action>
ANTES de editar, rodar `npm run test -- --reporter=dot` e anotar o número absoluto de testes passando como **N_BASELINE** (registrar no SUMMARY ao final). Esse número é o baseline atual da suíte global e será usado como referência para o acceptance criteria — não usar números absolutos hardcoded (123, 130, etc.).

Editar `src/lib/__tests__/cadencia.test.ts` ADICIONANDO ao final do arquivo um novo bloco `describe('resumo diário — elegibilidade NOTIF-02/NOTIF-03', () => { ... })` com os 7 casos descritos em `<behavior>`. O arquivo já importa `getNextCadenciaPoint` e `CADENCIA_DIAS` de `@/lib/cadencia` (linha 2) — REUSAR essas importações, NÃO adicionar import de `getCadenciaDueToday`. NÃO modificar os 14 testes existentes (devem continuar passando).

Adicionar importação de `TERMINAL_STAGES` de `@/lib/constants` no topo do arquivo (não está atualmente importado): `import { TERMINAL_STAGES } from '@/lib/constants'`.

Iterar `TERMINAL_STAGES` para o Test E — não hardcodar a lista (`for (const stage of TERMINAL_STAGES) { it(\`returns null for ${stage}\`, ...) }` ou `it.each(TERMINAL_STAGES)('returns null for %s', ...)`).

Usar os helpers existentes do arquivo (`makeDate`, `makeLead`, `makeInteracao`) — NÃO introduzir helpers novos.

Após escrever, rodar:
1. `npm run test -- src/lib/__tests__/cadencia.test.ts --reporter=dot` — confirmar que o arquivo tem 14 testes prévios + 7 novos = 21 testes (Test E é parametrizado por 4 stages, conta como 4 testes; ajustar contagem se usar `it.each`).
2. `npm run test -- --reporter=dot` — confirmar suíte global verde com EXATAMENTE **N_BASELINE + (novos testes adicionados)**.
  </action>
  <verify>
    <automated>npm run test -- src/lib/__tests__/cadencia.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - **PRÉ-REQUISITO:** rodar `npm run test -- --reporter=dot` ANTES da edição e anotar **N_BASELINE** (número absoluto de testes passando). Esse valor deve ser registrado em `.planning/phases/03-pull-back-notifications/03-02-SUMMARY.md` e no commit message do task 1 (ex.: `test(03-02): N_BASELINE=137 → 137+N_NOVOS`).
    - Após a edição, `npm run test -- src/lib/__tests__/cadencia.test.ts --reporter=dot` retorna exit 0 com 14 testes prévios + os novos testes do bloco `'resumo diário — elegibilidade NOTIF-02/NOTIF-03'` passando. Conta exata depende de `it.each` vs `for + it`: se Test E usar `it.each(TERMINAL_STAGES)`, contam 4 testes só do Test E (Test A + B + C + D + 4×E + F + G = 10 novos); se usar 1 `it` com loop interno, conta 7 novos.
    - Após a edição, `npm run test -- --reporter=dot` retorna exit 0 com EXATAMENTE **N_BASELINE + N_NOVOS** testes passando (N_NOVOS = número de novos `it`/`it.each` adicionados pelo bloco). Zero regressões em qualquer arquivo existente.
    - `grep -c "resumo diário — elegibilidade NOTIF-02/NOTIF-03" src/lib/__tests__/cadencia.test.ts` retorna 1.
    - `grep -c "getNextCadenciaPoint" src/lib/__tests__/cadencia.test.ts` ≥ 7 (importação + 7 ou mais usos no novo bloco).
    - `grep -c "getCadenciaDueToday" src/lib/__tests__/cadencia.test.ts` retorna 0 (esta task NÃO referencia a função antiga).
    - Bloco novo NÃO modifica `src/lib/cadencia.ts` (`git diff src/lib/cadencia.ts` é vazio).
    - Bloco novo importa `TERMINAL_STAGES` de `@/lib/constants` (verificável: `grep -n "TERMINAL_STAGES" src/lib/__tests__/cadencia.test.ts` retorna ao menos 2 linhas — import + uso).
  </acceptance_criteria>
  <done>Suíte `cadencia.test.ts` cobre os 7 casos de elegibilidade que a função PL/pgSQL `cron_resumo_diario` deve replicar (todos os casos baseados em `getNextCadenciaPoint`/`NextCadenciaResult`); toda a suíte global continua verde com baseline + novos testes; N_BASELINE registrado no SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Criar edge function notify-resumo-diario + migration 034 (cron + função PL/pgSQL)</name>
  <files>supabase/functions/notify-resumo-diario/index.ts, supabase/migrations/034_cron_resumo_diario.sql</files>
  <read_first>
    - supabase/functions/notify-tarefa/index.ts (analog primário — copiar literal: imports, `constantTimeAuthCheck`, `json()`, `findSlackUserId()`, `openDmChannel()`, `postDm()` com retry, estrutura do `serve()` handler — ordem method/auth/env/parse/validate/lógica)
    - supabase/functions/notify-renovacao/index.ts (analog secundário — padrão de `buildBlocks()` com section + fields + actions; observar como a action button monta URL para o app)
    - supabase/migrations/031_cron_renovacoes.sql (analog primário da migration — copiar literal: cabeçalho de comentário, `CREATE EXTENSION IF NOT EXISTS pg_cron/pg_net`, função `SECURITY DEFINER SET search_path = public`, leitura de `vault.decrypted_secrets`, `RAISE WARNING` + `RETURN` se secret ausente, padrão `net.http_post`, `cron.schedule` idempotente)
    - src/lib/cadencia.ts (lógica de D-point que precisa ser replicada em PL/pgSQL — em especial o branch "lead sem interação criado hoje/ontem → D1")
    - src/lib/constants.ts (valores exatos de `TERMINAL_STAGES` — usar `'ganho_assessoria', 'ganho_consultoria', 'perdido', 'cancelado'` no `WHERE l.status NOT IN (...)`; CONFIRMAR também se existe `'stand_by'` ou similar como status — ver instrução abaixo)
    - .planning/phases/03-pull-back-notifications/03-PATTERNS.md (seções `supabase/migrations/034_cron_resumo_diario.sql` e `supabase/functions/notify-resumo-diario/index.ts` — contêm o esqueleto exato com nomes corretos de secrets, job names, URLs; **alerta**: PATTERNS.md menciona explicitamente avaliar se `'stand_by'` deve entrar no `NOT IN (...)`)
    - .planning/phases/03-pull-back-notifications/03-RESEARCH.md (Pitfall 1 — UNION ALL para leads sem interação; Pitfall 4 — exclusão de terminal stages; Pitfall 5 — re-envio aceitável sem tabela de idempotência para Milestone 2)
    - supabase/migrations/030_perfis_slack.sql (confirma que `perfis` tem coluna `slack_user_id` e índice; e que `tipo` é o discriminador `'interno'`/`'cliente'`)
    - supabase/migrations/033_rls_tarefas_notificar.sql (confirma colunas `status`, `atribuido_a_id`, `data_vencimento`, `notificar` da tabela `tarefas`)
  </read_first>
  <action>
**PRÉ-DECISÃO sobre `stand_by` (PATTERNS.md alerta):** antes de escrever a query do cron, verificar se o status `'stand_by'` (ou variação como `'standby'`/`'em_espera'`) existe em produção. Executar via SQL Editor do Supabase:
```sql
SELECT DISTINCT status FROM leads ORDER BY status;
```
- Se `'stand_by'` (ou variação) NÃO existir entre os status retornados: registrar isso em comentário SQL na migration (`-- NOTA: status 'stand_by' não existe em produção em <data>; query trata apenas TERMINAL_STAGES`) e NÃO incluí-lo no `NOT IN (...)`.
- Se `'stand_by'` (ou variação) EXISTIR: decisão padrão é EXCLUIR do alerta de cadência (lead pausado não deve ser cobrado). Adicionar ao `NOT IN ('ganho_assessoria','ganho_consultoria','perdido','cancelado','stand_by')` em AMBOS os branches do UNION ALL. Documentar a decisão em comentário SQL na migration (`-- DECISÃO: 'stand_by' excluído porque leads pausados não devem disparar cadência; alinhado com TERMINAL_STAGES embora não terminal por definição.`).
- Em qualquer caso, registrar o resultado da consulta `SELECT DISTINCT status FROM leads` em `.planning/phases/03-pull-back-notifications/03-02-CRON-DEPLOY-LOG.md` na seção "Verificação de status `stand_by`".

Após essa decisão, criar DOIS arquivos novos. Não modificar arquivos existentes.

(A) `supabase/functions/notify-resumo-diario/index.ts` — Deno edge function. Estrutura espelha `notify-tarefa/index.ts`:
- Cabeçalho de comentário canônico (`// Edge Function: notify-resumo-diario` ...) listando os 4 env vars: `SLACK_BOT_TOKEN`, `WEBHOOK_RESUMO_SECRET`, `APP_URL`, `SUPABASE_SERVICE_ROLE_KEY`.
- Imports idênticos a notify-tarefa (`https://deno.land/std@0.224.0/http/server.ts`, `https://deno.land/std@0.224.0/crypto/timing_safe_equal.ts`, `https://esm.sh/@supabase/supabase-js@2`).
- `interface ResumoDiarioPayload { perfil_id: string; tarefas_hoje: number; leads_cadencia: Array<{ id: string; nome: string; d_point: number }> }`.
- Copiar literal de notify-tarefa: `constantTimeAuthCheck`, `json`, env-var block, `createClient(...)`, `findSlackUserId`, `openDmChannel`, `postDm` (com retry 3x backoff 500*2^i para 429/5xx).
- Adicionar `findPerfilNome(perfilId: string): Promise<string>` — `supabase.from('perfis').select('nome').eq('id', perfilId).maybeSingle()` retornando `data?.nome ?? 'consultor'` (fallback seguro caso o perfil seja apagado entre o cron e a invocação).
- Adicionar `buildResumoDiarioBlocks(nomeConsultor, tarefasHoje, leadsCadencia, appUrl): unknown[]` retornando blocks Slack: section greeting `Bom dia, *<nome>*! Aqui está seu resumo de hoje:`; section com 2 fields (`*Tarefas vencendo hoje*\n<n>` e `*Leads na cadência*\n<count>`); section opcional (se `leadsCadencia.length > 0`) listando `• <nome> (D<n>)` por linha; actions section com 1 button `Abrir CRM` apontando para `<appUrl>/me`.
- `serve()` handler na ordem fixa: method check (`!== 'POST'` → 405) → auth check (Bearer via `constantTimeAuthCheck` contra `WEBHOOK_RESUMO_SECRET`; quando secret está set, header ausente/errado → 401) → env check (`!SLACK_BOT_TOKEN` → 500) → `req.json()` com try/catch (parse falha → 400) → validação de payload (`!payload.perfil_id || typeof payload.tarefas_hoje !== 'number' || !Array.isArray(payload.leads_cadencia)` → 400) → resolve `slack_user_id` via `findSlackUserId` (não encontrou → 200 com `{ ok: true, skipped: 'no slack_user_id' }`, NÃO 404; é fluxo esperado para perfis sem Slack) → resolve nome via `findPerfilNome` → `buildResumoDiarioBlocks` → `postDm(slackUserId, 'Resumo diário CONSEJ', blocks)` → devolver `{ ok: true, ts }` em sucesso e `{ ok: false, error }` em falha (status 502 quando Slack falha após retries).
- Curto-circuito explícito: se `tarefas_hoje === 0 && leads_cadencia.length === 0` → retornar 200 `{ ok: true, skipped: 'empty payload' }` sem chamar Slack. A migration JÁ filtra antes de chamar, mas defesa em profundidade.

(B) `supabase/migrations/034_cron_resumo_diario.sql` — migration SQL. Estrutura espelha `031_cron_renovacoes.sql`:
- Cabeçalho de comentário canônico (`-- Migration 034: Cron de resumo diário (NOTIF-02 + NOTIF-03)` ...) explicando que o secret `webhook_resumo_secret` deve ser criado no Vault antes de aplicar a migration via `SELECT vault.create_secret('<valor>', 'webhook_resumo_secret', 'Bearer p/ notify-resumo-diario');`.
- Comentário SQL com a decisão tomada sobre `'stand_by'` (resultado da pré-decisão acima).
- `CREATE EXTENSION IF NOT EXISTS pg_cron; CREATE EXTENSION IF NOT EXISTS pg_net;` (idempotente).
- `CREATE OR REPLACE FUNCTION public.cron_resumo_diario() RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$` com:
  - Declare: `v_secret TEXT;`, `v_url TEXT := 'https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-resumo-diario';`, `p RECORD;`, `v_tarefas INT;`, `v_leads JSONB;`.
  - Leitura do secret de `vault.decrypted_secrets WHERE name = 'webhook_resumo_secret'`. Se `v_secret IS NULL`: `RAISE WARNING 'cron_resumo_diario: secret webhook_resumo_secret ausente no Vault'; RETURN;` (nunca `RAISE EXCEPTION` — o cron job deve continuar marcado como success).
  - `FOR p IN SELECT id, slack_user_id FROM perfis WHERE tipo = 'interno' AND slack_user_id IS NOT NULL LOOP`:
    - `SELECT COUNT(*) INTO v_tarefas FROM tarefas WHERE atribuido_a_id = p.id AND status IN ('aberta','em_andamento') AND data_vencimento::date = CURRENT_DATE;`
    - `SELECT COALESCE(jsonb_agg(jsonb_build_object('id', sub.id, 'nome', sub.nome, 'd_point', sub.d_point)), '[]'::jsonb) INTO v_leads FROM (...) sub;` onde `(...)` é o UNION ALL de dois branches:
      - Branch 1 (leads COM interação): `SELECT l.id, l.nome, (CURRENT_DATE - MAX(i.enviada_em)::date)::int AS d_point FROM leads l JOIN interacoes_lead i ON i.lead_id = l.id WHERE l.responsavel_id = p.id AND l.status NOT IN (<lista decidida acima>) GROUP BY l.id, l.nome HAVING (CURRENT_DATE - MAX(i.enviada_em)::date) IN (1,3,5,7,10)`.
      - Branch 2 (leads SEM interação): `SELECT l.id, l.nome, 1 AS d_point FROM leads l WHERE l.responsavel_id = p.id AND l.status NOT IN (<lista decidida acima>) AND NOT EXISTS (SELECT 1 FROM interacoes_lead i WHERE i.lead_id = l.id) AND (CURRENT_DATE - l.created_at::date) IN (0,1)` (D1 para criado hoje/ontem — espelha branch da função TS; hardcodar `1 AS d_point` é mais simples que `CASE WHEN ... THEN 1 END`).
    - `IF v_tarefas > 0 OR jsonb_array_length(v_leads) > 0 THEN PERFORM net.http_post(url := v_url, body := jsonb_build_object('perfil_id', p.id, 'tarefas_hoje', v_tarefas, 'leads_cadencia', v_leads), headers := jsonb_build_object('Content-Type', 'application/json', 'Authorization', 'Bearer ' || v_secret)); END IF;`
  - `END LOOP;`.
- `SELECT cron.schedule('resumo-diario-consultores', '0 10 * * *', 'SELECT public.cron_resumo_diario()');` (10:00 UTC = 07:00 BRT; `0 10 * * *` é o cron expression escolhido na pesquisa).

Não inserir o valor do secret na migration (NUNCA versionar secrets). NÃO usar `RAISE EXCEPTION`. NÃO criar tabela de idempotência (Milestone 2 aceita re-envio acidental — research Pitfall 5 confirma).

Após escrever os 2 arquivos, validar localmente que TypeScript da edge function compila com Deno (skip — sem Deno local, é validado no deploy da Task 3) e que o arquivo SQL parseia: `grep -c "cron_resumo_diario" supabase/migrations/034_cron_resumo_diario.sql` ≥ 3 (cabeçalho, função, schedule).
  </action>
  <verify>
    <automated>test -f supabase/functions/notify-resumo-diario/index.ts && test -f supabase/migrations/034_cron_resumo_diario.sql && grep -q "resumo-diario-consultores" supabase/migrations/034_cron_resumo_diario.sql && grep -q "webhook_resumo_secret" supabase/migrations/034_cron_resumo_diario.sql && grep -q "WEBHOOK_RESUMO_SECRET" supabase/functions/notify-resumo-diario/index.ts && grep -q "conversations.open" supabase/functions/notify-resumo-diario/index.ts && grep -q "chat.postMessage" supabase/functions/notify-resumo-diario/index.ts && grep -qE "stand_by|NOTA: status 'stand_by'" supabase/migrations/034_cron_resumo_diario.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/notify-resumo-diario/index.ts` existe e contém: `import { serve }`, `import { timingSafeEqual }`, `import { createClient }`, `interface ResumoDiarioPayload`, `function constantTimeAuthCheck`, `function findSlackUserId`, `function openDmChannel`, `function postDm`, `function buildResumoDiarioBlocks`, `serve(async (req) => {`. Verificável via grep.
    - `supabase/migrations/034_cron_resumo_diario.sql` existe e contém: `CREATE EXTENSION IF NOT EXISTS pg_cron`, `CREATE EXTENSION IF NOT EXISTS pg_net`, `CREATE OR REPLACE FUNCTION public.cron_resumo_diario()`, `SECURITY DEFINER`, `SET search_path = public`, `vault.decrypted_secrets`, `webhook_resumo_secret`, `RAISE WARNING`, `net.http_post`, `cron.schedule('resumo-diario-consultores', '0 10 * * *'`, `UNION ALL`, `NOT IN ('ganho_assessoria','ganho_consultoria','perdido','cancelado'` (com ou sem `,'stand_by'`), `IN (1,3,5,7,10)`. Verificável via grep.
    - Migration contém comentário SQL documentando a decisão sobre `'stand_by'` (verificável: `grep -E "stand_by" supabase/migrations/034_cron_resumo_diario.sql` retorna ao menos 1 linha — seja na lista `NOT IN`, seja em comentário de NOTA).
    - O log de deploy (`03-02-CRON-DEPLOY-LOG.md`) registra o resultado da consulta `SELECT DISTINCT status FROM leads` executada como pré-decisão.
    - Migration NÃO contém valor literal de secret (verificável: `grep -i "bearer [a-zA-Z0-9]" supabase/migrations/034_cron_resumo_diario.sql` retorna apenas a string template `'Bearer ' || v_secret`, nunca um secret hardcoded).
    - Edge function NÃO contém valor literal de secret (verificável: `grep -E "WEBHOOK_RESUMO_SECRET\s*=\s*['\"]" supabase/functions/notify-resumo-diario/index.ts` é vazio — secret só vem de `Deno.env.get`).
    - Suíte global `npm run test -- --reporter=dot` continua verde após esta task (mesmo total da Task 1).
  </acceptance_criteria>
  <done>Arquivos `notify-resumo-diario/index.ts` e `034_cron_resumo_diario.sql` criados; contratos casam (mesmas chaves `perfil_id`/`tarefas_hoje`/`leads_cadencia` no body do `net.http_post` e na `interface ResumoDiarioPayload`); secrets vêm de env/Vault e não estão hardcoded; decisão sobre `'stand_by'` documentada na migration e no log de deploy.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3 [BLOCKING]: Configurar secret no Vault, aplicar migration via `supabase db push`, deployar edge function, e verificar primeira execução real do cron com DM observada</name>
  <files>.planning/phases/03-pull-back-notifications/03-02-CRON-DEPLOY-LOG.md</files>
  <read_first>
    - supabase/migrations/034_cron_resumo_diario.sql (confirmar que migration está finalizada após Task 2)
    - supabase/functions/notify-resumo-diario/index.ts (confirmar que edge function está finalizada após Task 2)
    - .planning/phases/03-pull-back-notifications/03-RESEARCH.md (seções "Common Pitfalls — Pitfall 2" e "Environment Availability" — o secret no Vault DEVE ser criado ANTES do `supabase db push`; sem ele o cron registra success silencioso)
    - .planning/phases/03-pull-back-notifications/03-PATTERNS.md (seção "Secret no Vault — não em env var de código")
    - supabase/migrations/031_cron_renovacoes.sql (mesmo padrão de deploy já em produção — referência de que `vault.create_secret` é a forma correta)
  </read_first>
  <action>
Esta task é BLOQUEANTE e SEQUENCIAL — cada subpasso depende do anterior. Executar na ordem exata abaixo, registrando o resultado de cada subpasso em `.planning/phases/03-pull-back-notifications/03-02-CRON-DEPLOY-LOG.md` à medida que avança.

Subpasso 1 — Vault secret. Gerar string random ≥ 32 chars (ex.: `openssl rand -hex 32` ou `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`). NÃO commitar o valor em git e NÃO escrevê-lo no log de deploy. No Supabase Dashboard → SQL Editor, executar (substituindo `<valor>`): `SELECT vault.create_secret('<valor>', 'webhook_resumo_secret', 'Bearer p/ notify-resumo-diario');`. Confirmar via `SELECT name FROM vault.secrets WHERE name = 'webhook_resumo_secret';` que o secret existe. Registrar no log: data/hora da criação + comando SQL usado (sem o valor). Se o secret já existir, pular esta etapa e registrar "secret já presente".

Subpasso 2 — Configurar secret da edge function. No Supabase Dashboard → Edge Functions → (criar/selecionar) `notify-resumo-diario` → Secrets, definir `WEBHOOK_RESUMO_SECRET = <mesmo valor do subpasso 1>`. Confirmar que `SLACK_BOT_TOKEN`, `APP_URL`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` já estão presentes na função (herdados do projeto). Registrar no log: lista dos secrets configurados (apenas NOMES, nunca valores).

Subpasso 3 — Schema push. Garantir `SUPABASE_ACCESS_TOKEN` setado (`$env:SUPABASE_ACCESS_TOKEN` no PowerShell). Executar `supabase db push --linked` na raiz do repo. Capturar saída completa (stdout + stderr). Confirmar mensagem indicando `034_cron_resumo_diario.sql` aplicada. Se falhar por conflitos de schema, NÃO usar `--force`; investigar e corrigir. Registrar no log: comando exato, exit code, primeiras 30 linhas da saída, e timestamp.

Subpasso 4 — Verificação pós-push da função e job. Via SQL Editor: `SELECT proname FROM pg_proc WHERE proname = 'cron_resumo_diario';` (deve retornar 1 linha); `SELECT jobname, schedule, command FROM cron.job WHERE jobname = 'resumo-diario-consultores';` (deve retornar 1 linha com schedule `0 10 * * *`). Registrar saída exata no log.

Subpasso 5 — Deploy da edge function. Executar `supabase functions deploy notify-resumo-diario --project-ref wfnriqwkzdazdbuzbyug`. Capturar saída. Registrar URL final (`https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-resumo-diario`) e versão deployada.

Subpasso 6 — Smoke test direto da edge function (sem cron). Via terminal: `curl -X POST https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-resumo-diario -H "Authorization: Bearer <valor_do_secret>" -H "Content-Type: application/json" -d '{"perfil_id":"<id_do_proprio_Gabriel_em_perfis>","tarefas_hoje":2,"leads_cadencia":[{"id":"<uuid_lead_real>","nome":"Empresa Teste","d_point":3}]}'`. Substituir `<id_do_proprio_Gabriel_em_perfis>` por um UUID real de `perfis` com `slack_user_id` mapeado. Esperado: HTTP 200 `{ ok: true, ts: "..." }` e DM real no Slack de Gabriel com o resumo mostrando 2 tarefas + 1 lead em D3. Registrar: status HTTP, body da resposta, screenshot/timestamp da DM no Slack. Se 401: secret está errado entre Vault/edge function — corrigir. Se 400: payload errado — corrigir payload. Se 500/502: ler logs em Edge Functions → notify-resumo-diario → Logs.

Subpasso 7 — Smoke test do cron (disparo manual). Via SQL Editor: `SELECT public.cron_resumo_diario();` (executa síncrono o que o cron faria às 07:00 BRT). Aguardar até 1 minuto pelas DMs. Em seguida: `SELECT runid, status, return_message, start_time, end_time FROM cron.job_run_details WHERE jobid = (SELECT jobid FROM cron.job WHERE jobname = 'resumo-diario-consultores') ORDER BY start_time DESC LIMIT 5;` — NB: a chamada direta de função não cria entry em `job_run_details` (essa tabela só registra execuções do scheduler), então adicionalmente verificar `SELECT * FROM net._http_response ORDER BY id DESC LIMIT 10;` para confirmar que houve POSTs para `notify-resumo-diario` com status 200. Registrar: número de DMs observadas no Slack (Gabriel + outros consultores com slack_user_id), lista dos `_http_response` recentes (id, status_code, content), e qualquer warning capturado no log do Postgres.

Subpasso 8 — Auditoria final. Atualizar `.planning/phases/03-pull-back-notifications/03-02-CRON-DEPLOY-LOG.md` com: hash do commit da migration 034 (`git log -1 --format=%H supabase/migrations/034_cron_resumo_diario.sql`), hash da edge function, data/hora de cada subpasso, primeiro `_http_response.id` correspondente ao smoke test do cron, e PASS/FAIL final para NOTIF-02 (tarefas_hoje no resumo) e NOTIF-03 (leads_cadencia no resumo).

Esta task NÃO modifica nenhum arquivo de aplicação — apenas configura plataforma e escreve o log de deploy.
  </action>
  <verify>
    <human-check>
      Subpasso 7 (smoke test do cron) produziu ao menos 1 DM real no Slack do consultor de teste; `cron.job` mostra `resumo-diario-consultores` com schedule `0 10 * * *`; `net._http_response` mostra POST com status 200 ao endpoint `notify-resumo-diario`; arquivo `03-02-CRON-DEPLOY-LOG.md` registra `_http_response.id` real e a versão deployada da edge function.
    </human-check>
    <automated>test -f .planning/phases/03-pull-back-notifications/03-02-CRON-DEPLOY-LOG.md && grep -q "resumo-diario-consultores" .planning/phases/03-pull-back-notifications/03-02-CRON-DEPLOY-LOG.md && grep -q "_http_response" .planning/phases/03-pull-back-notifications/03-02-CRON-DEPLOY-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT name FROM vault.secrets WHERE name = 'webhook_resumo_secret'` retorna 1 linha (registrado no log).
    - `SELECT proname FROM pg_proc WHERE proname = 'cron_resumo_diario'` retorna 1 linha (registrado no log).
    - `SELECT jobname, schedule FROM cron.job WHERE jobname = 'resumo-diario-consultores'` retorna 1 linha com schedule `0 10 * * *` (registrado no log).
    - `supabase functions list --project-ref wfnriqwkzdazdbuzbyug` lista `notify-resumo-diario` (saída registrada no log).
    - Smoke test direto via curl (Subpasso 6) retornou HTTP 200 + DM real no Slack (status + body + timestamp da DM registrados no log).
    - Smoke test do cron (Subpasso 7) produziu ao menos 1 entry em `net._http_response` com status 200 e DM real no Slack (`_http_response.id` registrado no log).
    - Arquivo `03-02-CRON-DEPLOY-LOG.md` contém: comando `supabase db push` + exit code, lista de secrets configurados (sem valores), `_http_response.id`, hash dos arquivos commitados, e o resultado da consulta `SELECT DISTINCT status FROM leads` (pré-decisão `stand_by` da Task 2).
    - Valor do secret NÃO aparece em nenhum arquivo do repo (verificável: `grep -r "<valor>" .planning/ supabase/` é vazio — o tester executa esse grep substituindo `<valor>` pelo valor real conhecido apenas localmente).
  </acceptance_criteria>
  <done>NOTIF-02 e NOTIF-03 funcionam em produção: cron deployado, edge function deployada, secret no Vault, smoke test real disparou DM consolidada no Slack, log de deploy auditável commitado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron (DB) → `cron_resumo_diario()` PL/pgSQL | Função roda com `SECURITY DEFINER`; sem input externo do usuário |
| `cron_resumo_diario()` → edge function via `pg_net` | HTTPS interno Supabase + Bearer do Vault no header `Authorization` |
| Edge function → Supabase (read `perfis`) | Service role key da edge function lê `perfis` sem RLS |
| Edge function → Slack API | `chat.postMessage` autenticado com `SLACK_BOT_TOKEN` (env var da edge function) |
| Operador humano → SQL editor (criação do Vault secret) | Confiança no operador (Gabriel) — secret nunca passa por arquivo versionado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | POST forjado para `notify-resumo-diario` | mitigate | `constantTimeAuthCheck(received, WEBHOOK_RESUMO_SECRET)` na primeira linha do `serve()` handler — chamadas sem Bearer correto retornam 401. Padrão copiado de notify-tarefa (verificado em produção). |
| T-03-02-02 | Information Disclosure | Secret `webhook_resumo_secret` em arquivo versionado (migration ou log de deploy) | mitigate | Secret NUNCA aparece em SQL — só `vault.create_secret(...)` executado out-of-band no SQL Editor. Log de deploy registra apenas NOME do secret (`webhook_resumo_secret`), nunca o valor. Acceptance criteria do Task 3 inclui grep negativo do valor em `.planning/` e `supabase/`. |
| T-03-02-03 | Tampering | Injeção SQL via payload da edge function (se PL/pgSQL interpolasse strings) | mitigate | Função PL/pgSQL não recebe input externo — apenas `SELECT` em tabelas internas; `net.http_post` envia JSON construído com `jsonb_build_object` (sem string concat). Edge function valida tipos (`typeof tarefas_hoje !== 'number'` → 400). |
| T-03-02-04 | Elevation of Privilege | SSRF via `net.http_post` se `v_url` viesse de input | mitigate | `v_url` é constante hardcoded no SQL da função (`'https://wfnriqwkzdazdbuzbyug.supabase.co/functions/v1/notify-resumo-diario'`); nunca interpolado de dados do usuário. |
| T-03-02-05 | Spoofing | DM enviada para o consultor errado | mitigate | A função PL/pgSQL itera `SELECT id, slack_user_id FROM perfis WHERE tipo = 'interno'` com `SECURITY DEFINER` (não input externo); o `perfil_id` no payload é o `p.id` do loop SQL. Edge function resolve `slack_user_id` chamando `findSlackUserId(perfil_id)` lendo `perfis` com service role — sem confiar em valores externos do payload. |
| T-03-02-06 | Repudiation | Cron disparou DM mas não há trilha | accept | `net._http_response` registra cada POST do `pg_net` (status, content); `cron.job_run_details` registra runs do scheduler. Para Milestone 2 (2-5 usuários), isso é suficiente — sem necessidade de tabela de auditoria dedicada. |
| T-03-02-07 | Denial of Service | Cron envia DM duplicada se executado 2x | accept | Sem tabela de idempotência neste milestone (Pitfall 5 do RESEARCH.md). Time de 2-5 pessoas tolera re-envio acidental; risco baixo. |
| T-03-02-08 | Information Disclosure | DM contém dado sensível de lead para o consultor errado se `responsavel_id` estiver inconsistente | mitigate | Query usa `WHERE l.responsavel_id = p.id` — só o responsável pelo lead recebe o aviso. RLS de leads não se aplica (PL/pgSQL é SECURITY DEFINER), mas o filtro explícito por `responsavel_id` é equivalente. |
| T-03-02-SC | Tampering | Supply-chain (npm/pip/cargo installs) | accept | Phase 3 NÃO instala pacotes novos (Package Legitimacy Audit do RESEARCH.md: zero novos pacotes). Imports da edge function são URLs Deno std + esm.sh/supabase-js já validados em outras edge functions do projeto. |
</threat_model>

<verification>
- `npm run test -- --reporter=dot` continua verde após Task 1, com EXATAMENTE **N_BASELINE + N_NOVOS** testes (N_BASELINE anotado antes da edição; N_NOVOS = quantos `it`/`it.each` foram adicionados no novo `describe`).
- `grep -q "cron.schedule('resumo-diario-consultores'" supabase/migrations/034_cron_resumo_diario.sql` retorna 0.
- `grep -q "interface ResumoDiarioPayload" supabase/functions/notify-resumo-diario/index.ts` retorna 0.
- `SELECT proname FROM pg_proc WHERE proname = 'cron_resumo_diario'` retorna 1 linha em produção.
- `SELECT jobname, schedule FROM cron.job WHERE jobname = 'resumo-diario-consultores'` retorna 1 linha com `0 10 * * *`.
- Smoke test via curl (Subpasso 6 da Task 3) retornou 200 + DM real.
- Smoke test do cron (Subpasso 7 da Task 3) registrou ao menos 1 POST 200 em `net._http_response`.
- Log de deploy `03-02-CRON-DEPLOY-LOG.md` commitado com `_http_response.id` real e resultado da consulta `SELECT DISTINCT status FROM leads`.
</verification>

<success_criteria>
- NOTIF-02: consultor com tarefas vencendo hoje recebe DM Slack consolidada na primeira execução real do cron (verificado no smoke test do Subpasso 7).
- NOTIF-03: consultor com lead em D1/D3/D5/D7/D10 hoje recebe DM Slack listando o lead com o D-point correto (verificado no smoke test direto do Subpasso 6 com payload fixo, e no smoke test do cron do Subpasso 7).
- Leads em estágio terminal (`ganho_assessoria`, `ganho_consultoria`, `perdido`, `cancelado`) NÃO aparecem na DM (testado em `cadencia.test.ts` na Task 1; query SQL na migration 034 espelha o filtro). Decisão sobre `'stand_by'` documentada na migration e no log de deploy.
- Lead novo sem interação criado hoje/ontem aparece como D1 (testado em `cadencia.test.ts` na Task 1; Branch 2 do UNION ALL implementa isso na migration 034).
- Zero novos pacotes npm/Deno instalados (RESEARCH.md confirmou).
- Zero secrets hardcoded no repositório.
- Cron `resumo-diario-consultores` ativo com schedule `0 10 * * *` (07:00 BRT).
</success_criteria>

<output>
Create `.planning/phases/03-pull-back-notifications/03-02-SUMMARY.md` when done, contendo: arquivos criados (paths + line counts), commits realizados (hashes), resultado dos smoke tests (HTTP status + DM observada), `_http_response.id` do primeiro disparo do cron, estado final do cron (`jobname`, `schedule`, `active`), N_BASELINE da suíte global anotado antes da Task 1, e decisão sobre `'stand_by'`.
</output>
</output>
