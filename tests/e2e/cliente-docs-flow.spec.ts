import { test } from '@playwright/test'

// Wave 0 scaffold (RED) — Plan 04b ativa estes specs.
// Cobre o flow E2E completo dos documentos do cliente:
//   1. Cliente loga → vai para /portal/documentos → upload PDF → vê na lista
//   2. Consultor loga → abre ficha do cliente → vê doc subido pelo cliente
//      → aprova → cliente vê status atualizado

test.skip('cliente faz upload de PDF e vê o doc na lista do Portal', async ({ page }) => {
  // Plan 04b implementa
  void page
})

test.skip('consultor vê doc do cliente na ficha e aprova', async ({ page }) => {
  // Plan 04b implementa
  void page
})
